
//: Declare String Begin

/*: "mount_img" :*/
fileprivate let str_toValue:String = "mmaken"
fileprivate let str_userName:String = "model view birth up ranget_img"

/*: "#201E50" :*/
fileprivate let str_memoryName:[Character] = ["#","2","0","1","E","5"]
fileprivate let str_labData:String = "0"

/*: "#1F1624" :*/
fileprivate let str_viewAppData:String = "#1F1624next if"

/*: "quick_video_topview" :*/
fileprivate let str_toData:[Character] = ["q","u","i","c","k","_","v"]
fileprivate let str_plusAfterName:String = "imaged"
fileprivate let str_fillName:[Character] = ["e","o","_","t","o","p","v","i","e","w"]

/*: "nav_back_black_nor" :*/
fileprivate let str_userData:String = "sizea"
fileprivate let str_cellValue:String = "v_backvar remove var model"
fileprivate let str_centerData:String = "tap appk_nor"

/*: "Random Video" :*/
fileprivate let str_fatalTitle:String = "Random to refuse normal response file"
fileprivate let str_outsideValue:[Character] = ["V","i","d","e","o"]

/*: "quick_video_explain" :*/
fileprivate let str_toName:String = "quick_from err view"
fileprivate let str_viewData:String = "vwrap"
fileprivate let str_hiddenText:String = "make item equal self intimateeo_e"

/*: "get json error" :*/
fileprivate let str_blockTitle:[Character] = ["g"]
fileprivate let str_loadBottomValue:String = "et jas for private"
fileprivate let str_minimumData:String = "pathor"

/*: "Searching for perfect matches. please be patient." :*/
fileprivate let str_insidePointName:[UInt8] = [0x53,0x65,0x61,0x72,0x63,0x68,0x69,0x6e,0x67,0x20,0x66,0x6f,0x72,0x20,0x70,0x65,0x72,0x66,0x65,0x63,0x74,0x20,0x6d,0x61,0x74,0x63,0x68,0x65,0x73,0x2e,0x20,0x70,0x6c,0x65,0x61,0x73,0x65,0x20,0x62,0x65,0x20,0x70,0x61,0x74,0x69,0x65,0x6e,0x74,0x2e]

/*: "Matching..." :*/
fileprivate let str_makeValue:String = "Matchequal false source input content"
fileprivate let str_textName:[Character] = ["i","n","g",".",".","."]

/*: "matchId" :*/
fileprivate let str_showValue:String = "title objectmatchI"
fileprivate let str_colorValue:[Character] = ["d"]

/*: "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video." :*/
fileprivate let str_episodeText:[UInt8] = [0x2c,0x1f,0x10,0x1a,0x11,0x13,0x5e,0x28,0x17,0x1a,0x1b,0x11,0x5e,0x16,0x1b,0x12,0xe,0xd,0x5e,0x12,0x11,0x1d,0x1f,0xa,0x17,0x10,0x19,0x5e,0x7,0x11,0xb,0xc,0x5e,0x13,0x1f,0xa,0x1d,0x16,0x1b,0xd,0x5e,0x11,0x10,0x12,0x17,0x10,0x1b,0x5e,0xf,0xb,0x17,0x1d,0x15,0x12,0x7,0x50,0x5e,0x2c,0x1f,0x10,0x1a,0x11,0x13,0x5e,0x28,0x17,0x1a,0x1b,0x11,0xd,0x5e,0x1f,0xc,0x1b,0x5e,0xe,0xc,0x17,0x1d,0x1b,0x1a,0x5e,0x1f,0xa,0x5e,0x4a,0x4e,0x5e,0x19,0x11,0x12,0x1a,0x5e,0x1d,0x11,0x17,0x10,0xd,0x5e,0xe,0x1b,0xc,0x5e,0x13,0x17,0x10,0xb,0xa,0x1b,0x5e,0x1f,0x10,0x1a,0x5e,0x1f,0xc,0x1b,0x5e,0xe,0x1f,0x17,0x1a,0x5e,0x18,0x11,0xc,0x5e,0x1c,0x7,0x5e,0xa,0x16,0x1b,0x5e,0xe,0x1f,0xc,0xa,0x7,0x5e,0x1d,0x1f,0x12,0x12,0x17,0x10,0x19,0x5e,0xa,0x16,0x1b,0x5e,0x8,0x17,0x1a,0x1b,0x11,0x50]

/*: "OK" :*/
fileprivate let str_countData:String = "Omethod"

/*: "onVideoMatch" :*/
fileprivate let str_liveTitle:[Character] = ["o","n","V","i","d"]
fileprivate let str_childData:String = "title makeeoMatch"

/*: "retryAfter" :*/
fileprivate let str_goTitle:String = "retryAimage height cell add name"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AnalogDigitalConverterObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2022/11/4.
//

//: import UIKit
import UIKit

//: class TalkingQuickVideoMatchViewController: TalkingBaseViewController {
class AnalogDigitalConverterObjectProtocol: PropertyViewController {
	var textDoing: Bool = true
	var partySum: Int = 70
	var indexNumber: Double = -54.6
	var withName: String = "color"
	var fallArray: [AnyHashable] = []
	var addDictionary: [AnyHashable: String] = [:]
	var cellOff: Bool = false
	var styleNumber: Int = 58
	var headCount: Double = -8.2
	var dealText: String = "image"
	var withArray: [AnyHashable] = []
	var finishDictionary: [AnyHashable: String] = [:]
	var changeOpen: Bool = false
	var appTotal: Double = 0.2

    var partyColorImageView: UIImageView?

    //: private var headTimer: Timer?
    private var headTimer: Timer? // 头像展示定时器
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: req_videoMatchHeadPics()
        playerPics()
        //: startVideoMatch()
        listing()
    
            if (navView.isExclusiveTouch) && (navView.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: navView.bounds.standardized)
            pathLet.colorCommandInterval = matchId

            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.textDoing = viewSwitch
            
            var pathLet = self.hideNavi
                pathLet = true
                pathLet = !pathLet
            if pathLet != self.textDoing {
                self.textDoing = pathLet
            }
            
            return self.textDoing
            }
            pathLet.routeTotal = { [self] titleSum in
            self.partySum = titleSum
            
            var pathLet = matchId
                pathLet -= 1
                if pathLet != 87 {
                    pathLet = pathLet + 1
                }
            if pathLet > self.partySum {
                self.partySum = pathLet
            }
            
            return self.partySum
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.indexNumber = arrayMagnitude
            
            return self.indexNumber
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.withName = replyName
            
            do {
                self.withName = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.withName = error.localizedDescription
            }
            return self.withName
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.fallArray = partyArray
            
            guard var value = self.fallArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.addDictionary = tableDictionary
            
            guard var value = self.addDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                navView.addSubview(pathLet)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: stopVideoMatch()
        videoMatch()
    
            if (!self.shouldAutorotate) && (self.nibName != nil && self.nibName!.hasSuffix("free")) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let tapRoom = CleanController()
            tapRoom.primrosePathFirstSwitch = animated
            tapRoom.paraSum = matchId

            tapRoom.matchOff = { [self] viewSwitch in
            self.changeOpen = viewSwitch
            
            var tapRoom = self.hideNavi
                tapRoom = true
                tapRoom = !tapRoom
            if tapRoom != self.changeOpen {
                self.changeOpen = tapRoom
            }
            
            return self.changeOpen
            }
            tapRoom.actionSum = { [self] arrayMagnitude in
            self.appTotal = arrayMagnitude
            
            self.appTotal /= 6
            return self.appTotal
            }
                self.navigationController?.pushViewController(tapRoom.self, animated: true)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        pushEnable()
        //: setupSubViewsConstraint()
        magnitudeervalData()

        partyColorImageView = UIImageView(frame: self.view.frame.integral)
        if let partyColorImageView = partyColorImageView {
            self.partyColorImageView?.image = UIImage(named: (str_toValue.replacingOccurrences(of: "make", with: "ou") + String(str_userName.suffix(5))))
            if (partyColorImageView.undoManager != nil) && (partyColorImageView.animationImages?.count == (partyColorImageView.canBecomeFocused ? 9 : 4)) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(partyColorImageView)
            }
        }
    
            if (bgImgView.isExclusiveTouch) && (bgImgView.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: bgImgView.bounds.standardized)
            pathLet.colorCommandInterval = matchId

            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.cellOff = viewSwitch
            
            var pathLet = self.hideNavi
                pathLet = true
                pathLet = !pathLet
            if pathLet != self.cellOff {
                self.cellOff = pathLet
            }
            
            return self.cellOff
            }
            pathLet.routeTotal = { [self] titleSum in
            self.styleNumber = titleSum
            
            var pathLet = matchId
                pathLet -= 1
                if pathLet != 87 {
                    pathLet = pathLet + 1
                }
            if pathLet > self.styleNumber {
                self.styleNumber = pathLet
            }
            
            return self.styleNumber
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.headCount = arrayMagnitude
            
            return self.headCount
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.dealText = replyName
            
            do {
                self.dealText = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.dealText = error.localizedDescription
            }
            return self.dealText
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.withArray = partyArray
            
            guard var value = self.withArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.finishDictionary = tableDictionary
            
            guard var value = self.finishDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                bgImgView.addSubview(pathLet)
            }

	}

    //: deinit {
    deinit {
        //: stopVideoMatch()
        videoMatch()
    }

    // MARK: - Lazy Load

    //: private lazy var bgImgView: UIImageView = {
    private lazy var bgImgView: UIImageView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.image = UIImage.imageGradientTopToBottonColor(colors: [UIColor.init(hex: "#201E50")!.cgColor, UIColor.init(hex: "#1F1624")!.cgColor], size: CGSize(width: ScreenWidth, height: ScreenHeight))
        view.image = UIImage.makeData(colors: [UIColor(hex: (String(str_memoryName) + str_labData.capitalized))!.cgColor, UIColor(hex: (String(str_viewAppData.prefix(7))))!.cgColor], size: CGSize(width: a_blockValue, height: a_formalValue))
        //: view.isUserInteractionEnabled = true
        view.isUserInteractionEnabled = true

        //: let topView = UIImageView.init()
        let topView = UIImageView()
        //: topView.image = UIImage.outsideText(name: "quick_video_topview")
        topView.image = UIImage.outsideText(name: (String(str_toData) + str_plusAfterName.replacingOccurrences(of: "image", with: "i") + String(str_fillName)))
        //: topView.isUserInteractionEnabled = true
        topView.isUserInteractionEnabled = true
        //: view.addSubview(topView)
        view.addSubview(topView)
        //: topView.snp.makeConstraints { make in
        topView.snp.makeConstraints { make in
            //: make.leading.top.equalTo(0)
            make.leading.top.equalTo(0)
            //: make.width.height.equalTo(actualWidth(w: 218))
            make.width.height.equalTo(actualWidth(w: 218))
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var navView: UIView = {
    private lazy var navView: UIView = {
        //: let nav = UIView()
        let nav = UIView()
        //: nav.backgroundColor = .clear
        nav.backgroundColor = .clear
        // 返回
        //: let backBtn = UIButton(type: .custom)
        let backBtn = UIButton(type: .custom)
        //: let backBImg = UIImage.outsideText(name: "nav_back_black_nor").withRenderingMode(.alwaysTemplate)
        let backBImg = UIImage.outsideText(name: (str_userData.replacingOccurrences(of: "size", with: "n") + String(str_cellValue.prefix(6)) + "_blac" + String(str_centerData.suffix(5)))).withRenderingMode(.alwaysTemplate)
        //: backBtn.setImage(backBImg, for: .normal)
        backBtn.setImage(backBImg, for: .normal)
        //: backBtn.tintColor = .white
        backBtn.tintColor = .white
        //: backBtn.addTarget(self, action: #selector(clickBackButtonAction), for: .touchUpInside)
        backBtn.addTarget(self, action: #selector(clickFrom), for: .touchUpInside)
        //: nav.addSubview(backBtn)
        nav.addSubview(backBtn)
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.width.equalTo(40)
            make.width.equalTo(40)
            //: make.height.equalTo(NavigationBarHeight)
            make.height.equalTo(a_toText)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(a_limitRecordScreenName)
        }
        // 标题
        //: let titleLab = UILabel()
        let titleLab = UILabel()
        //: titleLab.font = UIFont.pingfangMediumFont(fontSize: 17)
        titleLab.font = UIFont.methodPlay(fontSize: 17)
        //: titleLab.textColor = .white
        titleLab.textColor = .white
        //: titleLab.textAlignment = .center
        titleLab.textAlignment = .center
        //: titleLab.text = "Random Video".localized
        titleLab.text = (String(str_fatalTitle.prefix(7)) + String(str_outsideValue)).localized
        //: nav.addSubview(titleLab)
        nav.addSubview(titleLab)
        //: titleLab.snp.makeConstraints { make in
        titleLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(backBtn)
            make.centerY.equalTo(backBtn)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        // 提示（审核模式隐藏）
        //: if MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue {
            //: let matchTipsBtn = UIButton(type: .custom)
            let matchTipsBtn = UIButton(type: .custom)
            //: let matchTipsImg = UIImage.outsideText(name: "quick_video_explain").withRenderingMode(.alwaysTemplate)
            let matchTipsImg = UIImage.outsideText(name: (String(str_toName.prefix(6)) + str_viewData.replacingOccurrences(of: "wrap", with: "id") + String(str_hiddenText.suffix(4)) + "xplain")).withRenderingMode(.alwaysTemplate)
            //: matchTipsBtn.setImage(matchTipsImg, for: .normal)
            matchTipsBtn.setImage(matchTipsImg, for: .normal)
            //: matchTipsBtn.tintColor = .white
            matchTipsBtn.tintColor = .white
            //: matchTipsBtn.addTarget(self, action: #selector(matchTipsButtonClick), for: .touchUpInside)
            matchTipsBtn.addTarget(self, action: #selector(tipsClick), for: .touchUpInside)
            //: nav.addSubview(matchTipsBtn)
            nav.addSubview(matchTipsBtn)
            //: matchTipsBtn.snp.makeConstraints { make in
            matchTipsBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(0)
                make.trailing.equalTo(0)
                //: make.centerY.equalTo(backBtn)
                make.centerY.equalTo(backBtn)
                //: make.size.equalTo(CGSize(width: 45, height: NavigationBarHeight))
                make.size.equalTo(CGSize(width: 45, height: a_toText))
            }
        }

        //: return nav
        return nav
        //: }()
    }()

    //: lazy var svgaPlayer: SVGAPlayer = {
    lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Quick_Video_BG)
        let url = MonochromeThen.default.telecasting(type: .Quick_Video_BG)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_blockTitle) + String(str_loadBottomValue.prefix(4)) + "son e" + str_minimumData.replacingOccurrences(of: "path", with: "rr")))
        }

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 15)
        lab.font = UIFont.drogueSize(fontSize: 15)
        //: lab.textColor = UIColor.init(white: 1, alpha: 0.7)
        lab.textColor = UIColor(white: 1, alpha: 0.7)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Searching for perfect matches. please be patient.".localized
        lab.text = String(bytes: str_insidePointName, encoding: .utf8)!.localized
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var matchingLab: UILabel = {
    private lazy var matchingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.methodPlay(fontSize: 18)
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Matching...".localized
        lab.text = (String(str_makeValue.prefix(5)) + String(str_textName)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: img.layer.cornerRadius = actualWidth(w: 90)/2
        img.layer.cornerRadius = actualWidth(w: 90) / 2
        //: img.image = UIImage.placeHolderImage(sex: Gender.female.rawValue)
        img.image = UIImage.tempWithGift(sex: SizeCustomReflectable.female.rawValue)
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var dataArr: [String] = {
    private lazy var dataArr: [String] = //: return Array<String>()
        .init()
    //: }()

    //: private lazy var resultDataArr: [TalkingMatchResultModel] = {
    private lazy var resultDataArr: [TallyModelType] = //: return Array<TallyModelType>()
        .init()
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingQuickVideoMatchViewController {
extension AnalogDigitalConverterObjectProtocol {
    /// 速配头像列表（男）
    //: private func req_videoMatchHeadPics() {
    private func playerPics() {
        //: TalkingQuickVideoManager.req_videoMatchHeadPics { [weak self] succeed, result, errorModel in
        DataLabThen.mentalPictureTop { [weak self] succeed, result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: self.dataArr.removeAll()
            self.dataArr.removeAll()
            //: self.dataArr = result as! [String]
            self.dataArr = result as! [String]

            //: var index = 0
            var index = 0
            //: self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { timer in
            self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { _ in
                // 实现无限循环
                //: if index >= self.dataArr.count { index = 0 }
                if index >= self.dataArr.count { index = 0 }
                // 网络加载图片，实现高斯模糊效果
                //: self.headImgView.setUrlImage(urlStr: self.dataArr[index]) { isSucceed in
                self.headImgView.recordFinish(urlStr: self.dataArr[index]) { _ in
                    //: guard let image = self.headImgView.image else { return }
                    guard let image = self.headImgView.image else { return }
                    //: self.headImgView.image = UIImage.blur(theImage: image, blurValue: 17)
                    self.headImgView.image = UIImage.engramVisualisation(theImage: image, blurValue: 17)
                }
                //: index += 1
                index += 1
                //: }, repeats: true)
            }, repeats: true)
            //: self.headTimer?.fire()
            self.headTimer?.fire()
        }
    }

    /// 开始视频速配（男）
    //: private func req_videoMatchToMatch() {
    private func can() {
        //: TalkingQuickVideoManager.req_videoMatchToMatch(matchId: self.matchId) { succeed, result, errorModel in
        DataLabThen.heading(matchId: self.matchId) { succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_showValue.suffix(6)) + String(str_colorValue))].intValue
        }
    }
}

// MARK: - Event

//: extension TalkingQuickVideoMatchViewController {
extension AnalogDigitalConverterObjectProtocol {
    /// 停止视频速配
    //: private func stopVideoMatch() {
    private func videoMatch() {
        // 销毁定时器
        //: if self.headTimer != nil {
        if self.headTimer != nil {
            //: self.headTimer?.invalidate()
            self.headTimer?.invalidate()
            //: self.headTimer = nil
            self.headTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }

        //: TalkingSocketManager.shared.videoMatchDelegate = nil
        SocketReactiveCompatible.shared.videoMatchDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SocketReactiveCompatible.shared.errorDelegate = nil
    }

    /// 开始视频速配
    //: private func startVideoMatch() {
    private func listing() {
        //: TalkingSocketManager.shared.videoMatchDelegate = self
        SocketReactiveCompatible.shared.videoMatchDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SocketReactiveCompatible.shared.errorDelegate = self
        //: req_videoMatchToMatch()
        can()
    }

    /// 返回按钮
    //: @objc private func clickBackButtonAction() {
    @objc private func clickFrom() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 打开视频速配说明
    //: @objc private func matchTipsButtonClick() {
    @objc private func tipsClick() {
        //: let message = "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video.".localized
        let message = String(bytes: str_episodeText.map{$0^126}, encoding: .utf8)!.localized
        //: TalkingAlertShow.alert(title: nil, message: message, rightBtnTitle: "OK".localized)
        TitleAlertShow.admittance(title: nil, message: message, rightBtnTitle: "OK".localized)
    }
}

// MARK: - TalkingSocketManagerDelegate

//: extension TalkingQuickVideoMatchViewController: SocketManagerVideoMatchDelegate {
extension AnalogDigitalConverterObjectProtocol: ViewOpenObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func videoEqual(data: [String: Any]) {
        // 停止速配，跳转速配结果界面
        //: stopVideoMatch()
        videoMatch()
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: guard let topVC = self.navigationController?.topViewController else { return }
            guard let topVC = self.navigationController?.topViewController else { return }
            //: guard !topVC.isKind(of: TalkingMatchResultViewController.self) else { return }
            guard !topVC.isKind(of: CoincideThen.self) else { return }
            //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
            guard let model = TallyModelType.deserialize(from: data) else { return }

            // v1.0.6 订阅用户 当前视频速配成功的次数大于等于2，则在其匹配中页面额外展示回放按钮
            //: if MeasurementAppManager.share.loginUserMode.loungePlus == true || MeasurementAppManager.share.loginUserMode.videoPlayback == true {
            if MeasurementAppManager.share.loginUserMode.loungePlus == true || MeasurementAppManager.share.loginUserMode.videoPlayback == true {
                //: if self.resultDataArr.count >= 2 {
                if self.resultDataArr.count >= 2 {
                    //: self.resultDataArr.removeFirst()
                    self.resultDataArr.removeFirst()
                }
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)

                //: } else {
            } else { // 非订阅用户只展示一条
                //: self.resultDataArr.removeAll()
                self.resultDataArr.removeAll()
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)
            }

            //: let vc = TalkingMatchResultViewController()
            let vc = CoincideThen()
            //: vc.showLastArr = self.resultDataArr
            vc.showLastArr = self.resultDataArr
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {}
    func limit(data _: [String: Any]) {}
}

// MARK: - OfErrorDelegate

//: extension TalkingQuickVideoMatchViewController: TalkingSocketManagerErrorDelegate {
extension AnalogDigitalConverterObjectProtocol: OfErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func mixUp(errorNo: Int, cmd: String, msg _: String, data: [String: Any]) {
        //: if cmd == "onVideoMatch" && errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_liveTitle) + String(str_childData.suffix(7))) && errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_goTitle.prefix(6)) + "fter")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_showValue.suffix(6)) + String(str_colorValue))].intValue
                //: self.startVideoMatch()
                self.listing()
                //: }, repeats: false)
            }, repeats: false)
        }
    }
}

// MARK: - Layout

//: extension TalkingQuickVideoMatchViewController {
extension AnalogDigitalConverterObjectProtocol {
    /// 添加视图
    //: private func setupSubviews() {
    private func pushEnable() {
        //: self.view.addSubview(bgImgView)
        self.view.addSubview(bgImgView)
        //: self.view.addSubview(navView)
        self.view.addSubview(navView)
        //: self.view.addSubview(svgaPlayer)
        self.view.addSubview(svgaPlayer)
        //: self.view.addSubview(tipsLab)
        self.view.addSubview(tipsLab)
        //: self.view.addSubview(matchingLab)
        self.view.addSubview(matchingLab)
        //: self.view.addSubview(headImgView)
        self.view.addSubview(headImgView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func magnitudeervalData() {
        //: bgImgView.snp.makeConstraints { make in
        bgImgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.top.width.equalToSuperview()
            make.leading.top.width.equalToSuperview()
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(a_itemTitle)
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: ScreenWidth, height: ScreenWidth))
            make.size.equalTo(CGSize(width: a_blockValue, height: a_blockValue))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalToSuperview().offset(-88-kDeviceSafeBottomHeight)
            make.bottom.equalToSuperview().offset(-88 - a_agentData)
        }
        //: matchingLab.snp.makeConstraints { make in
        matchingLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(tipsLab.snp.top).offset(-20)
            make.bottom.equalTo(tipsLab.snp.top).offset(-20)
        }
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.center.equalTo(svgaPlayer)
            make.center.equalTo(svgaPlayer)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
            make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
        }
    }
}
