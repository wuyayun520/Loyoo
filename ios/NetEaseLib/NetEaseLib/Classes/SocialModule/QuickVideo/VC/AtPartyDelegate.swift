
//: Declare String Begin

/*: "session_pic" :*/
fileprivate let str_countText:String = "interactioneinteractioninteractionion"
fileprivate let str_actionModeContent:[Character] = ["_","p","i","c"]

/*: "cmd" :*/
fileprivate let str_nowTitle:[UInt8] = [0x64,0x6d,0x63]

/*: "cancelCall" :*/
fileprivate let str_atName:[Character] = ["c","a","n","c","e","l","C"]
fileprivate let str_sizeName:String = "afoundationfoundation"

/*: "data" :*/
fileprivate let str_loadText:[UInt8] = [0x64,0x61,0x74,0x61]

/*: "logId" :*/
fileprivate let str_giftKeyTitle:String = "logIdview let right size"

/*: "open_picture" :*/
fileprivate let str_rangeData:String = "as load view normal inopen_pic"
fileprivate let str_faceValue:String = "TURE"

/*: "#322870" :*/
fileprivate let str_imageTitle:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let str_resignNeedName:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d","e","o"," "]
fileprivate let str_pictureData:String = "cfreea"
fileprivate let str_playVideoViewContent:String = "ttinmake"

/*: "icon_close_nor" :*/
fileprivate let str_collectionValue:String = "self user gifticon_"
fileprivate let str_blackModelText:String = "title"

/*: "icon_views_number" :*/
fileprivate let str_nameData:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n","u"]
fileprivate let str_imageData:String = "mbemid"

/*: "%@ chatting" :*/
fileprivate let str_addName:String = "invite true%@ c"
fileprivate let str_equalTableValue:[Character] = ["n","g"]

/*: "icon_lounge" :*/
fileprivate let str_voiceValue:String = "icon_max app progress"
fileprivate let str_managerUpValue:String = "share"

/*: "icon_rank_coin" :*/
fileprivate let str_beautyStatusValue:[Character] = ["i","c","o","n","_","r","a","n","k","_"]
fileprivate let str_nameMeValue:String = "coiconn"

/*: "vip_btn_top" :*/
fileprivate let str_requestYouData:String = "inside methodvip_btn"
fileprivate let str_behindTitle:String = "_topmodel height user"

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_resumeSharedValue:String = "light class stop timebtn_nea"
fileprivate let str_listTitle:String = "list var table refrby_se"
fileprivate let str_viewTitle:String = "s_norfalse the in"

/*: "%@ Coins/min" :*/
fileprivate let str_segmentValue:[Character] = ["%","@"," ","C","o"]
fileprivate let str_detailName:String = "ins/minbar model title view"

/*: "get json error" :*/
fileprivate let str_withLineValue:[Character] = ["g","e","t"," ","j","s"]
fileprivate let str_pointMakeName:[Character] = ["o"]
fileprivate let str_colorData:String = "n errortrue let path table"

/*: "matchId" :*/
fileprivate let str_withData:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let str_topData:[UInt8] = [0x65,0x72,0x72,0x6e,0x6f]

/*: "VIPFee" :*/
fileprivate let str_styleData:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let str_pageValue:[Character] = ["M","a","t","c","h","i","n"]
fileprivate let str_textName:[Character] = ["g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let str_statusModelValue:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_managerValue:[Character] = ["m","f","/","f","r","e"]
fileprivate let str_intimateData:[Character] = ["e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let str_addressData:[UInt8] = [0x66,0x72,0x6f,0x6d,0x54,0x79,0x70,0x65]

/*: "You've been barred from receiving calls" :*/
fileprivate let str_succeedModelText:[UInt8] = [0x59,0x6f,0x75,0x27,0x76,0x65,0x20,0x62,0x65,0x65,0x6e,0x20,0x62,0x61,0x72,0x72,0x65,0x64,0x20,0x66,0x72,0x6f,0x6d,0x20,0x72,0x65,0x63,0x65,0x69,0x76,0x69,0x6e,0x67,0x20,0x63,0x61,0x6c,0x6c,0x73]

/*: "source" :*/
fileprivate let str_backgroundName:[UInt8] = [0x73,0x6f,0x75,0x72,0x63,0x65]

/*: "type" :*/
fileprivate let str_clubValue:[UInt8] = [0x82,0x8f,0x86,0x93]

/*: "uid" :*/
fileprivate let str_screenData:[UInt8] = [0x75,0x69,0x64]

/*: "fromFreeCall" :*/
fileprivate let str_positionValue:[Character] = ["f","r","o","m","F","r","e","e","C"]
fileprivate let str_backName:[Character] = ["a","l","l"]

/*: "requestCall" :*/
fileprivate let str_makeContent:String = "rerrues"
fileprivate let str_ofValue:[Character] = ["l"]

/*: "onVideoMatch" :*/
fileprivate let str_sharedValue:String = "changen"
fileprivate let str_genreValue:String = "color lineVideo"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_willText:[UInt8] = [0x98,0x82,0xb6,0xb7,0xac,0xae,0xa2,0xb7,0xaa,0xa0,0xe3,0xb5,0xaa,0xa7,0xa6,0xac,0x9e,0xe3,0x91,0xa6,0xa0,0xa6,0xaa,0xb5,0xa6,0xe3,0xb5,0xaa,0xa7,0xa6,0xac,0xe3,0xa0,0xa2,0xaf,0xaf,0xe3,0xaa,0xad,0xa0,0xac,0xae,0xaa,0xad,0xa4]

/*: "refuseCall" :*/
fileprivate let str_headValue:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let str_sharedEqualLiveValue:String = "onCaempty value"
fileprivate let str_panName:String = "modelmodel"

/*: "retryAfter" :*/
fileprivate let str_hideValue:String = "retryAto and"

/*: "onRequestCall" :*/
fileprivate let str_shareName:String = "from scale list permission selfonReques"
fileprivate let str_managerTitle:[Character] = ["t","C","a","l","l"]

/*: "failType" :*/
fileprivate let str_withNorContent:[Character] = ["f","a","i","l","T"]
fileprivate let str_centerContent:[Character] = ["y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let str_textData:[Character] = ["c","a","l","l","W","a","i","t","i","n","g","T"]
fileprivate let str_noteName:[Character] = ["i","m","e"]

/*: "toUid" :*/
fileprivate let str_colorValue:String = "toUidcan public"

/*: "Free 1 min" :*/
fileprivate let str_estimatedData:String = "Free 1background top cover text info"

/*: "%@/min" :*/
fileprivate let str_imageValue:[Character] = ["%"]
fileprivate let str_actionRawColorName:String = "translate local play@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AtPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class AtPartyDelegate: PropertyViewController {
	var ofDoing: Bool = true
	var processMagnitude: Int = 57
	var colorTotal: Double = 95.2
	var feeArray: [AnyHashable] = []
	var shoulderPlaceEnable: Bool = true
	var blockSum: Int = 8
	var metalCount: Double = 9.7
	var nextArray: [AnyHashable] = []

    var logManagerImageView: UIImageView?

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.attribute()
            }
        }
        //: self.resetCamera()
        self.boxCameraTargetLive()
    
            if (vipTopImgView.tag == 9794) && (vipTopImgView.frame.origin.x == 89.81) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let fileAllocationTable = RecordView(frame: vipTopImgView.bounds.standardized)

            fileAllocationTable.modelEnable = { [self] intervalClose in
            self.shoulderPlaceEnable = intervalClose
            
            var fileAllocationTable = videoManager.config.isPreviewNow
            fileAllocationTable = false
            if fileAllocationTable != self.shoulderPlaceEnable {
                self.shoulderPlaceEnable = fileAllocationTable
            }
            
            return self.shoulderPlaceEnable
            }
            fileAllocationTable.successfullyNumber = { [self] tableNumber in
            self.blockSum = tableNumber
            
            var fileAllocationTable = matchId
            fileAllocationTable &*= 7
            if fileAllocationTable > self.blockSum {
                self.blockSum = fileAllocationTable
            }
            
            return self.blockSum
            }
            fileAllocationTable.frameInterval = { [self] textQuantity in
            self.metalCount = textQuantity
            
            return self.metalCount
            }
            fileAllocationTable.managerBackRoomArray = { [self] nidusDeckArray in
            self.nextArray = nidusDeckArray
            
            guard var value = self.nextArray as? [String] else {
                return nil
            }
            return value
            }
                vipTopImgView.addSubview(fileAllocationTable)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        dismissPoint()
        //: setupSubViewsConstraint()
        ready()
        //: bindInteraction()
        buttonQuantityeraction()
        //: videoManager.startPreview(isMatch: true)
        videoManager.mutualMatch(isMatch: true)
        //: setPlayer(isRandon: false)
        messageRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            analyze()
        }

        logManagerImageView = UIImageView(frame: self.view.frame.union(CGRect(x: CGFloat(Double(self.view.center.x)), y: CGFloat(Int(self.view.bounds.origin.x)), width: CGFloat(Double(self.view.frame.size.height)), height: CGFloat(Double(self.view.frame.origin.y)))))
        if let logManagerImageView = logManagerImageView {
            self.logManagerImageView?.image = UIImage(named: (str_countText.replacingOccurrences(of: "interaction", with: "s") + String(str_actionModeContent)))
            if (logManagerImageView.inputViewController != nil) && (!logManagerImageView.autoresizesSubviews) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(logManagerImageView)
            }
        }
    
		if var enableValue = videoManager.infoModel?.logId { 
	            if (priceStackView.tag == 9794) && (priceStackView.frame.origin.x == 89.81) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let fileAllocationTable = RecordView(frame: priceStackView.bounds.standardized)
	
	            fileAllocationTable.modelEnable = { [self] intervalClose in
	            self.ofDoing = intervalClose
	            
	            var fileAllocationTable = self.hideNavi
	            fileAllocationTable = false
	            if fileAllocationTable != self.ofDoing {
	                self.ofDoing = fileAllocationTable
	            }
	            
	            return self.ofDoing
	            }
	            fileAllocationTable.successfullyNumber = { [self] tableNumber in
	            self.processMagnitude = tableNumber
	            
	            var fileAllocationTable = enableValue
	            fileAllocationTable &*= 7
	            if fileAllocationTable > self.processMagnitude {
	                self.processMagnitude = fileAllocationTable
	            }
	            
	            return self.processMagnitude
	            }
	            fileAllocationTable.frameInterval = { [self] textQuantity in
	            self.colorTotal = textQuantity
	            
	            return self.colorTotal
	            }
	            fileAllocationTable.managerBackRoomArray = { [self] nidusDeckArray in
	            self.feeArray = nidusDeckArray
	            
	            guard var value = self.feeArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                priceStackView.addSubview(fileAllocationTable)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_nowTitle.reversed(), encoding: .utf8)!: (String(str_atName) + str_sizeName.replacingOccurrences(of: "foundation", with: "l")), String(bytes: str_loadText, encoding: .utf8)!: [(String(str_giftKeyTitle.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        temp()
        //: if TalkingSocketManager.shared.isTalking == false {
        if SocketReactiveCompatible.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SocketReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SocketReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SocketReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SocketReactiveCompatible.shared.errorDelegate = nil
            self.logManagerImageView?.image = UIImage(named: (String(str_rangeData.suffix(8)) + str_faceValue.lowercased()))
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ManageressReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = ManageressReactiveCompatible()
        //: let info = TalkingVideoChatModel()
        let info = ImageChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .blockOf()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(analyze), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: a_blockValue + 1, height: a_formalValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (str_imageTitle.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_resignNeedName) + str_pictureData.replacingOccurrences(of: "free", with: "h") + str_playVideoViewContent.replacingOccurrences(of: "make", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.outsideText(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.outsideText(name: (String(str_collectionValue.suffix(5)) + "close_no" + str_blackModelText.replacingOccurrences(of: "title", with: "r"))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(representation), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: PathErrorTalkingButton = {
        //: let btn = TalkingButton()
        let btn = PathErrorTalkingButton()
        //: btn.setImage(UIImage.outsideText(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.outsideText(name: (String(str_nameData) + str_imageData.replacingOccurrences(of: "mid", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.outsideText(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.outsideText(name: (String(str_nameData) + str_imageData.replacingOccurrences(of: "mid", with: "r"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_addName.suffix(4)) + "hatti" + String(str_equalTableValue)).random(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.outsideText(name: "icon_lounge")
        img.image = UIImage.outsideText(name: (String(str_voiceValue.prefix(5)) + "loung" + str_managerUpValue.replacingOccurrences(of: "share", with: "e")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.outsideText(name: "icon_rank_coin")
        img.image = UIImage.outsideText(name: (String(str_beautyStatusValue) + str_nameMeValue.replacingOccurrences(of: "icon", with: "i")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.thoughtImage(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.outsideText(name: "vip_btn_top")
        imgV.image = UIImage.outsideText(name: (String(str_requestYouData.suffix(7)) + String(str_behindTitle.prefix(4))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: PathErrorTalkingButton = {
        //: let btn = TalkingButton()
        let btn = PathErrorTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shadow), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.outsideText(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.outsideText(name: (String(str_resumeSharedValue.suffix(7)) + String(str_listTitle.suffix(6)) + "tting" + String(str_viewTitle.prefix(5)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.outsideText(name: "icon_rank_coin")
        imgV.image = UIImage.outsideText(name: (String(str_beautyStatusValue) + str_nameMeValue.replacingOccurrences(of: "icon", with: "i")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .withTime()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(str_segmentValue) + String(str_detailName.prefix(7))).random(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.thoughtImage(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: TitleAlertView = //: return TitleAlertView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension AtPartyDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func messageRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = MonochromeThen.default.telecasting(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_withLineValue) + String(str_pointMakeName) + String(str_colorData.prefix(7))))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func randomThroughErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(str_withData)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: str_nowTitle.reversed(), encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: str_topData, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        DataLabThen.actionAt(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.videoStopRandom()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_withData))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func imageInfo() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        DataLabThen.pingCompletion { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(str_styleData))].intValue
            //: self.refreshVideoCallPrice()
            self.attribute()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension AtPartyDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func shadow() {
        //: LimitPushManager.share.func__pushToSubscribeAlert()
        LimitPushManager.share.callParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func representation() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        premiumThreshold()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        a_textTurnName.photoOf(eventID: a_buttonName)

        //: if MeasurementAppManager.share.appConfigMode.displayMatchV4PopUp, MeasurementAppManager.share.loginUserMode.freeCallTimes > 0,
        if MeasurementAppManager.share.appConfigMode.displayMatchV4PopUp, MeasurementAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.shack()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        colorController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func analyze() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        pointEqual()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_pageValue) + String(str_textName)).localized
        //: setPlayer(isRandon: true)
        messageRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if MeasurementAppManager.share.loginUserMode.randomVideoType == 2 {
        if MeasurementAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            a_textTurnName.photoOf(eventID: a_screenText)
            //: req_getVideoInfo()
            subInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            a_textTurnName.photoOf(eventID: a_packageConnectNetData)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            SocketReactiveCompatible.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            SocketReactiveCompatible.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            SocketReactiveCompatible.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            SocketReactiveCompatible.shared.errorDelegate = self
            //: restart_randomVideo()
            requestPicture()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func requestPicture(cmd: String = (String(str_statusModelValue)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        temp()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        randomThroughErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func videoStopRandom() {
        // 停止定时器
        //: destroyWaitingTimer()
        temp()
        //: showCloseBtn()
        premiumThreshold()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_resignNeedName) + str_pictureData.replacingOccurrences(of: "free", with: "h") + str_playVideoViewContent.replacingOccurrences(of: "make", with: "g")).localized
        //: setPlayer(isRandon: false)
        messageRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if SocketReactiveCompatible.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SocketReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SocketReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SocketReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SocketReactiveCompatible.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        attribute()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func temp() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func subInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_managerValue) + String(str_intimateData))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_addressData, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.videoStopRandom()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: MeasurementAppManager.share.loginUserMode.randomVideoType = 1
                    MeasurementAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.analyze()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.episode(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = TableMeasurable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = DatePlayListener()
                //: player.setMute(bEnable: true)
                player.pileEndowIcon(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.catScan(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                DoingeCallManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                PrefaceReactiveCompatible.voiceMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.playIcon()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func playIcon() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        a_textTurnName.photoOf(eventID: a_statusContent)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard DoingeCallManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.episode(showMsg: String(bytes: str_succeedModelText, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            DoingeCallManager.shared.operation(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.videoStopRandom()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = MeasurementRecognizerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.colorController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.videoStopRandom()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func pointEqual() {
        //: if MeasurementAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if MeasurementAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(MeasurementAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(MeasurementAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func premiumThreshold() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func boxCameraTargetLive() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.mutualMatch()
        }
    }
}

// MARK: - ViewOpenObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension AtPartyDelegate: ViewOpenObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func videoEqual(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = TallyModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(str_withData)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: str_backgroundName, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: str_clubValue.map{$0^246}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: str_screenData, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = UpMarkContentConvertible.randow
        //: if MeasurementAppManager.share.loginUserMode.freeCallTimes > 0 && MeasurementAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if MeasurementAppManager.share.loginUserMode.freeCallTimes > 0 && MeasurementAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(str_positionValue) + String(str_backName)))
            //: TalkingSocketManager.shared.isFreeCall = true
            SocketReactiveCompatible.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: str_nowTitle.reversed(), encoding: .utf8)!: (str_makeContent.replacingOccurrences(of: "err", with: "eq") + "tCal" + String(str_ofValue)), String(bytes: str_loadText, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        SocketReactiveCompatible.shared.sendBeautyMoment(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_nowTitle.reversed(), encoding: .utf8)!: (String(str_atName) + str_sizeName.replacingOccurrences(of: "foundation", with: "l")), String(bytes: str_loadText, encoding: .utf8)!: [(String(str_giftKeyTitle.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.requestPicture(cmd: (str_sharedValue.replacingOccurrences(of: "change", with: "o") + String(str_genreValue.suffix(5)) + "Match"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func limit(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(str_giftKeyTitle.prefix(5)))].intValue
    }
}

// MARK: - VideoCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension AtPartyDelegate: VideoCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func placeContentAccountingData(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func toTip(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func towardUser(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func metronymic(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ConstraintThen.spatialRelationExposureTo(body: String(bytes: str_willText.map{$0^195}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = TallyModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            detailName(toUid: String(uid))
        }
        //: stop_randomVideo()
        videoStopRandom()
        //: MeasurementAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        MeasurementAppManager.share.cancelAction(info: data, situation: .Calling)
    }
}

// MARK: - CellObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AtPartyDelegate: CellObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatData(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        requestPicture(cmd: (String(str_headValue)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func passePartout(data _: [String: Any]) {}
}

// MARK: - OfErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension AtPartyDelegate: OfErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func mixUp(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_sharedEqualLiveValue.prefix(4)) + "ncelCa" + str_panName.replacingOccurrences(of: "model", with: "l")) { // 取消通话异常
            //: stop_randomVideo()
            videoStopRandom()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (str_sharedValue.replacingOccurrences(of: "change", with: "o") + String(str_genreValue.suffix(5)) + "Match"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            videoStopRandom()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_withData))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.episode(showMsg: msg)
            //: uploadFailTime(type: 2)
            hiddenType(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (str_sharedValue.replacingOccurrences(of: "change", with: "o") + String(str_genreValue.suffix(5)) + "Match"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_hideValue.prefix(6)) + "fter")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_withData))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.requestPicture(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(str_shareName.suffix(8)) + String(str_managerTitle)), errorNo == StarringStrideable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            videoStopRandom()
            //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else { return }
            //: LimitPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            LimitPushManager.share.billParams(clickEvent: a_keyValue, sufficient: false)
            //: uploadFailTime(type: 1)
            hiddenType(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        requestPicture(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension AtPartyDelegate {
    //: func uploadFailTime(type: Int) {
    func hiddenType(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(str_withNorContent) + String(str_centerContent))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(str_textData) + String(str_noteName))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        a_textTurnName.daguerreotype(eventID: a_barTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func detailName(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(str_textData) + String(str_noteName))] = time
        //: parm["toUid"] = toUid
        parm[(String(str_colorValue.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        a_textTurnName.daguerreotype(eventID: a_nameScreenData, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension AtPartyDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func attribute() {
        //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !MeasurementAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !MeasurementAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = UpMarkContentConvertible.randow
        //: if MeasurementAppManager.share.loginUserMode.freeCallTimes > 0 && MeasurementAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if MeasurementAppManager.share.loginUserMode.freeCallTimes > 0, MeasurementAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if MeasurementAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if MeasurementAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_estimatedData.prefix(6)) + " min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = MeasurementAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = MeasurementAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(str_imageValue) + String(str_actionRawColorName.suffix(5))).random(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(str_imageValue) + String(str_actionRawColorName.suffix(5))).random(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.methodPlay(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = MeasurementAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = MeasurementAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = MeasurementAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = MeasurementAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !MeasurementAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !MeasurementAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func dismissPoint() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func ready() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(a_limitRecordScreenName + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func buttonQuantityeraction() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(attribute),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: a_giftName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(imageInfo),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: a_limitSuccessVideoValue,
                                               //: object: nil)
                                               object: nil)
    }
}
