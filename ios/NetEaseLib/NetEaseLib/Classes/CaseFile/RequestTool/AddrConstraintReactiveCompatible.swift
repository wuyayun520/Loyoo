
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_toolLabText:String = "model closehttp:"
fileprivate let str_managerPriceViewData:[Character] = ["/","/"]

/*: "https://" :*/
fileprivate let str_modeValue:[Character] = ["h","t","t","p","s"]
fileprivate let str_textName:String = "://"

/*: "http:// :*/
fileprivate let str_computerLabelCornerData:String = "range section return mediumhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let str_centerName:String = "smodelrvmodelr"
fileprivate let str_willCountData:String = "view frame tap elseIndex = "

/*: "serverUrlStr =  :*/
fileprivate let str_nameValue:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," ","="]
fileprivate let str_frameChangeName:String = " "

/*: "app. :*/
fileprivate let str_tempData:String = "player view rankapp."

/*: .com" :*/
fileprivate let str_centerContent:String = ".comimage player result"

/*: "IM/getMyBlackList" :*/
fileprivate let str_transformData:[Character] = ["I","M","/","g","e","t","M","y","B","l","a"]
fileprivate let str_replyValue:String = "ckListobject var pic"

/*: "mf/business/sendMsg" :*/
fileprivate let str_centerImageData:String = "mf/bfrom section"
fileprivate let str_rangeUserName:String = "return image if/sen"

/*: "mf/business/sendGift" :*/
fileprivate let str_giftRoomName:[Character] = ["m","f","/","b","u","s","i","n","e","s","s"]
fileprivate let str_borderName:String = "super view make screen/send"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_buttonTitle:String = "image language tomf/chatR"
fileprivate let str_commentTitle:[Character] = ["o","o"]
fileprivate let str_callName:String = "star table view screen letm/sen"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_centerData:String = "var send self time entermf/ch"
fileprivate let str_postValue:String = "om/slet progress"

/*: "recharge/applePayNotify" :*/
fileprivate let str_textPointValue:String = "reblocka"
fileprivate let str_cameraName:String = "plePaif system model"

/*: "user/appleLogin" :*/
fileprivate let str_messageData:String = "USER"
fileprivate let str_listEqualValue:[Character] = ["L","o","g","i","n"]

/*: "en" :*/
fileprivate let str_thoughtData:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let str_scaleName:String = "containerr"

/*: "es" :*/
fileprivate let str_fileCompleteValue:[Character] = ["e","s"]

/*: "pt" :*/
fileprivate let str_rangeResumeValue:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let str_botTitle:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_titleName:String = "完成了检测,请\u{6c42}\u{72b6}"
fileprivate let str_videoName:[Character] = ["态","正","常"]

/*: "接口请求失败" :*/
fileprivate let str_capableAllData:String = "接\u{53e3}\u{8bf7}求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_addressValue:String = "完day检"
fileprivate let str_dataEditReturnText:String = "sex fill type list换ser"

/*: "HTTPProxy" :*/
fileprivate let str_modelValue:[Character] = ["H","T","T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let str_whiteInfoText:String = "var to online if path__SCOP"

/*: "tap" :*/
fileprivate let str_barText:String = "tablock"

/*: "tun" :*/
fileprivate let str_contentData:String = "eventun"

/*: "ipsec" :*/
fileprivate let str_ofValue:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let str_titleValue:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_sharedVoiceModelData:[Character] = ["f","u","n","c","_","_","u","p"]
fileprivate let str_makeValue:String = "clear afterdateT"
fileprivate let str_menuContent:String = "Cookiitem float cell name"

/*: "https:// :*/
fileprivate let str_buttonContent:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_statusData:[UInt8] = [0x66,0x75,0x6e,0x63,0x5f,0x5f,0x64,0x65,0x6c,0x65,0x74,0x65,0x53,0x61,0x6d,0x65,0x43,0x6f,0x6f,0x6b,0x69,0x65,0x73,0x57,0x69,0x74,0x68,0x55,0x72,0x6c,0x20,0x3d,0x20]

/*: "UID" :*/
fileprivate let str_globalText:String = "UInearby"

/*: "PHPSESSID" :*/
fileprivate let str_countTitle:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let str_timeData:String = "func_array make"
fileprivate let str_rangeActionValue:String = "main frame extended transform rightllCooki"
fileprivate let str_distributionTitle:String = "for betweenes = "

/*: "func__saveAllCookies  error" :*/
fileprivate let str_labelValue:[Character] = ["f","u","n","c","_","_","s","a","v"]
fileprivate let str_lastColorData:String = "top record leteAllCo"
fileprivate let str_indexName:String = "self indicator error"

/*: "🍪 删除 :*/
fileprivate let str_contentTitle:String = "🍪 删除"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AddrConstraintReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ActualWithSubscriptType: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class AddrConstraintReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = AddrConstraintReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        securePutIn()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))) || newValue.hasPrefix((String(str_modeValue) + str_textName.capitalized)) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if a_allValue && !a_freeTitle {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_computerLabelCornerData.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_computerLabelCornerData.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    visualCommunication(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func securePutIn() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = a_collectionName.integer(forKey: a_indexName)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (str_centerName.replacingOccurrences(of: "model", with: "e") + "Selct" + String(str_willCountData.suffix(8))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = sharedBind()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))
        //: if Environment_Formal && !Encryption_Request {
        if a_allValue && !a_freeTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(str_nameValue) + str_frameChangeName.capitalized) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func sharedBind() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func border(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_tempData.suffix(4))) + "\(a_bottomFoundText)" + (String(str_centerContent.prefix(4))), (String(str_tempData.suffix(4))) + "\(a_bottomFoundText)" + (String(str_centerContent.prefix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = ImageMacroDefine.getSystemLangCode()
        let lang = ImageMacroDefine.loadLang()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .encephalon(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(str_botTitle)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension AddrConstraintReactiveCompatible {
    //: func checkServerUrl() {
    func timeServer() {
        //: if MeasurementAppManager.share.networkStatus != .Unavailable {
        if MeasurementAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            seaPower()
            //: } else {
        } else {
            //: func_starCodeTime()
            readout()
        }
    }

    //: func func_starCodeTime() {
    func readout() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.seaPower()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func roomText() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func seaPower() {
        //: if MeasurementAppManager.share.networkStatus == .Unavailable {
        if MeasurementAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        roomText()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        TableReactiveCompatible.managerSub { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (str_titleName + String(str_videoName)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (str_capableAllData))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.readout()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (str_addressValue.replacingOccurrences(of: "day", with: "成了") + "\u{6d4b},需要\u{66f4}" + String(str_dataEditReturnText.suffix(4)) + "ver url"))
                    //: self.func__changeHostUrl()
                    self.currentStatus()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func currentStatus() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        roomText()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        a_collectionName.set(serverSelctIndex, forKey: a_indexName)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))
        //: if Environment_Formal && !Encryption_Request {
        if a_allValue, !a_freeTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension AddrConstraintReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func disappearUtilised() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(str_modelValue))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func cornerPost() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_whiteInfoText.suffix(6)) + "ED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((str_barText.replacingOccurrences(of: "block", with: "p"))) || keyStr.contains((str_contentData.replacingOccurrences(of: "event", with: "t"))) || keyStr.contains((String(str_ofValue))) || keyStr.contains((String(str_titleValue))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension AddrConstraintReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func visualCommunication(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(str_sharedVoiceModelData) + String(str_makeValue.suffix(5)) + "alking" + String(str_menuContent.prefix(5)) + "es = ") + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = tin()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData)), with: (String(str_modeValue) + str_textName.capitalized))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_modeValue) + str_textName.capitalized)) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(str_buttonContent)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            towardSmart(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        withCap()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func towardSmart(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_statusData, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func tin() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_toolLabText.suffix(5)) + String(str_managerPriceViewData))
        //: if Environment_Formal && !Encryption_Request {
        if a_allValue, !a_freeTitle {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_modeValue) + str_textName.capitalized)
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_tempData.suffix(4))) + "\(a_bottomFoundText)" + (String(str_centerContent.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func bignessGenerate(note _: Notification) {
        //: if MeasurementAppManager.share.loginUid.isEmptyString {
        if MeasurementAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            underRequire()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            count()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func underRequire() {
        //: var htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_globalText.replacingOccurrences(of: "nearby", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(str_countTitle)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(str_countTitle))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_globalText.replacingOccurrences(of: "nearby", with: "D")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(str_countTitle)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(str_countTitle))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((str_globalText.replacingOccurrences(of: "nearby", with: "D"))), h5Cookie.keys.contains((str_globalText.replacingOccurrences(of: "nearby", with: "D"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(str_countTitle))), h5Cookie.keys.contains((String(str_countTitle))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(str_countTitle))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(str_countTitle))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func count() {
        //: let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == MeasurementAppManager.share.loginUserMode.userID {
            if cookie.name == (str_globalText.replacingOccurrences(of: "nearby", with: "D")), cookie.value == MeasurementAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(str_countTitle)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_globalText.replacingOccurrences(of: "nearby", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(str_countTitle)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(str_countTitle))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((str_globalText.replacingOccurrences(of: "nearby", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_globalText.replacingOccurrences(of: "nearby", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(str_countTitle))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(str_countTitle))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func withCap() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(str_timeData.prefix(5)) + "_saveA" + String(str_rangeActionValue.suffix(7)) + String(str_distributionTitle.suffix(5))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            a_collectionName.set(cookieData, forKey: a_cellCameraValue)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_labelValue) + String(str_lastColorData.suffix(6)) + "okies " + String(str_indexName.suffix(6))))
        }
    }

    //: func func__setAppCookies() {
    func applicationLoadImage() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = a_collectionName.object(forKey: a_cellCameraValue)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func toCookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        a_collectionName.removeObject(forKey: a_cellCameraValue)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
