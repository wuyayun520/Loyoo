
//: Declare String Begin

/*: / :*/
fileprivate let str_appText:[Character] = ["/"]

/*:  ( :*/
fileprivate let str_clickTitle:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let str_socialRequestText:String = "; buildtop image"
fileprivate let str_succeedValue:[Character] = [":"]

/*: ; iOS  :*/
fileprivate let str_countText:[Character] = [";"," ","i","O","S"," "]

/*: ;  :*/
fileprivate let str_makeAreaName:[Character] = [";"," "]

/*: )" :*/
fileprivate let str_toText:[Character] = [")"]

/*: "audio" :*/
fileprivate let str_sizeCellText:String = "acornerio"

/*: + :*/
fileprivate let str_labelData:[Character] = ["+"]

/*: .wav" :*/
fileprivate let str_imageData:String = ".wavcenter info"

/*: "audio/wav" :*/
fileprivate let str_indexData:String = "bottomdi"
fileprivate let str_whiteText:[Character] = ["v"]

/*: .jpg" :*/
fileprivate let str_makeContent:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let str_viewMValue:[Character] = ["i","m","a","g","e","/","j","p","e","g"]

/*: "://app." :*/
fileprivate let str_contentNoName:String = "://apif equal error make"
fileprivate let str_hiddenCenterName:[Character] = ["p","."]

/*: "\"data\":null" :*/
fileprivate let str_valueContent:[Character] = ["\"","d","a","t","a","\"",":","n"]
fileprivate let str_frameEqualData:String = "timel"

/*: "\"data\":{}" :*/
fileprivate let str_centerName:String = "\"dat"
fileprivate let str_maleValue:String = "a\":{}"

/*: ":null" :*/
fileprivate let str_makeTimeText:String = ":nullmodel view room"

/*: ":\"\"" :*/
fileprivate let str_viewValue:String = ":\"\\"

/*: "Request failed, Try again later" :*/
fileprivate let str_modeText:[UInt8] = [0x60,0x57,0x43,0x47,0x57,0x41,0x46,0x12,0x54,0x53,0x5b,0x5e,0x57,0x56,0x1e,0x12,0x66,0x40,0x4b,0x12,0x53,0x55,0x53,0x5b,0x5c,0x12,0x5e,0x53,0x46,0x57,0x40]

/*: "json error" :*/
fileprivate let str_shareValue:String = "fillson"

/*: "platform=iphone&version= :*/
fileprivate let str_viewTitle:String = "titlelatfo"
fileprivate let str_inputValue:[Character] = ["e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let str_regularValue:String = "&packaposition data self"

/*: &bundleId= :*/
fileprivate let str_miniData:String = "model path&bundl"

/*: &lang= :*/
fileprivate let str_conversationTitle:[Character] = ["&"]
fileprivate let str_studData:[Character] = ["l","a","n","g","="]

/*: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| " :*/
fileprivate let str_normalText:String = "?!@#"
fileprivate let str_layerText:String = "*+,:"
fileprivate let str_theoryContent:[Character] = ["<",">","(",")","[","]","{","}","/","\\","|"," "]

/*: "/route/slowindex" :*/
fileprivate let str_beanText:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i","n"]
fileprivate let str_atData:[Character] = ["d","e","x"]

/*: "/route/index" :*/
fileprivate let str_modelHiddenTitle:[Character] = ["/","r","o","u","t","e","/","i","n"]
fileprivate let str_succeedName:String = "centerex"

/*: "?path= :*/
fileprivate let str_priceData:String = "?pathdata of gift return"
fileprivate let str_managerUserAddData:[Character] = ["="]

/*: "&GJNonceStr= :*/
fileprivate let str_cellValue:String = "quit frame address mode&GJN"
fileprivate let str_outsideData:String = "="

/*: "& :*/
fileprivate let str_titleData:String = "random"

/*: = :*/
fileprivate let str_succeedData:[Character] = ["="]

/*: "&params= :*/
fileprivate let str_layerTitle:[Character] = ["&","p"]
fileprivate let str_blockName:[Character] = ["a","r","a","m","s","="]

/*: "/ :*/
fileprivate let str_actionData:[Character] = ["/"]

/*: "? :*/
fileprivate let str_contentText:[Character] = ["?"]

/*: "token" :*/
fileprivate let str_bottomData:String = "tokeam"

/*: "请求成功 :*/
fileprivate let str_appearTitle:[Character] = ["请","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let str_titleEffectViewValue:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "AA" :*/
fileprivate let str_targetValue:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let str_userSizeDataTitle:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let str_blockTitle:String = "\u{4e0b}载完成 - "

/*: "/Documents/" :*/
fileprivate let str_intervalData:[Character] = ["/","D","o","c","u","m","e"]
fileprivate let str_giftData:String = "as break value start classnts/"

/*: ".mp4" :*/
fileprivate let str_errorValue:String = "hidden make.mp4"

/*: "移动地址: :*/
fileprivate let str_imageManagerValue:String = "view动地址:"

/*: "后台任务下载回来" :*/
fileprivate let str_sexData:String = "后台任\u{52a1}下载回"
fileprivate let str_observerValue:String = "image"

/*: "下载成功 : :*/
fileprivate let str_hiddenData:String = "下载成\u{529f} :"

/*: "Net Error" :*/
fileprivate let str_studValue:String = "Net return time text model mode"

/*: "下载失败 : :*/
fileprivate let str_targetQuoteValue:String = "format"
fileprivate let str_onData:String = "载\u{5931}败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToolSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let a_buttonData = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let a_clickText = "\(a_callValue)/\(a_askValue) (\(a_countDisplayValue)" + (String(str_socialRequestText.prefix(7)) + String(str_succeedValue)) + "\(a_transformValue)" + (String(str_countText)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: PtolemaicSystemErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let a_tabData = ToolSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class ToolSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func blackFor(model: TitleTheoryThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.film() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.colorKey(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.exceptValueCompletion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func colorKey(model: TitleTheoryThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.thanGift(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.femaleModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    changeSucessCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: a_userText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        episode(showMsg: a_userText)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (str_sizeCellText.replacingOccurrences(of: "corner", with: "ud")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.timeStamp())+\(a_pathValue)" + (String(str_imageData.prefix(4))), mimeType: (str_indexData.replacingOccurrences(of: "bottom", with: "au") + "o/wa" + String(str_whiteText)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(str_makeContent)), mimeType: (String(str_viewMValue)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    changeSucessCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: a_userText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        episode(showMsg: a_userText)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func exceptValueCompletion(model: TitleTheoryThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.thanGift(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.femaleModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: a_freeTitle ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = a_buttonData }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    changeSucessCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: a_userText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        episode(showMsg: a_userText)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = a_buttonData }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    changeSucessCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: a_userText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        episode(showMsg: a_userText)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func changeSucessCompletion(model: TitleTheoryThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            AddrConstraintReactiveCompatible.share.visualCommunication(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if a_freeTitle, model.requestServer.contains((String(str_contentNoName.prefix(5)) + String(str_hiddenCenterName))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.tableShared(key: AddrConstraintReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: (String(str_valueContent) + str_frameEqualData.replacingOccurrences(of: "time", with: "ul")), with: (str_centerName + str_maleValue))
        //: if !Encryption_Request {
        if !a_freeTitle {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(str_makeTimeText.prefix(5))), with: ":\"\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<BaseMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == LoadUpMeasurable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, PtolemaicSystemErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    episode(showMsg: responseModel.msg ?? String(bytes: str_modeText.map{$0^50}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case LoadUpMeasurable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: a_senseContent, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: (str_shareValue.replacingOccurrences(of: "fill", with: "j") + " error").localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func thanGift(model: TitleTheoryThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (str_viewTitle.replacingOccurrences(of: "title", with: "p") + "rm=iphon" + String(str_inputValue)) + "\(a_conversationTitle)" + (String(str_regularValue.prefix(6)) + "geId=") + "\(a_pathValue)" + (String(str_miniData.suffix(6)) + "eId=") + "\(a_countDisplayValue)" + (String(str_conversationTitle) + String(str_studData)) + "\(AddrConstraintReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if a_freeTitle, serverUrl.contains((String(str_contentNoName.prefix(5)) + String(str_hiddenCenterName))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if AddrConstraintReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_beanText) + String(str_atData)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_modelHiddenTitle) + str_succeedName.replacingOccurrences(of: "center", with: "d"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(str_priceData.prefix(5)) + String(str_managerUserAddData)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_cellValue.suffix(4)) + "onceStr" + str_outsideData.capitalized) + "\(AddrConstraintReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.computerFileNameTap(key: AddrConstraintReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(str_layerTitle) + String(str_blockName)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func femaleModel(model: TitleTheoryThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(a_clickText)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (str_bottomData.replacingOccurrences(of: "am", with: "n")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func empty(model: TitleTheoryThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.thanGift(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(str_appearTitle)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(str_titleEffectViewValue))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (str_blockTitle) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_intervalData) + String(str_giftData.suffix(4))) + NSDate.timeStamp() + (String(str_errorValue.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (str_imageManagerValue.replacingOccurrences(of: "view", with: "移")) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (str_sexData + str_observerValue.replacingOccurrences(of: "image", with: "来")))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ConstraintThen.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func roundCurrentCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: a_errBarName.upward6decrypt(), with: a_bottomFoundText)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_hiddenData) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.Normal.rawValue, errorMsg: (String(str_studValue.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: (String(str_studValue.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_targetQuoteValue.replacingOccurrences(of: "format", with: "下") + str_onData) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func detailVoice(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: a_errBarName.upward6decrypt(), with: a_bottomFoundText)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_hiddenData) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.Normal.rawValue, errorMsg: (String(str_studValue.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PtolemaicSystemErrorResponse(errorCode: LoadUpMeasurable.NetError.rawValue, errorMsg: (String(str_studValue.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_targetQuoteValue.replacingOccurrences(of: "format", with: "下") + str_onData) + "\(errMsg)")
            }
        }
    }
}
