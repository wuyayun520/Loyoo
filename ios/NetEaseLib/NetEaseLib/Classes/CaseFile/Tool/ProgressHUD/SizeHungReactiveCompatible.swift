
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_emptyLackContent:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeHungReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Joeyoung on 2022/9/1.
//

//: import UIKit
import UIKit

//: let kProgressHUD_W            = 80.0
let a_tagValue = 80.0
//: let kProgressHUD_cornerRadius = 14.0
let a_inviteQualityData = 14.0
//: let kProgressHUD_alpha        = 0.9
let a_tableData = 0.9
//: let kBackgroundView_alpha     = 0.6
let a_successTitle = 0.6
//: let kAnimationInterval        = 0.2
let a_marginData = 0.2
//: let kTransformScale           = 0.9
let a_intimateText = 0.9

//: open class ProgressHUD: UIView {
open class SizeHungReactiveCompatible: UIView {
	var sightOff: Bool = true
	var addTotal: Int = 94
	var storyTotal: Double = 67.0
	var viewName: String = "data"
	var textArray: [AnyHashable] = []
	var levelItemDictionary: [AnyHashable: String] = [:]
	var callEnable: Bool = false
	var effectBackNumber: Int = 4
	var itemQuantity: Double = -47.7
	var abdicateContent: String = "path"
	var dataArray: [AnyHashable] = []
	var upDictionary: [AnyHashable: String] = [:]
	var startPathClose: Bool = false
	var smallTotal: Int = 30
	var imageTotal: Double = -39.6
	var cancelText: String = "accounting"
	var headArray: [AnyHashable] = []
	var goldDictionary: [AnyHashable: String] = [:]

    var dubImageView: UIImageView?
    //: required public init?(coder: NSCoder) {
    public required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_emptyLackContent, encoding: .utf8)!)
    }

    //: static var shared = ProgressHUD()
    static var shared = SizeHungReactiveCompatible()
    //: private override init(frame: CGRect) {
    override private init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.frame = UIScreen.main.bounds
        self.frame = UIScreen.main.bounds
        //: self.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        self.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: self.backgroundColor = UIColor(white: 0, alpha: 0)
        self.backgroundColor = UIColor(white: 0, alpha: 0)
        //: self.addSubview(activityIndicator)

        var starView: UIView = activityIndicator
        if let imageView = self.dubImageView, (self.inputAccessoryViewController != nil) && (self.bounds.origin.x == 49.55) {
            //: SWIFT_CUSTOM_DANGER
            starView = imageView
        }
        self.addSubview(starView)
    }

    //: open override func copy() -> Any { return self }
    override open func copy() -> Any { 
            if (self.clipsToBounds) && (self.superview != nil && self.superview!.isHidden) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let iconSection = CleanView()


            
            iconSection.errorViewDoing = { [self] viewSwitch in
            self.startPathClose = viewSwitch
            
            return self.startPathClose
            }
            iconSection.routeTotal = { [self] titleSum in
            self.smallTotal = titleSum
            
            return self.smallTotal
            }
            iconSection.topPriceInterval = { [self] arrayMagnitude in
            self.imageTotal = arrayMagnitude
            
            return self.imageTotal
            }
            iconSection.harvestMoonTitle = { [self] replyName in
            self.cancelText = replyName
            
            self.cancelText = String("t")
            return self.cancelText
            }
            iconSection.bestowmentArray = { [self] partyArray in
            self.headArray = partyArray
            
            guard var value = self.headArray as? [String] else {
                return nil
            }
            return value
            }
            iconSection.canDictionary = { [self] tableDictionary in
            self.goldDictionary = tableDictionary
            
            guard var value = self.goldDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.addSubview(iconSection)
            }

		return self }
    //: open override func mutableCopy() -> Any { return self }
    override open func mutableCopy() -> Any { 
            if (activityIndicator.isExclusiveTouch) && (activityIndicator.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: activityIndicator.bounds.standardized)


            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.sightOff = viewSwitch
            
            return self.sightOff
            }
            pathLet.routeTotal = { [self] titleSum in
            self.addTotal = titleSum
            
            return self.addTotal
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.storyTotal = arrayMagnitude
            
            return self.storyTotal
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.viewName = replyName
            
            do {
                self.viewName = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.viewName = error.localizedDescription
            }
            return self.viewName
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.textArray = partyArray
            
            guard var value = self.textArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.levelItemDictionary = tableDictionary
            
            guard var value = self.levelItemDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                activityIndicator.addSubview(pathLet)
            }

		return self }

    //: class func show() {
    class func picCurrent() {
        //: show(superView: nil)
        exhibitRemoveCuttingEdge(superView: nil)
    }

    //: class func show(superView: UIView?) {
    class func exhibitRemoveCuttingEdge(superView: UIView?) {
        //: if superView != nil {
        if superView != nil {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: ProgressHUD.shared.frame = superView!.bounds
                SizeHungReactiveCompatible.shared.frame = superView!.bounds
                //: ProgressHUD.shared.activityIndicator.center = ProgressHUD.shared.center
                SizeHungReactiveCompatible.shared.activityIndicator.center = SizeHungReactiveCompatible.shared.center
                //: superView!.addSubview(ProgressHUD.shared)
                superView!.addSubview(SizeHungReactiveCompatible.shared)
            }
            //: } else {
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: ProgressHUD.shared.frame = UIScreen.main.bounds
                SizeHungReactiveCompatible.shared.frame = UIScreen.main.bounds
                //: ProgressHUD.shared.activityIndicator.center = ProgressHUD.shared.center
                SizeHungReactiveCompatible.shared.activityIndicator.center = SizeHungReactiveCompatible.shared.center
                //: ImageMacroDefine.getWindow().addSubview(ProgressHUD.shared)
                ImageMacroDefine.sumHungWindowObject().addSubview(SizeHungReactiveCompatible.shared)
            }
        }
        //: ProgressHUD.shared.hud_startAnimating()
        SizeHungReactiveCompatible.shared.willUser()
    }

    //: class func dismiss() {
    class func dataMessage() {
        //: ProgressHUD.shared.hud_stopAnimating()
        SizeHungReactiveCompatible.shared.pullUp()
    }

    //: private func hud_startAnimating() {
    private func willUser() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.backgroundColor = UIColor(white: 0, alpha: 0)
            self.backgroundColor = UIColor(white: 0, alpha: 0)
            //: self.activityIndicator.transform = CGAffineTransform(scaleX: kTransformScale, y: kTransformScale)
            self.activityIndicator.transform = CGAffineTransform(scaleX: a_intimateText, y: a_intimateText)
            //: self.activityIndicator.alpha = 0
            self.activityIndicator.alpha = 0
            //: UIView.animate(withDuration: kAnimationInterval) {
            UIView.animate(withDuration: a_marginData) {
                //: self.backgroundColor = UIColor(white: 0, alpha: kBackgroundView_alpha)
                self.backgroundColor = UIColor(white: 0, alpha: a_successTitle)
                //: self.activityIndicator.transform = CGAffineTransform(scaleX: 1, y: 1)
                self.activityIndicator.transform = CGAffineTransform(scaleX: 1, y: 1)
                //: self.activityIndicator.alpha = kProgressHUD_alpha
                self.activityIndicator.alpha = a_tableData
                //: self.activityIndicator.startAnimating()
                self.activityIndicator.startAnimating()
            }
        }
    
            if (activityIndicator.clipsToBounds) && (activityIndicator.superview != nil && activityIndicator.superview!.isHidden) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let iconSection = CleanView()


            
            iconSection.errorViewDoing = { [self] viewSwitch in
            self.callEnable = viewSwitch
            
            return self.callEnable
            }
            iconSection.routeTotal = { [self] titleSum in
            self.effectBackNumber = titleSum
            
            return self.effectBackNumber
            }
            iconSection.topPriceInterval = { [self] arrayMagnitude in
            self.itemQuantity = arrayMagnitude
            
            return self.itemQuantity
            }
            iconSection.harvestMoonTitle = { [self] replyName in
            self.abdicateContent = replyName
            
            self.abdicateContent = String("t")
            return self.abdicateContent
            }
            iconSection.bestowmentArray = { [self] partyArray in
            self.dataArray = partyArray
            
            guard var value = self.dataArray as? [String] else {
                return nil
            }
            return value
            }
            iconSection.canDictionary = { [self] tableDictionary in
            self.upDictionary = tableDictionary
            
            guard var value = self.upDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                activityIndicator.addSubview(iconSection)
            }

	}

    //: private func hud_stopAnimating() {
    private func pullUp() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: UIView.animate(withDuration: kAnimationInterval) {
            UIView.animate(withDuration: a_marginData) {
                //: self.backgroundColor = UIColor(white: 0, alpha: 0)
                self.backgroundColor = UIColor(white: 0, alpha: 0)
                //: self.activityIndicator.transform = CGAffineTransform(scaleX: kTransformScale, y: kTransformScale)
                self.activityIndicator.transform = CGAffineTransform(scaleX: a_intimateText, y: a_intimateText)
                //: self.activityIndicator.alpha = 0
                self.activityIndicator.alpha = 0
                //: } completion: { finished in
            } completion: { _ in
                //: self.activityIndicator.stopAnimating()
                self.activityIndicator.stopAnimating()
                //: ProgressHUD.shared.removeFromSuperview()
                SizeHungReactiveCompatible.shared.removeFromSuperview()
            }
        }
    }

    // MARK: - Lazy load

    //: private lazy var activityIndicator: UIActivityIndicatorView = {
    private lazy var activityIndicator: UIActivityIndicatorView = {
        //: let indicator = UIActivityIndicatorView(style: .whiteLarge)
        let indicator = UIActivityIndicatorView(style: .whiteLarge)
        //: indicator.bounds = CGRect(x: 0, y: 0, width: kProgressHUD_W, height: kProgressHUD_W)
        indicator.bounds = CGRect(x: 0, y: 0, width: a_tagValue, height: a_tagValue)
        //: indicator.center = self.center
        indicator.center = self.center
        //: indicator.backgroundColor = .black
        indicator.backgroundColor = .black
        //: indicator.layer.cornerRadius = kProgressHUD_cornerRadius
        indicator.layer.cornerRadius = a_inviteQualityData
        //: indicator.layer.masksToBounds = true
        indicator.layer.masksToBounds = true
        //: return indicator
        return indicator
        //: }()
    }()
}

//: extension ProgressHUD {
extension SizeHungReactiveCompatible {
    //: class func toast(_ str: String?) {
    class func toToast(_ str: String?) {
        //: toast(str, showTime: 1)
        matchAfter(str, showTime: 1)
    }

    //: class func toast(_ str: String?, showTime: CGFloat) {
    class func matchAfter(_ str: String?, showTime: CGFloat) {
        //: guard str != nil else { return }
        guard str != nil else { return }

        //: let titleLab = UILabel()
        let titleLab = UILabel()
        //: titleLab.backgroundColor = UIColor(white: 0, alpha: 0.8)
        titleLab.backgroundColor = UIColor(white: 0, alpha: 0.8)
        //: titleLab.layer.cornerRadius = 5
        titleLab.layer.cornerRadius = 5
        //: titleLab.layer.masksToBounds = true
        titleLab.layer.masksToBounds = true
        //: titleLab.text = str
        titleLab.text = str
        //: titleLab.font = .systemFont(ofSize: 16)
        titleLab.font = .systemFont(ofSize: 16)
        //: titleLab.textAlignment = .center
        titleLab.textAlignment = .center
        //: titleLab.numberOfLines = 0
        titleLab.numberOfLines = 0
        //: titleLab.textColor = .white
        titleLab.textColor = .white
        //: ImageMacroDefine.getWindow().addSubview(titleLab)
        ImageMacroDefine.sumHungWindowObject().addSubview(titleLab)
        //: let size = titleLab.sizeThatFits(CGSize(width: UIScreen.main.bounds.width - 40, height: CGFloat(MAXFLOAT)))
        let size = titleLab.sizeThatFits(CGSize(width: UIScreen.main.bounds.width - 40, height: CGFloat(MAXFLOAT)))
        //: titleLab.center = ImageMacroDefine.getWindow().center
        titleLab.center = ImageMacroDefine.sumHungWindowObject().center
        //: titleLab.bounds = CGRect(x: 0, y: 0, width: size.width + 30, height: size.height + 30)
        titleLab.bounds = CGRect(x: 0, y: 0, width: size.width + 30, height: size.height + 30)
        //: titleLab.alpha = 0
        titleLab.alpha = 0

        //: UIView.animate(withDuration: 0.2) {
        UIView.animate(withDuration: 0.2) {
            //: titleLab.alpha = 1
            titleLab.alpha = 1
            //: } completion: { finished in
        } completion: { _ in
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + showTime) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + showTime) {
                //: UIView.animate(withDuration: 0.2) {
                UIView.animate(withDuration: 0.2) {
                    //: titleLab.alpha = 1
                    titleLab.alpha = 1
                    //: } completion: { finished in
                } completion: { _ in
                    //: titleLab.removeFromSuperview()
                    titleLab.removeFromSuperview()
                }
            }
        }
    }
}
