
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_minimumFullText:String = "SOCwith"
fileprivate let str_lastName:String = "for video index blockDDR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_imageName:[UInt8] = [0x53,0x4f,0x43,0x4b,0x45,0x54,0x5f,0x56,0x49,0x44,0x45,0x4f,0x43,0x48,0x41,0x54,0x5f,0x52,0x45,0x51,0x55,0x45,0x53,0x54,0x43,0x41,0x4c,0x4c,0x5f,0x4e,0x4f,0x54,0x49,0x46,0x49,0x43,0x41,0x54,0x49,0x4f,0x4e]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_viewGiftName:[UInt8] = [0x52,0x45,0x43,0x49,0x56,0x45,0x5f,0x46,0x4c,0x4f,0x41,0x54,0x53,0x43,0x52,0x45,0x45,0x4e,0x5f,0x4e,0x4f,0x54,0x49,0x46,0x49,0x43,0x41,0x54,0x49,0x4f,0x4e]

/*: "isCalling" :*/
fileprivate let str_submitValue:[Character] = ["i","s","C","a","l"]
fileprivate let str_engineValue:String = "listyle"

/*: "isTalking" :*/
fileprivate let str_videoValue:String = "request"
fileprivate let str_playerControlValue:String = "sTalkingmake frame range true"

/*: "SOCKET 连接成功." :*/
fileprivate let str_addTitle:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{6210}","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_requestValue:String = "SmessageCK"
fileprivate let str_outsideText:String = "image wrapET 连"
fileprivate let str_playerValue:String = "rror:status make info window"

/*: ." :*/
fileprivate let str_labName:String = "."

/*: "SOCKET 失败 :*/
fileprivate let str_ofData:String = "SOCKET return gift self image"
fileprivate let str_convertData:String = "失scale"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_makeValue:[Character] = ["次",","," ","\u{5207}","换","过","S","o","c","k","e","t"]
fileprivate let str_bottomValue:String = "\u{57df}名\u{91cd}新尝\u{8bd5}"

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_awayText:String = "SOCKET input gesture model main add"
fileprivate let str_barText:[Character] = ["已","关","闭",","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let str_limitModelToName:String = ", reason:float key self else self"

/*: "ws:// :*/
fileprivate let str_computerData:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let str_lessGiftData:String = "/?sid=user height image else true"

/*: &uid= :*/
fileprivate let str_actualTitle:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let str_nameData:String = "&platdata field tag"
fileprivate let str_timeTitle:String = "ftab"
fileprivate let str_modelTitle:[Character] = ["m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let str_textShareData:[Character] = ["&","p","a","c","k","a","g","e"]
fileprivate let str_normalData:[Character] = ["I","d","="]

/*: "1v1server. :*/
fileprivate let str_largeSeekName:String = "tag as self super to1v1se"

/*: .com:7766" :*/
fileprivate let str_numberData:String = ".com:self system pop"

/*: "43.129.185.194:7767" :*/
fileprivate let str_backgroundValue:String = "43.129"
fileprivate let str_viewData:String = "4:77talk"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_lineThoughtText:[UInt8] = [0x53,0x4f,0x43,0x4b,0x45,0x54,0x20,0xe7,0xbd,0x91,0xe7,0xbb,0x9c,0x72,0x65,0x61,0x63,0x68,0x61,0x62,0x69,0x6c,0x69,0x74,0x79,0x43,0x68,0x61,0x6e,0x67,0x65,0x64,0xe7,0x9b,0x91,0xe5,0x90,0xac,0xef,0xbc,0x8c,0xe6,0x96,0xad,0xe7,0xbd,0x91,0xe5,0x85,0xb3,0xe9,0x97,0xad,0x2e]

/*: "cmd" :*/
fileprivate let str_fosterValue:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let str_signalData:String = "ptog"

/*: "callHeartbeat" :*/
fileprivate let str_showText:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a"]
fileprivate let str_okServiceContent:String = "label"

/*: "recievedDate" :*/
fileprivate let str_equalValue:[Character] = ["r","e","c"]
fileprivate let str_modeValue:[Character] = ["i","e","v","e","d","D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let str_sendData:String = "extension type self makeliveHea"
fileprivate let str_commentViewName:[Character] = ["r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let str_leadingValue:[Character] = ["🔗"," ","关","\u{95ed}"," ","s","o","c","k","e"]
fileprivate let str_emptyValue:String = "T"

/*: "The user logs out" :*/
fileprivate let str_colorValue:String = "if end var dataThe user "
fileprivate let str_modeData:[Character] = ["l","o"]
fileprivate let str_youName:[Character] = ["g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let str_displayValue:String = "call"
fileprivate let str_dataFatalImageText:[Character] = ["O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let str_sourceData:String = "重\u{8fde}"
fileprivate let str_succeedData:String = ". 重to次数："

/*: , 地址： :*/
fileprivate let str_mValue:String = ", free址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_rangeName:String = "SOloadK"
fileprivate let str_indexData:[Character] = ["需","\u{8981}","重","新","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_colorData:String = "kitOCKET"
fileprivate let str_listData:[Character] = ["要","重","新","连","\u{63a5}","."]

/*: "&resume=1" :*/
fileprivate let str_clearRenderText:[Character] = ["&","r","e","s","u","m","e","="]
fileprivate let str_viewName:String = "hidden"

/*: "&lang= :*/
fileprivate let str_backgroundEqualViewName:String = "&lang=live false"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_imageText:String = "SOlocationET"
fileprivate let str_makeData:String = " \u{5f00}\u{59cb}连"

/*: "onNetDisabled" :*/
fileprivate let str_equalSuccessfullyData:String = "var empty inonNe"
fileprivate let str_priceValue:[Character] = ["e","d"]

/*: "errno" :*/
fileprivate let str_infoShareContent:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let str_addValue:String = "dequala"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_toName:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","接","保","\u{6d3b}","c","m","d",":"," "]
fileprivate let str_frameName:[Character] = ["p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_tableContent:[UInt8] = [0x2e,0x74,0x61,0x65,0x62,0x74,0x72,0x61,0x65,0x48,0x6c,0x6c,0x61,0x63,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x63,0x74,0x72,0x74,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_textName:[UInt8] = [0x53,0x4f,0x43,0x4b,0x45,0x54,0x20,0x6c,0x69,0x76,0x65,0xe4,0xbf,0x9d,0xe6,0xb4,0xbb,0x63,0x6d,0x64,0x3a,0x20,0x6c,0x69,0x76,0x65,0x48,0x65,0x61,0x72,0x74,0x42,0x65,0x61,0x74,0x2e]

/*: "onP" :*/
fileprivate let str_labData:String = "bottom info live infoonP"

/*: "msg" :*/
fileprivate let str_countErrorContent:String = "msimage"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_managerTitle:String = "🔗 normal理消"
fileprivate let str_componentName:String = "Name:make manager text interval stuff"

/*: , errNo: :*/
fileprivate let str_layerData:String = ", errNo:to import"

/*: , msg: :*/
fileprivate let str_userValue:String = "path leading height, msg:"

/*: , data: :*/
fileprivate let str_errorName:String = "color user, data:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_libraryValue:String = "SOmanagerE"
fileprivate let str_blockData:String = "T 接收height shadow place height"
fileprivate let str_userName:String = "：cmdNquote of type collection"

/*: "onReceivedGift" :*/
fileprivate let str_giftBurnData:[Character] = ["o","n","R","e","c","e","i","v","e","d","G"]
fileprivate let str_resultValue:String = "keyft"

/*: "onVideoMatch" :*/
fileprivate let str_needData:[Character] = ["o","n","V","i","d"]
fileprivate let str_managerRangeName:[Character] = ["e"]
fileprivate let str_intimateData:[Character] = ["o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let str_objectData:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let str_listManagerText:String = "raw gift if info tableonCa"
fileprivate let str_colorBottomData:String = "size model view collection viewCall"

/*: "onRefuseCall" :*/
fileprivate let str_equalName:String = "view succeed text center coloronRefu"
fileprivate let str_makeErrorResultName:[Character] = ["s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let str_fileData:[Character] = ["o","n","A","c","c"]
fileprivate let str_callData:String = "eptCallicon user return"

/*: "onStopCall" :*/
fileprivate let str_statusValue:[Character] = ["o","n","S","t","o","p","C","a"]
fileprivate let str_fatalName:[Character] = ["l","l"]

/*: "freeCallTimes" :*/
fileprivate let str_resContent:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let str_presentationValue:[Character] = ["l"]
fileprivate let str_picName:String = "Timescount var in"

/*: "mfCoin" :*/
fileprivate let str_onMakeData:[Character] = ["m"]
fileprivate let str_inputTitle:[Character] = ["f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let str_toolTitle:[Character] = ["o"]
fileprivate let str_rawData:String = "nMissmake super"

/*: "acceptCall" :*/
fileprivate let str_userData:String = "accecall"
fileprivate let str_delayLogValue:String = "block"

/*: "stopCall" :*/
fileprivate let str_titleBottomValue:String = "share var view click datastopC"
fileprivate let str_backgroundTitle:[Character] = ["a","l","l"]

/*: "requestCall" :*/
fileprivate let str_cornerName:String = "rlocationues"

/*: "[Invite you to a video call...]" :*/
fileprivate let str_contactViewData:[UInt8] = [0x5b,0x49,0x6e,0x76,0x69,0x74,0x65,0x20,0x79,0x6f,0x75,0x20,0x74,0x6f,0x20,0x61,0x20,0x76,0x69,0x64,0x65,0x6f,0x20,0x63,0x61,0x6c,0x6c,0x2e,0x2e,0x2e,0x5d]

/*: "type" :*/
fileprivate let str_managerTextData:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_labelValue:[UInt8] = [0xc6,0xd4,0xf3,0xeb,0xf4,0xe9,0xf8,0xbd,0xe4,0xf2,0xe8,0xbd,0xe9,0xf2,0xbd,0xfc,0xbd,0xeb,0xf2,0xf4,0xfe,0xf8,0xbd,0xfe,0xfc,0xf1,0xf1,0xb3,0xb3,0xb3,0xc0]

/*: "uid" :*/
fileprivate let str_magnitudeValue:String = "uimaged"

/*: "nickname" :*/
fileprivate let str_firePlusText:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let str_toTitle:String = "headPictitle self table now"

/*: "onCallResume" :*/
fileprivate let str_buttonValue:String = "lab"
fileprivate let str_progressName:String = "screen key normal addnCall"

/*: "cancelCall" :*/
fileprivate let str_pairTitleUserName:String = "cavalue"
fileprivate let str_upValue:[Character] = ["e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let str_logTitle:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let str_momentName:[Character] = ["o","n","C","a","l","l","M","o","t"]
fileprivate let str_appTitle:[Character] = ["i","v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let str_resumeText:[Character] = ["o","n","L","i","v","e","W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let str_serverObjectTitle:[Character] = ["r","e","a","s","o"]
fileprivate let str_errorColorName:String = "wrap"

/*: "time" :*/
fileprivate let str_videoTitle:String = "TIME"

/*: "onStopLive" :*/
fileprivate let str_selectTitle:String = "onStlayer size"
fileprivate let str_jumpData:String = "VE"

/*: "onSendProgramGift" :*/
fileprivate let str_nameValue:[Character] = ["o","n","S","e","n","d","P","r"]
fileprivate let str_sizeEqualData:[Character] = ["o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let str_activeStrokeText:String = "onGameFsmodel gift video fatal"

/*: "broadStatus" :*/
fileprivate let str_pairColumnTitle:String = "broitemd"
fileprivate let str_mActionData:[Character] = ["S","t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let str_equalModelLeftTitle:String = "on info selfonCall"

/*: "onNotificationPopupWin" :*/
fileprivate let str_titlePointName:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i"]
fileprivate let str_styleWrapObjectTitle:[Character] = ["o","n","P","o","p"]
fileprivate let str_leadingData:[Character] = ["u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let str_indexPathData:String = "onUserBerror hide"
fileprivate let str_yearValue:String = "textock"

/*: "freeCallWin" :*/
fileprivate let str_screenValue:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let str_finishValue:String = "lWinexample self view format"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SocketReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let a_packageData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let a_endValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let a_textData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let a_randomName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let a_deviceData = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let a_ageName = (str_minimumFullText.replacingOccurrences(of: "with", with: "K") + "ET_A" + String(str_lastName.suffix(7)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let a_limitKeyTitle = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let a_okTitle = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let a_screenStatusTitle = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let a_seatTimeGiftText = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let a_keyName = NSNotification.Name(rawValue: String(bytes: str_imageName, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let a_replaceText = NSNotification.Name(rawValue: String(bytes: str_viewGiftName, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol OfErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func mixUp(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ViewOpenObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func videoEqual(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func limit(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol VideoCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func metronymic(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func toTip(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func placeContentAccountingData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func towardUser(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol CellObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func chatData(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func passePartout(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func startDown(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class SocketReactiveCompatible: NSObject {
	var pathMagnitude: Double = 32.4

    //: enum SocketRetryType: String {
    enum ObjectName: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ConstraintLanguageManager.shared.direction == .rightToLeft ? 0 : a_blockValue,
        //: y: StatusBarHeight+190
        y: a_limitRecordScreenName + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ConstraintLanguageManager.shared.direction == .rightToLeft ? 0 : a_blockValue,
        //: y: StatusBarHeight+190
        y: a_limitRecordScreenName + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ConstraintLanguageManager.shared.direction == .rightToLeft ? 0 : a_blockValue,
        //: y: StatusBarHeight+500
        y: a_limitRecordScreenName + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: OfErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ViewOpenObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: VideoCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: CellObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = SocketReactiveCompatible()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
		if var viewLiveValue = self.containerView { 
	            if (!viewLiveValue.isOpaque) && (viewLiveValue.intrinsicContentSize.width == 304.41) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let convertLet = TimePeriodView()
	            convertLet.bagOpen = isCalling

	            convertLet.labCount = { [self] detailInterval in
	            self.pathMagnitude = detailInterval
	            
	            return self.pathMagnitude
	            }
	                viewLiveValue.addSubview(convertLet)
	            }
	
		}
		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(behindApp(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(resolutionFor),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(str_submitValue) + str_engineValue.replacingOccurrences(of: "style", with: "ng")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.donation()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (str_videoValue.replacingOccurrences(of: "request", with: "i") + String(str_playerControlValue.prefix(8))))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.donation()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension SocketReactiveCompatible: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        TitleMediumLogTool.commitMedium(msg: (String(str_addTitle)))
        //: sendPing()
        shouldDown()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: a_maxScreenValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: a_ageName)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        windowContext()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        TitleMediumLogTool.commitMedium(msg: (str_requestValue.replacingOccurrences(of: "message", with: "O") + String(str_outsideText.suffix(4)) + "接失败, e" + String(str_playerValue.prefix(5))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = MeasurementAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = MeasurementAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == a_randomName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = withFollow(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            TitleMediumLogTool.commitMedium(msg: (String(str_ofData.prefix(7)) + str_convertData.replacingOccurrences(of: "scale", with: "败")) + "\(a_randomName)" + (String(str_makeValue) + str_bottomValue + "，地址\u{ff1a}") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.stigmatize(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.cipherMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        TitleMediumLogTool.commitMedium(msg: (String(str_awayText.prefix(7)) + String(str_barText)) + "\(code)" + (String(str_limitModelToName.prefix(9))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.designDismiss(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == a_seatTimeGiftText {
            //: return
            return
        }
        //: if let status = MeasurementAppManager.share.reachability?.connection, status != .unavailable {
        if let status = MeasurementAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.stigmatize(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func withFollow(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = MeasurementAppManager.share.appConfigMode.ws
        self.linkAddrArr = MeasurementAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = cellBind()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: a_ageName)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = MeasurementAppManager.share.loginUid
        let uid = MeasurementAppManager.share.loginUid
        //: let sid = MeasurementAppManager.share.loginSessionId
        let sid = MeasurementAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(str_lessGiftData.prefix(6))) + "\(sid)" + (String(str_actualTitle)) + "\(uid)" + (String(str_nameData.prefix(5)) + str_timeTitle.replacingOccurrences(of: "tab", with: "or") + String(str_modelTitle)) + "\(a_conversationTitle)" + (String(str_textShareData) + String(str_normalData)) + "\(a_pathValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func cellBind() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return a_allValue ? [(String(str_largeSeekName.suffix(5)) + "rver.") + "\(a_bottomFoundText)" + (String(str_numberData.prefix(5)) + "7766")] : [(str_backgroundValue.capitalized + ".185.19" + str_viewData.replacingOccurrences(of: "talk", with: "67"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func behindApp(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            TitleMediumLogTool.commitMedium(msg: String(bytes: str_lineThoughtText, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.designDismiss(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.stigmatize(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func resolutionFor() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.stigmatize(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.toWith()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// socket保活心跳包
    //: private func sendPing() {
    private func shouldDown() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: a_packageData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.sendBeautyMoment(info: [(String(str_fosterValue)): (str_signalData.replacingOccurrences(of: "to", with: "in"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func talkTurkey() {
        //: endTalkingPing()
        impingeOnName()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: a_endValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.colorController()?.isKind(of: HumanActivityPartyDelegate.self) == false && self.colorController()?.isKind(of: ToObjectProtocol.self) == false) && (self.currTalkingVC?.isKind(of: HumanActivityPartyDelegate.self) == false && self.currTalkingVC?.isKind(of: ToObjectProtocol.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.sendBeautyMoment(info: [(String(str_fosterValue)): (String(str_showText) + str_okServiceContent.replacingOccurrences(of: "label", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func impingeOnName() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func toWith() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(str_equalValue) + String(str_modeValue))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < a_timeClickContent {
                //: MeasurementAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                MeasurementAppManager.share.roomLoad(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// 开启直播心跳包
    //: func startLivePing() {
    func showCreate() {
        //: endLivePing()
        filing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: a_textData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.sendBeautyMoment(info: [(String(str_fosterValue)): (String(str_sendData.suffix(7)) + String(str_commentViewName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func filing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func windowContext() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MiniLeadingContainerView()
        //: let keyWindow = ImageMacroDefine.getWindow()
        let keyWindow = ImageMacroDefine.sumHungWindowObject()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func appeal() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard PropertyMManageressPushListener.giftShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard ImageToThen.labelShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func designDismiss(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(str_leadingValue) + str_emptyValue.lowercased()))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: a_seatTimeGiftText, reason: (String(str_colorValue.suffix(9)) + String(str_modeData) + String(str_youName)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        impingeOnName()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            appeal()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func equalSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = SocketReactiveCompatible.shared.withFollow(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.stigmatize(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func stigmatize(timeInterval: TimeInterval, type: ObjectName) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            TitleMediumLogTool.commitMedium(msg: (str_displayValue.replacingOccurrences(of: "call", with: "S") + String(str_dataFatalImageText)) + "\(type.rawValue)" + (str_sourceData + str_succeedData.replacingOccurrences(of: "to", with: "试")) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.removeBuild(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func removeBuild(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                TitleMediumLogTool.commitMedium(msg: (str_rangeName.replacingOccurrences(of: "load", with: "C") + "ET 连\u{63a5}正常 不" + String(str_indexData)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                TitleMediumLogTool.commitMedium(msg: (str_colorData.replacingOccurrences(of: "kit", with: "S") + " \u{6b63}在\u{8fde}\u{63a5} 不需" + String(str_listData)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(str_clearRenderText) + str_viewName.replacingOccurrences(of: "hidden", with: "1")))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(str_backgroundEqualViewName.prefix(6))) + "\(AddrConstraintReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        designDismiss(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = a_deviceData
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        TitleMediumLogTool.commitMedium(msg: (str_imageText.replacingOccurrences(of: "location", with: "CK") + str_makeData + "接, \u{5730}址：") + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func sendBeautyMoment(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if MeasurementAppManager.share.reachability?.connection == nil || MeasurementAppManager.share.reachability?.connection == .unavailable {
        if MeasurementAppManager.share.reachability?.connection == nil || MeasurementAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(str_fosterValue)): (String(str_equalSuccessfullyData.suffix(4)) + "tDisabl" + String(str_priceValue)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(str_infoShareContent)): "\(a_screenStatusTitle)",
                                       //: "data": [:]]
                                       (str_addValue.replacingOccurrences(of: "equal", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            cipherMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_fosterValue))] as? String == (str_signalData.replacingOccurrences(of: "to", with: "in")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                TitleMediumLogTool.commitMedium(msg: (String(str_toName) + String(str_frameName)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(str_fosterValue))] as? String == (String(str_showText) + str_okServiceContent.replacingOccurrences(of: "label", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                TitleMediumLogTool.commitMedium(msg: String(bytes: str_tableContent.reversed(), encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(str_fosterValue))] as? String == (String(str_sendData.suffix(7)) + String(str_commentViewName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                TitleMediumLogTool.commitMedium(msg: String(bytes: str_textName, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_fosterValue))] as? String == (str_signalData.replacingOccurrences(of: "to", with: "in")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(str_fosterValue)): (String(str_labData.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(str_infoShareContent)): "\(a_limitKeyTitle)",
                                           //: "data": [:]]
                                           (str_addValue.replacingOccurrences(of: "equal", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                cipherMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.stigmatize(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func cipherMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(str_fosterValue))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(str_infoShareContent))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(str_countErrorContent.replacingOccurrences(of: "image", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (str_managerTitle.replacingOccurrences(of: "normal", with: "处") + "息：cmd" + String(str_componentName.prefix(5))) + "\(cmdName)" + (String(str_layerData.prefix(8))) + "\(errorNo)" + (String(str_userValue.suffix(6))) + "\(msg)" + (String(str_errorName.suffix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        makeDelete(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            TitleMediumLogTool.commitMedium(msg: (str_libraryValue.replacingOccurrences(of: "manager", with: "CK") + String(str_blockData.prefix(4)) + "异\u{5e38}消息" + String(str_userName.prefix(5)) + "ame:") + "\(cmdName)" + (String(str_layerData.prefix(8))) + "\(errorNo)" + (String(str_userValue.suffix(6))) + "\(msg)" + (String(str_errorName.suffix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: MeasurementAppManager.share.loginUserMode.freeCallTimes = 0
                MeasurementAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !MeasurementAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !MeasurementAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ToViewWindowManager.shared.messagePlayer()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.mixUp(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_giftBurnData) + str_resultValue.replacingOccurrences(of: "key", with: "i")) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: a_useDetailScreenData, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_needData) + String(str_managerRangeName) + String(str_intimateData)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.videoEqual(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_objectData)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.limit(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_listManagerText.suffix(4)) + "ncel" + String(str_colorBottomData.suffix(4))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_equalName.suffix(6)) + String(str_makeErrorResultName)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_fileData) + String(str_callData.prefix(7))) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            talkTurkey()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.passePartout(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_statusValue) + String(str_fatalName)) { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            impingeOnName()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_resContent) + String(str_presentationValue) + String(str_picName.prefix(5)))] as? Int {
                //: MeasurementAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                MeasurementAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: MeasurementAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MeasurementAppManager.share.loginUserMode.mf_coin = json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(String(str_onMakeData) + String(str_inputTitle))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: a_cornerData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.currency()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(str_toolTitle) + String(str_rawData.prefix(5)) + "Coin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.towardUser(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (str_userData.replacingOccurrences(of: "call", with: "p") + "tCal" + str_delayLogValue.replacingOccurrences(of: "block", with: "l")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            talkTurkey()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.metronymic(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_titleBottomValue.suffix(5)) + String(str_backgroundTitle)) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            impingeOnName()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.toTip(data: data)
            // 更新余额
            //: MeasurementAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MeasurementAppManager.share.loginUserMode.mf_coin = json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(String(str_onMakeData) + String(str_inputTitle))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            betweenExhibit(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: a_cornerData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.currency()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (str_cornerName.replacingOccurrences(of: "location", with: "eq") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: a_keyName, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: MeasurementAppManager.share.recieve1v1TalkRequest(info: data)
                MeasurementAppManager.share.roomLoad(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(str_equalValue) + String(str_modeValue))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_contactViewData, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(String(str_managerTextData))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_labelValue.map{$0^157}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ConstraintThen.spatialRelationExposureTo(uid: String(json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(str_magnitudeValue.replacingOccurrences(of: "image", with: "i"))].intValue),
                                                            //: title: data["nickname"] as? String,
                                                            title: data[(String(str_firePlusText))] as? String,
                                                            //: body: body,
                                                            body: body,
                                                            //: imageUrl: data["headPic"] as? String)
                                                            imageUrl: data[(String(str_toTitle.prefix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ConstraintThen.shared.upDownBgm()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (str_buttonValue.replacingOccurrences(of: "lab", with: "o") + String(str_progressName.suffix(5)) + "Resume") { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            talkTurkey()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(str_buttonValue.replacingOccurrences(of: "lab", with: "o") + String(str_progressName.suffix(5)) + "Resume")] = "1"
            //: MeasurementAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            MeasurementAppManager.share.subjectMatterRequestInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.donation()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (str_pairTitleUserName.replacingOccurrences(of: "value", with: "nc") + String(str_upValue)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            impingeOnName()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.placeContentAccountingData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ConstraintThen.titleShow(identifier: String(json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(str_magnitudeValue.replacingOccurrences(of: "image", with: "i"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ConstraintThen.shared.timeOn()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(str_logTitle)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            impingeOnName()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.chatData(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_momentName) + String(str_appTitle)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.startDown?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_resumeText)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            PropertyMManageressPushListener.little(content: msg, reason: json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(String(str_serverObjectTitle) + str_errorColorName.replacingOccurrences(of: "wrap", with: "n"))].stringValue, time: json[(str_addValue.replacingOccurrences(of: "equal", with: "at"))][(str_videoTitle.lowercased())].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_selectTitle.prefix(4)) + "opLi" + str_jumpData.lowercased()) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            PropertyMManageressPushListener.giftShared().handleDown()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_nameValue) + String(str_sizeEqualData)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.afterTitle(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_activeStrokeText.prefix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(str_pairColumnTitle.replacingOccurrences(of: "item", with: "a") + String(str_mActionData))] as? Int ?? -1
            //: if MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: a_replaceText, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: a_useDetailScreenData, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: a_replaceText, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(str_equalModelLeftTitle.suffix(6)) + "ChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            DoingReactiveCompatible.toolShared().tillExplain(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_titlePointName) + String(str_styleWrapObjectTitle) + String(str_leadingData)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ToViewWindowManager.shared.heritage(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_indexPathData.prefix(7)) + str_yearValue.replacingOccurrences(of: "text", with: "l")) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            PropertyMManageressPushListener.giftShared().handleDown()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ImageToThen.labelShared().sprechgesang()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: a_senseContent, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension SocketReactiveCompatible {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func makeDelete(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (str_cornerName.replacingOccurrences(of: "location", with: "eq") + "tCall") || cmdName == (String(str_objectData)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if PropertyMManageressPushListener.giftShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                a_iconName = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                PropertyMManageressPushListener.toSatisfy(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                PropertyMManageressPushListener.giftShared().handleDown()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard a_iconName == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (str_pairTitleUserName.replacingOccurrences(of: "value", with: "nc") + String(str_upValue)) || cmdName == (String(str_listManagerText.suffix(4)) + "ncel" + String(str_colorBottomData.suffix(4))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(str_logTitle)) || cmdName == (String(str_equalName.suffix(6)) + String(str_makeErrorResultName)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_titleBottomValue.suffix(5)) + String(str_backgroundTitle)) || cmdName == (String(str_statusValue) + String(str_fatalName))
        {
            //: NEED_RESTORE_LIVE = false
            a_iconName = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = TitleAlertView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                PropertyMManageressPushListener.giftShared().frontCustomMake()
            }
            //: alert.show()
            alert.shack()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func betweenExhibit(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_resContent) + String(str_presentationValue) + String(str_picName.prefix(5)))] as? Int {
            //: MeasurementAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            MeasurementAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(str_screenValue) + String(str_finishValue.prefix(4)))] as? Bool
        //: if freeCallWin == true && MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = EntitleReactiveCompatible()
            //: view.show()
            view.duringAdd()
            //: view.setShowData(data: data)
            view.popData(data: data)
        }
    }
}
