
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_thoughtValue:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "All Numbers" :*/
fileprivate let str_roomData:String = "data application var segment pathAll N"
fileprivate let str_topName:String = "RS"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StartOutMenuCell.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/6.
//

//: import UIKit
import UIKit

//: class DropMemberMenuCell: UITableViewCell {
class StartOutMenuCell: UITableViewCell {
	var statusOff: Bool = false
	var tarotCardSum: Int = 24
	var nameCount: Double = -69.3
	var callSectionName: String = "up"
	var locationArray: [AnyHashable] = []
	var beforeDictionary: [AnyHashable: String] = [:]
	var hypothesisDoing: Bool = true
	var userCount: Int = 86
	var birthCount: Double = -0.2
	var areaConstraintText: String = "gift"
	var graduatedTableArray: [AnyHashable] = []
	var loadDictionary: [AnyHashable: String] = [:]
	var spectaclesEnable: Bool = true
	var inviteTotal: Int = 53
	var birthdaySum: Double = -34.0
	var cervixName: String = "size"
	var viewArray: [AnyHashable] = []
	var labVideoDictionary: [AnyHashable: String] = [:]

    var topImageView: UIImageView?

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: setupUI()
        pullOf()
        //: layoutSubViewsConstraints()
        clickConstraints()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_thoughtValue.reversed(), encoding: .utf8)!)
    }

    //: override func awakeFromNib() {
    override func awakeFromNib() {
        //: super.awakeFromNib()
        super.awakeFromNib()
    
            if (self.isExclusiveTouch) && (self.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: self.bounds.standardized)


            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.hypothesisDoing = viewSwitch
            
            return self.hypothesisDoing
            }
            pathLet.routeTotal = { [self] titleSum in
            self.userCount = titleSum
            
            return self.userCount
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.birthCount = arrayMagnitude
            
            return self.birthCount
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.areaConstraintText = replyName
            
            do {
                self.areaConstraintText = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.areaConstraintText = error.localizedDescription
            }
            return self.areaConstraintText
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.graduatedTableArray = partyArray
            
            guard var value = self.graduatedTableArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.loadDictionary = tableDictionary
            
            guard var value = self.loadDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.addSubview(pathLet)
            }

	}

    //: override func setSelected(_ selected: Bool, animated: Bool) {
    override func setSelected(_ selected: Bool, animated: Bool) {
        //: super.setSelected(selected, animated: animated)
        super.setSelected(selected, animated: animated)

        // Configure the view for the selected state
    
            if (self.isExclusiveTouch) && (self.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: self.bounds.standardized)


            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.statusOff = viewSwitch
            
            var pathLet = animated
                pathLet = true
                pathLet = !pathLet
            if pathLet != self.statusOff {
                self.statusOff = pathLet
            }
            
            return self.statusOff
            }
            pathLet.routeTotal = { [self] titleSum in
            self.tarotCardSum = titleSum
            
            return self.tarotCardSum
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.nameCount = arrayMagnitude
            
            return self.nameCount
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.callSectionName = replyName
            
            do {
                self.callSectionName = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.callSectionName = error.localizedDescription
            }
            return self.callSectionName
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.locationArray = partyArray
            
            guard var value = self.locationArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.beforeDictionary = tableDictionary
            
            guard var value = self.beforeDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.addSubview(pathLet)
            }

	}

    //: override func layoutSubviews() {
    override func layoutSubviews() {
        //: super.layoutSubviews()
        super.layoutSubviews()
    
            if (self.isExclusiveTouch) && (self.layer.position.y == 96.45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let pathLet = CleanView(frame: self.bounds.standardized)


            
            pathLet.errorViewDoing = { [self] viewSwitch in
            self.spectaclesEnable = viewSwitch
            
            return self.spectaclesEnable
            }
            pathLet.routeTotal = { [self] titleSum in
            self.inviteTotal = titleSum
            
            return self.inviteTotal
            }
            pathLet.topPriceInterval = { [self] arrayMagnitude in
            self.birthdaySum = arrayMagnitude
            
            return self.birthdaySum
            }
            pathLet.harvestMoonTitle = { [self] replyName in
            self.cervixName = replyName
            
            do {
                self.cervixName = try String(contentsOf: NSURL.fileURL(withPathComponents: ["temp", "year", "plist"])!, encoding: .utf8)
            } catch {
                self.cervixName = error.localizedDescription
            }
            return self.cervixName
            }
            pathLet.bestowmentArray = { [self] partyArray in
            self.viewArray = partyArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
            pathLet.canDictionary = { [self] tableDictionary in
            self.labVideoDictionary = tableDictionary
            
            guard var value = self.labVideoDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.addSubview(pathLet)
            }

	}

    // MARK: - Lazy load

    //: lazy var IconBtn: UIButton = {
    lazy var IconBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 34/2
        btn.layer.cornerRadius = 34 / 2
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var nameLB: UILabel = {
    lazy var nameLB: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 14)
        label.font = .thoughtImage(type: .Regular, fontSize: 14)
        //: label.textColor = UIColor.appValueColor()
        label.textColor = UIColor.complexion()
        //: return label
        return label
        //: }()
    }()
}

//: extension DropMemberMenuCell {
extension StartOutMenuCell {
    //: func setDropMemberMenuCell(model: TalkingChatRoomMemberModel) {
    func superposeWithModel(model: RegularThen) {
        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(str_roomData.suffix(5)) + "umbe" + str_topName.lowercased()).localized {
            //: IconBtn.setImage(UIImage.outsideText(name: model.headPic), for: .normal)
            IconBtn.setImage(UIImage.outsideText(name: model.headPic), for: .normal)
            //: }else {
        } else {
            //: IconBtn.setUrlImage(urlStr: model.headPic)
            IconBtn.fulfill(urlStr: model.headPic)
        }

        //: nameLB.text = model.nickname
        nameLB.text = model.nickname
    }
}

//: extension DropMemberMenuCell {
extension StartOutMenuCell {
    //: private func setupUI() {
    private func pullOf() {
        //: backgroundColor = .white
        backgroundColor = .white
        //: addSubview(IconBtn)
        addSubview(IconBtn)
        //: addSubview(nameLB)
        addSubview(nameLB)
    }

    //: func layoutSubViewsConstraints() {
    func clickConstraints() {
        //: IconBtn.snp.makeConstraints { make in
        IconBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(self).offset(3)
            make.leading.equalTo(self).offset(3)
            //: make.top.equalTo(self).offset(3)
            make.top.equalTo(self).offset(3)
            //: make.width.height.equalTo(34)
            make.width.height.equalTo(34)
        }

        //: nameLB.snp.makeConstraints { make in
        nameLB.snp.makeConstraints { make in
            //: make.leading.equalTo(IconBtn.snp.trailing).offset(6)
            make.leading.equalTo(IconBtn.snp.trailing).offset(6)
            //: make.trailing.equalTo(self).offset(-6)
            make.trailing.equalTo(self).offset(-6)
            //: make.centerY.equalTo(self)
            make.centerY.equalTo(self)
            //: make.height.height.equalTo(16)
            make.height.height.equalTo(16)
        }
    }
}
