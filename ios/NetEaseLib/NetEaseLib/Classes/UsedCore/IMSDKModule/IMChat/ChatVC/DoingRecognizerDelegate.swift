
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_mediumValue:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "price_image" :*/
fileprivate let str_modelData:String = "pricnow"
fileprivate let str_turnWindowHeightValue:[Character] = ["_","i","m","a","g","e"]

/*: "F5F5F5" :*/
fileprivate let str_centerText:[Character] = ["F","5","F","5","F"]
fileprivate let str_bottomName:String = "5"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_iconName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x","t","M","s","g"]
fileprivate let str_effectButtonText:String = "CellIDtrue count collection layer to"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_taskName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","I","m","a","g","e","M","s","g"]
fileprivate let str_matchData:String = "equal push view message withCellID"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_viewTitleData:String = "string true view roomTalki"
fileprivate let str_minData:String = "tVidecorner model"
fileprivate let str_searchName:String = "self viewllID"

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_modelColorViewData:String = "request frame right text topTalk"
fileprivate let str_topName:String = "user constraint error doubletGiftM"
fileprivate let str_stemBackData:String = "sgCelength detail return let at"

/*: "UserUpMsgCell" :*/
fileprivate let str_imageText:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s","k"]
fileprivate let str_loadContent:String = "user video data makeForGi"
fileprivate let str_databaseValue:String = "view touch var wrapftMsgCell"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_pageUpCenterName:String = "image make manager selfTalki"
fileprivate let str_viewText:String = "bar truetTipsMsg"
fileprivate let str_aspectListValue:String = "normal"
fileprivate let str_voiceValue:[Character] = ["e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_aboveDayContent:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M","a","t","c","h","M","s","g"]
fileprivate let str_engineGiftName:[Character] = ["C","e","l","l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_makeViewName:String = "Talkimodel make name"
fileprivate let str_locationName:String = "if price imageatAud"
fileprivate let str_videoKitName:[Character] = ["i","o","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_explainData:[UInt8] = [0x54,0x61,0x6c,0x6b,0x69,0x6e,0x67,0x43,0x68,0x61,0x74,0x53,0x79,0x73,0x74,0x65,0x6d,0x4e,0x6f,0x74,0x69,0x66,0x4a,0x75,0x6d,0x70,0x4d,0x73,0x67,0x43,0x65,0x6c,0x6c,0x49,0x44]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_toValue:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C","e","l","l","_","R"]
fileprivate let str_fillData:String = "euseIdadd float"

/*: "#128CFF" :*/
fileprivate let str_blockValue:String = "#button"
fileprivate let str_playerValue:String = "28CFFreturn cancel fatal"

/*: "Feedback" :*/
fileprivate let str_frameData:String = "view string self layer appFe"
fileprivate let str_imageSourceName:[Character] = ["e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let str_modelValue:String = "#8370Ftap make view"
fileprivate let str_viewIndexIfValue:[Character] = ["F"]

/*: "Quick Greeting" :*/
fileprivate let str_whiteValue:String = "time type talk string selfQuick"
fileprivate let str_videoName:[Character] = ["e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let str_voiceTitle:String = "view view ifBeau"
fileprivate let str_blueValue:String = "sectionos"

/*: "url" :*/
fileprivate let str_imageContent:String = "tol"

/*: "unlockGift" :*/
fileprivate let str_stackToolContent:String = "UNLOCK"

/*: "%d" :*/
fileprivate let str_directionValue:String = "any will empty manager page%d"

/*: "isBurn" :*/
fileprivate let str_liveValue:[UInt8] = [0x69,0x73,0x42,0x75,0x72,0x6e]

/*: "giftId" :*/
fileprivate let str_hiddenContent:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let str_tempTitle:String = "fatal title content resultrYMsgTy"
fileprivate let str_myData:String = "PE"

/*: "GJ:CallCustom" :*/
fileprivate let str_imageAllCornerValue:String = "value modeGJ:CallCu"
fileprivate let str_centerPhoneData:String = "removetom"

/*: "changeDirection" :*/
fileprivate let str_kitData:String = "playerange"
fileprivate let str_timeValue:[Character] = ["i","o","n"]

/*: "extra" :*/
fileprivate let str_draftText:String = "containertra"

/*: "msgInfo" :*/
fileprivate let str_saveValue:String = "msgInshadow image color manager of"
fileprivate let str_colorTitle:[Character] = ["f","o"]

/*: "toUid" :*/
fileprivate let str_soundTitle:String = "self modeltoUid"

/*: "wantGift" :*/
fileprivate let str_addLocationName:String = "guard sizewantGi"
fileprivate let str_errorContent:String = "FT"

/*: "jumpKey" :*/
fileprivate let str_fieldValue:String = "jumpKsign black center equal"
fileprivate let str_domainValue:[Character] = ["e","y"]

/*: "truePersonAuth" :*/
fileprivate let str_managerTitle:[Character] = ["t","r","u","e","P","e","r","s","o","n","A","u","t","h"]

/*: "logType" :*/
fileprivate let str_priceData:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let str_leadingName:[UInt8] = [0x24,0x3a,0x2e,0x1d,0x30,0x39,0x2c]

/*: "txt" :*/
fileprivate let str_colorValue:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let str_controlMakeValue:String = "typathe"

/*: "QuickMsg" :*/
fileprivate let str_colorData:[Character] = ["Q","u","i"]
fileprivate let str_gameAddTitle:[Character] = ["c","k","M","s","g"]

/*: "params" :*/
fileprivate let str_windowCenterValue:[UInt8] = [0x70,0x61,0x72,0x61,0x6d,0x73]

/*: "content" :*/
fileprivate let str_myValue:[UInt8] = [0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74]

/*: "id" :*/
fileprivate let str_hiddenName:String = "message"

/*: "txtId" :*/
fileprivate let str_makeSendName:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let str_labelTaskValue:[UInt8] = [0x6d,0x65,0x73,0x73,0x61,0x67,0x65]

/*: "TxtMsg" :*/
fileprivate let str_showSocialValue:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_playerName:[UInt8] = [0x56,0x6f,0x69,0x63,0x65,0x20,0x68,0x65,0x6c,0x6c,0x6f,0x20,0x64,0x6f,0x77,0x6e,0x6c,0x6f,0x61,0x64,0x20,0x66,0x61,0x69,0x6c,0x65,0x64,0x2e,0x20,0x50,0x72,0x6f,0x63,0x65,0x64,0x75,0x72,0x65]

/*: "length" :*/
fileprivate let str_downTitle:String = "lengtmessage"

/*: "audio" :*/
fileprivate let str_sizeCellValue:String = "ainsidedio"

/*: "audioId" :*/
fileprivate let str_giftTopValue:String = "view sendaudioId"

/*: "audioLength" :*/
fileprivate let str_timeData:[UInt8] = [0x61,0x75,0x64,0x69,0x6f,0x4c,0x65,0x6e,0x67,0x74,0x68]

/*: "audioSandbox" :*/
fileprivate let str_directionData:[Character] = ["a","u","d","i","o","S","a","n","d"]
fileprivate let str_customName:[Character] = ["b","o","x"]

/*: "senderId" :*/
fileprivate let str_infoData:[UInt8] = [0x73,0x65,0x6e,0x64,0x65,0x72,0x49,0x64]

/*: "AudioMsg" :*/
fileprivate let str_requestValue:String = "Audiotext self"
fileprivate let str_applicationData:String = "Msgbackground space if place"

/*: "Can't send blank message" :*/
fileprivate let str_appAcceptViewData:String = "Can\'"
fileprivate let str_viewData:String = "block para up load bla"
fileprivate let str_aspectData:[Character] = ["n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let str_backgroundValue:[Character] = ["q","u","o","t","e","M"]
fileprivate let str_giftValue:String = "cell render frame heightsgId"

/*: "quoteMsgInfo" :*/
fileprivate let str_neverData:String = "nextu"
fileprivate let str_makeTimeAppName:String = "oteMscon extension center play"

/*: "contentType" :*/
fileprivate let str_valueText:[UInt8] = [0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x54,0x79,0x70,0x65]

/*: "ImgMsg" :*/
fileprivate let str_shareName:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let str_effectName:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_colorName:[UInt8] = [0x69,0x6d,0x61,0x67,0x65,0x55,0x72,0x69]

/*: "customSend" :*/
fileprivate let str_equalValue:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let str_addPathText:String = "pilab"

/*: "VideoMsg" :*/
fileprivate let str_willData:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let str_modelShareTitle:String = "indexideo"

/*: "coverImg" :*/
fileprivate let str_mediumReValue:[UInt8] = [0x63,0x6f,0x76,0x65,0x72,0x49,0x6d,0x67]

/*: "videoUrl" :*/
fileprivate let str_positionTextValue:[UInt8] = [0x76,0x69,0x64,0x65,0x6f,0x55,0x72,0x6c]

/*: "duration" :*/
fileprivate let str_frameTitle:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let str_toName:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let str_managerName:String = "Documecenter height every"
fileprivate let str_editName:String = "true frame register toser/"

/*: "Documents/User/record/" :*/
fileprivate let str_styleText:String = "view show make true appDocum"
fileprivate let str_infoContent:String = "self import editents/"
fileprivate let str_toData:String = "/record/view equal self height text"

/*: "msgIncome" :*/
fileprivate let str_makeValue:String = "M"
fileprivate let str_televisionTitle:String = "bean name object promptsgIncome"

/*: "mfBean" :*/
fileprivate let str_coinCountTitle:[UInt8] = [0x6d,0x66,0x42,0x65,0x61,0x6e]

/*: "messageType" :*/
fileprivate let str_positionValue:String = "mecirclecircleage"
fileprivate let str_tableValue:[Character] = ["T","y","p","e"]

/*: "WantGift" :*/
fileprivate let str_textData:String = "total next returnWantGift"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_itemName:String = "center intimate raw_Pri"
fileprivate let str_labData:String = "ChatTall share"
fileprivate let str_commentTitle:[Character] = ["e","_"]

/*: "msgId" :*/
fileprivate let str_makeTitle:String = "equal cell list class heartmsgId"

/*: "totalMfCoin" :*/
fileprivate let str_frameName:[Character] = ["t","o","t","a","l"]
fileprivate let str_proContent:String = "MfCoinself version catch name to"

/*: "attachSysMsg" :*/
fileprivate let str_infoName:String = "videotac"
fileprivate let str_targetValue:[Character] = ["h","S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let str_buttonMessageName:String = "intimstart"
fileprivate let str_equalData:String = "titleo"

/*: _ :*/
fileprivate let str_hireData:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let str_clearName:[Character] = ["t","o","t","a","l"]
fileprivate let str_detailTitle:[Character] = ["I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let str_systemData:[UInt8] = [0x64,0x69,0x75]

/*: "intimate" :*/
fileprivate let str_secureContent:[UInt8] = [0x2e,0x29,0x33,0x2e,0x2a,0x26,0x33,0x22]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_managerVideoValue:[UInt8] = [0x43,0x61,0x6e,0x27,0x74,0x20,0x75,0x73,0x65,0x20,0x74,0x68,0x65,0x20,0x66,0x75,0x6e,0x63,0x74,0x69,0x6f,0x6e,0x20,0x64,0x75,0x65,0x20,0x74,0x6f,0x20,0x79,0x6f,0x75,0x72,0x20,0x6c,0x61,0x63,0x6b,0x20,0x6f,0x66,0x20,0x69,0x6e,0x74,0x69,0x6d,0x61,0x63,0x79,0x20,0x6c,0x65,0x76,0x65,0x6c,0x2e]

/*: "giftNum" :*/
fileprivate let str_taskData:String = "giftNumtime mode"

/*: "pkgItemsetId" :*/
fileprivate let str_nameShareText:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let str_managerLinkTitle:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let str_emptyTableText:[Character] = ["w","a","n","t","G","i","f"]
fileprivate let str_addValue:String = "with year all ontId"

/*: "WantGiftMsg" :*/
fileprivate let str_rangeMediumTitle:[Character] = ["W","a","n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let str_intimateData:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let str_smallText:String = "tab"
fileprivate let str_actionData:String = "model returniew "

/*: "Notes" :*/
fileprivate let str_removeTitle:String = "block manager sourceNotes"

/*: "Report" :*/
fileprivate let str_timeColorData:String = "Reporcenter self name"
fileprivate let str_emptyText:String = "manager"

/*: "View read receipts?" :*/
fileprivate let str_detailValue:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "He" :*/
fileprivate let str_titleRegularText:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let str_ofData:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let str_valueTitle:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t","o","o"," ","l","o","w"," ","a"," ","v","e"]
fileprivate let str_effectValue:[Character] = ["r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_clearNameData:String = "route scale var self listShoot"
fileprivate let str_viewValue:[Character] = [" ","a","t"," ","l","e","a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let str_episodeData:String = "input modelUpload"
fileprivate let str_selectionContentName:[Character] = ["e","d"]

/*: "播放状态: :*/
fileprivate let str_backgroundData:[Character] = ["播","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let str_lengthName:String = "status following place log sourcec2c_"

/*: "msg" :*/
fileprivate let str_stageData:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_textValue:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_styleTitle:[UInt8] = [0x7b,0x4d,0x57,0x2,0x41,0x43,0x4c,0x2,0x57,0x51,0x47,0x2,0x56,0x4a,0x47,0x2,0x54,0x4b,0x46,0x47,0x4d,0x2,0x41,0x4a,0x43,0x56,0x2,0x44,0x47,0x43,0x56,0x57,0x50,0x47,0x2,0x56,0x4d,0x2,0x45,0x4b,0x54,0x47,0x2,0x7,0x62,0x2,0x43,0x2,0x41,0x43,0x4e,0x4e]

/*: "cellData" :*/
fileprivate let str_capacityName:[Character] = ["c","e","l"]
fileprivate let str_coverData:String = "for datelData"

/*: "System notification" :*/
fileprivate let str_plusScopeContent:String = "female makeSystem"
fileprivate let str_addRedName:String = " notiguard equal"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DoingRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum UpGoldFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class DoingRecognizerDelegate: PropertyViewController {
	var writeTotal: Double = -30.3
	var beginVideoName: String = "field"
	var colorNumber: Double = 75.9
	var imageName: String = "generate"
	var streetSmartCount: Double = -62.2
	var showTitle: String = "selected"

    var reloadImageView: UIImageView?

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = CountMediumCacheModel()
    private var userWrap = CountMediumCacheModel() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = UpGoldFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: CountMediumCacheModel = CountMediumCacheModel()) {
    init(chatID: String, isFrom: UpGoldFromEnum = .Normal, userWrap: CountMediumCacheModel = CountMediumCacheModel()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        DoingRecognizerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        PrivateThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == ImageMacroDefine.getXiaoMiID() {
        if targetId == ImageMacroDefine.dataFormatting() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == ImageMacroDefine.getCustomerServiceID() {
        } else if targetId == ImageMacroDefine.executeByTitle() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_mediumValue, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MeasurementAppManager.share.appConfigMode.disableShootScreen {
            if MeasurementAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HypsometryReactiveCompatible(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
                self.reloadImageView?.image = UIImage(named: (str_modelData.replacingOccurrences(of: "now", with: "e") + String(str_turnWindowHeightValue)))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        actionAnimation()
        //: createUI()
        towardTable()
        //: createUIConstraints()
        imageConstraints()
        //: bindInteraction()
        attach()
        //: addTapGestureRecognizer()
        moveRecognizer()
        //: func__requestChatInfo()
        warpath()
        //: func__getFillDraftText()
        draftTitle()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SolarDayReactiveCompatible.shared.shrugOff()
        //: func__addVoiceNotification()
        halt()
        //: addReadPeceiptNotification()
        skimErase()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            dataSub()
            //: func__TipsNotification()
            carryingOut()
            //: if MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue {
                //: func__setReplyTipsView()
                databaseView()
            }
        }

        reloadImageView = UIImageView(frame: self.view.frame.insetBy(dx: CGFloat(Int(self.view.center.y)), dy: CGFloat(0)))
        if let reloadImageView = reloadImageView {
            self.reloadImageView?.image = UIImage(named: (str_modelData.replacingOccurrences(of: "now", with: "e") + String(str_turnWindowHeightValue)))
            if (reloadImageView.layer.contentsRect.origin.y != 0) && (reloadImageView.preservesSuperviewLayoutMargins) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(reloadImageView)
            }
        }
    
            if (notesTopView.layer.anchorPoint.y != 0.5) && (notesTopView.motionEffects.count == 12) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let successNumber = TalkView(frame: notesTopView.frame.union(CGRect(x: CGFloat(457.47), y: CGFloat(384.65), width: CGFloat(0), height: CGFloat(103.50))))
            successNumber.imageAcrossClose = isHaveSession
            successNumber.priceSumSum = msgInputView.inputTextView.placeholderLeftOffset
            
            successNumber.blockDataNumber = { [self] imaginationImageTotal in
            self.streetSmartCount = imaginationImageTotal
            
            var successNumber = msgInputView.giftView.contentHeight
                successNumber += 1
                if successNumber != 75 {
                    successNumber = successNumber - 1
                }
            if successNumber < self.streetSmartCount {
                self.streetSmartCount = successNumber
            }
            
                self.streetSmartCount += 1
                if self.streetSmartCount < 39 {
                    self.streetSmartCount = self.streetSmartCount - 1
                }
            return self.streetSmartCount
            }
            successNumber.commentGiftName = { [self] dataServiceContent in
            self.showTitle = dataServiceContent
            
            var successNumber = targetId
            let scan = successNumber.prefix(through: successNumber.index(successNumber.startIndex, offsetBy: (successNumber.dropFirst().isEmpty ? 4 : 0))).isEmpty
            successNumber.removeAll(keepingCapacity: scan)
            if successNumber.hasPrefix(self.showTitle) {
                self.showTitle = successNumber
            }
            
            if let margin = self.showTitle.last {
                if self.showTitle.dropFirst(5).contains(margin) {
                    NotificationCenter.default.post(Notification(name: NSNotification.Name("choice"), object: self.showTitle.hasPrefix(self.showTitle.lowercased() + "position")))
                }
            }
            return self.showTitle
            }
                notesTopView.addSubview(successNumber)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        chorusLine(isAdd: true)
    
		if var timeValue = videoCallManager.uid { 
	            if (beautyPhotosBtn.layer.anchorPoint.y != 0.5) && (beautyPhotosBtn.motionEffects.count == 12) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let successNumber = TalkView(frame: beautyPhotosBtn.frame.union(CGRect(x: CGFloat(457.47), y: CGFloat(384.65), width: CGFloat(0), height: CGFloat(103.50))))
	            successNumber.imageAcrossClose = isShowIntimateView
	            successNumber.priceSumSum = msgInputView.inputTextView.placeholderLeftOffset
	            
	            successNumber.blockDataNumber = { [self] imaginationImageTotal in
	            self.writeTotal = imaginationImageTotal
	            
	            var successNumber = msgInputView.giftView.contentHeight
	                successNumber += 1
	                if successNumber != 75 {
	                    successNumber = successNumber - 1
	                }
	            if successNumber < self.writeTotal {
	                self.writeTotal = successNumber
	            }
	            
	                self.writeTotal += 1
	                if self.writeTotal < 39 {
	                    self.writeTotal = self.writeTotal - 1
	                }
	            return self.writeTotal
	            }
	            successNumber.commentGiftName = { [self] dataServiceContent in
	            self.beginVideoName = dataServiceContent
	            
	            var successNumber = timeValue
	            let scan = successNumber.prefix(through: successNumber.index(successNumber.startIndex, offsetBy: (successNumber.dropFirst().isEmpty ? 4 : 0))).isEmpty
	            successNumber.removeAll(keepingCapacity: scan)
	            if successNumber.hasPrefix(self.beginVideoName) {
	                self.beginVideoName = successNumber
	            }
	            
	            if let margin = self.beginVideoName.last {
	                if self.beginVideoName.dropFirst(5).contains(margin) {
	                    NotificationCenter.default.post(Notification(name: NSNotification.Name("choice"), object: nil, userInfo: ["should": self.beginVideoName.hasPrefix(self.beginVideoName.lowercased() + "position")]))
	                }
	            }
	            return self.beginVideoName
	            }
	                beautyPhotosBtn.addSubview(successNumber)
	            }
	
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        chorusLine(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            compose()
        }
    
            if (coverView.tintAdjustmentMode == .dimmed) && (coverView.window != nil && coverView.window!.windowLevel == .statusBar) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let picGift = TalkView()
            picGift.imageAcrossClose = giftEffectView.giftAnimatView.needStopAll
            picGift.priceSumSum = msgInputView.inputTextView.placeholderLeftOffset
            
            picGift.blockDataNumber = { [self] imaginationImageTotal in
            self.colorNumber = imaginationImageTotal
            
            var picGift = msgInputView.inputTextView.placeholderLeftOffset
                picGift += 1
                if picGift >= 0 {
                    picGift = picGift - 1
                }
            if picGift < self.colorNumber {
                self.colorNumber = picGift
            }
            
            self.colorNumber = 0
            return self.colorNumber
            }
            picGift.commentGiftName = { [self] dataServiceContent in
            self.imageName = dataServiceContent
            
            var picGift = userWrap.constellation
            picGift.reserveCapacity(picGift.reversed().count + (picGift.isEmpty ? 5 : 0))
            if picGift.hasPrefix(self.imageName) {
                self.imageName = picGift
            }
            
            var will: String.Encoding = .utf8
            if let willString = try? String(contentsOfFile: self.imageName.uppercased() + "state", usedEncoding: &will) {
                self.imageName = willString
            }
            return self.imageName
            }
                coverView.addSubview(picGift)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(str_centerText) + str_bottomName.capitalized))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(DateMsgCell.self, forCellReuseIdentifier: (String(str_iconName) + String(str_effectButtonText.prefix(6))))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(MentalRepresentationView.self, forCellReuseIdentifier: (String(str_taskName) + String(str_matchData.suffix(6))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(LayerEnableThen.self, forCellReuseIdentifier: (String(str_viewTitleData.suffix(5)) + "ngCha" + String(str_minData.prefix(5)) + "oMsgCe" + String(str_searchName.suffix(4))))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(AtLeadingMessageCell.self, forCellReuseIdentifier: (String(str_modelColorViewData.suffix(4)) + "ingCha" + String(str_topName.suffix(6)) + String(str_stemBackData.prefix(4)) + "llID"))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(UserUpMsgCell.self, forCellReuseIdentifier: (String(str_imageText) + String(str_loadContent.suffix(5)) + String(str_databaseValue.suffix(9))))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(LabView.self, forCellReuseIdentifier: (String(str_pageUpCenterName.suffix(5)) + "ngCha" + String(str_viewText.suffix(8)) + str_aspectListValue.replacingOccurrences(of: "normal", with: "C") + String(str_voiceValue)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(ActualReactiveCompatible.self, forCellReuseIdentifier: (String(str_aboveDayContent) + String(str_engineGiftName)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(InstrumentationView.self, forCellReuseIdentifier: (String(str_makeViewName.prefix(5)) + "ngCh" + String(str_locationName.suffix(5)) + String(str_videoKitName)))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(CellView.self, forCellReuseIdentifier: String(bytes: str_explainData, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_toValue) + String(str_fillData.prefix(6))))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: LaterErrorDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = LaterErrorDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: AlbumThen = {
        //: var type: InputViewType = .PrivateChat
        var type: RegularViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = AlbumThen(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (str_blockValue.replacingOccurrences(of: "button", with: "1") + String(str_playerValue.prefix(5))))!, .font: UIFont.thoughtImage(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_frameData.suffix(2)) + String(str_imageSourceName)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(frame), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: EquidistantThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = EquidistantThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: RegularLimitThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = RegularLimitThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_modelValue.prefix(6)) + String(str_viewIndexIfValue))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_whiteValue.suffix(5)) + " Gre" + String(str_videoName)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.offt().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sendTo), for: .touchUpInside)
        //: btn.isHidden = !MeasurementAppManager.share.appUserConfigMode.showDirectGreet || MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue || MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !MeasurementAppManager.share.appUserConfigMode.showDirectGreet || MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue || MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: LayerLabView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = LayerLabView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: TextReactiveCompatible = {
        //: let v = TalkingSelectTranslationView()
        let v = TextReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.vanguard(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ToCloseResultView = {
        //: let v = TalkingTranslationResultView()
        let v = ToCloseResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.nameModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_modelValue.prefix(6)) + String(str_viewIndexIfValue))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_voiceTitle.suffix(4)) + "ty Ph" + str_blueValue.replacingOccurrences(of: "section", with: "ot")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.offt().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(beauty), for: .touchUpInside)
        //: btn.isHidden = MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue || MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue || MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: DocumentVideoReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = DocumentVideoReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: ObjectBannerDelegate = {
        //: let view = TalkingPrivateBannerView.init()
        let view = ObjectBannerDelegate()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PositionView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PositionView()
        //: v.isHidden = (MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: PathTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = PathTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.addFinish()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension DoingRecognizerDelegate: AddBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func field(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.equalChannel(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension DoingRecognizerDelegate: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func sendTo() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        PrivateThen.statisticalTable(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.episode(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.petition(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func beauty() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        PrivateThen.statusBy(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = BeginViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().posit(urlStr: dict?[(str_imageContent.replacingOccurrences(of: "to", with: "ur"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(str_stackToolContent.lowercased() + "Gift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = GlassesModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_liveValue, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_hiddenContent))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.enterAcrossModel(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ConcordCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_tempTitle.suffix(7)) + str_myData.lowercased())].stringValue == (String(str_imageAllCornerValue.suffix(9)) + str_centerPhoneData.replacingOccurrences(of: "remove", with: "s")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(str_kitData.replacingOccurrences(of: "player", with: "ch") + "Direct" + String(str_timeValue))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(str_draftText.replacingOccurrences(of: "container", with: "ex"))][(String(str_saveValue.prefix(5)) + String(str_colorTitle))][(String(str_soundTitle.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = OfCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = OfCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: ValueViewReactiveCompatible.self) == true || cellData.isKind(of: DocumentCellData.self) == true || cellData.isKind(of: OfCellData.self) == true || cellData.isKind(of: ToCellData.self) == true {
        if cellData.isKind(of: ValueViewReactiveCompatible.self) == true || cellData.isKind(of: DocumentCellData.self) == true || cellData.isKind(of: OfCellData.self) == true || cellData.isKind(of: ToCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        PrivateThen.userState(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        PrivateThen.giveAndTake(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = glossyData(data: data)

        //: if data.isKind(of: OfCellData.self) {
        if data.isKind(of: OfCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = DateMsgCell(style: .default, reuseIdentifier: (String(str_iconName) + String(str_effectButtonText.prefix(6))))
            //: let cellData = data as! OfCellData
            let cellData = data as! OfCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.originMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == MeasurementAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == MeasurementAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = MeasurementAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = MeasurementAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: DocumentCellData.self) {
        if data.isKind(of: DocumentCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = MentalRepresentationView(style: .default, reuseIdentifier: (String(str_taskName) + String(str_matchData.suffix(6))))
            //: let cellData = data as! DocumentCellData
            let cellData = data as! DocumentCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ToCellData.self) {
        if data.isKind(of: ToCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = LayerEnableThen(style: .default, reuseIdentifier: (String(str_viewTitleData.suffix(5)) + "ngCha" + String(str_minData.prefix(5)) + "oMsgCe" + String(str_searchName.suffix(4))))
            //: let cellData = data as! ToCellData
            let cellData = data as! ToCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ConstraintReactiveCompatible.self) {
        if data.isKind(of: ConstraintReactiveCompatible.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = AtLeadingMessageCell(style: .default, reuseIdentifier: (String(str_modelColorViewData.suffix(4)) + "ingCha" + String(str_topName.suffix(6)) + String(str_stemBackData.prefix(4)) + "llID"))
            //: let cellData = data as! ConstraintReactiveCompatible
            let cellData = data as! ConstraintReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ChatValueCellData.self) {
        if data.isKind(of: ChatValueCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = UserUpMsgCell(style: .default, reuseIdentifier: (String(str_imageText) + String(str_loadContent.suffix(5)) + String(str_databaseValue.suffix(9))))
            //: let cellData = data as! ChatValueCellData
            let cellData = data as! ChatValueCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = WithMeasurable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.plainspoken(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_addLocationName.suffix(6)) + str_errorContent.lowercased()))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: ValueViewReactiveCompatible.self) {
        if data.isKind(of: ValueViewReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = InstrumentationView(style: .default, reuseIdentifier: (String(str_makeViewName.prefix(5)) + "ngCh" + String(str_locationName.suffix(5)) + String(str_videoKitName)))
            //: let cellData = data as! ValueViewReactiveCompatible
            let cellData = data as! ValueViewReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: ConcordCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = ActualReactiveCompatible(style: .default, reuseIdentifier: (String(str_aboveDayContent) + String(str_engineGiftName)))
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! ConcordCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: LabOffThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = LabView(style: .default, reuseIdentifier: (String(str_pageUpCenterName.suffix(5)) + "ngCha" + String(str_viewText.suffix(8)) + str_aspectListValue.replacingOccurrences(of: "normal", with: "C") + String(str_voiceValue)))
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! LabOffThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: SpringCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = CellView(style: .default, reuseIdentifier: String(bytes: str_explainData, encoding: .utf8)!)
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! SpringCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != ImageMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != ImageMacroDefine.executeByTitle() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: LimitPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            LimitPushManager.share.funcToStuffDetailUtilizerUid(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: MentalRepresentationView.self) { // 图片
            //: let imageCellData = cell.messageData as! DocumentCellData
            let imageCellData = cell.messageData as! DocumentCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = PhotoPreviewVc(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.createEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: a_screenName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.noDedication(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.lastDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                isochronalStack()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(a_cancelName)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = GoldObjectMeasurable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = CustomMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [GoldObjectMeasurable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = CellViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                isochronalStack()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: LayerEnableThen.self) { // 视频
            //: let videoCellData = cell.messageData as! ToCellData
            let videoCellData = cell.messageData as! ToCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = LargePlayerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                isochronalStack()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = PropertyThen(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.createEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: a_screenName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.noDedication(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.lastDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                isochronalStack()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: InstrumentationView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! InstrumentationView
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! ValueViewReactiveCompatible, cellView: audioCell)
            toOf(cellData: audioCell.cellData as! ValueViewReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DateMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DateMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_draftText.replacingOccurrences(of: "container", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_fieldValue.prefix(5)) + String(str_domainValue))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_fieldValue.prefix(5)) + String(str_domainValue))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(str_managerTitle)) {
                    //: if MeasurementAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if MeasurementAppManager.share.loginUserMode.isTPAuth == LaterUserValueConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if MeasurementAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if MeasurementAppManager.share.loginUserMode.isTPAuth == LaterUserValueConvertible.unknown.rawValue {
                        //: LimitPushManager.share.func__pushUserVerifyController(toast: nil)
                        LimitPushManager.share.invitationTo(toast: nil)
                        //: } else if MeasurementAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if MeasurementAppManager.share.loginUserMode.isTPAuth == LaterUserValueConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = ManagerVerificationVc()
                        //: LimitPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        LimitPushManager.share.beforeRoom()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: LimitPushManager.share.func__pushUserVerifyController(toast: nil)
                        LimitPushManager.share.invitationTo(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<DataDateJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LimitPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LimitPushManager.share.reverence(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_tempTitle.suffix(7)) + str_myData.lowercased())] as? String == (String(str_imageAllCornerValue.suffix(9)) + str_centerPhoneData.replacingOccurrences(of: "remove", with: "s")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_saveValue.prefix(5)) + String(str_colorTitle))][(String(str_priceData))].intValue == 1 {
                    //: checkAndCallVoice()
                    cellVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    dispose()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: LabView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! LabView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_draftText.replacingOccurrences(of: "container", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_fieldValue.prefix(5)) + String(str_domainValue))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<DataDateJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LimitPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LimitPushManager.share.reverence(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: ActualReactiveCompatible.self) {
            //: LimitPushManager.share.func__pushToUserDetailVC(uid: targetId)
            LimitPushManager.share.funcToStuffDetailUtilizerUid(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: AppCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: MentalRepresentationView.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! MentalRepresentationView
            //: cellData = imageCell.cellData as! DocumentCellData
            cellData = imageCell.cellData as! DocumentCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: LayerEnableThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! LayerEnableThen
            //: cellData = videoCell.cellData as! ToCellData
            cellData = videoCell.cellData as! ToCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: InstrumentationView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! InstrumentationView
            //: cellData = audioCell.cellData as! ValueViewReactiveCompatible
            cellData = audioCell.cellData as! ValueViewReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DateMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DateMsgCell
            //: cellData = textCell.cellData as! OfCellData
            cellData = textCell.cellData as! OfCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: ToCellData.self) {
                if reCellData!.isKind(of: ToCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    totalRoom(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    untilField(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = PrivateThen.theme(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        PrivateThen.playerMenu(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func glossyData(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(MeasurementAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(MeasurementAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if MeasurementAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if MeasurementAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = MeasurementAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = MeasurementAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = MeasurementAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = MeasurementAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func quick() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_colorValue))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_colorData) + String(str_gameAddTitle))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.untilField(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func petition(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_myValue, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_myValue, encoding: .utf8)!: content, (String(str_makeSendName)): greetId, String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_colorValue))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_labelTaskValue, encoding: .utf8)!: content, (String(str_makeSendName)): greetId, (str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_showSocialValue))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.untilField(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            LabelObjectProtocol.shared.themeWith(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.episode(showMsg: String(bytes: str_playerName, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        PrivateThen.sizeCurrent(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(str_downTitle.replacingOccurrences(of: "message", with: "h"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (str_sizeCellValue.replacingOccurrences(of: "inside", with: "u")),
                               //: "audioId": greetId,
                               (String(str_giftTopValue.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: str_timeData, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(str_directionData) + String(str_customName)): model.unZipFilePath,
                               //: "senderId": MeasurementAppManager.share.loginUserMode.userID]
                               String(bytes: str_infoData, encoding: .utf8)!: MeasurementAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(str_giftTopValue.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_timeData, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(str_directionData) + String(str_customName)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_requestValue.prefix(5)) + String(str_applicationData.prefix(3)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.untilField(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func nameModel(msgStr: String, quoteModel: IsochronalModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            episode(showMsg: (str_appAcceptViewData + "t send" + String(str_viewData.suffix(4)) + String(str_aspectData)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_myValue, encoding: .utf8)!: temStr, String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_colorValue))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_labelTaskValue, encoding: .utf8)!: temStr, (str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_showSocialValue))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(str_backgroundValue) + String(str_giftValue.suffix(4)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(str_neverData.replacingOccurrences(of: "next", with: "q") + String(str_makeTimeAppName.prefix(5)) + "gInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_windowCenterValue, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? IsochronalModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        untilField(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func enterAcrossModel(img: UIImage, sendModel: GlassesModel) {
        //: let imagePath = DocumentCellData.getChatImagePath(image: img)
        let imagePath = DocumentCellData.everyTargetShow(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_valueText, encoding: .utf8)!: (String(str_shareName)), String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_effectName)), String(bytes: str_colorName, encoding: .utf8)!: imagePath, String(bytes: str_equalValue.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(str_addPathText.replacingOccurrences(of: "lab", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_shareName)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_liveValue, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_hiddenContent)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        untilField(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func totalRoom(msgData: TUIMessageCellData?, sendModel: GlassesModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_valueText, encoding: .utf8)!: (String(str_willData)), String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (str_modelShareTitle.replacingOccurrences(of: "index", with: "v")), String(bytes: str_equalValue.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_willData)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_mediumReValue, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_positionTextValue, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_frameTitle.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_liveValue, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_hiddenContent)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.groupName(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.groupName(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        untilField(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func phonation(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (str_sizeCellValue.replacingOccurrences(of: "inside", with: "u")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_timeData, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(str_directionData) + String(str_customName)): voicePath,
                       //: "senderId": MeasurementAppManager.share.loginUserMode.userID]
                       String(bytes: str_infoData, encoding: .utf8)!: MeasurementAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(str_directionData) + String(str_customName)): voicePath,
                         //: "type": "AudioMsg",
                         (str_controlMakeValue.replacingOccurrences(of: "path", with: "p")): (String(str_requestValue.prefix(5)) + String(str_applicationData.prefix(3))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_toName.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        untilField(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func untilField(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            episode(showMsg: a_userText)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_soundTitle.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: ToCellData.self) == false && msgData.isKind(of: ChatValueCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: ToCellData.self) == false, msgData.isKind(of: ChatValueCellData.self) == false, params[(str_controlMakeValue.replacingOccurrences(of: "path", with: "p"))] as? String != (String(str_colorData) + String(str_gameAddTitle)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((str_addPathText.replacingOccurrences(of: "lab", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(str_addPathText.replacingOccurrences(of: "lab", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(str_addPathText.replacingOccurrences(of: "lab", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(str_controlMakeValue.replacingOccurrences(of: "path", with: "p"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_requestValue.prefix(5)) + String(str_applicationData.prefix(3))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(str_directionData) + String(str_customName))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(str_directionData) + String(str_customName))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(str_directionData) + String(str_customName)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_managerName.prefix(6)) + "nts/U" + String(str_editName.suffix(4)) + "voice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = a_infoName + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_styleText.suffix(5)) + String(str_infoContent.suffix(5)) + "User" + String(str_toData.prefix(8)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = a_successSessionName + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(str_giftTopValue.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(str_sizeCellValue.replacingOccurrences(of: "inside", with: "u"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: DoingSheRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        DoingSheRequestTool.doingCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_colorData) + String(str_gameAddTitle)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.createEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: a_profileImageTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_makeSendName))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    PrivateThen.sizeCurrent(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_saveValue.prefix(5)) + String(str_colorTitle))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((str_makeValue.lowercased() + String(str_televisionTitle.suffix(8)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(str_makeValue.lowercased() + String(str_televisionTitle.suffix(8)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if PropertyMManageressPushListener.giftShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: a_videoDeviceData, object: nil, userInfo: [String(bytes: str_coinCountTitle, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.backCover(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(str_positionValue.replacingOccurrences(of: "circle", with: "s") + String(str_tableValue))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_colorData) + String(str_gameAddTitle)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!] = (String(str_textData.suffix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    a_textTurnName.photoOf(eventID: a_formatValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_colorData) + String(str_gameAddTitle)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!] = (String(str_colorValue))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(MeasurementAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(MeasurementAppManager.share.loginUserMode.userID)" + (String(str_itemName.suffix(4)) + "vate" + String(str_labData.prefix(5)) + "ipsTitl" + String(str_commentTitle)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue, type != (String(str_colorData) + String(str_gameAddTitle)) {
                //: self.removeReplyTipsView()
                self.lightPush()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_makeTitle.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            PrivateThen.giveAndTake(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_frameName) + String(str_proContent.prefix(6)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_frameName) + String(str_proContent.prefix(6)))] as! NSNumber
                //: MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((str_infoName.replacingOccurrences(of: "video", with: "at") + String(str_targetValue))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(str_infoName.replacingOccurrences(of: "video", with: "at") + String(str_targetValue))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.startTo(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((str_buttonMessageName.replacingOccurrences(of: "start", with: "a") + "tePho" + str_equalData.replacingOccurrences(of: "title", with: "t"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(str_buttonMessageName.replacingOccurrences(of: "start", with: "a") + "tePho" + str_equalData.replacingOccurrences(of: "title", with: "t"))] as! [String: Any]
                //: let model = InputTalkingModel(dic: intimatePhotoDict)
                let model = InputTalkingModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((str_modelShareTitle.replacingOccurrences(of: "index", with: "v"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(str_modelShareTitle.replacingOccurrences(of: "index", with: "v"))] as! [String: Any]
                //: let model = FullPhaseOfTheMoonJsonModel(dic: intimateVideoDict)
                let model = FullPhaseOfTheMoonJsonModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        modelEqual()

        //: updateSession()
        occurrence()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func occurrence() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        InfoReactiveCompatible.suit(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func createEvent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard MeasurementAppManager.share.loginUserMode.status != 1 else {
            guard MeasurementAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    episode(showMsg: errorStr)
                }
                //: return
                return
            }

            //: LimitPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            LimitPushManager.share.billParams(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.gameAcrossView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = UtmostView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
            //: view.show()
            view.studShow()
            //: } else if errorCode == 60003, !MeasurementAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !MeasurementAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ToViewWindowManager.shared.messagePlayer()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                episode(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func lastDict(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_saveValue.prefix(5)) + String(str_colorTitle))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.backCover(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func previous() {
        //: if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.modifyNo(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(MeasurementAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(MeasurementAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (a_collectionName.array(forKey: a_environmentBarTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.conversationRequest()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                a_collectionName.set(intimateCacheData, forKey: a_environmentBarTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func targetNotification(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(str_draftText.replacingOccurrences(of: "container", with: "ex"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_saveValue.prefix(5)) + String(str_colorTitle))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_clearName) + String(str_detailTitle))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.countimate(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func backCover(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_clearName) + String(str_detailTitle))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_clearName) + String(str_detailTitle))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.countimate(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func countimate(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = InfoReactiveCompatible.pointOfReferenceWearerClickCachedGetSubstance(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.modifyNo(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.nailDetail(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.quantityeraction(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.quantityeraction(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        InfoReactiveCompatible.showKeyTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_systemData.reversed(), encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_secureContent.map{$0^71}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: a_messageInfoName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension DoingRecognizerDelegate: PanoramicViewBlockThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func atRequest() {
        //: guard MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.sample(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func groupRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func funcTextType(actionType: MediaTablePropertyProtocol) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    streetwisePhotos()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    bilge()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                streetwisePhotos()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if MeasurementAppManager.share.loginUserMode.loungePlus {
                if MeasurementAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.drop(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                bilge()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    funcLikeIkon()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    bilge()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                funcLikeIkon()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.analogDigitalConverterFrom(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func bilge() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.episode(showMsg: String(bytes: str_managerVideoValue, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func imageModel(giftModel _: WithMeasurable, giftNum _: String, model _: RegularThen) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func omit(giftModel: WithMeasurable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        plainspoken(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ChatValueCellData?, fromStr: String?) {
    func plainspoken(giftModel: WithMeasurable, giftNum: String, cellData: ChatValueCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            episode(showMsg: a_userText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_soundTitle.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_hiddenContent))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_taskData.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_nameShareText))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(str_managerLinkTitle))] = fromStr
        }
        // 送礼接口
        //: DoingSheRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        DoingSheRequestTool.adminPlayer(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.createEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: a_endName)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.bags(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.noDedication(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.occurrence()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_saveValue.prefix(5)) + String(str_colorTitle))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_clearName) + String(str_detailTitle))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_clearName) + String(str_detailTitle))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.countimate(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                a_textTurnName.photoOf(eventID: a_licenseName)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func equalTime(giftModel: WithMeasurable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        a_textTurnName.photoOf(eventID: a_voiceData)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            episode(showMsg: a_userText)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= MeasurementAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            bilge()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(str_emptyTableText) + String(str_addValue.suffix(3)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(str_controlMakeValue.replacingOccurrences(of: "path", with: "p"))] = (String(str_rangeMediumTitle))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_textData.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_saveValue.prefix(5)) + String(str_colorTitle)): msgInfo, String(bytes: str_windowCenterValue, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        untilField(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func moreAcross(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(a_screenServerValue - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func accommodate(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func bubble(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.bubble(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                giftOn(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func equip() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SolarDayReactiveCompatible.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SolarDayReactiveCompatible.shared.scorer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func noDedication(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_saveValue.prefix(5)) + String(str_colorTitle))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!] = (String(str_intimateData))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_saveValue.prefix(5)) + String(str_colorTitle))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((str_makeValue.lowercased() + String(str_televisionTitle.suffix(8)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(str_makeValue.lowercased() + String(str_televisionTitle.suffix(8)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_makeTitle.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        direct(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_frameName) + String(str_proContent.prefix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_frameName) + String(str_proContent.prefix(6)))] as! NSNumber
            //: MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.titleCoin(needReload: false, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func dataSub() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.listWithId(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func direct(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.titleTheory(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func administratorArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.place(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func everyClick() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func cellVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.complection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.occurrence()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func dispose() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.domainTitleComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.occurrence()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func frame() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = FeedbackTalkingViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        isochronalStack()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func awake() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MediumDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.messageWith(cellTitleList: [(str_smallText.replacingOccurrences(of: "tab", with: "V") + String(str_actionData.suffix(4)) + "info").localized, (String(str_removeTitle.suffix(5))).localized, (String(str_timeColorData.prefix(5)) + str_emptyText.replacingOccurrences(of: "manager", with: "t")).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .blockOf()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: LimitPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                LimitPushManager.share.funcToStuffDetailUtilizerUid(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                addFinish()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = LimitMediaAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.icon(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func addFinish() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = DeleteNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.fromObject(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.visualPropertyImage(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func contentAcross() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            giftOn(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            isochronalStack()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func showBy(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = AmbientHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.afterHandle(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func skimErase() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(fitDismiss(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: a_indexData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(local),
                                               //: name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION,
                                               name: a_imageTitle,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func fitDismiss(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard MeasurementAppManager.share.loginUserMode.loungePlus == true else { return }
        guard MeasurementAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func local() {
        //: MeasurementAppManager.share.loginUserMode.loungePlus = true
        MeasurementAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func enableline() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if PrivateThen.targetData(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.dataToLocal(tipStr: (String(str_detailValue)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension DoingRecognizerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func streetwisePhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PrefaceReactiveCompatible.beModel(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StreetSmartThen.censusVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.replacementDown(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func stackName() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PrefaceReactiveCompatible.appBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.replacementDown(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func replacementDown(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = GlassesModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.enterAcrossModel(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = CellViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = GlassesModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.enterAcrossModel(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        isochronalStack()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func funcLikeIkon() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard PrivateThen.facilityVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == SizeCustomReflectable.male.rawValue ? "He".localized : (String(str_ofData)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.episode(showMsg: (String(str_valueTitle) + String(str_effectValue)).random(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.pushForPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func pushForPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = StreetSmartThen.censusVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            StreetSmartThen.mTheory(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.gestureSecondTelevision(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.episode(showMsg: (String(str_clearNameData.suffix(5)) + " video" + String(str_viewValue)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func gestureSecondTelevision(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: SizeEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = UpRecognizerDelegate(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = GlassesModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.totalRoom(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        SizeHungReactiveCompatible.dataMessage()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        isochronalStack()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func groupName(msgData: TUIMessageCellData, sendModel: GlassesModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        MediumLabThen.shared.all(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.episode(showMsg: (String(str_episodeData.suffix(6)) + " fail" + String(str_selectionContentName)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.totalRoom(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    //: func func__addVoiceNotification() {
    func halt() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(titleBy),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(trademark),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(isSub(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(signError(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: a_successBarData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(targetNotification(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: a_showData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func chorusLine(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SolarDayReactiveCompatible.shared.shrugOff()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SolarDayReactiveCompatible.shared.countDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func titleBy() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        chorusLine(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func trademark() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        chorusLine(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func isSub(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            chorusLine(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            chorusLine(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: ValueViewReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func toOf(cellData: ValueViewReactiveCompatible, cellView: InstrumentationView) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: ValueViewReactiveCompatible.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: ValueViewReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! ValueViewReactiveCompatible
            let audioModel = indexModel as! ValueViewReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = InfoReactiveCompatible.person(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SolarDayReactiveCompatible.shared.scorer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SolarDayReactiveCompatible.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(str_backgroundData)) + "\(SolarDayReactiveCompatible.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SolarDayReactiveCompatible.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SolarDayReactiveCompatible.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SolarDayReactiveCompatible.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SolarDayReactiveCompatible.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: ValueViewReactiveCompatible.self) {
                if subModel.isKind(of: ValueViewReactiveCompatible.self) {
                    //: let audioModel = subModel as! ValueViewReactiveCompatible
                    let audioModel = subModel as! ValueViewReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = InfoReactiveCompatible.person(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ObjectReactiveCompatible.containerMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SolarDayReactiveCompatible.shared.redGiantWithinPlay(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func warpath() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.outsideSize()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.petition(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: ValueViewReactiveCompatible.self) == true || indexModel?.isKind(of: DocumentCellData.self) == true || indexModel?.isKind(of: OfCellData.self) == true || indexModel?.isKind(of: ToCellData.self) == true {
                if indexModel?.isKind(of: ValueViewReactiveCompatible.self) == true || indexModel?.isKind(of: DocumentCellData.self) == true || indexModel?.isKind(of: OfCellData.self) == true || indexModel?.isKind(of: ToCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.modelEqual()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.addMsg()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.enableline()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func outsideSize() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.recordFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.adjustBeauty(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.previous()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == MeasurementAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == MeasurementAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == MeasurementAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == MeasurementAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.visualPropertyImage(userWrap)
    }

    //: func func__getFillDraftText() {
    func draftTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_lengthName.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.atRequest()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func compose() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_lengthName.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    //: func func__TipsNotification() {
    func carryingOut() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(policy(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: a_voiceTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func policy(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: LabOffThen = userinfo[(String(str_stageData))] as! LabOffThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func dataToLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_myValue, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_leadingName.map{$0^73}, encoding: .utf8)!: (String(str_textValue))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func addMsg() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        PrivateThen.makeFinish { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.dataToLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func startTo(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.dataToLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func modelEqual() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        PrivateThen.subsequently(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.dataToLocal(tipStr: String(bytes: str_styleTitle.map{$0^34}, encoding: .utf8)!.random(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func signError(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_capacityName) + String(str_coverData.suffix(5)))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension DoingRecognizerDelegate: ToQuantityTipsProtocol {
    //: func func__setReplyTipsView() {
    func databaseView() {
        //: let key =  "\(MeasurementAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(MeasurementAppManager.share.loginUserMode.userID)" + (String(str_itemName.suffix(4)) + "vate" + String(str_labData.prefix(5)) + "ipsTitl" + String(str_commentTitle)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.set()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            field(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            lightPush()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func blockSubInfo(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        nameModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        lightPush()
    }

    //: func removeReplyTipsView() {
    func lightPush() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            field(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func actionAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: a_callData, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: a_formalValue - a_screenServerValue, width: a_blockValue, height: a_formalValue)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: a_blockValue,
                                     //: height: ScreenHeight)
                                     height: a_formalValue)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func giftOn(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.colorController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if ImageToThen.labelShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: a_giftTitle, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: AddDataManagerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: a_formalValue - a_screenServerValue,
                                     //: width: ScreenWidth,
                                     width: a_blockValue,
                                     //: height: ScreenHeight)
                                     height: a_formalValue)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if ImageToThen.labelShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: a_giftTitle, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension DoingRecognizerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func towardTable() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_plusScopeContent.suffix(6)) + String(str_addRedName.prefix(5)) + "fication").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func imageConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? a_screenServerValue : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.barRadii(width: contentView.width,
                                 //: height: contentView.height,
                                 height: contentView.height,
                                 //: corners: [.topLeft, .topRight],
                                 corners: [.topLeft, .topRight],
                                 //: cornerRadii: .init(width: 8, height: 8))
                                 cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? a_toText : a_itemTitle)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + a_agentData)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? a_toText : a_itemTitle)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + a_agentData)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + a_agentData)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func attach() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.contentAcross()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.awake()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.funcTextType(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                a_textTurnName.photoOf(eventID: a_packageContent, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
