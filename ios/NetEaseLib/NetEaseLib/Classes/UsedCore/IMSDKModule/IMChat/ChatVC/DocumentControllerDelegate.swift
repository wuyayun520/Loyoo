
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_makeName:[UInt8] = [0xd1,0xd6,0xd1,0xcc,0x90,0xdb,0xd7,0xdc,0xdd,0xca,0x82,0x91,0x98,0xd0,0xd9,0xcb,0x98,0xd6,0xd7,0xcc,0x98,0xda,0xdd,0xdd,0xd6,0x98,0xd1,0xd5,0xc8,0xd4,0xdd,0xd5,0xdd,0xd6,0xcc,0xdd,0xdc]

/*: "inside_img" :*/
fileprivate let str_addData:String = "insiname"
fileprivate let str_progressTitle:String = "gift"

/*: "lab_manager_image" :*/
fileprivate let str_colorData:[Character] = ["l","a"]
fileprivate let str_toolText:[Character] = ["b","_","m","a","n","a","g","e","r","_","i","m","a","g","e"]

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let str_pinData:[Character] = ["T","a","l","k","i"]
fileprivate let str_headName:String = "ngGroucolor count self high"
fileprivate let str_titleData:String = "class block to tab colorText"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_colorValue:String = "Talkdisk leading between top index"
fileprivate let str_contentViewData:String = "exit import continueatIm"
fileprivate let str_borderValue:String = "up view view self selfCellID"

/*: "GroupViewCell" :*/
fileprivate let str_shareText:[Character] = ["T","a","l","k","i","n","g","G","r","o","u","p","C","h","a","t","G"]
fileprivate let str_homeValue:String = "status end add background importiftMsg"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_textModeTitle:[Character] = ["T","a","l","k","i","n"]
fileprivate let str_actualData:[Character] = ["g","C","h","a"]
fileprivate let str_sessionValue:String = "tTipsMequal color shared normal"

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_directionData:[Character] = ["T","S","y","s","t","e"]
fileprivate let str_colorServerForValue:String = "table extension color self selfmMes"
fileprivate let str_toModelData:String = "Cellthought else match"

/*: "roomId" :*/
fileprivate let str_halfText:String = "roomIbackground app"
fileprivate let str_mediumViewText:String = "fee"

/*: "type" :*/
fileprivate let str_atName:[UInt8] = [0x74,0x79,0x70,0x65]

/*: "extra" :*/
fileprivate let str_lessData:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let str_valueData:[UInt8] = [0x79,0x65,0x4b,0x70,0x6d,0x75,0x6a]

/*: "Send Gift" :*/
fileprivate let str_sizeCenterName:String = "Send Giftnest make else"

/*: "View info" :*/
fileprivate let str_infoMessageValue:[Character] = ["V","i","e","w"," ","i"]
fileprivate let str_gestureNameData:[Character] = ["n","f","o"]

/*: "Report" :*/
fileprivate let str_videoValue:String = "Reportpath return top"

/*: "uid" :*/
fileprivate let str_centerData:[UInt8] = [0x75,0x69,0x64]

/*: "name" :*/
fileprivate let str_userName:[UInt8] = [0x6e,0x61,0x6d,0x65]

/*: "Can't send blank message" :*/
fileprivate let str_episodeText:String = "Can\'"
fileprivate let str_sizeContent:String = " blahidden raw seat user table"
fileprivate let str_roomData:String = "view finish voice hidden imagenk mes"

/*: "user" :*/
fileprivate let str_transformValue:String = "uneedr"

/*: "content" :*/
fileprivate let str_firstName:[UInt8] = [0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74]

/*: "msgType" :*/
fileprivate let str_countryEqualText:String = "equal addmsgType"

/*: "txt" :*/
fileprivate let str_picName:String = "framexframe"

/*: "isGroup" :*/
fileprivate let str_containerColorData:[Character] = ["i","s","G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let str_sharedName:String = "jurequests"

/*: "message" :*/
fileprivate let str_shareName:[UInt8] = [0x87,0x8f,0x99,0x99,0x8b,0x8d,0x8f]

/*: "TxtMsg" :*/
fileprivate let str_afterContent:String = "TxtMscontent tap bag range"
fileprivate let str_touchName:String = "mode"

/*: "toUid" :*/
fileprivate let str_wantValue:String = "toUidinterval line"

/*: "msgInfo" :*/
fileprivate let str_momentValue:String = "empty"
fileprivate let str_sizePhoneData:[Character] = ["s","g","I","n","f","o"]

/*: "params" :*/
fileprivate let str_borderData:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let str_deadlineValue:[UInt8] = [0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x54,0x79,0x70,0x65]

/*: "ImgMsg" :*/
fileprivate let str_jumpValue:String = "manager systemImgMsg"

/*: "img" :*/
fileprivate let str_toText:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_blockMomentViewName:[UInt8] = [0x69,0x6d,0x61,0x67,0x65,0x55,0x72,0x69]

/*: "pic" :*/
fileprivate let str_endMediumLabText:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let str_collectionValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_messageValue:String = "bottomobottomal"
fileprivate let str_succeedData:String = "icolor"

/*: "giftId" :*/
fileprivate let str_trackName:String = "giftIdfloat to number"

/*: "giftNum" :*/
fileprivate let str_indexCalendarValue:String = "giftNumreturn path selected mask"

/*: "pkgItemsetId" :*/
fileprivate let str_equalName:[Character] = ["p","k","g","I","t","e","m","s","e"]
fileprivate let str_refName:[Character] = ["t","I","d"]

/*: "msg" :*/
fileprivate let str_videoMonthName:String = "mnameg"

/*: "tips" :*/
fileprivate let str_selectedName:String = "tipadd"

/*: "gift" :*/
fileprivate let str_errorText:String = "gidata"

/*: "nickname" :*/
fileprivate let str_actualName:String = "nicknamprofile"

/*: "headPic" :*/
fileprivate let str_showValue:[UInt8] = [0x68,0x65,0x61,0x64,0x50,0x69,0x63]

/*: "All Numbers" :*/
fileprivate let str_pairData:String = "input key row view tableAll "
fileprivate let str_leadingCellQuitName:String = "shares"

/*: "sendNum" :*/
fileprivate let str_ofData:[Character] = ["s","e","n","d","N","u","m"]

/*: "toUser" :*/
fileprivate let str_cardVideoValue:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let str_shareData:String = "add to pointmsgI"

/*: "isConnection" :*/
fileprivate let str_errorSexValue:String = "text click windowisCo"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DocumentControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class DocumentControllerDelegate: PropertyViewController {
	var lowOff: Bool = false
	var acrossBlockQuantity: Int = 3
	var upNameArray: [AnyHashable] = []
	var centerDictionary: [AnyHashable: String] = [:]
	var micOfEnable: Bool = false
	var skilletQuantity: Int = 20
	var modelArray: [AnyHashable] = []
	var textDictionary: [AnyHashable: String] = [:]

    var pathScaleImageView: UIImageView?

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = LeadingModelType() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = AdministratorReactiveCompatible() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = LeadingModelType.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_makeName.map{$0^184}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MeasurementAppManager.share.appConfigMode.disableShootScreen {
            if MeasurementAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HypsometryReactiveCompatible(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
                self.pathScaleImageView?.image = UIImage(named: (str_addData.replacingOccurrences(of: "name", with: "d") + "e_im" + str_progressTitle.replacingOccurrences(of: "gift", with: "g")))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        generate()
        //: createUIConstraints()
        umbra()
        //: bindInteraction()
        roomData()
        //: addTapGestureRecognizer()
        moveRecognizer()
        //: func__chatRoom_insertTipMsg()
        clickMake()
        //: chatSettings()
        addApplication()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            backgroundEffect()
        }

        pathScaleImageView = UIImageView(frame: self.view.bounds.offsetBy(dx: CGFloat(self.view.isExclusiveTouch ? 3 : 2), dy: CGFloat(0)))
        if let pathScaleImageView = pathScaleImageView {
            self.pathScaleImageView?.image = UIImage(named: (String(str_colorData) + String(str_toolText)))
            if (pathScaleImageView.textInputMode != nil) && (pathScaleImageView.bounds.size.height == 191.34) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(pathScaleImageView)
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    
            if (giftTrackView.layer.position.y == 15.19) && (giftTrackView.layer.mask != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let menuLet = GiftShotView(frame: giftTrackView.frame.insetBy(dx: CGFloat(62), dy: CGFloat(623.02)))
            menuLet.dataName = infoModel.name
            menuLet.unsleepingSwitch = { [self] showClose in
            self.lowOff = showClose
            
            var menuLet = giftEffectView.isPlayAnimat
                menuLet = true
                menuLet = false
            if menuLet != self.lowOff {
                self.lowOff = menuLet
            }
            
                self.lowOff = true
                self.lowOff = !self.lowOff
            return self.lowOff
            }
            menuLet.awakeMagnitude = { [self] momentMoveVoiceSum in
            self.acrossBlockQuantity = momentMoveVoiceSum
            
            var menuLet = infoModel.identity
            menuLet &*= 3
            if menuLet < self.acrossBlockQuantity {
                self.acrossBlockQuantity = menuLet
            }
            
            return self.acrossBlockQuantity
            }
            menuLet.snapArray = { [self] lastArray in
            self.upNameArray = lastArray
            
            guard var value = self.upNameArray as? [String] else {
                return nil
            }
            return value
            }
            menuLet.indexDictionary = { [self] managerBagDictionary in
            self.centerDictionary = managerBagDictionary
            
            guard var value = self.centerDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                giftTrackView.addSubview(menuLet)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.consultingService(join: false)
        //: self.destroyTipsTimer()
        self.equalTimer()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(NameTalkingGroupImageCompartmentViewCell.self, forCellReuseIdentifier: (String(str_pinData) + String(str_headName.prefix(6)) + "pChat" + String(str_titleData.suffix(4)) + "MsgCellID"))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(MentalRepresentationView.self, forCellReuseIdentifier: (String(str_colorValue.prefix(4)) + "ingCh" + String(str_contentViewData.suffix(4)) + "ageMsg" + String(str_borderValue.suffix(6))))
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(GroupViewCell.self, forCellReuseIdentifier: (String(str_shareText) + String(str_homeValue.suffix(6)) + "Cell"))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(LabView.self, forCellReuseIdentifier: (String(str_textModeTitle) + String(str_actualData) + String(str_sessionValue.prefix(6)) + "sgCellID"))
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_directionData) + String(str_colorServerForValue.suffix(4)) + "sage" + String(str_toModelData.prefix(4)) + "_ReuseId"))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: AlbumThen = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = AlbumThen(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: EquidistantThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = EquidistantThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: CountTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = CountTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: RegularLimitThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = RegularLimitThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: CustomModelView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = CustomModelView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func addApplication() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        DirectorChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        consultingService(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func consultingService(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(str_halfText.prefix(5)) + str_mediumViewText.replacingOccurrences(of: "fee", with: "d")): self.conversationData.groupID, String(bytes: str_atName, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: DoingSheRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        DoingSheRequestTool.pokeAt(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TMessageControllerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension DocumentControllerDelegate: TMessageControllerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        //: if data.isKind(of: PathCellData.self) {
        if data.isKind(of: PathCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = NameTalkingGroupImageCompartmentViewCell(style: .default, reuseIdentifier: (String(str_pinData) + String(str_headName.prefix(6)) + "pChat" + String(str_titleData.suffix(4)) + "MsgCellID"))
            //: let cellData = data as! PathCellData
            let cellData = data as! PathCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.buttonUid(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: DocumentCellData.self) {
        if data.isKind(of: DocumentCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = MentalRepresentationView(style: .default, reuseIdentifier: (String(str_colorValue.prefix(4)) + "ingCh" + String(str_contentViewData.suffix(4)) + "ageMsg" + String(str_borderValue.suffix(6))))
            //: let cellData = data as! DocumentCellData
            let cellData = data as! DocumentCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: RecordAlbumCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = GroupViewCell(style: .default, reuseIdentifier: (String(str_shareText) + String(str_homeValue.suffix(6)) + "Cell"))
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! RecordAlbumCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: LabOffThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = LabView(style: .default, reuseIdentifier: (String(str_textModeTitle) + String(str_actualData) + String(str_sessionValue.prefix(6)) + "sgCellID"))
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! LabOffThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(MeasurementAppManager.share.loginUid) {
            if userModel.uid == Int(MeasurementAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                buttonUid(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                isothermal(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: MentalRepresentationView.self) { // 图片
            //: let imageCellData = cell.messageData as! DocumentCellData
            let imageCellData = cell.messageData as! DocumentCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(a_cancelName)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = GoldObjectMeasurable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = CustomMeasurable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [GoldObjectMeasurable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = CellViewController(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.colorController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: NameTalkingGroupImageCompartmentViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! NameTalkingGroupImageCompartmentViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_lessData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_valueData.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<DataDateJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LimitPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LimitPushManager.share.reverence(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: LabView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! LabView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_lessData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_valueData.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<DataDateJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LimitPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LimitPushManager.share.reverence(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        equalAcross(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func equalTimer() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func isothermal(user: DestroyJsonModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MediumDataSource(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.messageWith(cellTitleList: [(String(str_sizeCenterName.prefix(9))).localized, "@", (String(str_infoMessageValue) + String(str_gestureNameData)).localized, (String(str_videoValue.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.someone(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.dark(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.buttonUid(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.subKey(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func someone(user: DestroyJsonModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.areaBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = RegularThen()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.queryed(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func dark(user: DestroyJsonModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: str_centerData, encoding: .utf8)!: String(user.uid), String(bytes: str_userName, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.equalReceive(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func buttonUid(uid: Int) {
        //: LimitPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        LimitPushManager.share.funcToStuffDetailUtilizerUid(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func subKey(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = LimitMediaAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.icon(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func nameModel(msgStr: String, quoteModel _: IsochronalModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            episode(showMsg: (str_episodeText + "t send" + String(str_sizeContent.prefix(4)) + String(str_roomData.suffix(6)) + "sage").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: str_valueData.reversed(), encoding: .utf8)!: (str_transformValue.replacingOccurrences(of: "need", with: "se")), String(bytes: str_centerData, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: str_firstName, encoding: .utf8)!: temStr, (String(str_countryEqualText.suffix(7))): (str_picName.replacingOccurrences(of: "frame", with: "t")), (String(str_containerColorData)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(str_sharedName.replacingOccurrences(of: "request", with: "mp"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: str_shareName.map{$0^234}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: str_atName, encoding: .utf8)!: (String(str_afterContent.prefix(5)) + str_touchName.replacingOccurrences(of: "mode", with: "g")),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_halfText.prefix(5)) + str_mediumViewText.replacingOccurrences(of: "fee", with: "d")): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(str_wantValue.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(str_momentValue.replacingOccurrences(of: "empty", with: "m") + String(str_sizePhoneData)): msgInfo, String(bytes: str_borderData.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        equalAcross(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func pathQuick(img: UIImage) {
        //: let imagePath = DocumentCellData.getChatImagePath(image: img)
        let imagePath = DocumentCellData.everyTargetShow(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: str_deadlineValue, encoding: .utf8)!: (String(str_jumpValue.suffix(6))), (String(str_countryEqualText.suffix(7))): (String(str_toText)), String(bytes: str_blockMomentViewName, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(str_endMediumLabText)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: str_atName, encoding: .utf8)!: (String(str_jumpValue.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_halfText.prefix(5)) + str_mediumViewText.replacingOccurrences(of: "fee", with: "d")): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(str_momentValue.replacingOccurrences(of: "empty", with: "m") + String(str_sizePhoneData)): msgInfo, String(bytes: str_borderData.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        equalAcross(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func equalAcross(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            episode(showMsg: a_userText)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(str_endMediumLabText))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(str_endMediumLabText))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(str_endMediumLabText))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: DoingSheRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        DoingSheRequestTool.showCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.methodResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(str_momentValue.replacingOccurrences(of: "empty", with: "m") + String(str_sizePhoneData))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(str_collectionValue))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((str_messageValue.replacingOccurrences(of: "bottom", with: "t") + "MfCo" + str_succeedData.replacingOccurrences(of: "color", with: "n"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(str_messageValue.replacingOccurrences(of: "bottom", with: "t") + "MfCo" + str_succeedData.replacingOccurrences(of: "color", with: "n"))] as! NSNumber
                //: MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func methodResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MeasurementAppManager.share.loginUserMode.status != 1 else {
            guard MeasurementAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    episode(showMsg: errorStr)
                }
                //: return
                return
            }

            //: LimitPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            LimitPushManager.share.billParams(clickEvent: a_endName, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.gameAcrossView()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            imageLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = UtmostView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
            //: view.show()
            view.studShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                episode(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension DocumentControllerDelegate: PanoramicViewBlockThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func equalTime(giftModel _: WithMeasurable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func atRequest() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func groupRange(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = CloseDataSource()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.equalReceive(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func funcTextType(actionType: MediaTablePropertyProtocol) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            appearSaveFace()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func omit(giftModel _: WithMeasurable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func imageModel(giftModel: WithMeasurable, giftNum: String, model: RegularThen) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            episode(showMsg: a_userText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(str_wantValue.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(str_trackName.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_indexCalendarValue.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(str_halfText.prefix(5)) + str_mediumViewText.replacingOccurrences(of: "fee", with: "d"))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_equalName) + String(str_refName))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: DoingSheRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        DoingSheRequestTool.chat(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.methodResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.bags(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.aboveModel(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func moreAcross(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func accommodate(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func bubble(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.bubble(tap)
    }

    //: func func__starRecordBtnDown() {}
    func equip() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func phonation(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension DocumentControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func appearSaveFace() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PrefaceReactiveCompatible.beModel(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StreetSmartThen.censusVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: LimitPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                LimitPushManager.share.beforeRoom()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.pathQuick(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func camara() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PrefaceReactiveCompatible.appBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.pathQuick(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func shadowInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(str_halfText.prefix(5)) + str_mediumViewText.replacingOccurrences(of: "fee", with: "d"))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    //: func func__TipsNotification() {
    func makeFor() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(tips(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: a_voiceTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func tips(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: LabOffThen = userinfo[(str_videoMonthName.replacingOccurrences(of: "name", with: "s"))] as! LabOffThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func imageLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: str_firstName, encoding: .utf8)!: tipStr, (String(str_countryEqualText.suffix(7))): (str_selectedName.replacingOccurrences(of: "add", with: "s"))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func clickMake() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard MeasurementAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard MeasurementAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: MeasurementAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.imageLocal(tipStr: MeasurementAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func aboveModel(extral: [String: Any?], model: RegularThen, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(str_momentValue.replacingOccurrences(of: "empty", with: "m") + String(str_sizePhoneData))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(str_containerColorData))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(str_countryEqualText.suffix(7)))] = (str_errorText.replacingOccurrences(of: "data", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(str_momentValue.replacingOccurrences(of: "empty", with: "m") + String(str_sizePhoneData))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: str_centerData, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (str_actualName.replacingOccurrences(of: "profile", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: str_showValue, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(str_pairData.suffix(4)) + "Numb" + str_leadingCellQuitName.replacingOccurrences(of: "share", with: "er")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((str_errorText.replacingOccurrences(of: "data", with: "ft"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(str_errorText.replacingOccurrences(of: "data", with: "ft"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(str_actualName.replacingOccurrences(of: "profile", with: "e"))] = "\(giftdic[(String(str_ofData))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(str_cardVideoValue))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_shareData.suffix(4)) + "ncome")) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_shareData.suffix(4)) + "ncome")] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_collectionValue))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        atModel(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((str_messageValue.replacingOccurrences(of: "bottom", with: "t") + "MfCo" + str_succeedData.replacingOccurrences(of: "color", with: "n"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(str_messageValue.replacingOccurrences(of: "bottom", with: "t") + "MfCo" + str_succeedData.replacingOccurrences(of: "color", with: "n"))] as! NSNumber
            //: MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MeasurementAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.titleCoin(needReload: false, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.magnitudeeraction()
    }

    //: func initCachEffectData() {
    func backgroundEffect() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.listWithId(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func atModel(giftDic: [String: Any], model: RegularThen) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.messageUser(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in GestureReactiveCompatible.share.textView() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MediaArrModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.terraceFirst(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.titleTheory(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func holdfastEndGiftHorse(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in GestureReactiveCompatible.share.textView() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MediaArrModel]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.terraceFirst(model: giftModel as! EventHandyJSON)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.place(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension DocumentControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func generate() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func umbra() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(a_itemTitle)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(a_itemTitle)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - a_agentData)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func roomData() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = CloseDataSource()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TUIMessageCellData).isKind(of: PathCellData.self) {
                    if (cellData as! TUIMessageCellData).isKind(of: PathCellData.self) {
                        //: let groupCellData = cellData as! PathCellData
                        let groupCellData = cellData as! PathCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(str_collectionValue))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: TalkingIMManager.shared.rx
        DateThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(str_errorSexValue.suffix(4)) + "nnection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
