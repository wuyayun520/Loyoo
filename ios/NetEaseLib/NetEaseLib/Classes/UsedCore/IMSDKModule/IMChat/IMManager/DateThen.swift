
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let str_targetTitle:String = "self self i ifIM退出"
fileprivate let str_infoData:String = "，code:permission float string else"

/*: , desc: :*/
fileprivate let str_deviceName:[Character] = [","," ","d","e","s"]
fileprivate let str_colorValue:String = "if intimate imagec:"

/*: ." :*/
fileprivate let str_hideData:String = "."

/*: "IM登录失败，code: :*/
fileprivate let str_scaleManagerData:[Character] = ["I","M","登","\u{5f55}","失","败","\u{ff0c}","c"]
fileprivate let str_viewData:String = "ode:as array model"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let str_modelText:[Character] = ["I","M","获","取","未","读","消","\u{606f}","\u{603b}","数","失","败","，","c","o","d"]
fileprivate let str_buttonTitleName:[Character] = ["e",":"]

/*: "crushBadge" :*/
fileprivate let str_normalPathName:[Character] = ["c","r","u","s","h","B","a","d"]
fileprivate let str_greetData:String = "titlee"

/*: "badNumber" :*/
fileprivate let str_labData:[UInt8] = [0x72,0x65,0x62,0x6d,0x75,0x4e,0x64,0x61,0x62]

/*: "plaza" :*/
fileprivate let str_nameLabelEqualData:String = "countaza"

/*: "roomNum" :*/
fileprivate let str_userValue:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let str_toErrorTitle:String = "targetm"

/*: "onlyOneRoomId" :*/
fileprivate let str_titleName:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let str_validLabelData:String = "LIVE-CHAedit self data layer"
fileprivate let str_cellValue:[Character] = ["T"]

/*: "PARTY-CHAT" :*/
fileprivate let str_ageValue:[Character] = ["P","A","R","T","Y"]
fileprivate let str_toValue:String = "-CHATequal my log var thought"

/*: "extra" :*/
fileprivate let str_touchData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let str_photoData:[Character] = ["m","s","g","T","y","p"]
fileprivate let str_actionText:[Character] = ["e"]

/*: "newPkgGift" :*/
fileprivate let str_meValue:[Character] = ["n","e","w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let str_textValue:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i","o"]
fileprivate let str_afterValue:String = "nMsgtitle scale"

/*: "uid" :*/
fileprivate let str_labName:[UInt8] = [0x75,0x69,0x64]

/*: "onlineStatus" :*/
fileprivate let str_viewText:[UInt8] = [0x44,0x45,0x47,0x42,0x45,0x4e,0x78,0x5f,0x4a,0x5f,0x5e,0x58]

/*: "isNewUser" :*/
fileprivate let str_momentCollectionValue:[UInt8] = [0x31,0x2b,0x16,0x3d,0x2f,0xd,0x2b,0x3d,0x2a]

/*: "c2c_ :*/
fileprivate let str_liveValue:String = "c2c_share color item text value"

/*: "IM获取会话失败，convId: :*/
fileprivate let str_hiddenBackData:String = "if intimate please if buttonIM获取会话"
fileprivate let str_addTitle:String = "view live height modelvId:"

/*: , code: :*/
fileprivate let str_upData:[Character] = [","," ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let str_pathTitle:String = "IM\u{63d2}入"
fileprivate let str_modeObjectData:[Character] = ["a"]
fileprivate let str_coverName:[Character] = ["r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let str_imageValue:String = "cviewnge"
fileprivate let str_nameText:String = "ctioshare"

/*: "rYMsgType" :*/
fileprivate let str_resultTableValue:String = "click"
fileprivate let str_sendImageData:[Character] = ["Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let str_rawName:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let str_captureName:String = "add begin photo call borderCustom"

/*: "IM获取会话列表失败，code: :*/
fileprivate let str_inputTitle:[Character] = ["I","M","获","取","会","话","列","表","失","\u{8d25}"]
fileprivate let str_succeedValue:[Character] = ["，","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let str_selectContent:[Character] = ["m","s","g","I","n"]
fileprivate let str_equalViewTitle:String = "fchange"

/*: "needFold" :*/
fileprivate let str_taskData:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let str_toAgeTrueName:String = "group_equal show"

/*: "IM删除会话失败，convID: :*/
fileprivate let str_withData:[Character] = ["I","M","删","除","会"]
fileprivate let str_subData:String = "话失败\u{ff0c}"
fileprivate let str_originTitle:String = "convID:right border"

/*: "IM置顶会话失败，convId: :*/
fileprivate let str_makeValue:[Character] = ["I","M","置","\u{9876}","会","话","失","\u{8d25}","，","c","o","n","v","I","d"]
fileprivate let str_throughName:String = ":"

/*: "SDK 正在连接到服务器" :*/
fileprivate let str_quickText:String = "SDK \u{6b63}在"
fileprivate let str_modelAppArrayName:[Character] = ["连","接","\u{5230}","服","务","\u{5668}"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let str_cornerData:[Character] = ["S","D","K"," ","\u{5df2}","经","成","功","连","接","到","服","务","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let str_progressData:String = "keep game height if clickIM连接服务"
fileprivate let str_pathText:[Character] = ["c","o","d","e",":"]

/*: , error: :*/
fileprivate let str_premiumValue:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let str_liveName:String = "IM账\u{53f7}被"
fileprivate let str_minValue:String = "\u{6324}\u{4e0b}线。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let str_touchName:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let str_centerName:String = "Offlas shared value"
fileprivate let str_statusTitle:String = "make inNotific"
fileprivate let str_theMessageValue:[Character] = ["a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let str_commitData:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let str_outsidePostName:String = "equal class observer agentRe L"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DateThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol LoadObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func titleManagerMake(data: [AdministratorReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func innumerableness(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func appEvent(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum PicDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class TalkingIMManager: NSObject {
@objcMembers public class DateThen: NSObject {
    //: public static let shared = TalkingIMManager()
    public static let shared = DateThen()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [AdministratorReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [AdministratorReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [AdministratorReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [AdministratorReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [AdministratorReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: AdministratorReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.quantity()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func viewInTo() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(a_bottomContent, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func quantity() {
        //: func__removeAllDelegate()
        ginmill()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.nowList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func nowList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = AdministratorReactiveCompatible.finishInShow(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = AdministratorReactiveCompatible.finishInShow(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func split() -> [AdministratorReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == ImageMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == ImageMacroDefine.executeByTitle() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension TalkingIMManager {
extension DateThen {
    //: func func__removeAllDelegate() {
    func ginmill() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func delegate(_ delegate: LoadObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func worldView(_ delegate: LoadObjectProtocol) {
        //: self.func__invokeDelegate {
        self.shouldLoad {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func shouldLoad(_ invocation: (LoadObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! LoadObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension TalkingIMManager {
extension DateThen {
    /// 退出登录IM
    //: func func__LogingOut() {
    func circulating() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            TitleMediumLogTool.commitMedium(msg: (String(str_targetTitle.suffix(4)) + "登录失\u{8d25}" + String(str_infoData.prefix(6))) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.quantity()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func eventBlock(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = MeasurementAppManager.share.appUserConfigMode.uSig
        let uSigStr = MeasurementAppManager.share.appUserConfigMode.uSig
        //: let uidStr = MeasurementAppManager.share.loginUserMode.userID
        let uidStr = MeasurementAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.partCount()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            TitleMediumLogTool.commitMedium(msg: (String(str_scaleManagerData) + String(str_viewData.prefix(4))) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func equalList() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || MeasurementAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || MeasurementAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.episode(showMsg: a_userText)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension TalkingIMManager {
extension DateThen {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func partCount() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            TitleMediumLogTool.commitMedium(msg: (String(str_modelText) + String(str_buttonTitleName)) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func nonePush() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: DoingSheRequestTool.req_sysMessageList { _, result in
        DoingSheRequestTool.inform { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(str_normalPathName) + str_greetData.replacingOccurrences(of: "title", with: "g"))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: a_halfData, object: self, userInfo: [String(bytes: str_labData.reversed(), encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func upWith(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: DoingSheRequestTool.req_getChatRoomInfo { succeed, result, _ in
        DoingSheRequestTool.signal { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(str_nameLabelEqualData.replacingOccurrences(of: "count", with: "pl"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(str_userValue))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = AdministratorReactiveCompatible.finishInShow(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(str_toErrorTitle.replacingOccurrences(of: "target", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(str_titleName))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension TalkingIMManager: V2TIMAdvancedMsgListener {
extension DateThen: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(str_validLabelData.prefix(8)) + String(str_cellValue))) || msg.groupID.contains((String(str_ageValue) + String(str_toValue.prefix(5))))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            SizeDanmuManager.appShared().shank(msg: msg)
            //: return
            return
        }
        //: if msg.sender == MeasurementAppManager.share.loginUserMode.userID { return }
        if msg.sender == MeasurementAppManager.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(str_touchData))] as! [AnyHashable: Any]
        //: let msgModel = ConfinementModel(dic: extraDic)
        let msgModel = ConfinementModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: a_showData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(str_photoData) + String(str_actionText))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(str_meValue)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: a_formalBottomValue, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                ObjectReactiveCompatible.shankBoard(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.titleUser(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            NameAnimatTool.shared.heliogram(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            NameAnimatTool.shared.modelShowToday(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(str_textValue) + String(str_afterValue.prefix(4))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            DirectorChatManager.share.mention(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: PrivateThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: PrivateThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != ImageMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != ImageMacroDefine.dataFormatting() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.shouldLoad { $0.appEvent?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ManagerEventReactiveCompatible.shared.metadata(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: str_labName, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: str_viewText.map{$0^43}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: str_momentCollectionValue.map{$0^88}, encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: a_randomData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            InfoReactiveCompatible.trailhead(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            PrivateThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: a_indexData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func compartment(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.giftEqual(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(str_liveValue.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.removeToPointList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.announcement(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                TitleMediumLogTool.commitMedium(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func giftEqual(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = MeasurementAppManager.share.loginUserMode.userID
        let senderStr = MeasurementAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                TitleMediumLogTool.commitMedium(msg: (str_pathTitle + "消息失败，t" + String(str_modeObjectData) + String(str_coverName)) + "\(targetId)" + (String(str_upData)) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func titleUser(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(str_imageValue.replacingOccurrences(of: "view", with: "ha") + "Dire" + str_nameText.replacingOccurrences(of: "share", with: "n"))].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && LimitPushManager.share.beforeRoom()!.isKind(of: DoingRecognizerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: a_inputStatusValue) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: a_inputStatusValue)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension TalkingIMManager: V2TIMConversationListener {
extension DateThen: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.removeToPointList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.announcement(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.removeToPointList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.announcement(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: MeasurementAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        MeasurementAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.shouldLoad {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.innumerableness?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func announcement(conVersationList: [AdministratorReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if InfoReactiveCompatible.colorButton().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                InfoReactiveCompatible.colorButton().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.shouldLoad {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.titleManagerMake?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func oralPresentationImage(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [AdministratorReactiveCompatible] = []

        // V2TIMConversation->AdministratorReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = AdministratorReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.nextConv(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [AdministratorReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = AmmunitionInfoTable.breakUpSomebody(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.roundModel(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension TalkingIMManager {
extension DateThen {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func listLoadCompletion(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.oralPresentationImage(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.announcement(conVersationList: arr as? [AdministratorReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            TitleMediumLogTool.commitMedium(msg: (String(str_inputTitle) + String(str_succeedValue)) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func bullSEye(targetId: String) -> AdministratorReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func removeToPointList(convList: [V2TIMConversation]) -> [AdministratorReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [AdministratorReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = AdministratorReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.nextConv(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.roundModel(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func roundModel(convModel: AdministratorReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.nowList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: PicDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MeasurementAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MeasurementAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = InfoReactiveCompatible.pointOfReferenceWearerClickCachedGetSubstance(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(str_touchData))][(String(str_selectContent) + str_equalViewTitle.replacingOccurrences(of: "change", with: "o"))][(String(str_taskData))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.pushAside(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.pushAside(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func pushAside(convList: [AdministratorReactiveCompatible], dataType: PicDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func infoType(targetId: String, dataType: PicDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(str_liveValue.prefix(4))) + "\(targetId)" : (String(str_toAgeTrueName.prefix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            InfoReactiveCompatible.toStreetwise(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(MeasurementAppManager.share.loginUserMode.userID, toUid: targetId)
            ObjectReactiveCompatible.footballer(MeasurementAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.partCount()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            TitleMediumLogTool.commitMedium(msg: (String(str_withData) + str_subData + String(str_originTitle.prefix(7))) + "\(convID)" + (String(str_upData)) + "\(code)" + (String(str_deviceName) + String(str_colorValue.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func streetSmartTop(convModel: AdministratorReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            TitleMediumLogTool.commitMedium(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.pushAside(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.pushAside(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension TalkingIMManager: V2TIMSDKListener {
extension DateThen: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (str_quickText + String(str_modelAppArrayName)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(str_cornerData)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        TitleMediumLogTool.commitMedium(msg: (String(str_progressData.suffix(6)) + "器失\u{8d25}，" + String(str_pathText)) + "\(code)" + (String(str_premiumValue)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        TitleMediumLogTool.commitMedium(msg: (str_liveName + str_minValue))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        SocketReactiveCompatible.shared.designDismiss(userLoginOut: true)
        //: TalkingIMManager.shared.isConnection = false
        DateThen.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (ViewLoadKingfisherCompatible.shared as? ViewLoadKingfisherCompatible)?.afterSkip()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: str_touchName, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = MarkAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.ar.rawValue || AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.methodPlay(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        TitleAlertShow.admittance()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        TitleAlertShow.admittance(title: (String(str_centerName.prefix(4)) + "ine " + String(str_statusTitle.suffix(7)) + String(str_theMessageValue)).localized, message: msg, leftBtnTitle: (String(str_commitData)).localized, rightBtnTitle: (String(str_outsidePostName.suffix(4)) + "ogin").localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            PropertyMManageressPushListener.giftShared().handleDown()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ImageToThen.labelShared().sprechgesang()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            LoginReactiveCompatible.index { t in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: a_senseContent, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            LoginReactiveCompatible.service { t in
                //: if t {
                if t {
                    //: TalkingIMManager.shared.func__LogingIn { _ in
                    DateThen.shared.eventBlock { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if PropertyMManageressPushListener.giftShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(PropertyMManageressPushListener.giftShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    SocketReactiveCompatible.shared.equalSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
