
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let str_momentFromData:[UInt8] = [0x50,0x41,0x52,0x54,0x59,0x5f,0x53,0x54,0x41,0x54,0x55,0x53,0x5f,0x43,0x48,0x41,0x4e,0x47,0x45,0x5f,0x4e,0x4f,0x54,0x49,0x46,0x49,0x43,0x41,0x54,0x49,0x4f,0x4e]

/*: "p- :*/
fileprivate let str_fileData:String = "time self as type sizep-"

/*: - :*/
fileprivate let str_frameData:String = "-"

/*: "redirectUrl" :*/
fileprivate let str_sessionValue:String = "REDI"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let str_viewTitle:[Character] = ["V","O","I","C","E","R"]
fileprivate let str_userInsideContent:String = "OOM: to content self"
fileprivate let str_toTitle:String = "解析失\u{8d25}。"

/*: "Party room exception" :*/
fileprivate let str_textContent:String = "Party rvar before of sex"
fileprivate let str_quoteButtonData:[Character] = ["o","o","m"," ","e","x","c","e","p","t","i","o","n"]

/*: _ :*/
fileprivate let str_viewText:String = "_"

/*: "male" :*/
fileprivate let str_contentData:[UInt8] = [0x65,0x6c,0x61,0x6d]

/*: "female" :*/
fileprivate let str_actionValue:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let str_viewValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","群","\u{804a}","进","房","失","败","。","g","r","o","u","p","I","d"]
fileprivate let str_labelValue:[Character] = [":"," "]

/*: ." :*/
fileprivate let str_pathValue:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let str_taskName:String = "centerOICERO"
fileprivate let str_objectKeyContent:String = "version view by recordOM: 切"
fileprivate let str_invitationText:String = "析失\u{8d25}。"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let str_nameValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," "]
fileprivate let str_attentionData:[Character] = ["群","\u{804a}","切","房","失","败","\u{3002}","g","r","o","u","p","I","d",":"," "]

/*: "errCode" :*/
fileprivate let str_myInfoData:[UInt8] = [0x65,0x72,0x72,0x43,0x6f,0x64,0x65]

/*: "custom_kickout_message" :*/
fileprivate let str_valueName:String = "CUST"
fileprivate let str_atBlockTitle:String = "equalut"
fileprivate let str_textManagerValue:String = "greply"

/*: "closeRoom" :*/
fileprivate let str_modeName:[Character] = ["c","l","o","s","e","R","o","o"]
fileprivate let str_pointValue:[Character] = ["m"]

/*: "adminCloseRoom" :*/
fileprivate let str_textMakeValue:String = "addatain"
fileprivate let str_effectValue:String = "result size action float detaileRoo"
fileprivate let str_fillEqualValue:String = "block"

/*: "The room is closed, please go to another room" :*/
fileprivate let str_sizeContent:[UInt8] = [0x54,0x68,0x65,0x20,0x72,0x6f,0x6f,0x6d,0x20,0x69,0x73,0x20,0x63,0x6c,0x6f,0x73,0x65,0x64,0x2c,0x20,0x70,0x6c,0x65,0x61,0x73,0x65,0x20,0x67,0x6f,0x20,0x74,0x6f,0x20,0x61,0x6e,0x6f,0x74,0x68,0x65,0x72,0x20,0x72,0x6f,0x6f,0x6d]

/*: "OK" :*/
fileprivate let str_mediumTitle:[Character] = ["O","K"]

/*: "You were kicked out of the room" :*/
fileprivate let str_styleValue:[UInt8] = [0x59,0x6f,0x75,0x20,0x77,0x65,0x72,0x65,0x20,0x6b,0x69,0x63,0x6b,0x65,0x64,0x20,0x6f,0x75,0x74,0x20,0x6f,0x66,0x20,0x74,0x68,0x65,0x20,0x72,0x6f,0x6f,0x6d]

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let str_numberData:String = "VOviewC"
fileprivate let str_itemData:String = "M：进房error with app"
fileprivate let str_messageName:String = "if gift newmID: "

/*: , error:  :*/
fileprivate let str_subText:[Character] = [","," ","e","r","r","o","r",":"," "]

/*: , reason:  :*/
fileprivate let str_leadingContent:[Character] = [","," ","r","e","a","s","o","n",":"," "]

/*: , extendedData:  :*/
fileprivate let str_managerRowText:String = "value format to, ext"
fileprivate let str_modelSearchFieldContent:[Character] = ["a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let str_photoData:[Character] = ["V","O","I","C","E","R","O"]
fileprivate let str_addValue:String = "OM: 当\u{524d}"
fileprivate let str_cancelColorTitle:String = "败\u{ff0c}room"

/*: , errorCode:  :*/
fileprivate let str_styleName:String = "turn"
fileprivate let str_oldName:String = "make image error"

/*: , streamID:  :*/
fileprivate let str_faceData:[Character] = [","," ","s","t","r","e","a","m","I","D",":"]
fileprivate let str_dateValue:[Character] = [" "]

/*: "An error occurred, please try again" :*/
fileprivate let str_titleData:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let str_countryValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," "]
fileprivate let str_colorValue:String = "拉流\u{5931}败，"
fileprivate let str_indexRawTitle:String = "roomID: remove cell view"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let str_photoText:String = "voic"
fileprivate let str_picText:String = "M: 信\u{4ee4}"
fileprivate let str_tingName:String = "let make changecommand: "

/*: , roomID:  :*/
fileprivate let str_pathImageValue:String = ",section"
fileprivate let str_sizeName:[Character] = ["r","o","o","m","I","D",":"," "]

/*: "data" :*/
fileprivate let str_callTitle:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let str_nearbyName:String = "PO"
fileprivate let str_toData:[Character] = ["s","i","t","i","o","n"]

/*: "xxxx--  :*/
fileprivate let str_allowAppTitle:[Character] = ["x","x","x","x","-","-"," "]

/*: "opType" :*/
fileprivate let str_atText:String = "opTypefile view let user"

/*: "party:changeMike" :*/
fileprivate let str_afterName:[Character] = ["p","a","r","t","y",":"]
fileprivate let str_stateText:String = "toan"

/*: "type" :*/
fileprivate let str_thoughtName:[Character] = ["t","y","p","e"]

/*: "toPosition" :*/
fileprivate let str_loopCellValue:String = "the true status let stringtoPos"
fileprivate let str_countBackgroundData:String = "label"

/*: "party:adminChangeMike" :*/
fileprivate let str_moreText:String = "party:any in user"
fileprivate let str_systemText:String = "show nor text containerChange"
fileprivate let str_countValue:String = "point native model elementMike"

/*: "You were kicked off the seat" :*/
fileprivate let str_retData:String = "You were view file color name return"
fileprivate let str_checkData:String = "moveicmoveed"
fileprivate let str_modelData:String = "add appearance the "

/*: "party:changeCoin" :*/
fileprivate let str_redDomainContent:[Character] = ["p","a","r","t","y",":","c","h","a","n"]
fileprivate let str_styleContent:String = "of click enable ongeCoin"

/*: "party:closeStream" :*/
fileprivate let str_goldText:String = "paleadingty"
fileprivate let str_targetContent:String = "gift import do center whiteseSt"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImageToThen.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let a_clickData = NSNotification.Name(rawValue: String(bytes: str_momentFromData, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let a_nextName = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum NameMultiplierTarget: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class ImageToThen: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = CommunicationTransformable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: a_clickData, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: ImageToThen?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: BlockManagerDelegate?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: WithDoingHandyJSON]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = produce()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func labelShared() -> ImageToThen {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = ImageToThen()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func requestPop() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if ImageToThen._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            ImageToThen._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: QualifyMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = QualifyMiniView.root()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.beWhenGoback()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func addPosition(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        SprechstimmeEventReqTool.userServer(roomId: partyModel.roomId,
                                           //: uid: Int(MeasurementAppManager.share.loginUid) ?? 0,
                                           uid: Int(MeasurementAppManager.share.loginUid) ?? 0,
                                           //: type: type,
                                           type: type,
                                           //: position: position,
                                           position: position,
                                           //: toPos: toPosition)
                                           toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.today()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = WithDoingHandyJSON.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.polePosition(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.today()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func beyondAll(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        SprechstimmeEventReqTool.stateNow(roomId: partyModel.roomId,
                                         //: uid: uid,
                                         uid: uid,
                                         //: type: type,
                                         type: type,
                                         //: position: position)
                                         position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func today() -> String {
        //: return "p-\(MeasurementAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(MeasurementAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func actionTap(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PrefaceReactiveCompatible.pryUpward(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.episode(showMsg: a_matchData)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            SprechstimmeEventReqTool.sunParlorTime(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: LimitPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            LimitPushManager.share.comb(urlStr: result[(str_sessionValue.lowercased() + "rectUrl")] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.episode(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard SocketReactiveCompatible.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.episode(showMsg: a_viewNetValue)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = CommunicationTransformable.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    TitleMediumLogTool.commitMedium(msg: (String(str_viewTitle) + String(str_userInsideContent.prefix(5)) + "\u{8fdb}房模型" + str_toTitle))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.episode(showMsg: (String(str_textContent.prefix(7)) + String(str_quoteButtonData)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = BlockManagerDelegate()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.isochronalStack()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.restore()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.upCreate()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                a_textTurnName.photoOf(eventID: "\(a_tableRandomData)_\(MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue ? String(bytes: str_contentData.reversed(), encoding: .utf8)! : (String(str_actionValue)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                SizeDanmuManager.saveSelect(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        TitleMediumLogTool.commitMedium(msg: (String(str_viewValue) + String(str_labelValue)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.episode(showMsg: (String(str_textContent.prefix(7)) + String(str_quoteButtonData)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.sprechgesang()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func willEnd(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        SprechstimmeEventReqTool.cleanRoom(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.allModelPop()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == MeasurementAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == MeasurementAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = CommunicationTransformable.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                TitleMediumLogTool.commitMedium(msg: (str_taskName.replacingOccurrences(of: "center", with: "V") + String(str_objectKeyContent.suffix(5)) + "\u{623f}模型解" + str_invitationText))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.episode(showMsg: (String(str_textContent.prefix(7)) + String(str_quoteButtonData)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = BlockManagerDelegate()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.isochronalStack()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.restore()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.ergodic(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(MeasurementAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            a_textTurnName.photoOf(eventID: "\(a_tableRandomData)_\(MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue ? String(bytes: str_contentData.reversed(), encoding: .utf8)! : (String(str_actionValue)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            SizeDanmuManager.saveSelect(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    TitleMediumLogTool.commitMedium(msg: (String(str_nameValue) + String(str_attentionData)) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.episode(showMsg: (String(str_textContent.prefix(7)) + String(str_quoteButtonData)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.sprechgesang()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func toTitle() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.manager()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func beWhenGoback() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = colorController() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: DoingRecognizerDelegate.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func sprechgesang() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.allModelPop() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.addPosition(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            ImageToThen.requestPop() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func upCreate() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = ImageMacroDefine.getVoiceRoomAppId()
        profile.appID = ImageMacroDefine.blockId()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: MeasurementAppManager.share.loginUid)
        let user = ZegoUser(userID: MeasurementAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func ergodic(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension ImageToThen: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            SprechstimmeEventReqTool.afterFlooring(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.gestureEqual(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.clickMode()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == NameMultiplierTarget.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.addPosition(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                SprechstimmeEventReqTool.afterFlooring(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.gestureEqual(key: key, model: value)
                        //: if String(value.uid) == MeasurementAppManager.share.loginUid {
                        if String(value.uid) == MeasurementAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.today()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.clickMode()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                a_textTurnName.daguerreotype(eventID: a_clickName, parameterStr: [String(bytes: str_myInfoData, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(str_valueName.lowercased() + "om_kic" + str_atBlockTitle.replacingOccurrences(of: "equal", with: "ko") + "_messa" + str_textManagerValue.replacingOccurrences(of: "reply", with: "e"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(str_modeName) + String(str_pointValue)) || value == (str_textMakeValue.replacingOccurrences(of: "data", with: "m") + "Clos" + String(str_effectValue.suffix(4)) + str_fillEqualValue.replacingOccurrences(of: "block", with: "m")) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    sprechgesang()
                    //: let config = ShowAlertConfig()
                    let config = MarkAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    TitleAlertShow.appearAll(message: String(bytes: str_sizeContent, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: a_qualityStatusPlayContent, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    episode(showMsg: String(bytes: str_styleValue, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                TitleMediumLogTool.commitMedium(msg: (str_numberData.replacingOccurrences(of: "view", with: "I") + "EROO" + String(str_itemData.prefix(4)) + "失\u{8d25}，roo" + String(str_messageName.suffix(5))) + "\(roomID)" + (String(str_subText)) + "\(errorCode)" + (String(str_leadingContent)) + "\(reason)" + (String(str_managerRowText.suffix(5)) + "endedDat" + String(str_modelSearchFieldContent)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                episode(showMsg: (String(str_textContent.prefix(7)) + String(str_quoteButtonData)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                a_textTurnName.daguerreotype(eventID: a_clickName, parameterStr: [String(bytes: str_myInfoData, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            sprechgesang()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            TitleMediumLogTool.commitMedium(msg: (String(str_photoData) + str_addValue + "用户推流\u{5931}" + str_cancelColorTitle + "ID: ") + "\(partyModel.roomId)" + (str_styleName.replacingOccurrences(of: "turn", with: ",") + String(str_oldName.suffix(6)) + "Code: ") + "\(errorCode)" + (String(str_faceData) + String(str_dateValue)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            episode(showMsg: String(bytes: str_titleData.reversed(), encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = remark()
            //: for (position, model) in dict where String(model.uid) == MeasurementAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == MeasurementAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                addPosition(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                vantage(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                sprechgesang()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            TitleMediumLogTool.commitMedium(msg: (String(str_countryValue) + str_colorValue + String(str_indexRawTitle.prefix(8))) + "\(partyModel.roomId)" + (str_styleName.replacingOccurrences(of: "turn", with: ",") + String(str_oldName.suffix(6)) + "Code: ") + "\(errorCode)" + (String(str_faceData) + String(str_dateValue)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                sprechgesang()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.nearData(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = remark()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.nearData(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            TitleMediumLogTool.commitMedium(msg: (str_photoText.uppercased() + "EROO" + str_picText + "解析失\u{8d25}，" + String(str_tingName.suffix(9))) + "\(command)" + (str_pathImageValue.replacingOccurrences(of: "section", with: " ") + String(str_sizeName)) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(str_callTitle))][(str_nearbyName.lowercased() + String(str_toData))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < a_nextName else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = WithDoingHandyJSON.deserialize(from: json[(String(str_callTitle))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(str_allowAppTitle)) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(str_atText.prefix(6)))].stringValue == (String(str_afterName) + str_stateText.replacingOccurrences(of: "to", with: "ch") + "geMike") { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(str_callTitle))][(String(str_thoughtName))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = mikeKey(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    vantage(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                vantage(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(str_callTitle))][(String(str_loopCellValue.suffix(5)) + "itio" + str_countBackgroundData.replacingOccurrences(of: "label", with: "n"))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                polePosition(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.delayIcon()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(str_atText.prefix(6)))].stringValue == (String(str_moreText.prefix(6)) + "admin" + String(str_systemText.suffix(6)) + String(str_countValue.suffix(4))) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(str_callTitle))][(String(str_thoughtName))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = mikeKey(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    vantage(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.delayIcon()
                    //: guard String(oldModel.uid) == MeasurementAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == MeasurementAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    episode(showMsg: (String(str_retData.prefix(9)) + str_checkData.replacingOccurrences(of: "move", with: "k") + " off" + String(str_modelData.suffix(5)) + "seat").localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == MeasurementAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == MeasurementAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == MeasurementAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == MeasurementAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(str_atText.prefix(6)))].stringValue == (String(str_redDomainContent) + String(str_styleContent.suffix(6))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            ImageToThen.labelShared().remark().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    polePosition(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(str_atText.prefix(6)))].stringValue == (str_goldText.replacingOccurrences(of: "leading", with: "r") + ":clo" + String(str_targetContent.suffix(4)) + "ream") { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func polePosition(position: Int, model: WithDoingHandyJSON) {
        //: modifyMikePosition(key: position, model: model)
        gestureEqual(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.openPosition(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        accept(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func vantage(position: Int, model: WithDoingHandyJSON) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        gestureEqual(key: position, model: WithDoingHandyJSON())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.openPosition(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        accept(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func accept(uid: Int) {
        //: guard String(uid) == MeasurementAppManager.share.loginUid else { return }
        guard String(uid) == MeasurementAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = remark()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.re()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension ImageToThen {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func produce() -> [Int: WithDoingHandyJSON] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< a_nextName {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            gestureEqual(key: index, model: WithDoingHandyJSON())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func gestureEqual(key: Int, model: WithDoingHandyJSON) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func mikeKey(key: Int) -> WithDoingHandyJSON? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func remark() -> [Int: WithDoingHandyJSON] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
