
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_modelSizeContent:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "snap_img" :*/
fileprivate let str_sharedMaxVarValue:String = "snap_play to type attribute tab"
fileprivate let str_picName:[Character] = ["i","m","g"]

/*: "present_picture" :*/
fileprivate let str_modelText:String = "prefille"
fileprivate let str_matchData:String = "turbind"

/*: "point_image" :*/
fileprivate let str_scaleLogData:String = "containerin"

/*: "session_pic" :*/
fileprivate let str_faceTitle:String = "simagessi"

/*: "uid" :*/
fileprivate let str_sizeData:[UInt8] = [0x75,0x69,0x64]

/*: "headPic" :*/
fileprivate let str_labContent:String = "headPiclabel array view"

/*: "nickname" :*/
fileprivate let str_nameData:String = "ncolor"
fileprivate let str_managerWithInputData:String = "KNAME"

/*: "birthday" :*/
fileprivate let str_listData:String = "birthimage"
fileprivate let str_atData:String = "ablock"

/*: "videoUrl" :*/
fileprivate let str_topTitle:[Character] = ["v"]
fileprivate let str_tableTitle:[Character] = ["i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let str_nameText:String = "cosharedr"

/*: "location" :*/
fileprivate let str_lastName:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_fieldTitle:[UInt8] = [0x63,0x6d,0x64]

/*: "refuseCall" :*/
fileprivate let str_makeValue:[Character] = ["r","e","f","u","s","e","C","a","l"]
fileprivate let str_blockColorText:String = "data"

/*: "data" :*/
fileprivate let str_bottomName:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let str_backContent:[Character] = ["C","a","l","l"," ","i","n","t"]
fileprivate let str_colorValue:String = "elabel"
fileprivate let str_frameValue:String = "ruptmaken"

/*: "stopCall" :*/
fileprivate let str_displayData:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let str_iconIndexText:String = "with"

/*: "logId" :*/
fileprivate let str_valueName:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let str_burnBackgroundData:String = "sourceansource"

/*: "No answer" :*/
fileprivate let str_actionData:[Character] = ["N","o"," "]
fileprivate let str_makeName:String = "anaspecter"

/*: "duration" :*/
fileprivate let str_frameName:String = "duradirectionon"

/*: "She has hung up" :*/
fileprivate let str_byValue:String = "She himage bar"
fileprivate let str_viewToolName:String = "ung upequal call app"

/*: "cancelSex" :*/
fileprivate let str_statusValue:String = "call"
fileprivate let str_ofContent:String = "ancelSexself show to"

/*: "He has hung up" :*/
fileprivate let str_roomOriginTitle:String = "He hasfile value"
fileprivate let str_itemData:String = "data"

/*: "endType" :*/
fileprivate let str_voiceContent:String = "after videoendTyp"
fileprivate let str_toTitle:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let str_kitTitle:[Character] = ["s","t","o","p","S","e"]
fileprivate let str_userContentData:[Character] = ["x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let str_equalData:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x73,0x6e,0x69,0x6f,0x63,0x20,0x64,0x6c,0x6f,0x67,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let str_centerShareValue:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n"]
fileprivate let str_blockArrayName:String = "target path to newg u"
fileprivate let str_toAppTitle:[Character] = ["p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_jumpData:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_picText:String = "jumpUlet view self"
fileprivate let str_centerCountName:[Character] = ["r","l"]

/*: "Cancel" :*/
fileprivate let str_colorTitle:String = "c"
fileprivate let str_makeCellToValue:String = "antoel"

/*: "Get gold coins" :*/
fileprivate let str_viewData:String = "Get goin style app array"
fileprivate let str_textTitle:[Character] = ["l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let str_modelData:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_playerSignData:String = "runiversalfusuniversal"
fileprivate let str_labData:[Character] = ["S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_frameData:String = "self view table make succeedonAcce"

/*: "acceptCall" :*/
fileprivate let str_popViewPostTitle:String = "ahiddenhiddenept"

/*: "Calls are cancelled" :*/
fileprivate let str_equalContent:String = "Callsapp type direction view index"
fileprivate let str_playerName:String = "namencel"
fileprivate let str_trackSucceedData:String = "LED"

/*: "The call has ended" :*/
fileprivate let str_blockValue:[Character] = ["T","h","e"," ","c","a","l","l"," "]
fileprivate let str_errorIndicatorData:[Character] = ["h","a"]
fileprivate let str_leadingData:[Character] = ["s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HumanActivityPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let a_videoMessageValue = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class HumanActivityPartyDelegate: PropertyViewController {
	var shadowEnable: Bool = true
	var dataMagnitude: Double = -79.4
	var socialArray: [AnyHashable] = []
	var controlDictionary: [AnyHashable: String] = [:]
	var whenOff: Bool = true
	var dataSum: Double = -6.7
	var cancelArray: [AnyHashable] = []
	var liveDictionary: [AnyHashable: String] = [:]
	var ofDoing: Bool = false
	var sizeToMagnitude: Double = -32.7
	var popArray: [AnyHashable] = []
	var handleContentDictionary: [AnyHashable: String] = [:]

    var showImageView: UIImageView?

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ImageChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ManagerCallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: AppReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: EndueView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SocketReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SocketReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SocketReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_modelSizeContent, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ConstraintNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.outsideTotaleraction()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            fromVideo()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            judgeTo(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
            self.showImageView?.image = UIImage(named: (String(str_sharedMaxVarValue.prefix(5)) + String(str_picName)))
        }
        //: interactionView.initCachEffectData()
        interactionView.sumerchange()
    
		if var viewValue = self.chatModel?.isCaller { 
			if var priceValue = chatModel?.logId { 
		            if (windowView.renderView.intrinsicContentSize.height == 268.60) && (windowView.renderView.convert(CGPoint(x: CGFloat(0), y: 0), from: windowView.renderView.superview).x == 42.78) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let renewLet = UpwardlyView(frame: windowView.renderView.bounds.insetBy(dx: CGFloat(478.60), dy: CGFloat(66)))
		            renewLet.tipTotal = priceValue
		            renewLet.eventQuantity = interactionView.giftView.contentHeight
		            renewLet.viewUserDoing = { [self] contentViewSwitch in
		            self.whenOff = contentViewSwitch
		            
		            var renewLet = viewValue
		            renewLet = true
		            if renewLet != self.whenOff {
		                self.whenOff = renewLet
		            }
		            
		            return self.whenOff
		            }
		            renewLet.valueInterval = { [self] mSayMagnitude in
		            self.dataSum = mSayMagnitude
		            
		            var renewLet = interactionView.giftView.contentHeight
		                renewLet -= 1
		                if renewLet != 67 {
		                    renewLet = renewLet + 1
		                }
		            if renewLet > self.dataSum {
		                self.dataSum = renewLet
		            }
		            
		            return self.dataSum
		            }
		            renewLet.nameArray = { [self] freeArray in
		            self.cancelArray = freeArray
		            
		            guard var value = self.cancelArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            renewLet.bottomDictionary = { [self] windowDictionary in
		            self.liveDictionary = windowDictionary
		            
		            guard var value = self.liveDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                windowView.renderView.addSubview(renewLet)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        cardSubviews()
        //: setupSubViewsConstraint()
        startPrice()
        //: initNotificationObserver()
        toApp()
        //: req_callGetUserInfo()
        anySize()
        //: timeoutRules()
        compareRemove()
        //: setUpBGMPlayer()
        golfPlayer()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.tapPushPreview()
        }

        showImageView = UIImageView(frame: self.view.bounds.offsetBy(dx: CGFloat(Int(self.view.frame.size.height)), dy: CGFloat(0)))
        if let showImageView = showImageView {
            self.showImageView?.image = UIImage(named: (str_modelText.replacingOccurrences(of: "fill", with: "s") + "nt_pic" + str_matchData.replacingOccurrences(of: "bind", with: "e")))
		
		//: if_extract_code "bottomOf" begin
		
		var bottomOf = false
		if !bottomOf {
		    var isValue = false
		    if let nextResponder = showImageView.next {
		        isValue = nextResponder.inputView != nil
		    }
		    bottomOf = isValue
		}
		
		//: if_extract_code "bottomOf" end
		
            if (showImageView.animationDuration == TimeInterval(Double(showImageView.center.y))) && bottomOf {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(showImageView)
            }
        }
    
		if var deadlineValue = miniView?.pointAnimView.isPlayAnimation { 
			if var appearSwitchlineValue = self.chatModel?.fee { 
			if let showImageView = showImageView {
		
		            if ((showImageView.inputAssistantItem.trailingBarButtonGroups.count == 8) && (showImageView.inputAssistantItem.leadingBarButtonGroups.count == 5)) && (showImageView.layer.contentsRect.size.width != 1) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let saveData = UpwardlyView()
		            saveData.tipTotal = appearSwitchlineValue
		            saveData.eventQuantity = interactionView.giftView.contentHeight
		            saveData.viewUserDoing = { [self] contentViewSwitch in
		            self.ofDoing = contentViewSwitch
		            
		            var saveData = deadlineValue
		            saveData = false
		            if saveData != self.ofDoing {
		                self.ofDoing = saveData
		            }
		            
		            return self.ofDoing
		            }
		            saveData.valueInterval = { [self] mSayMagnitude in
		            self.sizeToMagnitude = mSayMagnitude
		            
		            var saveData = interactionView.giftView.contentHeight
		                saveData += 1
		                if Int(saveData) > -82 {
		                    saveData = saveData - 1
		                }
		            if saveData < self.sizeToMagnitude {
		                self.sizeToMagnitude = saveData
		            }
		            
		            return self.sizeToMagnitude
		            }
		            saveData.nameArray = { [self] freeArray in
		            self.popArray = freeArray
		            
		            guard var value = self.popArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            saveData.bottomDictionary = { [self] windowDictionary in
		            self.handleContentDictionary = windowDictionary
		            
		            guard var value = self.handleContentDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                showImageView.addSubview(saveData)
		            }
		
			}
		
			}
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MeasurementAppManager.share.appConfigMode.disableShootScreen {
            if MeasurementAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HypsometryReactiveCompatible(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
                self.showImageView?.image = UIImage(named: (str_scaleLogData.replacingOccurrences(of: "container", with: "po") + "t_image"))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        SocketReactiveCompatible.shared.isTalking = false
        self.showImageView?.image = UIImage(named: (str_faceTitle.replacingOccurrences(of: "image", with: "e") + "on_pic"))
        //: self.stopVCAllActivties()
        self.entitle()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: MediaVideoView = {
        //: let v = TalkingBGVideoView()
        let v = MediaVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.callOpen(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: CounteractionView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CounteractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: BeginReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = BeginReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = a_blockValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ConstraintLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: a_limitRecordScreenName + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.equalCancel()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ManageressReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = ManageressReactiveCompatible()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func anySize() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ManageressReactiveCompatible.commentCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_sizeData, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_labContent.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_nameData.replacingOccurrences(of: "color", with: "ic") + str_managerWithInputData.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_listData.replacingOccurrences(of: "image", with: "d") + str_atData.replacingOccurrences(of: "block", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_topTitle) + String(str_tableTitle))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(str_nameText.replacingOccurrences(of: "shared", with: "ve"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_lastName))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.accountingData()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.countimateBy()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func countimateBy() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = EnableCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = EnableCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.everySignature(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.grind(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func fromVideo() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        PrefaceReactiveCompatible.voiceMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.episode(showMsg: a_matchData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_makeValue) + str_blockColorText.replacingOccurrences(of: "data", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_sizeData, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.member()
                //: return
                return
            }

            //: self.startPreview()
            self.tapPushPreview()
            //: self.stopBGM()
            self.dedication()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ManagerCallPlayer.indexType(type: .Response)
            //: self.startBGM()
            self.outsideTotaleraction()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func tapPushPreview() {
        //: self.videoManager.startPreview()
        self.videoManager.mutualMatch()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func entitle() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.dedication()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        DoingReactiveCompatible.resourceSquare()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SocketReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SocketReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SocketReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func member(animated: Bool = true) {
        //: self.stopBGM()
        self.dedication()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func equalCancel() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.groupRequest()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimateBy()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func proceed() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.iconPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimateBy()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.conversationWith()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func conversationWith() {
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }
        guard MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.male.rawValue else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = EndueView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.impute()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func toApp() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(coloredNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(colorModel(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: a_versionButtonCornerTitle,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(refresh(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: a_maxScreenValue,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        a_videoMessageValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func coloredNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            member()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func refresh(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SocketReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SocketReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SocketReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SocketReactiveCompatible.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.behindStart()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func colorModel(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimateBy()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension HumanActivityPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.episode(showMsg: (String(str_backContent) + str_colorValue.replacingOccurrences(of: "label", with: "r") + str_frameValue.replacingOccurrences(of: "make", with: "io")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_displayData) + str_iconIndexText.replacingOccurrences(of: "with", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_valueName.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (str_burnBackgroundData.replacingOccurrences(of: "source", with: "c") + "elCall"), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_valueName.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_makeValue) + str_blockColorText.replacingOccurrences(of: "data", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_sizeData, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.sortExit()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SocketReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            member()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func golfPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? MediaScalar.Call : MediaScalar.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ManagerCallPlayer.indexType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func outsideTotaleraction() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.stigmatise()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func dedication() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.priceTag()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func compareRemove() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? a_netValue : self.chatModel?.answerRow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            addThingAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.addThingAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func addThingAction() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !SocketReactiveCompatible.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.episode(showMsg: (String(str_actionData) + str_makeName.replacingOccurrences(of: "aspect", with: "sw")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_fieldTitle, encoding: .utf8)!: (str_burnBackgroundData.replacingOccurrences(of: "source", with: "c") + "elCall"), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_valueName.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_makeValue) + str_blockColorText.replacingOccurrences(of: "data", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_sizeData, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SocketReactiveCompatible.shared.sendBeautyMoment(info: socketDict)
        //: popCurrentViewController()
        member()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func contentRaw(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_valueName.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_frameName.replacingOccurrences(of: "direction", with: "ti"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.example(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.stage()
            //: createTalkRoom()
            proceed()
            //: self.stopBGM()
            self.dedication()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func judgeTo(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.groupRequest()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.callOpen(show: false)
        //: self.stopBGM()
        self.dedication()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.stage()
        //: self.createTalkRoom()
        self.proceed()
    }
}

// MARK: - VideoCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension HumanActivityPartyDelegate: VideoCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func placeContentAccountingData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_byValue.prefix(5)) + "as h" + String(str_viewToolName.prefix(6))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_statusValue.replacingOccurrences(of: "call", with: "c") + String(str_ofContent.prefix(8)))].stringValue == SizeCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_roomOriginTitle.prefix(6)) + " hung u" + str_itemData.replacingOccurrences(of: "data", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        member()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func metronymic(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        judgeTo(logId: json[String(bytes: str_valueName.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func toTip(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_voiceContent.suffix(6)) + String(str_toTitle))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_byValue.prefix(5)) + "as h" + String(str_viewToolName.prefix(6))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_kitTitle) + String(str_userContentData))].stringValue == SizeCustomReflectable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_roomOriginTitle.prefix(6)) + " hung u" + str_itemData.replacingOccurrences(of: "data", with: "p")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: str_equalData.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_centerShareValue) + String(str_blockArrayName.suffix(3)) + String(str_toAppTitle)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.meanwhile()
        //: popCurrentViewController()
        member()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func towardUser(msg: String, data: [String: Any]) {
        //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_jumpData))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_picText.prefix(5)) + String(str_centerCountName))] as? String else { return }
            //: let httpsUrl = MeasurementAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MeasurementAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = LabConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LimitPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LimitPushManager.share.comb(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TitleAlertShow.admittance(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (str_colorTitle.uppercased() + str_makeCellToValue.replacingOccurrences(of: "to", with: "c")).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(str_viewData.prefix(6)) + String(str_textTitle)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TitleAlertShow.lastAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: LimitPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                LimitPushManager.share.billParams(clickEvent: a_recordClickValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func appearFinish(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.mediumShow(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func startDown(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_modelData))].doubleValue
        let str = "\(addBean)"
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.infoPoint(point: str)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.offline(point: str)
        }
    }
}

// MARK: - CellObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension HumanActivityPartyDelegate: CellObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_byValue.prefix(5)) + "as h" + String(str_viewToolName.prefix(6))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_playerSignData.replacingOccurrences(of: "universal", with: "e") + String(str_labData))].stringValue == SizeCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_roomOriginTitle.prefix(6)) + " hung u" + str_itemData.replacingOccurrences(of: "data", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        member()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func passePartout(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        judgeTo(logId: json[String(bytes: str_valueName.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - OfErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension HumanActivityPartyDelegate: OfErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func mixUp(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(str_makeValue) + str_blockColorText.replacingOccurrences(of: "data", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.episode(showMsg: (String(str_actionData) + str_makeName.replacingOccurrences(of: "aspect", with: "sw")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.sortExit()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SocketReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            member()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_frameData.suffix(6)) + "ptCall") && errorNo == 101 { // 接听通话时，余额不足
            //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else { return }
            //: LimitPushManager.share.func__jumpToWebRecharge(sufficient: false)
            LimitPushManager.share.billParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_frameData.suffix(6)) + "ptCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.episode(showMsg: msg)
            //: popCurrentViewController()
            member()
        }
    }
}

// MARK: - LaterObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension HumanActivityPartyDelegate: LaterObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func pastModel() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (str_popViewPostTitle.replacingOccurrences(of: "hidden", with: "c") + "Call"), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_sizeData, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func dub() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.episode(showMsg: (String(str_equalContent.prefix(5)) + " are " + str_playerName.replacingOccurrences(of: "name", with: "ca") + str_trackSucceedData.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (str_burnBackgroundData.replacingOccurrences(of: "source", with: "c") + "elCall"), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_valueName.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.episode(showMsg: (String(str_equalContent.prefix(5)) + " are " + str_playerName.replacingOccurrences(of: "name", with: "ca") + str_trackSucceedData.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_makeValue) + str_blockColorText.replacingOccurrences(of: "data", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_sizeData, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.episode(showMsg: (String(str_blockValue) + String(str_errorIndicatorData) + String(str_leadingData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_fieldTitle, encoding: .utf8)!: (String(str_displayData) + str_iconIndexText.replacingOccurrences(of: "with", with: "l")), String(bytes: str_bottomName.reversed(), encoding: .utf8)!: [String(bytes: str_valueName.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        member()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func blockDown(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.modelTarget(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func holderMatch(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.callOpen(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.useViewHood(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimateBy()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func skirt() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        SocketReactiveCompatible.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = AppReactiveCompatible.countervalStartView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.modeLimit()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.dataFile()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.colorController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        member()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func imageSizeSucceed() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension HumanActivityPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func cardSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.fillFirst(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func startPrice() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
