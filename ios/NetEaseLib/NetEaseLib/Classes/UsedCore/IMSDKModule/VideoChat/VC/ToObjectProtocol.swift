
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_makeConvertTitle:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "point_image" :*/
fileprivate let str_grayCoverData:[Character] = ["p","o","i","n","t"]
fileprivate let str_downBorderName:[Character] = ["_","i","m","a","g","e"]

/*: "equal_observe_pic" :*/
fileprivate let str_addName:String = "equal_commit info send origin image"
fileprivate let str_indexValueContent:[Character] = ["r"]
fileprivate let str_selectedText:String = "info data target if lessve_pic"

/*: "inside_img" :*/
fileprivate let str_toolData:[Character] = ["i","n"]
fileprivate let str_miniContent:String = "status selfside_img"

/*: "uid" :*/
fileprivate let str_sendDomainName:[UInt8] = [0x75,0x69,0x64]

/*: "headPic" :*/
fileprivate let str_titleValue:String = "headPicadd size resistance pin self"

/*: "nickname" :*/
fileprivate let str_styleBorderData:String = "logck"

/*: "birthday" :*/
fileprivate let str_emptyName:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let str_shareName:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let str_outsideTitle:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_videoValue:[UInt8] = [0x61,0x6f,0x66]

/*: "refuseCall" :*/
fileprivate let str_timeValue:String = "refname"

/*: "data" :*/
fileprivate let str_dismissContent:[UInt8] = [0x64,0x61,0x74,0x61]

/*: "Call interruption" :*/
fileprivate let str_stickName:String = "select formal right letCall"
fileprivate let str_rowText:String = "ERRU"

/*: "stopCall" :*/
fileprivate let str_requestName:String = "stopCalanguage pair"
fileprivate let str_thanData:[Character] = ["l","l"]

/*: "logId" :*/
fileprivate let str_groupValue:[UInt8] = [0x6c,0x6f,0x67,0x49,0x64]

/*: "cancelCall" :*/
fileprivate let str_outsideName:String = "cviewcel"

/*: "No answer" :*/
fileprivate let str_usFullData:String = "No anlet text round return"

/*: "duration" :*/
fileprivate let str_portData:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let str_responseValue:String = "She haimage other view"
fileprivate let str_complyValue:String = "for equalg up"

/*: "cancelSex" :*/
fileprivate let str_conversationPostContent:[Character] = ["c","a","n","c","e","l","S"]
fileprivate let str_genderEdgeValue:[Character] = ["e","x"]

/*: "He has hung up" :*/
fileprivate let str_byText:String = "He hreturn frame aspect message"
fileprivate let str_timeName:String = "ng upmake equal"

/*: "endType" :*/
fileprivate let str_buttonValue:String = "finish color imageendType"

/*: "stopSex" :*/
fileprivate let str_toValue:[Character] = ["s","t","o","p","S","e"]
fileprivate let str_kitTitle:String = "player"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let str_maxCostContent:[UInt8] = [0x49,0x6e,0x73,0x75,0x66,0x66,0x69,0x63,0x69,0x65,0x6e,0x74,0x20,0x67,0x6f,0x6c,0x64,0x20,0x63,0x6f,0x69,0x6e,0x73,0x2c,0x20,0x63,0x61,0x6c,0x6c,0x20,0x68,0x61,0x73,0x20,0x65,0x6e,0x64,0x65,0x64]

/*: "The other hung up the phone" :*/
fileprivate let str_imageValue:String = "extension add model let dataThe "
fileprivate let str_lastValue:String = "let if data imager hung u"
fileprivate let str_numberTitle:String = "should heightp the"
fileprivate let str_requestData:[Character] = [" ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_itemData:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_wrapTitle:[Character] = ["j","u","m","p","U"]
fileprivate let str_viewData:String = "rcolor"

/*: "Cancel" :*/
fileprivate let str_iconSelectedContent:[Character] = ["C","a","n","c","e"]
fileprivate let str_userDataTitle:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let str_searchText:[Character] = ["G","e","t"," ","g","o","l","d"," "]
fileprivate let str_norViewName:[Character] = ["c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let str_modelValue:String = "addBeancell string"

/*: "refuseSex" :*/
fileprivate let str_managerButtonTitle:String = "end self time equalrefuseSex"

/*: "onAcceptCall" :*/
fileprivate let str_modeName:[Character] = ["o","n","A","c","c"]
fileprivate let str_requestViewValue:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let str_roomName:String = "apope"
fileprivate let str_showData:String = "make"

/*: "Calls are cancelled" :*/
fileprivate let str_extraScaleViewData:String = "Calls amode size import view load"
fileprivate let str_partyTitle:[Character] = ["r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_postName:[Character] = ["T","h","e"," ","c","a","l","l"]
fileprivate let str_scaleViewContent:[Character] = [" ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let a_keyMarginText = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ToObjectProtocol: PropertyViewController {
	var resultSubSum: Int = 54
	var sectionSum: Double = -54.6
	var viewArray: [AnyHashable] = []
	var effectSum: Int = 42
	var viewTotal: Int = 72
	var iconWithMagnitude: Double = 23.6
	var pieceArray: [AnyHashable] = []

    var windowImageView: UIImageView?

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ImageChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ManagerCallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: CellMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SocketReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SocketReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SocketReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_makeConvertTitle, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ConstraintNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        self.windowImageView?.image = UIImage(named: (String(str_grayCoverData) + String(str_downBorderName)))
        //: self.startBGM()
        self.contentBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            appearData()
        }
    
            if (self.preferredStatusBarUpdateAnimation == .slide) && (self.title != nil && self.title!.contains("ting")) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let doingHead = PhoneRateController()
            doingHead.someoneCount = interactionView.giftView.contentHeight
            doingHead.dataName = interactionView.giftView.chatRoomID
            doingHead.clickCount = { [self] lodestarQuantity in
            self.resultSubSum = lodestarQuantity
            
            var doingHead = interactionView.giftView.keyborHeight
                doingHead += 1
                if doingHead != 58 {
                    doingHead = doingHead - 1
                }
            if doingHead > self.resultSubSum {
                self.resultSubSum = doingHead
            }
            
            return self.resultSubSum
            }
            doingHead.managerInterval = { [self] equalInterval in
            self.sectionSum = equalInterval
            
            var doingHead = interactionView.giftView.contentHeight
            doingHead -= 1
            if doingHead < self.sectionSum {
                self.sectionSum = doingHead
            }
            
                self.sectionSum += 1
                if Int(self.sectionSum) > -62 {
                    self.sectionSum = self.sectionSum - 1
                }
            return self.sectionSum
            }
            doingHead.buttonArray = { [self] betwixtArray in
            self.viewArray = betwixtArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
                self.navigationController?.present(doingHead.self, animated: false) { [self] in
            self.sectionSum = 336.71
                }
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            self.windowImageView?.image = UIImage(named: (String(str_grayCoverData) + String(str_downBorderName)))
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sum()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        untilUp()
        //: setupSubViewsConstraint()
        penalise()
        //: initNotificationObserver()
        notification()
        //: req_callGetUserInfo()
        sharedFor()
        //: timeoutRules()
        rules()
        //: resumeTalk()
        detailUser()
        //: setUpBGMPlayer()
        scaleSearch()

        windowImageView = UIImageView(frame: self.view.frame.standardized)
        if let windowImageView = windowImageView {
            self.windowImageView?.image = UIImage(named: (String(str_addName.prefix(6)) + "obse" + String(str_indexValueContent) + String(str_selectedText.suffix(6))))
            if (windowImageView.isMultipleTouchEnabled) && (windowImageView.layer.contentsRect.origin.y != 0) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(windowImageView)
            }
        }
    
		if var withValue = self.chatModel?.logId { 
			if var manageressValue = miniView?.pointAnimView.isPlayAnimation { 
			if let windowImageView = windowImageView {
		
		
		//: if_extract_code "modelTalk" begin
		
		var modelTalk = false
		if !modelTalk {
		    var isValue = false
		    if #available(iOS 13.0, *) {
		        isValue = windowImageView.editingInteractionConfiguration == .none
		    }
		    modelTalk = isValue
		}
		
		//: if_extract_code "modelTalk" end
		
		            if (modelTalk) && (windowImageView.clipsToBounds) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let messageTotalimate = PhoneRateView(frame: windowImageView.bounds.standardized)
		            messageTotalimate.passAwayDoing = manageressValue
		            messageTotalimate.numerosityCount = interactionView.giftView.contentHeight
		            messageTotalimate.shadowRoomQuantity = { [self] lodestarQuantity in
		            self.effectSum = lodestarQuantity
		            
		            var messageTotalimate = withValue
		            messageTotalimate &= 9
		            if messageTotalimate < self.effectSum {
		                self.effectSum = messageTotalimate
		            }
		            
		            return self.effectSum
		            }
		                windowImageView.addSubview(messageTotalimate)
		            }
		
			}
		
			}
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MeasurementAppManager.share.appConfigMode.disableShootScreen {
            if MeasurementAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HypsometryReactiveCompatible(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        self.windowImageView?.image = UIImage(named: (String(str_toolData) + String(str_miniContent.suffix(8))))
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        SocketReactiveCompatible.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.translationTo()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: ModelImageView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ModelImageView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: DateModelReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = DateModelReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func sharedFor() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ManageressReactiveCompatible.commentCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_sendDomainName, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_titleValue.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_styleBorderData.replacingOccurrences(of: "log", with: "ni") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(str_emptyName))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_shareName))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_outsideTitle))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.startDetail()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func appearData() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PrefaceReactiveCompatible.pryUpward(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.episode(showMsg: a_matchData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_timeValue.replacingOccurrences(of: "name", with: "us") + "eCall"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_sendDomainName, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.fosterFamilyAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.counterrupt()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ManagerCallPlayer.indexType(type: .Response)
            //: self.startBGM()
            self.contentBgm()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func translationTo() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.counterrupt()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SocketReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SocketReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SocketReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func fosterFamilyAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.counterrupt()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func levelWith() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.valid(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func detailUser() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            levelWith()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func notification() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(afterApplication(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(slipUpTitle(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: a_maxScreenValue,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        a_keyMarginText.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func afterApplication(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            fosterFamilyAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func slipUpTitle(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SocketReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SocketReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SocketReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SocketReactiveCompatible.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.stopInPerusalHospitalRoom(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ToObjectProtocol: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.episode(showMsg: (String(str_stickName.suffix(4)) + " int" + str_rowText.lowercased() + "ption").localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (String(str_requestName.prefix(6)) + String(str_thanData)), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_groupValue, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_outsideName.replacingOccurrences(of: "view", with: "an") + "Call"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_groupValue, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_timeValue.replacingOccurrences(of: "name", with: "us") + "eCall"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_sendDomainName, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.sortExit()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SocketReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            fosterFamilyAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.needAllEnable(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func scaleSearch() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? MediaScalar.Call : MediaScalar.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ManagerCallPlayer.indexType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func contentBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.stigmatise()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func counterrupt() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.priceTag()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 超时定时器
    //: private func timeoutRules() {
    private func rules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? a_netValue : self.chatModel?.answerRow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            muscleCounto()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.muscleCounto()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func muscleCounto() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !SocketReactiveCompatible.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.episode(showMsg: (String(str_usFullData.prefix(5)) + "swer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_outsideName.replacingOccurrences(of: "view", with: "an") + "Call"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_groupValue, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_timeValue.replacingOccurrences(of: "name", with: "us") + "eCall"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_sendDomainName, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SocketReactiveCompatible.shared.sendBeautyMoment(info: socketDict)
        //: popCurrentViewController()
        fosterFamilyAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func observerOf(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_groupValue, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_portData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.auspicate(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.showSizePeriodOfTime()
            //: createTalkRoom()
            levelWith()
            //: self.stopBGM()
            self.counterrupt()
        }
    }
}

// MARK: - VideoCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ToObjectProtocol: VideoCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func placeContentAccountingData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_responseValue.prefix(6)) + "s hun" + String(str_complyValue.suffix(4))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_conversationPostContent) + String(str_genderEdgeValue))].stringValue == SizeCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_byText.prefix(4)) + "as hu" + String(str_timeName.prefix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        fosterFamilyAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func metronymic(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_groupValue, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.counterrupt()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.showSizePeriodOfTime()
        //: self.createTalkRoom()
        self.levelWith()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func toTip(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_buttonValue.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_responseValue.prefix(6)) + "s hun" + String(str_complyValue.suffix(4))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_toValue) + str_kitTitle.replacingOccurrences(of: "player", with: "x"))].stringValue == SizeCustomReflectable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_byText.prefix(4)) + "as hu" + String(str_timeName.prefix(5))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: str_maxCostContent, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_imageValue.suffix(4)) + "othe" + String(str_lastValue.suffix(8)) + String(str_numberTitle.suffix(5)) + String(str_requestData)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.doGift()
        //: self.popCurrentViewController()
        self.fosterFamilyAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: a_cornerData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func towardUser(msg: String, data: [String: Any]) {
        //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_itemData))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_wrapTitle) + str_viewData.replacingOccurrences(of: "color", with: "l"))] as? String else { return }
            //: let httpsUrl = MeasurementAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MeasurementAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = LabConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LimitPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LimitPushManager.share.comb(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TitleAlertShow.admittance(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(str_iconSelectedContent) + String(str_userDataTitle)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(str_searchText) + String(str_norViewName)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TitleAlertShow.lastAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: LimitPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                LimitPushManager.share.billParams(clickEvent: a_recordClickValue, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func startDown(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_modelValue.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.statuteName(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.bombUp(point: addBean)
        }
    }
}

// MARK: - CellObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ToObjectProtocol: CellObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_responseValue.prefix(6)) + "s hun" + String(str_complyValue.suffix(4))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_managerButtonTitle.suffix(9)))].stringValue == SizeCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_byText.prefix(4)) + "as hu" + String(str_timeName.prefix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.episode(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.sortExit()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SocketReactiveCompatible.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        fosterFamilyAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func passePartout(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_groupValue, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.counterrupt()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.showSizePeriodOfTime()
        //: self.createTalkRoom()
        self.levelWith()
    }
}

// MARK: - OfErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ToObjectProtocol: OfErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func mixUp(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_timeValue.replacingOccurrences(of: "name", with: "us") + "eCall") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.episode(showMsg: (String(str_usFullData.prefix(5)) + "swer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.sortExit()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SocketReactiveCompatible.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.fosterFamilyAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_modeName) + String(str_requestViewValue)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MeasurementAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MeasurementAppManager.share.appStatus == LabCustomReflectable.normal.rawValue else { return }
            //: LimitPushManager.share.func__jumpToWebRecharge(sufficient: false)
            LimitPushManager.share.billParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_modeName) + String(str_requestViewValue)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.episode(showMsg: msg)
            //: self.popCurrentViewController()
            self.fosterFamilyAnimated()
        }
    }
}

// MARK: - TextViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ToObjectProtocol: TextViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func jobLot() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_roomName.replacingOccurrences(of: "pop", with: "cc") + "ptCal" + str_showData.replacingOccurrences(of: "make", with: "l")), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_sendDomainName, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func endTo() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.episode(showMsg: (String(str_extraScaleViewData.prefix(7)) + String(str_partyTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_outsideName.replacingOccurrences(of: "view", with: "an") + "Call"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_groupValue, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.episode(showMsg: (String(str_extraScaleViewData.prefix(7)) + String(str_partyTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (str_timeValue.replacingOccurrences(of: "name", with: "us") + "eCall"), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_sendDomainName, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.episode(showMsg: (String(str_postName) + String(str_scaleViewContent)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            SocketReactiveCompatible.shared.sendBeautyMoment(info: [String(bytes: str_videoValue.map{$0^2}, encoding: .utf8)!: (String(str_requestName.prefix(6)) + String(str_thanData)), String(bytes: str_dismissContent, encoding: .utf8)!: [String(bytes: str_groupValue, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.doGift()
        //: self.popCurrentViewController()
        self.fosterFamilyAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func addMore(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.adjust(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func giveAndTakeErrorHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.headTo(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func shortlist() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        SocketReactiveCompatible.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = CellMiniView.vocalisationButton()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.colorController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.viewWithAdd()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.talking(talkingTime)
        //: popCurrentViewController()
        fosterFamilyAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func callTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.talking(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func labBench(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.mottle(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ToObjectProtocol {
    /// 添加视图
    //: private func setupSubviews() {
    private func untilUp() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func penalise() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
