
//: Declare String Begin

/*: "nil" :*/
fileprivate let str_momentName:[Character] = ["n","i","l"]

/*: "Edit profiles" :*/
fileprivate let str_infoData:[Character] = ["E","d","i"]
fileprivate let str_nameValue:String = "t profbefore var for m"

/*: "Done" :*/
fileprivate let str_topText:[Character] = ["D","o","n","e"]

/*: "#999999" :*/
fileprivate let str_requestName:[Character] = ["#","9","9","9","9","9","9"]

/*: "leading_add_image" :*/
fileprivate let str_fromTimeTitle:[Character] = ["l","e","a","d","i"]
fileprivate let str_pathValue:[Character] = ["n","g","_"]
fileprivate let str_requestData:[Character] = ["a","d","d","_","i","m","a","g","e"]

/*: "uid" :*/
fileprivate let str_coinValue:[Character] = ["u","i","d"]

/*: "MediumViewCell" :*/
fileprivate let str_pointData:String = "Talkingself cell to view"
fileprivate let str_roomValue:[Character] = ["E","d","i","t","N","a","m","e","C","e","l","l"]

/*: "LabSignCell" :*/
fileprivate let str_atCheckedData:String = "Talkto path let view"
fileprivate let str_norData:[Character] = ["i","n","g","E","d","i","t","S","i","g","n","C","e","l","l"]

/*: "Save the change?" :*/
fileprivate let str_maxValue:String = "Save tlet name"
fileprivate let str_miniData:String = "user"
fileprivate let str_makeName:[Character] = ["e"," ","c","h","a","n","g","e","?"]

/*: "Cancel" :*/
fileprivate let str_valueAddDeleteText:[Character] = ["C","a","n","c","e","l"]

/*: "Save" :*/
fileprivate let str_layerHideText:String = "number in nameSave"

/*: "nickname" :*/
fileprivate let str_indexLabText:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let str_countVideoName:String = "BIRT"

/*: "desc" :*/
fileprivate let str_modelTitle:String = "desview"

/*: "Modify the success" :*/
fileprivate let str_addTitle:String = "Modiapp gift year"
fileprivate let str_equalText:String = "he sutrue image self icon lab"
fileprivate let str_toUserValue:String = "ccstartss"

/*: "pic" :*/
fileprivate let str_modeName:String = "PIC"

/*: "[ :*/
fileprivate let str_requestValue:[Character] = ["["]

/*: ]" :*/
fileprivate let str_kitData:[Character] = ["]"]

/*: "pids" :*/
fileprivate let str_inputData:[Character] = ["p","i","d","s"]

/*: "aboutMe" :*/
fileprivate let str_managerData:[Character] = ["a","b"]
fileprivate let str_errorTotalData:String = "medium self returnoutMe"

/*: "interest" :*/
fileprivate let str_requestEqualData:[Character] = ["i","n"]
fileprivate let str_coinData:String = "currentrest"

/*: "tagIds" :*/
fileprivate let str_byName:String = "with"
fileprivate let str_topData:String = "extension writing for skin modelagIds"

/*: "category" :*/
fileprivate let str_userValue:String = "categolayer"

/*: "An error occurred, please try again" :*/
fileprivate let str_nameRawTitle:[UInt8] = [0x41,0x6e,0x20,0x65,0x72,0x72,0x6f,0x72,0x20,0x6f,0x63,0x63,0x75,0x72,0x72,0x65,0x64,0x2c,0x20,0x70,0x6c,0x65,0x61,0x73,0x65,0x20,0x74,0x72,0x79,0x20,0x61,0x67,0x61,0x69,0x6e]

/*: "imgUrl" :*/
fileprivate let str_textTimeName:[Character] = ["i","m","g","U","r","l"]

/*: "videoUrl" :*/
fileprivate let str_containValue:String = "center user make stylevideoUrl"

/*: "Video cover submitted successfully" :*/
fileprivate let str_intervalValue:[UInt8] = [0x56,0x69,0x64,0x65,0x6f,0x20,0x63,0x6f,0x76,0x65,0x72,0x20,0x73,0x75,0x62,0x6d,0x69,0x74,0x74,0x65,0x64,0x20,0x73,0x75,0x63,0x63,0x65,0x73,0x73,0x66,0x75,0x6c,0x6c,0x79]

/*: "TelevisionReactiveCompatible" :*/
fileprivate let str_editText:String = "Talkilet name beauty self"
fileprivate let str_fillThoughtContent:String = "Videview section"

/*: "video_url" :*/
fileprivate let str_replyWillValue:[Character] = ["v","i","d","e","o","_","u","r"]
fileprivate let str_originValue:String = "play"

/*: "img_url" :*/
fileprivate let str_sizeTitle:String = "img_urlfalse succeed make model capacity"

/*: "status" :*/
fileprivate let str_indexNameValue:String = "modeltatumodel"

/*: "SheView" :*/
fileprivate let str_fitValue:String = "Talkif name view"
fileprivate let str_startColorText:String = "table truetPhoto"

/*: "LeadingBirthdayCell" :*/
fileprivate let str_playerData:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","B","i","r","t","h","d","a","y"]
fileprivate let str_labValue:String = "if else video content equalCell"

/*: "VisualImageView" :*/
fileprivate let str_dismissName:[Character] = ["T","a","l","k","i","n","g","E"]
fileprivate let str_viewEdgeEqualData:String = "label name video imageditA"
fileprivate let str_giftName:[Character] = ["M","e","C","e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let str_formatValue:String = "UITabtop tab"
fileprivate let str_showContent:String = "leVielse component on input model"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TableModelViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class TableModelViewController: PropertyViewController {
	var viewMagnitude: Double = -27.0
	var moveArray: [AnyHashable] = []
	var playerSum: Double = 67.2
	var postNameArray: [AnyHashable] = []
	var indexDoing: Bool = false

    var sharedImageView: UIImageView?

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [OneTransformable] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [OneTransformable] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [CellMeasurable] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (String(str_momentName))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.playerEqual()
        //: self.title = "Edit profiles".localized
        self.title = (String(str_infoData) + String(str_nameValue.prefix(6)) + "iles").localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(str_topText)).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_requestName))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moveIndicator), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = MeasurementAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = MeasurementAppManager.share.loginUserMode.aboutMe!
        //: seleteInters = MeasurementAppManager.share.loginUserMode.interest!
        seleteInters = MeasurementAppManager.share.loginUserMode.interest!
        //: designView()
        jabbing()
        //: GetGallery()
        audience()
        //: getVideoInfo()
        details()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(deck(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)

        sharedImageView = UIImageView(frame: self.view.frame.insetBy(dx: CGFloat(0), dy: CGFloat(0)))
        if let sharedImageView = sharedImageView {
            self.sharedImageView?.image = UIImage(named: (String(str_fromTimeTitle) + String(str_pathValue) + String(str_requestData)))
            if (sharedImageView.layoutMargins.left == 1.25) && (sharedImageView.motionEffects.count == 19) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(sharedImageView)
            }
        }
    
            if (self.childForStatusBarHidden != nil && self.childForStatusBarHidden!.edgesForExtendedLayout == .right) && (self.transitioningDelegate != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let getStartedModel = PlayerReasonController()
            getStartedModel.limitOn = self.isdeleteInters
            getStartedModel.colorCount = videoStatus

            
            getStartedModel.imageCount = { [self] pagingNumber in
            self.playerSum = pagingNumber
            
            return self.playerSum
            }
            getStartedModel.priceArray = { [self] constraintLiveArray in
            self.postNameArray = constraintLiveArray
            
            guard var value = self.postNameArray as? [String] else {
                return nil
            }
            return value
            }
                self.navigationController?.present(getStartedModel.self, animated: false) { [self] in
            self.indexDoing = false
                }
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		
		//: if_extract_code "subjectSharedTop" begin
		
		var subjectSharedTop = false
		if !subjectSharedTop {
		    var isValue = false
		    if let nextResponder = next {
		        isValue = nextResponder.inputView != nil
		    }
		    subjectSharedTop = isValue
		}
		
		//: if_extract_code "subjectSharedTop" end
		
            if (subjectSharedTop) && (self.prefersStatusBarHidden) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let tableVoice = PlayerReasonController()
            tableVoice.limitOn = animated
            tableVoice.colorCount = videoStatus

            
            tableVoice.imageCount = { [self] pagingNumber in
            self.viewMagnitude = pagingNumber
            
            return self.viewMagnitude
            }
            tableVoice.priceArray = { [self] constraintLiveArray in
            self.moveArray = constraintLiveArray
            
            guard var value = self.moveArray as? [String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(tableVoice.self, animated: true)
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        SizeHungReactiveCompatible.dataMessage()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension TableModelViewController {
    //: func GetGallery() {
    func audience() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = MeasurementAppManager.share.loginUserMode.userID
        dict[(String(str_coinValue))] = MeasurementAppManager.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        ConstraintRequestTool.roomCompletion(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<CellMeasurable>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [CellMeasurable])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func noNavi() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_pointData.prefix(7)) + String(str_roomValue)) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: MediumViewCell = i as! MediumViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_atCheckedData.prefix(4)) + String(str_norData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabSignCell = i as! LabSignCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&MeasurementAppManager.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&MeasurementAppManager.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && MeasurementAppManager.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && MeasurementAppManager.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && MeasurementAppManager.share.loginUserMode.birthday != self.birthday) || (signStr != (String(str_momentName)) && MeasurementAppManager.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = MarkAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            TitleAlertShow.appearAll(message: (String(str_maxValue.prefix(6)) + str_miniData.replacingOccurrences(of: "user", with: "h") + String(str_makeName)).localized, leftBtnTitle: (String(str_valueAddDeleteText)).localized, rightBtnTitle: (String(str_layerHideText.suffix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                TitleAlertShow.lastAlert()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                TitleAlertShow.lastAlert()
                //: self.uploadTool(isBack: true)
                self.makeArray(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func moveIndicator() {
        //: uploadTool(isBack: false)
        makeArray(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func makeArray(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_pointData.prefix(7)) + String(str_roomValue)) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: MediumViewCell = i as! MediumViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_atCheckedData.prefix(4)) + String(str_norData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabSignCell = i as! LabSignCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && MeasurementAppManager.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, MeasurementAppManager.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(String(str_indexLabText))] = nameStr
        }
        //: if birthday.count > 0 && MeasurementAppManager.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, MeasurementAppManager.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(str_countVideoName.lowercased() + "hday")] = birthday
        }
        //: if signStr != "nil" && MeasurementAppManager.share.loginUserMode.signature != signStr {
        if signStr != (String(str_momentName)), MeasurementAppManager.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(str_modelTitle.replacingOccurrences(of: "view", with: "c"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        loadSize(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue {
            //: uploadVideo()
            line()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        ConstraintRequestTool.indexOf(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.afterTitle(showMsg: (String(str_addTitle.prefix(4)) + "fy t" + String(str_equalText.prefix(5)) + str_toUserValue.replacingOccurrences(of: "start", with: "e")).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: MeasurementAppManager.share.loginUserMode.nickname = self.nameStr
                    MeasurementAppManager.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: MeasurementAppManager.share.loginUserMode.birthday = self.birthday
                    MeasurementAppManager.share.loginUserMode.birthday = self.birthday
                }
                //: MeasurementAppManager.share.loginUserMode.signature = self.signStr
                MeasurementAppManager.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.episode(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func loadSize(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            formatPhoto()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                prolusion()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            deleteInType(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            deleteInType(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func prolusion() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].priceVideo()! as NSData
            //: dict["pic"] = resultData
            dict[(str_modeName.lowercased())] = resultData
            //: ProgressHUD.show()
            SizeHungReactiveCompatible.picCurrent()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            ConstraintRequestTool.arrayCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.afterTitle(showMsg: (String(str_addTitle.prefix(4)) + "fy t" + String(str_equalText.prefix(5)) + str_toUserValue.replacingOccurrences(of: "start", with: "e")).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.audience()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func formatPhoto() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(String(str_inputData))] = str
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        ConstraintRequestTool.perspectiveCompletion(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.prolusion()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.afterTitle(showMsg: (String(str_addTitle.prefix(4)) + "fy t" + String(str_equalText.prefix(5)) + str_toUserValue.replacingOccurrences(of: "start", with: "e")).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.audience()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func deleteInType(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [OneTransformable] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (String(str_managerData) + String(str_errorTotalData.suffix(5)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(str_requestEqualData) + str_coinData.replacingOccurrences(of: "current", with: "te"))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(str_byName.replacingOccurrences(of: "with", with: "t") + String(str_topData.suffix(5)))] = str
        //: dict["category"] = category
        dict[(str_userValue.replacingOccurrences(of: "layer", with: "ry"))] = category
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        ConstraintRequestTool.paramsComponent(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.afterTitle(showMsg: (String(str_addTitle.prefix(4)) + "fy t" + String(str_equalText.prefix(5)) + str_toUserValue.replacingOccurrences(of: "start", with: "e")).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: MeasurementAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    MeasurementAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: MeasurementAppManager.share.loginUserMode.interest = self.seleteInters
                    MeasurementAppManager.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func line() {
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        MediumLabThen.shared.all(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.episode(showMsg: String(bytes: str_nameRawTitle, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(str_textTimeName))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(str_containValue.suffix(8)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            ConstraintRequestTool.equalReceiveNumberimate(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.episode(showMsg: String(bytes: str_nameRawTitle, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.afterTitle(showMsg: String(bytes: str_intervalValue, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(str_editText.prefix(5)) + "ngEdit" + String(str_fillThoughtContent.prefix(4)) + "oCell") {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: TelevisionReactiveCompatible = i as! TelevisionReactiveCompatible
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.informantPath(videoPlayPath: dict[(String(str_containValue.suffix(8)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.panoramicView(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func details() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        ConstraintRequestTool.price { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(str_replyWillValue) + str_originValue.replacingOccurrences(of: "play", with: "l"))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(str_sizeTitle.prefix(7)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(str_indexNameValue.replacingOccurrences(of: "model", with: "s"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension TableModelViewController {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func deck(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_pointData.prefix(7)) + String(str_roomValue)) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: MediumViewCell = i as! MediumViewCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_atCheckedData.prefix(4)) + String(str_norData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabSignCell = i as! LabSignCell
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension TableModelViewController: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((a_blockValue - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.window(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.window(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(str_editText.prefix(5)) + "ngEdit" + String(str_fillThoughtContent.prefix(4)) + "oCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TelevisionReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = TelevisionReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.panoramicView(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.roundItemPath(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.informantPath(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(str_fitValue.prefix(4)) + "ingEdi" + String(str_startColorText.suffix(6)) + "Cell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? SheView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = SheView(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.qualify(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.imageOfLive()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(str_pointData.prefix(7)) + String(str_roomValue))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? MediumViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = MediumViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(str_playerData) + String(str_labValue.suffix(4)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? LeadingBirthdayCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = LeadingBirthdayCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(str_atCheckedData.prefix(4)) + String(str_norData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? LabSignCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = LabSignCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(str_dismissName) + String(str_viewEdgeEqualData.suffix(4)) + "bout" + String(str_giftName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? VisualImageView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = VisualImageView(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.sizeTitle()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.outline(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.outline(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = AtBlockViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.anima(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.anima(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.anima(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.anima(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(str_formatValue.prefix(5)) + String(str_showContent.prefix(4)) + "ewCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func window(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [OneTransformable]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.thoughtImage(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > a_blockValue - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - MarkTagDelegate

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension TableModelViewController: MarkTagDelegate {
    //: func freshSeleteTag() {
    func searchedTag() {
        //: seleteAboutme =  MeasurementAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = MeasurementAppManager.share.loginUserMode.aboutMe!
        //: seleteInters =  MeasurementAppManager.share.loginUserMode.interest!
        seleteInters = MeasurementAppManager.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - ErrorPhotoDelegate

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension TableModelViewController: ErrorPhotoDelegate {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func marque(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = CellMeasurable()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func errorView(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func beforeWith(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension TableModelViewController {
    //: private func designView() {
    private func jabbing() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(TelevisionReactiveCompatible.self, forCellReuseIdentifier: (String(str_editText.prefix(5)) + "ngEdit" + String(str_fillThoughtContent.prefix(4)) + "oCell"))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(SheView.self, forCellReuseIdentifier: (String(str_fitValue.prefix(4)) + "ingEdi" + String(str_startColorText.suffix(6)) + "Cell"))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(MediumViewCell.self, forCellReuseIdentifier: (String(str_pointData.prefix(7)) + String(str_roomValue)))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(LeadingBirthdayCell.self, forCellReuseIdentifier: (String(str_playerData) + String(str_labValue.suffix(4))))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(LabSignCell.self, forCellReuseIdentifier: (String(str_atCheckedData.prefix(4)) + String(str_norData)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(VisualImageView.self, forCellReuseIdentifier: (String(str_dismissName) + String(str_viewEdgeEqualData.suffix(4)) + "bout" + String(str_giftName)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
