
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_viewName:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "Voice greeting" :*/
fileprivate let str_actionLayerPickContent:String = "model voiceVoic"
fileprivate let str_listText:String = "selectedg"

/*: "mount_img" :*/
fileprivate let str_fitValue:String = "mount_self view image mode self"
fileprivate let str_accessTitle:[Character] = ["i","m","g"]

/*: "Add" :*/
fileprivate let str_giftName:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let str_buttonData:String = "ttoppe"

/*: "addStatus" :*/
fileprivate let str_resultName:[Character] = ["a","d","d","S","t","a","t","u","s"]

/*: "list" :*/
fileprivate let str_commitName:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let str_liveImageName:String = "audiindex"

/*: "length" :*/
fileprivate let str_maleTapValue:String = "LENGTH"

/*: "url" :*/
fileprivate let str_centerName:String = "uresume"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_mStateValue:[UInt8] = [0x6c,0x61,0x76,0x6f,0x72,0x70,0x70,0x61,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x64,0x64,0x61,0x20,0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53]

/*: "Are you sure to delete this message？" :*/
fileprivate let str_styleTitle:[UInt8] = [0x41,0x72,0x65,0x20,0x79,0x6f,0x75,0x20,0x73,0x75,0x72,0x65,0x20,0x74,0x6f,0x20,0x64,0x65,0x6c,0x65,0x74,0x65,0x20,0x74,0x68,0x69,0x73,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65,0xef,0xbc,0x9f]

/*: "Cancel" :*/
fileprivate let str_addData:String = "status sharedCancel"

/*: "Delete" :*/
fileprivate let str_removeValue:String = "Deletewith equal view"

/*: "id" :*/
fileprivate let str_playerName:String = "ibubble"

/*: "Please wait for the audit result" :*/
fileprivate let str_imageTitle:[UInt8] = [0x50,0x6c,0x65,0x61,0x73,0x65,0x20,0x77,0x61,0x69,0x74,0x20,0x66,0x6f,0x72,0x20,0x74,0x68,0x65,0x20,0x61,0x75,0x64,0x69,0x74,0x20,0x72,0x65,0x73,0x75,0x6c,0x74]

/*: "The upper limit has been reached" :*/
fileprivate let str_viewValue:[UInt8] = [0x54,0x68,0x65,0x20,0x75,0x70,0x70,0x65,0x72,0x20,0x6c,0x69,0x6d,0x69,0x74,0x20,0x68,0x61,0x73,0x20,0x62,0x65,0x65,0x6e,0x20,0x72,0x65,0x61,0x63,0x68,0x65,0x64]

/*: "filePath" :*/
fileprivate let str_dataMethodName:String = "filePathmake var return self"

/*: "time" :*/
fileprivate let str_squareGiftName:String = "timseek"

/*: "RegularViewCell" :*/
fileprivate let str_toFrameValue:String = "Talkimage of"
fileprivate let str_textContent:String = "group m red height playetAudi"

/*: "#FF506D" :*/
fileprivate let str_bottomContentValue:String = "type bottom#F"
fileprivate let str_valueIconText:String = "F5startD"

/*: "btn_message_delete_nor" :*/
fileprivate let str_titleName:String = "btn_meclass lab collection back"
fileprivate let str_tableName:String = "_deleequal app text"

/*: "Currently mute" :*/
fileprivate let str_femaleMessageTitle:[Character] = ["C","u","r","r","e","n","t","l","y"," ","m","u"]
fileprivate let str_officialValue:String = "style"

/*: "Edit Title" :*/
fileprivate let str_blockValue:[Character] = ["E","d","i","t"," ","T","i","t"]
fileprivate let str_managerValue:String = "colore"

/*: "OK" :*/
fileprivate let str_giftObjectValue:[Character] = ["O","K"]

/*: "remark" :*/
fileprivate let str_logText:[Character] = ["r","e","m","a","r","k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_withText:[UInt8] = [0xb,0x24,0x21,0x2b,0x23,0x68,0x3c,0x20,0x2d,0x68,0x2a,0x3d,0x3c,0x3c,0x27,0x26,0x68,0x2a,0x2d,0x24,0x27,0x3f,0x68,0x3c,0x27,0x68,0x29,0x2c,0x2c,0x42,0x2b,0x27,0x26,0x3c,0x2d,0x26,0x3c]

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let str_textValue:[UInt8] = []

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LayerAudioVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class LayerAudioVc: PropertyViewController {
	var tableSum: Int = 80
	var arrowDictionary: [AnyHashable: String] = [:]
	var fastenerCount: Int = 81
	var secondVideoDictionary: [AnyHashable: String] = [:]
	var modelCount: Int = 92
	var showDictionary: [AnyHashable: String] = [:]

    var whiteSizeImageView: UIImageView?

    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: MThen?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: MediaPlayerDelegate?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: RegularViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_viewName, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var scienceLaboratoryStoreValue = recordView?.filePath { 
		if let whiteSizeImageView = whiteSizeImageView {
	
	            if (whiteSizeImageView.clipsToBounds) && (whiteSizeImageView.layer.contains(CGPoint(x: CGFloat(55), y: CGFloat(98)))) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let takeAwayLet = StandView()
	            takeAwayLet.positionTitle = scienceLaboratoryStoreValue
	            
	            
	            takeAwayLet.lastQuantity = { [self] emptyQuantity in
	            self.modelCount = emptyQuantity
	            
	            var takeAwayLet = addStatus
	            takeAwayLet /= 5
	            if takeAwayLet < self.modelCount {
	                self.modelCount = takeAwayLet
	            }
	            
	            return self.modelCount
	            }
	            takeAwayLet.pathFrameAcceptDictionary = { [self] abdicateDictionary in
	            self.showDictionary = abdicateDictionary
	            
	            guard var value = self.showDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                whiteSizeImageView.addSubview(takeAwayLet)
	            }
	
		}
	
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var labelPasseRoomValue = self.recordView?.filePath { 
		if let whiteSizeImageView = whiteSizeImageView {
	
	            if (whiteSizeImageView.autoresizingMask == .flexibleLeftMargin) && (whiteSizeImageView.layer.contentsRect.origin.y != 0) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let blockEqual = StandView(frame: whiteSizeImageView.bounds.union(CGRect(x: CGFloat(0), y: CGFloat(97), width: CGFloat(336.97), height: CGFloat(0))))
	            blockEqual.positionTitle = labelPasseRoomValue
	            
	            
	            blockEqual.lastQuantity = { [self] emptyQuantity in
	            self.fastenerCount = emptyQuantity
	            
	            var blockEqual = self.addStatus
	            blockEqual = 0
	            if blockEqual < self.fastenerCount {
	                self.fastenerCount = blockEqual
	            }
	            
	            return self.fastenerCount
	            }
	            blockEqual.pathFrameAcceptDictionary = { [self] abdicateDictionary in
	            self.secondVideoDictionary = abdicateDictionary
	            
	            guard var value = self.secondVideoDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                whiteSizeImageView.addSubview(blockEqual)
	            }
	
		}
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(str_actionLayerPickContent.suffix(4)) + "e greet" + str_listText.replacingOccurrences(of: "selected", with: "in")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.playerEqual()
        //: designView()
        voice()
        //: getAudioListData()
        full()
        //: setMutedDetection()
        background()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(startSwitchline(notification:)), name: a_keyName, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(componentDismiss(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(behindItemPlay),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: a_keyName,
                                               //: object: nil)
                                               object: nil)

        whiteSizeImageView = UIImageView(frame: self.view.frame.insetBy(dx: CGFloat(Double(self.view.center.x)), dy: CGFloat(Int(self.view.bounds.size.height))))
        if let whiteSizeImageView = whiteSizeImageView {
            self.whiteSizeImageView?.image = UIImage(named: (String(str_fitValue.prefix(6)) + String(str_accessTitle)))
        }
    
		if var gestureValue = recordView?.filePath { 
		if let currenCell = currenCell {
	
	            if (currenCell.backView.autoresizingMask == .flexibleLeftMargin) && (currenCell.backView.layer.contentsRect.origin.y != 0) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let blockEqual = StandView(frame: currenCell.backView.bounds.union(CGRect(x: CGFloat(0), y: CGFloat(97), width: CGFloat(336.97), height: CGFloat(0))))
	            blockEqual.positionTitle = gestureValue
	            
	            
	            blockEqual.lastQuantity = { [self] emptyQuantity in
	            self.tableSum = emptyQuantity
	            
	            var blockEqual = self.addStatus
	            blockEqual = 0
	            if blockEqual < self.tableSum {
	                self.tableSum = blockEqual
	            }
	            
	            return self.tableSum
	            }
	            blockEqual.pathFrameAcceptDictionary = { [self] abdicateDictionary in
	            self.arrowDictionary = abdicateDictionary
	            
	            guard var value = self.arrowDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                currenCell.backView.addSubview(blockEqual)
	            }
	
		}
	
		}
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.phonePlay()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PossibilityTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [PossibilityTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_giftName)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.stopAcross(colors: UIColor.doRange(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(upBind), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension LayerAudioVc {
    //: func getAudioListData() {
    func full() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(str_buttonData.replacingOccurrences(of: "top", with: "y"))] = 1
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ConstraintRequestTool.subCompletion(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.click()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_resultName))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(str_commitName))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PossibilityTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PossibilityTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func eigenvalueOfAMatrixManager(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(str_buttonData.replacingOccurrences(of: "top", with: "y"))] = 1
        //: dict["audio"] = data
        dict[(str_liveImageName.replacingOccurrences(of: "index", with: "o"))] = data
        //: dict["length"] = Int(time)
        dict[(str_maleTapValue.lowercased())] = Int(time)

        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        ConstraintRequestTool.card(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(str_centerName.replacingOccurrences(of: "resume", with: "rl"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.afterTitle(showMsg: String(bytes: str_mStateValue.reversed(), encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.full()
                //: self.recordView?.dismiss()
                self.recordView?.route()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func pushMake(index: Int) {
        //: let config = ShowAlertConfig()
        let config = MarkAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.thoughtImage(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        TitleAlertShow.appearAll(message: String(bytes: str_styleTitle, encoding: .utf8)!.localized, leftBtnTitle: (String(str_addData.suffix(6))).localized, rightBtnTitle: (String(str_removeValue.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()
            //: self.stopPlay()
            self.behindItemPlay()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PossibilityTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ConstraintRequestTool.indexCompletion(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.full()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension LayerAudioVc {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func startSwitchline(notification _: NSNotification) {
        //: stopPlay()
        behindItemPlay()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func componentDismiss(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            behindItemPlay()
        }
    }

    //: @objc func addButtonClick() {
    @objc func upBind() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PrefaceReactiveCompatible.pryUpward(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.episode(showMsg: a_matchData)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.episode(showMsg: String(bytes: str_imageTitle, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.episode(showMsg: String(bytes: str_viewValue, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.behindItemPlay()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = MThen(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.large()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(str_dataMethodName.prefix(8)))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.eigenvalueOfAMatrixManager(data: data!, time: dict[(str_squareGiftName.replacingOccurrences(of: "seek", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension LayerAudioVc {
    //: func setMutedDetection() {
    func background() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        ImageVideoReactiveCompatible.shared.blockApp()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        ImageVideoReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension LayerAudioVc: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(str_toFrameValue.prefix(4)) + "ingGre" + String(str_textContent.suffix(6)) + "oCell")
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: RegularViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! RegularViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.titleLast(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.currencyOn(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.currencyOn(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            ImageVideoReactiveCompatible.shared.blockApp()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.video(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.behindItemPlay()
            //: self.editTitle(index: indexPath.section)
            self.fill(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_removeValue.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.pushMake(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_bottomContentValue.suffix(2)) + str_valueIconText.replacingOccurrences(of: "start", with: "06")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.pushMake(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.outsideText(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.outsideText(name: (String(str_titleName.prefix(6)) + "ssage" + String(str_tableName.prefix(5)) + "te_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = AneWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func video(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isTalking else {
        guard !SocketReactiveCompatible.shared.isTalking else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.phonePlay()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? RegularViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.currencyOn(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.episode(showMsg: (String(str_femaleMessageTitle) + str_officialValue.replacingOccurrences(of: "style", with: "te")).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = MediaPlayerDelegate.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.phonePlay()
        //: self.player?.play(urlStr: model.content)
        self.player?.neglect(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? RegularViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.currencyOn(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func fill(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = UtilizerViewDelegate(title: (String(str_blockValue) + str_managerValue.replacingOccurrences(of: "color", with: "l")).localized, leftBtnTitle: (String(str_addData.suffix(6))).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.imageShow()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            SizeHungReactiveCompatible.picCurrent()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: PossibilityTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(str_logText))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            ConstraintRequestTool.when(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func behindItemPlay() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.phonePlay()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.currencyOn(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension LayerAudioVc: ErrorToolDelegate {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func bag(status: SheInsetTarget) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            behindItemPlay()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            behindItemPlay()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension LayerAudioVc {
    //: private func setupEmptyDataView() {
    private func click() {
        //: var style = EmptyStyle()
        var style = PlunderedEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_withText.map{$0^72}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .playerEqual()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func voice() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(RegularViewCell.self, forCellReuseIdentifier: (String(str_toFrameValue.prefix(4)) + "ingGre" + String(str_textContent.suffix(6)) + "oCell"))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.observerShared()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.drogueSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: str_textValue.map{$0^139}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
