
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_midTargetData:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "Text greeting" :*/
fileprivate let str_infoContent:String = "var effect giftText gr"
fileprivate let str_mName:String = "eetinkit"

/*: "equal_observe_pic" :*/
fileprivate let str_compareLengthInfoName:[Character] = ["e","q","u","a","l","_","o"]
fileprivate let str_greetInputName:[Character] = ["b","s","e","r","v","e","_","p","i","c"]

/*: "Add" :*/
fileprivate let str_selectedMoreValue:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let str_equalName:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_outsideText:[Character] = ["a","d","d","S","t","a"]
fileprivate let str_makeName:String = "tpaths"

/*: "list" :*/
fileprivate let str_createContent:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let str_liveData:[UInt8] = [0x41,0x72,0x65,0x20,0x79,0x6f,0x75,0x20,0x73,0x75,0x72,0x65,0x20,0x74,0x6f,0x20,0x64,0x65,0x6c,0x65,0x74,0x65,0x20,0x74,0x68,0x69,0x73,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65,0xef,0xbc,0x9f]

/*: "Cancel" :*/
fileprivate let str_imageData:String = "c"
fileprivate let str_appearActiveData:String = "ANCEL"

/*: "Delete" :*/
fileprivate let str_shadowTitle:String = "image location var let clickDelete"

/*: "id" :*/
fileprivate let str_cellDataName:[Character] = ["i","d"]

/*: "The upper limit has been reached" :*/
fileprivate let str_textAppValue:[UInt8] = [0x54,0x68,0x65,0x20,0x75,0x70,0x70,0x65,0x72,0x20,0x6c,0x69,0x6d,0x69,0x74,0x20,0x68,0x61,0x73,0x20,0x62,0x65,0x65,0x6e,0x20,0x72,0x65,0x61,0x63,0x68,0x65,0x64]

/*: "text" :*/
fileprivate let str_photoRootData:String = "okexok"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_labelOfTitle:[UInt8] = [0x53,0x75,0x63,0x63,0x65,0x73,0x73,0x66,0x75,0x6c,0x6c,0x79,0x20,0x61,0x64,0x64,0x65,0x64,0x2c,0x20,0x70,0x6c,0x65,0x61,0x73,0x65,0x20,0x77,0x61,0x69,0x74,0x20,0x66,0x6f,0x72,0x20,0x61,0x70,0x70,0x72,0x6f,0x76,0x61,0x6c]

/*: "Any-Hex/Java" :*/
fileprivate let str_modeName:String = "model letAny-H"
fileprivate let str_runText:String = "viewa"

/*: "OverloadTextCell" :*/
fileprivate let str_imageTitle:[Character] = ["T","a","l","k","i","n","g","G","r"]
fileprivate let str_labText:[Character] = ["e","e","t","T","e","x","t","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let str_roomName:String = "model inside self#FF506"
fileprivate let str_kindUpgradeData:[Character] = ["D"]

/*: "btn_message_delete_nor" :*/
fileprivate let str_layerModelData:String = "btn_messindex true self"
fileprivate let str_postButtonData:String = "age_dmodel length"
fileprivate let str_modeDefineText:String = "select self data make type_nor"

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_equalText:[UInt8] = [0x43,0x6c,0x69,0x63,0x6b,0x20,0x74,0x68,0x65,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x62,0x65,0x6c,0x6f,0x77,0x20,0x74,0x6f,0x20,0x61,0x64,0x64,0xa,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let str_awakeData:[UInt8] = [0x57,0x68,0x65,0x6e,0x20,0x61,0x64,0x64,0x69,0x6e,0x67,0x20,0x6d,0x75,0x6c,0x74,0x69,0x70,0x6c,0x65,0x20,0x74,0x65,0x78,0x74,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65,0x2c,0x20,0x74,0x68,0x65,0x20,0x73,0x79,0x73,0x74,0x20,0x65,0x6d,0x20,0x77,0x69,0x6c,0x6c,0x20,0x73,0x65,0x6e,0x64,0x20,0x74,0x68,0x65,0x6d,0x20,0x62,0x79,0x20,0x74,0x75,0x72,0x6e,0x73,0x3b,0x73,0x6c,0x69,0x64,0x65,0x20,0x74,0x6f,0x20,0x74,0x68,0x65,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65,0x20,0x74,0x6f,0x20,0x64,0x65,0x6c,0x65,0x74,0x65]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CurtsyRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class CurtsyRecognizerDelegate: PropertyViewController {
	var exceptImageOn: Bool = false
	var readyMagnitude: Int = 87
	var inputArray: [AnyHashable] = []

    var colorImageView: UIImageView?

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: BoundaryThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_midTargetData, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var largesseValue = recordView?.popView?.isRemoveTapGes { 
		if let colorImageView = colorImageView {
	
	            if (colorImageView.inputViewController != nil) && (colorImageView.layer.isDoubleSided != true) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let addTransition = SectionView(frame: colorImageView.bounds)
	            addTransition.voiceOff = largesseValue
	            addTransition.viewAtQuantity = self.addStatus

	            
	            addTransition.bottomFreeClose = { [self] toolPriceDoing in
	            self.exceptImageOn = toolPriceDoing
	            
	            var addTransition = self.hideNavi
	                addTransition = true
	                addTransition = !addTransition
	            if addTransition != self.exceptImageOn {
	                self.exceptImageOn = addTransition
	            }
	            
	            return self.exceptImageOn
	            }
	            addTransition.groupMagnitude = { [self] awardSum in
	            self.readyMagnitude = awardSum
	            
	            var addTransition = self.addStatus
	            addTransition &= 6
	            if addTransition < self.readyMagnitude {
	                self.readyMagnitude = addTransition
	            }
	            
	            return self.readyMagnitude
	            }
	            addTransition.moveArray = { [self] targetMoonArray in
	            self.inputArray = targetMoonArray
	            
	            guard var value = self.inputArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                colorImageView.addSubview(addTransition)
	            }
	
		}
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(str_infoContent.suffix(7)) + str_mName.replacingOccurrences(of: "kit", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.playerEqual()
        //: designView()
        map()
        //: getAudioListData()
        path()

        colorImageView = UIImageView(frame: self.view.frame)
        if let colorImageView = colorImageView {
            self.colorImageView?.image = UIImage(named: (String(str_compareLengthInfoName) + String(str_greetInputName)))
            if (colorImageView.gestureRecognizers != nil && colorImageView.gestureRecognizers!.count == 20) && (colorImageView.highlightedImage != nil) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(colorImageView)
            }
        }
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PossibilityTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [PossibilityTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_selectedMoreValue)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.stopAcross(colors: UIColor.doRange(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(snap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension CurtsyRecognizerDelegate {
    //: func getAudioListData() {
    func path() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(str_equalName))] = 2
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ConstraintRequestTool.subCompletion(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.addToView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_outsideText) + str_makeName.replacingOccurrences(of: "path", with: "u"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(str_createContent))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PossibilityTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PossibilityTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func beforeUser(index: Int) {
        //: let config = ShowAlertConfig()
        let config = MarkAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.thoughtImage(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        TitleAlertShow.appearAll(message: String(bytes: str_liveData, encoding: .utf8)!.localized, leftBtnTitle: (str_imageData.uppercased() + str_appearActiveData.lowercased()).localized, rightBtnTitle: (String(str_shadowTitle.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PossibilityTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            SizeHungReactiveCompatible.picCurrent()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ConstraintRequestTool.indexCompletion(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.path()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension CurtsyRecognizerDelegate {
    //: @objc func addButtonClick() {
    @objc func snap() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.episode(showMsg: String(bytes: str_textAppValue, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = BoundaryThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.guideShow()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.withEqual(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(str_equalName))] = 2
            //: dict["text"] = tempStr
            dict[(str_photoRootData.replacingOccurrences(of: "ok", with: "t"))] = tempStr
            //: ProgressHUD.show()
            SizeHungReactiveCompatible.picCurrent()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            ConstraintRequestTool.card(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                SizeHungReactiveCompatible.dataMessage()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.afterTitle(showMsg: String(bytes: str_labelOfTitle, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.path()
                //: self.recordView?.dismiss()
                self.recordView?.listUp()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func withEqual(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(str_modeName.suffix(5)) + "ex/J" + str_runText.replacingOccurrences(of: "view", with: "av")) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension CurtsyRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: a_blockValue - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.thoughtImage(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(str_imageTitle) + String(str_labText))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: OverloadTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! OverloadTextCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.fromPosition(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_shadowTitle.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.beforeUser(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_roomName.suffix(6)) + String(str_kindUpgradeData)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.beforeUser(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.outsideText(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.outsideText(name: (String(str_layerModelData.prefix(8)) + String(str_postButtonData.prefix(5)) + "elete" + String(str_modeDefineText.suffix(4)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = AneWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension CurtsyRecognizerDelegate {
    //: private func setupEmptyDataView() {
    private func addToView() {
        //: var style = EmptyStyle()
        var style = PlunderedEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_equalText, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .playerEqual()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func map() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(OverloadTextCell.self, forCellReuseIdentifier: (String(str_imageTitle) + String(str_labText)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.observerShared()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.drogueSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: str_awakeData, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
