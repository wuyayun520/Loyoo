
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_videoName:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "btn_chatsettings_unchoice" :*/
fileprivate let str_liveStartValue:String = "btn_regular log vertical index center"
fileprivate let str_withInfoName:String = "change type data elsetings_unc"
fileprivate let str_messageData:String = "hosizee"

/*: "Free" :*/
fileprivate let str_inputTitle:[Character] = ["F","r","e","e"]

/*: "%@ Gold coins / Message" :*/
fileprivate let str_contentData:String = "gift thought group%@ G"
fileprivate let str_insertName:String = "coins show finish max live host"
fileprivate let str_locationValue:String = "atage"

/*: "%@ Gold coins / Min" :*/
fileprivate let str_statusValue:String = "name equal i%@ G"
fileprivate let str_succeedLoadContent:String = "coibys"

/*: "btn_chatsettings_choiced" :*/
fileprivate let str_actionName:[Character] = ["b","t","n"]
fileprivate let str_vantageData:String = "_chatscase cell"
fileprivate let str_centerName:String = "add mode error view modes_ch"

/*: "LV.%d" :*/
fileprivate let str_timeValue:String = "LV.%dcurrent view array capital"

/*: "#E9E9E9" :*/
fileprivate let str_backModelName:String = "#E9E9E9profile self"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualLayerReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/1.
//

//: import UIKit
import UIKit

//: class ChatPriceModel: HandyJSON {
class ImagePriceModel: HandyJSON {
    //: var price: Int = 0
    var price: Int = 0
    //: var levelLimit: Int = 0
    var levelLimit: Int = 0
    //: var isSelected: Bool = false
    var isSelected: Bool = false

    //: required init() {}
    required init() {}
}

//: class TalkingFemaleChatSetCell: UITableViewCell {
class EqualLayerReactiveCompatible: UITableViewCell {
	var pastEnable: Bool = false
	var exampleQuantity: Int = 42
	var phaseOfTheMoonSum: Double = -13.5
	var sectionInfoArray: [AnyHashable] = []
	var atOn: Bool = true
	var containerTotal: Int = 18
	var glassesTotal: Double = -28.9
	var cuttingRoomArray: [AnyHashable] = []

    var doingImageView: UIImageView?

    //: override func awakeFromNib() {
    override func awakeFromNib() {
        //: super.awakeFromNib()
        super.awakeFromNib()
    
            if (levelLab.layoutMargins.left == 1.86) && (levelLab.layer.position.y == 9.19) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let quantityryRank = RecordView()

            quantityryRank.modelEnable = { [self] intervalClose in
            self.pastEnable = intervalClose
            
            return self.pastEnable
            }
            quantityryRank.successfullyNumber = { [self] tableNumber in
            self.exampleQuantity = tableNumber
            
            return self.exampleQuantity
            }
            quantityryRank.frameInterval = { [self] textQuantity in
            self.phaseOfTheMoonSum = textQuantity
            
            return self.phaseOfTheMoonSum
            }
            quantityryRank.managerBackRoomArray = { [self] nidusDeckArray in
            self.sectionInfoArray = nidusDeckArray
            
            guard var value = self.sectionInfoArray as? [String] else {
                return nil
            }
            return value
            }
                levelLab.addSubview(quantityryRank)
            }

	}

    //: override func setSelected(_ selected: Bool, animated: Bool) {
    override func setSelected(_ selected: Bool, animated: Bool) {
        //: super.setSelected(selected, animated: animated)
        super.setSelected(selected, animated: animated)
    
	if let doingImageView = doingImageView {

            if (doingImageView.tag == 9794) && (doingImageView.frame.origin.x == 89.81) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let fileAllocationTable = RecordView(frame: doingImageView.bounds.standardized)

            fileAllocationTable.modelEnable = { [self] intervalClose in
            self.atOn = intervalClose
            
            var fileAllocationTable = animated
            fileAllocationTable = false
            if fileAllocationTable != self.atOn {
                self.atOn = fileAllocationTable
            }
            
            return self.atOn
            }
            fileAllocationTable.successfullyNumber = { [self] tableNumber in
            self.containerTotal = tableNumber
            
            return self.containerTotal
            }
            fileAllocationTable.frameInterval = { [self] textQuantity in
            self.glassesTotal = textQuantity
            
            return self.glassesTotal
            }
            fileAllocationTable.managerBackRoomArray = { [self] nidusDeckArray in
            self.cuttingRoomArray = nidusDeckArray
            
            guard var value = self.cuttingRoomArray as? [String] else {
                return nil
            }
            return value
            }
                doingImageView.addSubview(fileAllocationTable)
            }

	}

	}

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: self.selectionStyle = .none
        self.selectionStyle = .none
        //: self.setupSubviews()
        self.writeWindowTo()
        //: self.setupSubViewsConstraint()
        self.input()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_videoName, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var desLabel: UILabel = {
    lazy var desLabel: UILabel = {
        //: let lb = UILabel()
        let lb = UILabel()
        //: lb.textColor = .appTitleColor()
        lb.textColor = .blockOf()
        //: lb.font = .pingfangFont(type: .Regular, fontSize: 16)
        lb.font = .thoughtImage(type: .Regular, fontSize: 16)
        //: return lb
        return lb
        //: }()
    }()

    //: lazy var levelView: UIView = {
    lazy var levelView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.appThemeColor()
        view.backgroundColor = UIColor.offt()
        //: view.layer.cornerRadius = 10
        view.layer.cornerRadius = 10
        //: view.layer.masksToBounds = true
        view.layer.masksToBounds = true
        //: return view
        return view
        //: }()
    }()

    //: lazy var levelLab: UILabel = {
    lazy var levelLab: UILabel = {
        //: let lb = UILabel()
        let lb = UILabel()
        //: lb.textColor = UIColor.white
        lb.textColor = UIColor.white
        //: lb.font = .pingfangFont(type: .Regular, fontSize: 12)
        lb.font = .thoughtImage(type: .Regular, fontSize: 12)
        //: lb.textAlignment = .center
        lb.textAlignment = .center
        //: return lb
        return lb
        //: }()
    }()

    //: lazy var selectedImageView: UIImageView = {
    lazy var selectedImageView: UIImageView = {
        //: let imgView = UIImageView()
        let imgView = UIImageView()
        //: imgView.image = UIImage.outsideText(name: "btn_chatsettings_unchoice")
        imgView.image = UIImage.outsideText(name: (String(str_liveStartValue.prefix(4)) + "chatset" + String(str_withInfoName.suffix(9)) + str_messageData.replacingOccurrences(of: "size", with: "ic")))
        //: return imgView
        return imgView
        //: }()
    }()
}

// MARK: - update || fitHeight

//: extension TalkingFemaleChatSetCell {
extension EqualLayerReactiveCompatible {
    //: public func updateCellModel(cellModel: ChatPriceModel, index: Int) {
    public func restorePathIndex(cellModel: ImagePriceModel, index: Int) {
        //: switch index {
        switch index {
        //: case 0:
        case 0:
            //: desLabel.text = cellModel.price == 0 ? "Free".localized : "%@ Gold coins / Message".localizedArguments(cellModel.price)
            desLabel.text = cellModel.price == 0 ? (String(str_inputTitle)).localized : (String(str_contentData.suffix(4)) + "old " + String(str_insertName.prefix(6)) + "/ Mes" + str_locationValue.replacingOccurrences(of: "at", with: "s")).random(cellModel.price)
        //: case 1, 2:
        case 1, 2:
            //: desLabel.text = "%@ Gold coins / Min".localizedArguments(cellModel.price)
            desLabel.text = (String(str_statusValue.suffix(4)) + "old " + str_succeedLoadContent.replacingOccurrences(of: "by", with: "n") + " / Min").random(cellModel.price)
        //: default:
        default:
            //: break
            break
        }

        //: selectedImageView.image = cellModel.isSelected == true ? UIImage.outsideText(name: "btn_chatsettings_choiced") : UIImage.outsideText(name: "btn_chatsettings_unchoice")
        selectedImageView.image = cellModel.isSelected == true ? UIImage.outsideText(name: (String(str_actionName) + String(str_vantageData.prefix(6)) + "etting" + String(str_centerName.suffix(4)) + "oiced")) : UIImage.outsideText(name: (String(str_liveStartValue.prefix(4)) + "chatset" + String(str_withInfoName.suffix(9)) + str_messageData.replacingOccurrences(of: "size", with: "ic")))

        //: levelLab.text = String(format: "LV.%d", cellModel.levelLimit)
        levelLab.text = String(format: "LV.%d", cellModel.levelLimit)
        //: levelView.isHidden = cellModel.levelLimit == 0
        levelView.isHidden = cellModel.levelLimit == 0
        //: levelLab.isHidden = cellModel.levelLimit == 0
        levelLab.isHidden = cellModel.levelLimit == 0

        //: contentView.backgroundColor = cellModel.levelLimit > (Int(MeasurementAppManager.share.loginUserMode.level) ?? 1) ? UIColor(hex: "#E9E9E9") : .white
        contentView.backgroundColor = cellModel.levelLimit > (Int(MeasurementAppManager.share.loginUserMode.level) ?? 1) ? UIColor(hex: (String(str_backModelName.prefix(7)))) : .white
    }
}

// MARK: Layout

//: extension TalkingFemaleChatSetCell {
extension EqualLayerReactiveCompatible {
    //: private func setupSubviews() {
    private func writeWindowTo() {
        //: contentView.addSubview(desLabel)
        contentView.addSubview(desLabel)
        //: contentView.addSubview(levelView)
        contentView.addSubview(levelView)
        //: contentView.addSubview(levelLab)
        contentView.addSubview(levelLab)
        //: contentView.addSubview(selectedImageView)
        contentView.addSubview(selectedImageView)
    }

    //: private func setupSubViewsConstraint() {
    private func input() {
        //: desLabel.snp.makeConstraints { make in
        desLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.top.equalTo(17)
            make.top.equalTo(17)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: levelView.snp.makeConstraints { make in
        levelView.snp.makeConstraints { make in
            //: make.leading.equalTo(desLabel.snp.trailing).offset(4)
            make.leading.equalTo(desLabel.snp.trailing).offset(4)
            //: make.trailing.equalTo(levelLab.snp.trailing).offset(7)
            make.trailing.equalTo(levelLab.snp.trailing).offset(7)
            //: make.centerY.equalTo(desLabel.snp.centerY)
            make.centerY.equalTo(desLabel.snp.centerY)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: levelLab.snp.makeConstraints { make in
        levelLab.snp.makeConstraints { make in
            //: make.leading.equalTo(levelView.snp.leading).offset(7)
            make.leading.equalTo(levelView.snp.leading).offset(7)
            //: make.centerY.equalTo(levelView.snp.centerY)
            make.centerY.equalTo(levelView.snp.centerY)
            //: make.centerX.equalTo(levelView.snp.centerX)
            make.centerX.equalTo(levelView.snp.centerX)
        }
        //: selectedImageView.snp.makeConstraints { make in
        selectedImageView.snp.makeConstraints { make in
            //: make.trailing.equalTo(-15)
            make.trailing.equalTo(-15)
            //: make.centerY.equalTo(desLabel.snp.centerY)
            make.centerY.equalTo(desLabel.snp.centerY)
            //: make.size.equalTo(CGSize(width: 21.0, height: 21.0))
            make.size.equalTo(CGSize(width: 21.0, height: 21.0))
        }
    }
}
