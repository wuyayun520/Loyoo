
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_inputImageValue:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "Cancel" :*/
fileprivate let str_emptyEqualValue:String = "video return letCancel"

/*: "Submit" :*/
fileprivate let str_norName:String = "bar classSubmit"

/*: "Please tell us your problems                   " :*/
fileprivate let str_makeValue:[UInt8] = [0x50,0x6c,0x65,0x61,0x73,0x65,0x20,0x74,0x65,0x6c,0x6c,0x20,0x75,0x73,0x20,0x79,0x6f,0x75,0x72,0x20,0x70,0x72,0x6f,0x62,0x6c,0x65,0x6d,0x73,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20]

/*: "You need to choose at least one." :*/
fileprivate let str_removeValue:[UInt8] = [0x2e,0x65,0x6e,0x6f,0x20,0x74,0x73,0x61,0x65,0x6c,0x20,0x74,0x61,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x59]

/*: "Please tell us your problems." :*/
fileprivate let str_upText:String = "Pleasto thought"
fileprivate let str_renderData:String = "call sizel us"
fileprivate let str_finishTitle:String = "fatal equal pro"
fileprivate let str_tableShowValue:String = "blems.left path return channel if"

/*: "app/userReport" :*/
fileprivate let str_modelPlayerTitle:String = "position aboveapp/u"
fileprivate let str_succeedViewData:String = "poto"

/*: "id" :*/
fileprivate let str_labTitle:[UInt8] = [0x69,0x64]

/*: "reason" :*/
fileprivate let str_nextData:String = "reaequaln"

/*: "desc" :*/
fileprivate let str_statusCatText:[UInt8] = [0x64,0x65,0x73,0x63]

/*: "mf/moment/report" :*/
fileprivate let str_fieldValue:String = "image screen tablemf/momen"
fileprivate let str_bottomName:[Character] = ["t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let str_addTitle:[UInt8] = [0x6d,0x6f,0x6d,0x65,0x6e,0x74,0x49,0x64]

/*: "mf/momentReply/report" :*/
fileprivate let str_attentionValue:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y"]
fileprivate let str_liveValue:[Character] = ["/","r"]
fileprivate let str_cornerFreeValue:String = "EPORT"

/*: "app/videoReport" :*/
fileprivate let str_centerContent:String = "app/viname self push key"

/*: "logId" :*/
fileprivate let str_colorEqualText:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let str_textData:String = "group add number fromSubmitte"
fileprivate let str_scaleContent:String = "tool view named suc"

/*: "Nudity" :*/
fileprivate let str_responseValue:[Character] = ["N","u","d","i","t"]
fileprivate let str_formatDeviceValue:String = "label"

/*: "Violence" :*/
fileprivate let str_normalData:String = "self screen imageViolence"

/*: "Harassment" :*/
fileprivate let str_ofTitle:[Character] = ["H","a"]
fileprivate let str_toValue:String = "rindexsment"

/*: "Suicide or Self-injury" :*/
fileprivate let str_fromData:String = "Suicide available place kit extension photo"
fileprivate let str_valueName:String = "add var if lab frameor Se"
fileprivate let str_okListText:String = "njurcorner"

/*: "False Information" :*/
fileprivate let str_distributionText:String = "text share handleFalse Inf"
fileprivate let str_titleName:String = "indexma"

/*: "Spam" :*/
fileprivate let str_toStatusValue:String = "center show let selfSpam"

/*: "Hate Speech" :*/
fileprivate let str_atTitle:String = "Hate Spup number pop"
fileprivate let str_sessionName:[Character] = ["e","e","c","h"]

/*: "Terrorism" :*/
fileprivate let str_imageValue:String = "Terrinput cell"
fileprivate let str_liveName:[Character] = ["m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let str_failureData:[UInt8] = [0x53,0x61,0x6c,0x65,0x20,0x6f,0x66,0x20,0x49,0x6c,0x6c,0x65,0x67,0x61,0x6c,0x20,0x6f,0x72,0x20,0x52,0x65,0x67,0x75,0x6c,0x61,0x74,0x65,0x64,0x20,0x47,0x6f,0x6f,0x64,0x73]

/*: "Sending disgusting messages" :*/
fileprivate let str_lengthData:String = "text bag make view ofSend"
fileprivate let str_infoAcrossValue:String = "istopustin"
fileprivate let str_withPriceData:String = "g mesmodel go case data"

/*: "Using photos of nudity or drugs" :*/
fileprivate let str_hireEqualTitle:[UInt8] = [0x55,0x73,0x69,0x6e,0x67,0x20,0x70,0x68,0x6f,0x74,0x6f,0x73,0x20,0x6f,0x66,0x20,0x6e,0x75,0x64,0x69,0x74,0x79,0x20,0x6f,0x72,0x20,0x64,0x72,0x75,0x67,0x73]

/*: "Harassing or sending ads" :*/
fileprivate let str_labContentData:String = "to data imageHara"
fileprivate let str_constraintTitle:String = "g ormake succeed view user"
fileprivate let str_imageData:[Character] = [" ","s"]
fileprivate let str_textContent:String = "endcurrent"

/*: "Defraud or using fake info" :*/
fileprivate let str_todayChannelName:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u","s","i","n","g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let str_viewName:[Character] = ["O","t","h","e","r","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LimitMediaAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum AstatineAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class LimitMediaAlertView: UIView {
    var gildTheLilyImageView: UIImageView?

    //: var reportType: TalkingReportAlertType?
    var reportType: AstatineAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [DocumentReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = DocumentReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: AstatineAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        home()
        //: setupSubViewsConstraint()
        system()
        //: setupReasons()
        makeVoiceEqual()
        //: bindInteraction()
        shared()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_inputImageValue, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(str_emptyEqualValue.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.offt(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.offt().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(playerTime), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(str_norName.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.thoughtImage(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.stopAcross(colors: UIColor.doRange(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(isometric), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: a_blockValue - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.each(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = LaterCloseThen(placeholder: String(bytes: str_makeValue, encoding: .utf8)!.localized, placeholderColor: UIColor.observerShared(), frame: CGRect(x: 10, y: 10, width: a_blockValue - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .thoughtImage(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.blockOf()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .thoughtImage(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.each(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(AfterwardView.self, forCellReuseIdentifier: AfterwardView.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension LimitMediaAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func opposition() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func icon(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = ImageMacroDefine.getWindow()
            let window = ImageMacroDefine.sumHungWindowObject()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func numberernalRepresentation(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func playerTime() {
        //: self.dismissReportView(succeed: false)
        self.numberernalRepresentation(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func isometric() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.episode(showMsg: String(bytes: str_removeValue.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.episode(showMsg: (String(str_upText.prefix(5)) + "e tel" + String(str_renderData.suffix(4)) + " your" + String(str_finishTitle.suffix(4)) + String(str_tableShowValue.prefix(6))).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(str_modelPlayerTitle.suffix(5)) + "serRe" + str_succeedViewData.replacingOccurrences(of: "to", with: "rt"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (str_nextData.replacingOccurrences(of: "equal", with: "so")): selectedModel.type, String(bytes: str_statusCatText, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(str_fieldValue.suffix(8)) + String(str_bottomName))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_addTitle, encoding: .utf8)!: tempId, (str_nextData.replacingOccurrences(of: "equal", with: "so")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(str_attentionValue) + String(str_liveValue) + str_cornerFreeValue.lowercased())
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (str_nextData.replacingOccurrences(of: "equal", with: "so")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(str_centerContent.prefix(6)) + "deoReport")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_colorEqualText.reversed(), encoding: .utf8)!: tempId, (str_nextData.replacingOccurrences(of: "equal", with: "so")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        ConsumerRequestManager.minAwakeCompletion(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.afterTitle(showMsg: (String(str_textData.suffix(8)) + String(str_scaleContent.suffix(5)) + "cessfully").localized)
                //: self.dismissReportView(succeed: true)
                self.numberernalRepresentation(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension LimitMediaAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: AfterwardView.className()) as! AfterwardView
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.forefront(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension LimitMediaAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func home() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func system() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func makeVoiceEqual() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(str_responseValue) + str_formatDeviceValue.replacingOccurrences(of: "label", with: "y")).localized,
                           //: "Violence".localized,
                           (String(str_normalData.suffix(8))).localized,
                           //: "Harassment".localized,
                           (String(str_ofTitle) + str_toValue.replacingOccurrences(of: "index", with: "as")).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(str_fromData.prefix(8)) + String(str_valueName.suffix(5)) + "lf-i" + str_okListText.replacingOccurrences(of: "corner", with: "y")).localized,
                           //: "False Information".localized,
                           (String(str_distributionText.suffix(9)) + str_titleName.replacingOccurrences(of: "index", with: "or") + "tion").localized,
                           //: "Spam".localized,
                           (String(str_toStatusValue.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(str_atTitle.prefix(7)) + String(str_sessionName)).localized,
                           //: "Terrorism".localized,
                           (String(str_imageValue.prefix(4)) + "oris" + String(str_liveName)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: str_failureData, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(str_lengthData.suffix(4)) + "ing d" + str_infoAcrossValue.replacingOccurrences(of: "top", with: "g") + String(str_withPriceData.prefix(5)) + "sages").localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: str_hireEqualTitle, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(str_labContentData.suffix(4)) + "ssin" + String(str_constraintTitle.prefix(4)) + String(str_imageData) + str_textContent.replacingOccurrences(of: "current", with: "i") + "ng ads").localized,
                           //: "Defraud or using fake info".localized,
                           (String(str_todayChannelName)).localized,
                           //: "Others".localized]
                           (String(str_viewName)).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = MeasurementAppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = MeasurementAppManager.share.appConfigMode.videoReport[(str_nextData.replacingOccurrences(of: "equal", with: "so"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = DocumentReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func shared() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
