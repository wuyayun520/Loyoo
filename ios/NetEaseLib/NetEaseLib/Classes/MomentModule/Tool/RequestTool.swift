
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let str_managerPriceValue:[Character] = ["m"]
fileprivate let str_makeData:String = "player for up titlef/mome"

/*: "mf/moment/moments" :*/
fileprivate let str_cellValue:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let str_labelData:String = "else cell view equal maket/mo"

/*: "mf/moment/like" :*/
fileprivate let str_addName:String = "mf/moguard make top"
fileprivate let str_pathMediumContent:String = "MENT"

/*: "mf/crush/send" :*/
fileprivate let str_keyText:[Character] = ["m","f","/","c","r","u"]
fileprivate let str_modeName:[Character] = ["s","h","/","s","e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let str_makeValue:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let str_timeTitleName:String = "mf/mview text player"
fileprivate let str_voiceData:String = "eptop"
fileprivate let str_barValue:String = "extension succeed other in frame/list"

/*: "mf/momentReply/save" :*/
fileprivate let str_progressValue:[Character] = ["m","f","/","m","o","m","e"]
fileprivate let str_equalValue:[Character] = ["n","t","R","e","p","l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let str_hiddenTitle:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d"]
fileprivate let str_handleName:String = "eoSignno return exit"

/*: "dynamicPic.zip" :*/
fileprivate let str_toData:[Character] = ["d","y"]
fileprivate let str_requestText:[Character] = ["n","a","m","i","c","P","i","c",".","z","i","p"]

/*: "pic" :*/
fileprivate let str_equalName:[UInt8] = [0x70,0x69,0x63]

/*: "content" :*/
fileprivate let str_viewValue:String = "coinsideeinside"

/*: "mf/moment/save" :*/
fileprivate let str_itemData:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a"]
fileprivate let str_managerValue:String = "head"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let str_selectValue:[UInt8] = [0x50,0x75,0x62,0x6c,0x69,0x73,0x68,0x20,0x53,0x75,0x63,0x63,0x65,0x73,0x73,0x66,0x75,0x6c,0x2c,0x20,0x47,0x6f,0x20,0x74,0x6f,0x20,0x4d,0x79,0x20,0x44,0x79,0x6e,0x61,0x6d,0x69,0x63,0x73,0x20,0x74,0x6f,0x20,0x76,0x69,0x65,0x77]

/*: "mf/moment/saveVideo" :*/
fileprivate let str_sizeContent:[Character] = ["m","f","/","m","o","m"]
fileprivate let str_itemViewTitle:String = "ent/sif extra owner"
fileprivate let str_timeName:[Character] = ["a","v","e","V","i","d","e","o"]

/*: .png" :*/
fileprivate let str_touchValue:String = "title image type text.png"

/*: "dynamicPic" :*/
fileprivate let str_acrossData:String = "dynamicPbottom view text"
fileprivate let str_tempData:[Character] = ["i","c"]

/*: "freeImage" :*/
fileprivate let str_blackTitle:[Character] = ["f","r","e","e","I","m","a"]
fileprivate let str_viewUserTitle:[Character] = ["g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let str_photoUpValue:String = "mf/mtype height yet selected"
fileprivate let str_imageTitle:String = "bottom equal let ifReply"
fileprivate let str_replyData:String = "/"
fileprivate let str_callText:String = "reindexy"

/*: "mf/moment/delete" :*/
fileprivate let str_backName:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e"]
fileprivate let str_withData:String = "pathte"

/*: "mf/momentReply/delete" :*/
fileprivate let str_sizeName:String = "make falsemf/mom"
fileprivate let str_giftViewValue:String = "progress var letentRep"
fileprivate let str_arrayValue:String = "ly/deview shared block layer model"

/*: "mf/moment/pin" :*/
fileprivate let str_userEachData:String = "cell bottommf/mo"
fileprivate let str_layerName:String = "/pinvalue fatal"

/*: "mf/moment/remindNum" :*/
fileprivate let str_toRawName:String = "mf/momting spring photo image class"
fileprivate let str_likeData:String = "emcolor"
fileprivate let str_substanceName:String = "ndNumcenter right if"

/*: "mf/moment/remindList" :*/
fileprivate let str_constraintData:String = "self kindmf/m"
fileprivate let str_callData:String = "and processor currently if maket/re"
fileprivate let str_centerData:[Character] = ["s","t"]

/*: "page" :*/
fileprivate let str_areaTitle:[UInt8] = [0x70,0x61,0x67,0x65]

/*: "cos/getCredential" :*/
fileprivate let str_topValue:[Character] = ["c","o","s","/","g","e","t","C","r","e","d","e","n","t","i","a","l"]

/*: "type" :*/
fileprivate let str_progressName:String = "TYPE"

/*: "user/reportUserInfo" :*/
fileprivate let str_layerContent:[Character] = ["u","s","e","r","/","r","e","p","o","r"]
fileprivate let str_tingValue:[Character] = ["t","U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let str_errorName:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let str_frameName:[Character] = ["d","e","v","i","c"]
fileprivate let str_titleData:[Character] = ["e","I","d"]

/*: "deviceName" :*/
fileprivate let str_emptyData:[Character] = ["d","e","v","i","c","e","N","a","m"]
fileprivate let str_imageText:[Character] = ["e"]

/*: "iphone" :*/
fileprivate let str_roomData:String = "iphorowe"

/*: "fileUrl" :*/
fileprivate let str_hiddenEdgeName:[Character] = ["f","i","l","e","U","r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class RequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func makeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(str_managerPriceValue) + String(str_makeData.suffix(6)) + "nt/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func thrusting(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(str_cellValue) + String(str_labelData.suffix(4)) + "ments")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func pinpointGroup(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(str_addName.prefix(5)) + str_pathMediumContent.lowercased() + "/like")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func number(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(str_keyText) + String(str_modeName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func will(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(str_makeValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func areaBeauty(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(str_timeTitleName.prefix(4)) + "omentR" + str_voiceData.replacingOccurrences(of: "top", with: "ly") + String(str_barValue.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func property(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(str_progressValue) + String(str_equalValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func equalCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(str_hiddenTitle) + String(str_handleName.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func accumulation(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        linkFile()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            gestureUserVideo(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(str_toData) + String(str_requestText)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: str_equalName, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(str_viewValue.replacingOccurrences(of: "inside", with: "nt"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = TitleTheoryThen()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(str_itemData) + str_managerValue.replacingOccurrences(of: "head", with: "ve"))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.afterTitle(showMsg: String(bytes: str_selectValue, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.linkFile()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func doingDetail(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(str_sizeContent) + String(str_itemViewTitle.prefix(5)) + String(str_timeName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func gestureUserVideo(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.priceVideo()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(str_touchValue.suffix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(str_acrossData.prefix(8)) + String(str_tempData)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func linkFile() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(str_blackTitle) + String(str_viewUserTitle)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func withCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(str_photoUpValue.prefix(4)) + "oment" + String(str_imageTitle.suffix(5)) + str_replyData.capitalized + str_callText.replacingOccurrences(of: "index", with: "pl") + "list")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func regard(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(str_backName) + str_withData.replacingOccurrences(of: "path", with: "le"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func fileAllocationTable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(str_sizeName.suffix(6)) + String(str_giftViewValue.suffix(6)) + String(str_arrayValue.prefix(5)) + "lete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func equalTheme(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(str_userEachData.suffix(5)) + "ment" + String(str_layerName.prefix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func primary(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(str_toRawName.prefix(6)) + "ent/r" + str_likeData.replacingOccurrences(of: "color", with: "i") + String(str_substanceName.prefix(5)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func nearTime(page: Int, completion: @escaping (_ data: [CoequalListModel]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(str_constraintData.suffix(4)) + "omen" + String(str_callData.suffix(4)) + "mindLi" + String(str_centerData))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: str_areaTitle, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [CoequalListModel] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<CoequalListModel>.deserialize(from: arr as? Array) as? [CoequalListModel] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func withLocal(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(str_topValue))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(str_progressName.lowercased()): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func beyondName(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestServer = MeasurementAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = MeasurementAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(str_layerContent) + String(str_tingValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = MeasurementAppManager.share.loginUserMode.userID
        params[(String(str_errorName))] = MeasurementAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(str_frameName) + String(str_titleData))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(str_emptyData) + String(str_imageText))] = (str_roomData.replacingOccurrences(of: "row", with: "n"))
        //: params["type"] = 1
        params[(str_progressName.lowercased())] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(str_hiddenEdgeName))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
