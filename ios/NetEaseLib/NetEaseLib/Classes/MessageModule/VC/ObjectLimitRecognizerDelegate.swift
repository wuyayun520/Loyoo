
//: Declare String Begin

/*: "leading_add_image" :*/
fileprivate let str_giftData:String = "leawithin"
fileprivate let str_mediumContent:String = "d_imagemake key"

/*: "bg_message_top" :*/
fileprivate let str_succeedName:[Character] = ["b","g","_","m","e","s","s","a","g","e","_","t","o","p"]

/*: "777777" :*/
fileprivate let str_fillPostData:[Character] = ["7","7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let str_colorData:String = "Messimage height from"

/*: "Who like me" :*/
fileprivate let str_bottomTitle:String = "Who equal extra range"

/*: "Call" :*/
fileprivate let str_appText:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let str_colorContentTitle:[Character] = ["#","F"]
fileprivate let str_countValue:[Character] = ["F","2","3","4","8"]

/*: "Current network unavailable" :*/
fileprivate let str_maxName:String = "value self view thoughtCurrent"
fileprivate let str_imageData:String = " netwname content view request"
fileprivate let str_errValue:String = "navafrom"

/*: "icon_yidu_pre" :*/
fileprivate let str_makeTitle:String = "ICON"

/*: "99+" :*/
fileprivate let str_toolValue:String = "follow+"

/*: "transform.rotation" :*/
fileprivate let str_originText:String = "TRANSF"
fileprivate let str_inputCoinData:[Character] = ["o"]
fileprivate let str_itData:String = "blackablackion"

/*: "transform.scale" :*/
fileprivate let str_stackData:[Character] = ["t","r","a","n","s","f","o","r","m","."]
fileprivate let str_colorValue:[Character] = ["s","c","a","l","e"]

/*: "zoom&shake" :*/
fileprivate let str_centerData:String = "path labelzoom&"
fileprivate let str_noData:String = "plainake"

/*: "yyyy-MM-dd" :*/
fileprivate let str_dataContent:[Character] = ["y","y","y"]
fileprivate let str_toName:[Character] = ["y","-","M","M","-","d","d"]

/*: "Allow %@ to send you notifications?" :*/
fileprivate let str_countTitle:[UInt8] = [0x41,0x6c,0x6c,0x6f,0x77,0x20,0x25,0x40,0x20,0x74,0x6f,0x20,0x73,0x65,0x6e,0x64,0x20,0x79,0x6f,0x75,0x20,0x6e,0x6f,0x74,0x69,0x66,0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x73,0x3f]

/*: "Cancel" :*/
fileprivate let str_hiddenName:String = "Cancemake data view top"
fileprivate let str_topInputText:[Character] = ["l"]

/*: "Settings" :*/
fileprivate let str_tapDownValue:String = "store self viewSettin"
fileprivate let str_indexText:String = "table"

/*: "badNumber" :*/
fileprivate let str_pathValue:String = "badNuthat for make coin"
fileprivate let str_playerData:String = "celler"

/*: "isConnection" :*/
fileprivate let str_shareTitle:String = "child image jumpisCon"

/*: "networkStatus" :*/
fileprivate let str_selectName:String = "labtwor"
fileprivate let str_addTitle:[Character] = ["k","S"]
fileprivate let str_imageBirdContent:[Character] = ["t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let str_atData:[Character] = ["u","n","r","e","a","d","M","e","s","s","a","g","e"]
fileprivate let str_tempData:[Character] = ["N","u","m"]

/*: "Do you want to mark all messages as read?" :*/
fileprivate let str_myRemoveName:[UInt8] = [0x44,0x6f,0x20,0x79,0x6f,0x75,0x20,0x77,0x61,0x6e,0x74,0x20,0x74,0x6f,0x20,0x6d,0x61,0x72,0x6b,0x20,0x61,0x6c,0x6c,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65,0x73,0x20,0x61,0x73,0x20,0x72,0x65,0x61,0x64,0x3f]

/*: "OK" :*/
fileprivate let str_listValue:String = "ok"

/*: "消息列表一键已读失败：code: :*/
fileprivate let str_allName:[Character] = ["消","息","列","表","一","键","已","\u{8bfb}","失","败","：","c","o","d"]
fileprivate let str_numberHiddenMName:String = "cost make extensione:"

/*: , desc: :*/
fileprivate let str_viewValue:String = "select return, desc:"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ObjectLimitRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class ObjectLimitRecognizerDelegate: PropertyViewController {
	var astatineTotal: Double = 46.4
	var iconDictionary: [AnyHashable: String] = [:]
	var rowMagnitude: Double = 91.3
	var progressDictionary: [AnyHashable: String] = [:]

    var dataImageView: UIImageView?

    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        amimatCancel()
    
            if (badgLB.preservesSuperviewLayoutMargins) && (badgLB.layer.mask != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let resultModify = UpCommentView()
            resultModify.beautyOpen = slideVC.isHidenBottomLine
            resultModify.centralCityMagnitude = slideVC.topDistance
            
            resultModify.requestTotal = { [self] cornerCount in
            self.rowMagnitude = cornerCount
            
            var resultModify = slideVC.topDistance
                resultModify += 1
                if resultModify != 27 {
                    resultModify = resultModify - 1
                }
            if resultModify < self.rowMagnitude {
                self.rowMagnitude = resultModify
            }
            
            return self.rowMagnitude
            }
            resultModify.viewDictionary = { [self] playerDictionary in
            self.progressDictionary = playerDictionary
            
            guard var value = self.progressDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                badgLB.addSubview(resultModify)
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            if (badgLB.preservesSuperviewLayoutMargins) && (badgLB.layer.mask != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let resultModify = UpCommentView()
            resultModify.beautyOpen = animated
            resultModify.centralCityMagnitude = slideVC.topDistance
            
            resultModify.requestTotal = { [self] cornerCount in
            self.astatineTotal = cornerCount
            
            var resultModify = slideVC.rightSpace
                resultModify += 1
                if resultModify != 27 {
                    resultModify = resultModify - 1
                }
            if resultModify < self.astatineTotal {
                self.astatineTotal = resultModify
            }
            
            return self.astatineTotal
            }
            resultModify.viewDictionary = { [self] playerDictionary in
            self.iconDictionary = playerDictionary
            
            guard var value = self.iconDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                badgLB.addSubview(resultModify)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        toUi()
        //: bindInteraction()
        lineTo()
        //: func__turnOnSystemNotification()
        changeWith()

        dataImageView = UIImageView(frame: self.view.bounds.standardized)
        if let dataImageView = dataImageView {
            self.dataImageView?.image = UIImage(named: (str_giftData.replacingOccurrences(of: "with", with: "d") + "g_ad" + String(str_mediumContent.prefix(7))))
            if (dataImageView.inputViewController != nil) && (dataImageView.canBecomeFirstResponder) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(dataImageView)
            }
        }
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: 100 + a_itemTitle))
        //: colorV.image = UIImage.outsideText(name: "bg_message_top")
        colorV.image = UIImage.outsideText(name: (String(str_succeedName)))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: MTipView = {
        //: let V = TalkingNoticeTipView()
        let V = MTipView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: AppValueReactiveCompatible = {
        //: let vc = QYSlideNavigationViewController()
        let vc = AppValueReactiveCompatible()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .thoughtImage(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .thoughtImage(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (String(str_fillPostData)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.blockOf()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [PropertyViewController] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, SymmetricDataSource()]
        //: if MeasurementAppManager.share.loginUserMode.callTabSwitch == 1 {
        if MeasurementAppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(SocietalDataSource(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: AddDataManagerDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = AddDataManagerDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(str_colorData.prefix(4)) + "ages").localized, (String(str_bottomTitle.prefix(4)) + "like me").localized]
        //: if MeasurementAppManager.share.loginUserMode.callTabSwitch == 1 {
        if MeasurementAppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(str_appText)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: a_itemTitle, width: a_blockValue, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.drogueSize(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(str_colorContentTitle) + String(str_countValue)))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(str_maxName.suffix(7)) + String(str_imageData.prefix(5)) + "ork u" + str_errValue.replacingOccurrences(of: "from", with: "i") + "lable").localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: CustomTextView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = CustomTextView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: CustomTextView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = CustomTextView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.outsideText(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.outsideText(name: (str_makeTitle.lowercased() + "_yidu_pre")), for: .normal)
        //: btn.setImage(UIImage.outsideText(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.outsideText(name: (str_makeTitle.lowercased() + "_yidu_pre")), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - AtNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension ObjectLimitRecognizerDelegate: AtNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func failure(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: AddDataManagerDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            amimatCancel()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: SymmetricDataSource.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            a_textTurnName.photoOf(eventID: a_clickValue)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension ObjectLimitRecognizerDelegate {
    //: func setIsTopAll() {
    func penetrate() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? AddDataManagerDelegate
            //: vc?.resetToTopItemView()
            vc?.equalDigitizer()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.tv(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func fromWeightView() {
        //: if MeasurementAppManager.share.networkStatus != .Unavailable && TalkingIMManager.shared.isConnection {
        if MeasurementAppManager.share.networkStatus != .Unavailable, DateThen.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func backIndexNum(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func amimatCancel() {
        //: let unreadMsgCount = MeasurementAppManager.share.unreadMessageNum
        let unreadMsgCount = MeasurementAppManager.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: a_successData) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.playerAnimat()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func playerAnimat() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (str_originText.lowercased() + "orm.r" + String(str_inputCoinData) + str_itData.replacingOccurrences(of: "black", with: "t")))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(str_stackData) + String(str_colorValue)))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(str_centerData.suffix(5)) + str_noData.replacingOccurrences(of: "plain", with: "sh")))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func changeWith() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        PrefaceReactiveCompatible.nonsolidColour { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.displayError(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.barValue(date: Date(), dateFormat: (String(str_dataContent) + String(str_toName)))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = a_collectionName.string(forKey: a_senseData), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.displayError(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.displayError(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = a_collectionName.bool(forKey: a_errData)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        a_collectionName.set(true, forKey: a_errData)
                        //: TalkingAlertShow.alert(title: nil,
                        TitleAlertShow.admittance(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: str_countTitle, encoding: .utf8)!.random(a_callValue),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (String(str_hiddenName.prefix(5)) + String(str_topInputText)).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(str_tapDownValue.suffix(6)) + str_indexText.replacingOccurrences(of: "table", with: "gs")).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            TitleAlertShow.lastAlert()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func displayError(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(a_limitRecordScreenName)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = a_formalValue - a_limitRecordScreenName - a_userTitle
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(a_limitRecordScreenName + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = a_formalValue - self.noticeView.bottom - a_userTitle
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension ObjectLimitRecognizerDelegate {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func sufficientNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(str_pathValue.prefix(5)) + str_playerData.replacingOccurrences(of: "cell", with: "mb"))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension ObjectLimitRecognizerDelegate {
    /// UI
    //: private func createUI() {
    private func toUi() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(a_limitRecordScreenName)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(str_bottomTitle.prefix(4)) + "like me").localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(str_colorData.prefix(4)) + "ages").localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func lineTo() {
        //: TalkingIMManager.shared.rx
        DateThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(str_shareTitle.suffix(5)) + "nection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.fromWeightView()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: MeasurementAppManager.share.rx.observeWeakly(Int.self, "networkStatus")
        MeasurementAppManager.share.rx.observeWeakly(Int.self, (str_selectName.replacingOccurrences(of: "lab", with: "ne") + String(str_addTitle) + String(str_imageBirdContent)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.fromWeightView()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: MeasurementAppManager.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        MeasurementAppManager.share.rx.observeWeakly(Int.self, (String(str_atData) + String(str_tempData)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.backIndexNum(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = MarkAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                TitleAlertShow.appearAll(message: String(bytes: str_myRemoveName, encoding: .utf8)!.localized, leftBtnTitle: (String(str_hiddenName.prefix(5)) + String(str_topInputText)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    TitleAlertShow.lastAlert()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: a_successData)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        TitleMediumLogTool.commitMedium(msg: (String(str_allName) + String(str_numberHiddenMName.suffix(2))) + "\(code)" + (String(str_viewValue.suffix(7))) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(sufficientNotification(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: a_halfData,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.barValue(date: Date(), dateFormat: (String(str_dataContent) + String(str_toName)))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            a_collectionName.set(today, forKey: a_senseData)
            //: self.func__hideNotificationTipView(hide: true)
            self.displayError(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.changeWith()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
