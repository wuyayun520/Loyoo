
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let str_addTitle:String = "mf/bmodel server error selected height"
fileprivate let str_onGiftValue:String = "ss/gshared fatal bottom after"
fileprivate let str_buttonValue:String = "path"
fileprivate let str_borderTitle:String = "leading to interactionftList"

/*: "type" :*/
fileprivate let str_fatalName:[UInt8] = [0x67,0x6a,0x63,0x76]

/*: "mf/chatRoom/giftList" :*/
fileprivate let str_freeActionLimitName:String = "mf/cwith in view m"
fileprivate let str_edgeValue:String = "om/gifbutton value let"
fileprivate let str_targetValue:String = "info professionaltList"

/*: "live/giftList" :*/
fileprivate let str_blockModelValue:String = "image content equal data centerlive/"

/*: "party/giftList" :*/
fileprivate let str_labelValue:[Character] = ["p","a","r","t","y","/","g","i","f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let str_whiteTitle:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let str_acrossValue:String = "mf/badd view self"
fileprivate let str_statusText:String = "s/sendMsgblock else"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_viewName:String = "M"
fileprivate let str_stateData:String = "f/chatmodel length lack"
fileprivate let str_textData:String = "endMsgdata self to"

/*: "mf/business/sendGift" :*/
fileprivate let str_addData:String = "mf/bustate in"
fileprivate let str_nameText:String = "no size datas/sendGif"
fileprivate let str_indexData:[Character] = ["t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_willKindValue:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let str_shTitle:[Character] = ["o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let str_makeTitle:[Character] = ["l","i","v","e","/","s","e","n"]
fileprivate let str_blockValue:String = "add in guarddGift"

/*: "party/sendGift" :*/
fileprivate let str_toData:[Character] = ["p","a","r","t","y","/"]
fileprivate let str_addValue:String = "label normalsendGift"

/*: "mf/user/getChatInfo" :*/
fileprivate let str_textValue:String = "self make minimf/use"
fileprivate let str_errorValue:String = "r/geimage online"
fileprivate let str_pastData:[Character] = ["t","C","h","a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let str_dropData:String = "data thought method selfIM/ge"
fileprivate let str_modelCenterName:String = "view wrap to size attributeageUse"
fileprivate let str_toMomentContent:String = "isperformance"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let str_equalData:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i","k","e","M","e"]
fileprivate let str_layerValue:[Character] = ["L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let str_toTitle:[Character] = ["m","f","/","c","r","u","s"]
fileprivate let str_giftTitle:[Character] = ["h","/","i","g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let str_tapValue:[Character] = ["m","f","/","c","r","u"]
fileprivate let str_somethingText:[Character] = ["s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let str_toName:String = "make frame let agentmf/us"
fileprivate let str_dataDoingValue:String = "T"
fileprivate let str_requestData:[Character] = ["R","e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let str_succeedContent:[UInt8] = [0x75,0x73,0x65,0x72,0x2f,0x61,0x74,0x74,0x65,0x6e,0x74,0x69,0x6f,0x6e]

/*: "user/removeAttention" :*/
fileprivate let str_menuTitle:[Character] = ["u","s","e","r","/","r","e","m","o","v","e"]
fileprivate let str_titlePingData:String = "Attentiongesture image"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let str_tipData:[UInt8] = [0x43,0x61,0x6e,0x27,0x74,0x20,0x65,0x61,0x72,0x6e,0x20,0x70,0x6f,0x69,0x6e,0x74,0x73,0x20,0x69,0x66,0x20,0x79,0x6f,0x75,0x20,0x66,0x6f,0x6c,0x6c,0x6f,0x77,0x20,0x65,0x61,0x63,0x68,0x20,0x6f,0x74,0x68,0x65,0x72,0x7e]

/*: "follow Success" :*/
fileprivate let str_videoTitle:[Character] = ["f","o","l","l","o","w"," ","S","u","c"]
fileprivate let str_blockName:[Character] = ["c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let str_formalInputValue:String = "unfovaluevalueow"
fileprivate let str_arrayName:String = " Stype normal"
fileprivate let str_labSizeFrameData:String = "UCCESS"

/*: "translate/msg" :*/
fileprivate let str_modeData:String = "TRANSLA"
fileprivate let str_managerName:[Character] = ["t","e","/","m","s","g"]

/*: "msgId" :*/
fileprivate let str_countValue:[UInt8] = [0x6d,0x73,0x67,0x49,0x64]

/*: "targetLng" :*/
fileprivate let str_kitValue:[UInt8] = [0x74,0x61,0x72,0x67,0x65,0x74,0x4c,0x6e,0x67]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let str_blackText:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u","t","o","G","r","e"]
fileprivate let str_byValue:String = "gift list finishetCo"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let str_nameData:String = "name female imagemf/gree"
fileprivate let str_backgroundName:[Character] = ["t","/","g","e","t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let str_normalOfValue:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "mf/chatRoom/info" :*/
fileprivate let str_imageData:String = "mf/chavar style"
fileprivate let str_factData:String = "size length/info"

/*: "mf/chatRoom/list" :*/
fileprivate let str_countData:String = "mf/chmanager model through equal case"
fileprivate let str_shareTitle:String = "atRolive tag top"

/*: "roomId" :*/
fileprivate let str_topName:[UInt8] = [0x72,0x6f,0x6f,0x6d,0x49,0x64]

/*: "needExtraInfo" :*/
fileprivate let str_equalContent:[UInt8] = [0xa7,0xac,0xac,0xad,0x8c,0xb1,0xbd,0xbb,0xa8,0x80,0xa7,0xaf,0xa6]

/*: "mf/chatRoom/check" :*/
fileprivate let str_popData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let str_withText:[Character] = ["/","c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let str_makeText:String = "mf/cshared text label var at"
fileprivate let str_scaleData:String = "model nameom/jo"
fileprivate let str_titleValue:[Character] = ["i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let str_photoName:String = "mf/chmake size"
fileprivate let str_toolColorTitle:String = "leading elseatRo"
fileprivate let str_countLengthTitle:String = "managermbmanagerrs"

/*: "translate/text" :*/
fileprivate let str_someoneName:String = "tsourceansl"
fileprivate let str_colorName:String = "frame inate/text"

/*: "text" :*/
fileprivate let str_managerValue:[UInt8] = [0x74,0x65,0x78,0x74]

/*: "mf/user/queryStatus" :*/
fileprivate let str_countMakeData:[Character] = ["m","f","/","u","s","e","r","/","q","u","e","r","y","S","t","a","t"]
fileprivate let str_cellValue:String = "uprice"

/*: "toUids" :*/
fileprivate let str_rawReadName:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let str_readyStartData:[Character] = ["c","a","l","l","/","g","e","t"]
fileprivate let str_modelValue:String = "CallTabself let"

/*: "mf/user/remark" :*/
fileprivate let str_bottomData:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a","r","k"]

/*: "content" :*/
fileprivate let str_ofTitle:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "top" :*/
fileprivate let str_subData:[UInt8] = [0x70,0x6f,0x74]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DoingSheRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class DoingSheRequestTool: NSObject {
public class DoingSheRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [CountMediumCacheModel]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [CountMediumCacheModel]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func decoke(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(str_addTitle.prefix(4)) + "usine" + String(str_onGiftValue.prefix(4)) + str_buttonValue.replacingOccurrences(of: "path", with: "i") + String(str_borderTitle.suffix(6)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: str_fatalName.map{$0^19}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func form(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(str_freeActionLimitName.prefix(4)) + "hatRo" + String(str_edgeValue.prefix(6)) + String(str_targetValue.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func mailingList(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(str_blockModelValue.suffix(5)) + "giftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func alignCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(str_labelValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func inform(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(str_whiteTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func doingCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(str_acrossValue.prefix(4)) + "usines" + String(str_statusText.prefix(9)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func showCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (str_viewName.lowercased() + String(str_stateData.prefix(6)) + "Room/s" + String(str_textData.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func adminPlayer(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(str_addData.prefix(5)) + "sines" + String(str_nameText.suffix(9)) + String(str_indexData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func chat(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(str_willKindValue) + String(str_shTitle))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func splitWith(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(str_makeTitle) + String(str_blockValue.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func requestLiberalDemocratParty(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(str_toData) + String(str_addValue.suffix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modelImageCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(str_textValue.suffix(6)) + String(str_errorValue.prefix(4)) + String(str_pastData))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func exceptResign(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(str_dropData.suffix(5)) + "tMess" + String(str_modelCenterName.suffix(6)) + "rInfoL" + str_toMomentContent.replacingOccurrences(of: "performance", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<CountMediumCacheModel>.deserialize(from: result as? Array) {
            if let datas = Array<CountMediumCacheModel>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [CountMediumCacheModel])
                completion(succeed, datas as! [CountMediumCacheModel])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func rawData(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        a_tabData.roundCurrentCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func halfFinish(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(str_equalData) + String(str_layerValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func just(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(str_toTitle) + String(str_giftTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(str_tapValue) + String(str_somethingText))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func global(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(str_toName.suffix(5)) + "er/ge" + str_dataDoingValue.lowercased() + String(str_requestData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func trackSize(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: str_succeedContent, encoding: .utf8)! : (String(str_menuTitle) + String(str_titlePingData.prefix(9)))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if MeasurementAppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if MeasurementAppManager.share.loginUserMode.sex == SizeCustomReflectable.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        a_infoTitle.afterTitle(showMsg: String(bytes: str_tipData, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        a_infoTitle.afterTitle(showMsg: (String(str_videoTitle) + String(str_blockName)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    a_infoTitle.afterTitle(showMsg: (str_formalInputValue.replacingOccurrences(of: "value", with: "l") + String(str_arrayName.prefix(2)) + str_labSizeFrameData.lowercased()).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                a_infoTitle.episode(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func repeatScreen(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (str_modeData.lowercased() + String(str_managerName))
        //: reqModel.params = ["msgId": msgId, "targetLng": ImageMacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: str_countValue, encoding: .utf8)!: msgId, String(bytes: str_kitValue, encoding: .utf8)!: ImageMacroDefine.loadLang(), String(bytes: str_fatalName.map{$0^19}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func belowUp(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(str_blackText) + String(str_byValue.suffix(4)) + "ntent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func getTextMake(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(str_nameData.suffix(7)) + String(str_backgroundName))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: str_normalOfValue.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func signal(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(str_imageData.prefix(6)) + "tRoom" + String(str_factData.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func listTap(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(str_countData.prefix(5)) + String(str_shareTitle.prefix(4)) + "om/list")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func afterDoingPremium(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: str_topName, encoding: .utf8)!: roomId, String(bytes: str_equalContent.map{$0^201}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(str_popData) + String(str_withText))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func pokeAt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(str_makeText.prefix(4)) + "hatRo" + String(str_scaleData.suffix(5)) + String(str_titleValue))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func failName(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(str_photoName.prefix(5)) + String(str_toolColorTitle.suffix(4)) + "om/m" + str_countLengthTitle.replacingOccurrences(of: "manager", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func itemCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (str_someoneName.replacingOccurrences(of: "source", with: "r") + String(str_colorName.suffix(8)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: str_managerValue, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func linguisticContext(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(str_countMakeData) + str_cellValue.replacingOccurrences(of: "price", with: "s"))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: str_rawReadName.reversed(), encoding: .utf8)!: toUids, String(bytes: str_fatalName.map{$0^19}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func distil(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(str_readyStartData) + String(str_modelValue.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func blockCompletion(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = TitleTheoryThen()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(str_bottomData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: str_ofTitle.reversed(), encoding: .utf8)!: content, String(bytes: str_subData.reversed(), encoding: .utf8)!: isTop, String(bytes: str_normalOfValue.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        a_tabData.blackFor(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
