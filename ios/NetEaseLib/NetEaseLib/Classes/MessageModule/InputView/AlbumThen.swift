
//: Declare String Begin

/*: "btn_talk_voice_nor" :*/
fileprivate let str_valuePathData:String = "hidden destroy letbtn_t"
fileprivate let str_toName:String = "oicsize"
fileprivate let str_pathTitle:String = "color app if_nor"

/*: "btn_talk_voice_pre" :*/
fileprivate let str_makeText:String = "btn_tastart image normal of"
fileprivate let str_appearData:[Character] = ["e","_","p","r","e"]

/*: "contentSize" :*/
fileprivate let str_oldData:[Character] = ["c","o","n","t","e","n","t","S","i","z","e"]

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_leadingText:[UInt8] = [0x16,0x11,0x16,0xb,0x57,0x1c,0x10,0x1b,0x1a,0xd,0x45,0x56,0x5f,0x17,0x1e,0xc,0x5f,0x11,0x10,0xb,0x5f,0x1d,0x1a,0x1a,0x11,0x5f,0x16,0x12,0xf,0x13,0x1a,0x12,0x1a,0x11,0xb,0x1a,0x1b]

/*: "view_image" :*/
fileprivate let str_fatalValue:String = "vpic"
fileprivate let str_viewPathRemoveValue:[Character] = ["e","w","_","i","m","a","g","e"]

/*: "btn_talk_more_photo_nor" :*/
fileprivate let str_pointTitle:[Character] = ["b","t","n","_","t","a"]
fileprivate let str_imageValue:String = "lk_morself image add"
fileprivate let str_waitAfterName:[Character] = ["o","t","o","_","n","o","r"]

/*: "btn_news_shotVideo_nor" :*/
fileprivate let str_statusData:[Character] = ["b","t","n","_","n","e","w"]
fileprivate let str_messageModelData:String = "s_shotcorner data m min let"
fileprivate let str_viewData:[Character] = ["o","_","n","o","r"]

/*: "btn_news_video_nor" :*/
fileprivate let str_originContent:[Character] = ["b","t","n","_","n","e","w","s","_","v","i","d","e","o","_","n","o","r"]

/*: "btn_talk_gift_nor" :*/
fileprivate let str_ageValue:[Character] = ["b","t","n","_","t","a","l","k","_","g","i","f","t","_","n","o"]
fileprivate let str_labData:String = "receive"

/*: "btn_more_game_nor" :*/
fileprivate let str_makeName:[Character] = ["b","t","n","_","m","o"]
fileprivate let str_customValue:[Character] = ["r","e","_","g","a","m"]
fileprivate let str_cellContent:[Character] = ["e","_","n","o","r"]

/*: "live_picture" :*/
fileprivate let str_sharedIconTableValue:String = "return medium extensionlive_"
fileprivate let str_listValue:String = "sign"

/*: "F5F5F5" :*/
fileprivate let str_activityTitleValueText:[Character] = ["F","5","F","5","F","5"]

/*: "btn_talk_send_pre" :*/
fileprivate let str_statusBagUserValue:[Character] = ["b","t","n","_","t","a","l","k","_","s","e"]
fileprivate let str_labName:String = "nd_preall message table"

/*: "btn_talk_send_nor" :*/
fileprivate let str_managerValue:[Character] = ["b","t","n","_","t","a","l","k","_","s","e","n","d","_"]
fileprivate let str_makeTitle:String = "nstatusr"

/*: "#EBEBEB" :*/
fileprivate let str_indexShowValue:String = "view start hidden point#EBEBEB"

/*: "#999999" :*/
fileprivate let str_joinValue:String = "#999999"

/*: "Type a message…" :*/
fileprivate let str_toTableValue:[Character] = ["T","y","p","e"," "]
fileprivate let str_topTitle:String = "self image laba mes"

/*: "Hold to talk" :*/
fileprivate let str_frameData:String = "Hold tin var false storage"

/*: "#DDDDDD" :*/
fileprivate let str_recordStyleValue:[Character] = ["#"]
fileprivate let str_noTitle:String = "likelikelike"

/*: "leading_add_image" :*/
fileprivate let str_roomTitle:[Character] = ["l","e","a","d","i","n","g","_","a","d","d","_","i"]
fileprivate let str_labelAddValue:[Character] = ["m","a","g","e"]

/*: "A maximum of 10 people can be @ one message" :*/
fileprivate let str_femaleData:[UInt8] = [0x41,0x20,0x6d,0x61,0x78,0x69,0x6d,0x75,0x6d,0x20,0x6f,0x66,0x20,0x31,0x30,0x20,0x70,0x65,0x6f,0x70,0x6c,0x65,0x20,0x63,0x61,0x6e,0x20,0x62,0x65,0x20,0x40,0x20,0x6f,0x6e,0x65,0x20,0x6d,0x65,0x73,0x73,0x61,0x67,0x65]

/*: "@ :*/
fileprivate let str_viewName:String = "@"

/*: "name" :*/
fileprivate let str_coverName:String = "giftme"

/*: "length" :*/
fileprivate let str_okName:[Character] = ["l","e","n","g","t","h"]

/*: "<at>@ :*/
fileprivate let str_sayFillName:String = "<at>@self data sound"

/*: </at> " :*/
fileprivate let str_tableText:[Character] = ["<","/","a","t",">"," "]

/*: "uid" :*/
fileprivate let str_keyText:[Character] = ["u","i","d"]

/*: "btn_talk_keyboard_nor" :*/
fileprivate let str_clickMakeValue:String = "manager true nowbtn_ta"
fileprivate let str_messageTitle:String = "if self var indexyboard_n"
fileprivate let str_equalName:String = "bottomr"

/*: "btn_talk_keyboard_pre" :*/
fileprivate let str_sizeData:String = "video view closebtn_"
fileprivate let str_makeContent:String = "omax"
fileprivate let str_canValue:String = "d_preleft enable data copy"

/*: "wav" :*/
fileprivate let str_textData:String = "wastate"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AlbumThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/5.
//

//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// 聊天类型
//: enum InputViewType {
enum RegularViewType {
    //: case PrivateChat
    case PrivateChat // 私聊
    //: case GroupChat
    case GroupChat // 群聊
    //: case CustomerChat
    case CustomerChat // 客服
    //: case LiveRoom
    case LiveRoom // 直播间
}

//: enum MoreActionType: Int {
enum MediaTablePropertyProtocol: Int {
    //: case Photo        = 0
    case Photo = 0
    //: case VideoCall
    case VideoCall
    //: case ShortVideo
    case ShortVideo
    //: case Gift
    case Gift
    //: case Game
    case Game
}

//: protocol ChatInputViewDelegate: NSObject {
protocol PanoramicViewBlockThen: NSObject {
    //: func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?)
    func nameModel(msgStr: String, quoteModel: IsochronalModel?)

    //: func func__selectMoreFunAction(actionType: MoreActionType)
    func funcTextType(actionType: MediaTablePropertyProtocol)

    /// 发送礼物
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String)
    func omit(giftModel: WithMeasurable, giftNum: String)

    /// 索要礼物
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String)
    func equalTime(giftModel: WithMeasurable, giftNum: String)

    /// 聊天室发送礼物
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    ///   - model: 聊天室成员信息
    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel)
    func imageModel(giftModel: WithMeasurable, giftNum: String, model: RegularThen)

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int)
    func phonation(voicePath: String, voiceTime: Int)

    //: func heightToBottomChanged(heightToBottom: CGFloat)
    func moreAcross(heightToBottom: CGFloat)

    //: func inputViewHeightChanged(height: CGFloat)
    func accommodate(height: CGFloat)

    //: func func__starRecordBtnDown()
    func equip()

    /// 群聊@
    //: func func__selectMentionUser(range: NSRange)
    func groupRange(range: NSRange)

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond()
    func atRequest()
}

//: class TalkingChatInputView: UIView {
class AlbumThen: UIView {
	var reasonDoing: Bool = false
	var collectionStarRowNumber: Int = 19
	var levelArray: [AnyHashable] = []
	var wrapUpDictionary: [AnyHashable: String] = [:]
	var nextOpen: Bool = true
	var liveQuantity: Int = 80
	var dismissArray: [AnyHashable] = []
	var fullDictionary: [AnyHashable: String] = [:]
	var frameScriptOn: Bool = true
	var popCount: Int = 84
	var terminateArray: [AnyHashable] = []
	var sizeDictionary: [AnyHashable: String] = [:]

    var frameImageView: UIImageView?

    //: open weak var delegate: ChatInputViewDelegate?
    open weak var delegate: PanoramicViewBlockThen?
    //: open var inputStyle: InputViewType = .PrivateChat
    open var inputStyle: RegularViewType = .PrivateChat
    //: open var recordView: TalkingChatRecordView?
    open var recordView: ShareholdingReactiveCompatible?

    //: open var quoteModel: AbTalkingChatMsgQuoteModel? {
    open var quoteModel: IsochronalModel? {
        //: didSet {
        didSet {
            //: if let tempModel = quoteModel {
            if let tempModel = quoteModel {
                //: self.quoteView.isHidden = false
                self.quoteView.isHidden = false
                //: self.quoteView.quoteModel = tempModel
                self.quoteView.quoteModel = tempModel
                //: self.inputTextView.becomeFirstResponder()
                self.inputTextView.becomeFirstResponder()

                //: self.voiceBtn.setImage(UIImage.outsideText(name: "btn_talk_voice_nor"), for: .normal)
                self.voiceBtn.setImage(UIImage.outsideText(name: (String(str_valuePathData.suffix(5)) + "alk_v" + str_toName.replacingOccurrences(of: "size", with: "e") + String(str_pathTitle.suffix(4)))), for: .normal)
                //: self.voiceBtn.setImage(UIImage.outsideText(name: "btn_talk_voice_pre"), for: .highlighted)
                self.voiceBtn.setImage(UIImage.outsideText(name: (String(str_makeText.prefix(6)) + "lk_voic" + String(str_appearData))), for: .highlighted)
                //: self.recordButton.isHidden = true
                self.recordButton.isHidden = true
                //: self.inputTextView.becomeFirstResponder()
                self.inputTextView.becomeFirstResponder()

                //: }else{
            } else {
                //: self.quoteView.isHidden = true
                self.quoteView.isHidden = true
            }
        }
    }

    /** 界面高度 */
    //: let BackViewHeight = 98 + kDeviceSafeBottomHeight
    let BackViewHeight = 98 + a_agentData

    /** 输入框界面高度 */
    //: let InputViewHeight = 40
    let InputViewHeight = 40

    /** 间隙 */
    //: let InputViewPadingHeight = 10
    let InputViewPadingHeight = 10

    /** 录音最大时长 */
    //: let RecordMaxDuration = 30.0
    let RecordMaxDuration = 30.0
    // 群聊@ 消息
    //: var atArr = [String]()
    var atArr = [String]()

    //: var recorder: AVAudioRecorder?
    var recorder: AVAudioRecorder?
    //: var recordStartTime: NSDate = NSDate.init()
    var recordStartTime: NSDate = .init()
    //: var recordTimer: Timer?
    var recordTimer: Timer?
    //: var duration: Int = 0
    var duration: Int = 0

    //: var chatRoomID = ""
    var chatRoomID = ""

    //: var showOtherInputView = false
    var showOtherInputView = false

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var inputAttr = [NSAttributedString.Key: Any]()
    private var inputAttr = [NSAttributedString.Key: Any]()
    /// 输入框文本长度限制
    //: private let chatlimitCount = 300
    private let chatlimitCount = 300

    //: init(type: InputViewType) {
    init(type: RegularViewType) {
        //: super.init(frame: .zero)
        super.init(frame: .zero)
        //: self.inputStyle = type
        self.inputStyle = type

        //: customUI()
        misplayUi()
        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardWasShown(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(errorToSuccess(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardWillBeHidden(notification:)), name: UIResponder.keyboardWillHideNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(search(notification:)), name: UIResponder.keyboardWillHideNotification, object: nil)
        //: inputTextView.rx.observeWeakly(UITextView.self, "contentSize").subscribe(onNext: { [weak self] (change) in
        inputTextView.rx.observeWeakly(UITextView.self, (String(str_oldData))).subscribe(onNext: { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.func__updateInputContentView()
            self.lab()
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        //: inputTextView.rx.text.orEmpty.subscribe(onNext: { [weak self] in
        inputTextView.rx.text.orEmpty.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if $0.utf16.count > self.chatlimitCount {
            if $0.utf16.count > self.chatlimitCount {
                //: let selectedRange = self.inputTextView.markedTextRange
                let selectedRange = self.inputTextView.markedTextRange
                // 没有在拼写状态再判断
                //: if selectedRange == nil {
                if selectedRange == nil {
                    // 通过字符串截取实现限制100字符长度
                    //: self.inputTextView.text = ($0 as NSString).substring(to: self.chatlimitCount)
                    self.inputTextView.text = ($0 as NSString).substring(to: self.chatlimitCount)
                }
            }

            // 检测是否需要展示翻译
            //: if $0.utf16.count <= 0 {
            if $0.utf16.count <= 0 { // 当输入框为空时，立即调用
                //: self.interval_checkInputContent()
                self.removeName()
                //: self.sendBtn.isSelected = false
                self.sendBtn.isSelected = false
                //: } else {
            } else {
                //: self.interval_cancelPreviousPerformRequest()
                self.appRequest()
                //: self.interval_startPerformRequest()
                self.drogue()
                //: self.sendBtn.isSelected = true
                self.sendBtn.isSelected = true
            }

            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        //: bindAction()
        misnomer()
        //: initMoreBtnView()
        bulge()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_leadingText.map{$0^127}, encoding: .utf8)!)
    }

    //: private func customUI () {
    private func misplayUi() {
        //: backgroundColor = .white
        backgroundColor = .white
        self.frameImageView?.image = UIImage(named: (str_fatalValue.replacingOccurrences(of: "pic", with: "i") + String(str_viewPathRemoveValue)))
        //: if inputStyle == .PrivateChat {
        if inputStyle == .PrivateChat {
            //: voiceBtn.snp.makeConstraints { make in
            voiceBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(self).offset(8)
                make.leading.equalTo(self).offset(8)
                //: make.centerY.equalTo(contentView)
                make.centerY.equalTo(contentView)
                //: make.width.height.equalTo(26)
                make.width.height.equalTo(26)
            }
            //: sendBtn.snp.makeConstraints { make in
            sendBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(self.snp.trailing).offset(-8)
                make.trailing.equalTo(self.snp.trailing).offset(-8)
                //: make.centerY.equalTo(contentView)
                make.centerY.equalTo(contentView)
                //: make.width.height.equalTo(26)
                make.width.height.equalTo(26)
            }

            //: quoteView.snp.makeConstraints { make in
            quoteView.snp.makeConstraints { make in
                //: make.top.equalTo(self).offset(0)
                make.top.equalTo(self).offset(0)
                //: make.leading.equalTo(8)
                make.leading.equalTo(8)
                //: make.trailing.equalTo(-8)
                make.trailing.equalTo(-8)
                //: make.height.equalTo(0)
                make.height.equalTo(0)
            }

            //: contentView.snp.makeConstraints { make in
            contentView.snp.makeConstraints { make in
                //: make.top.equalTo(quoteView.snp.bottom).offset(8)
                make.top.equalTo(quoteView.snp.bottom).offset(8)
                //: make.leading.equalTo(voiceBtn.snp.trailing).offset(8)
                make.leading.equalTo(voiceBtn.snp.trailing).offset(8)
                //: make.trailing.equalTo(sendBtn.snp.leading).offset(-8)
                make.trailing.equalTo(sendBtn.snp.leading).offset(-8)
                //: make.height.equalTo(inputTextView)
                make.height.equalTo(inputTextView)
            }

            //: inputTextView.snp.makeConstraints { make in
            inputTextView.snp.makeConstraints { make in
                //: make.top.equalTo(contentView)
                make.top.equalTo(contentView)
                //: make.leading.equalTo(contentView.snp.leading).offset(8)
                make.leading.equalTo(contentView.snp.leading).offset(8)
                //: make.width.equalTo(contentView).offset(-16)
                make.width.equalTo(contentView).offset(-16)
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: bottomView.snp.makeConstraints { make in
            bottomView.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(42)
                make.height.equalTo(42)
                //: make.top.equalTo(contentView.snp.bottom).offset(8)
                make.top.equalTo(contentView.snp.bottom).offset(8)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
                make.bottom.equalTo(-a_agentData)
            }
            //: recordButton.snp.makeConstraints { make in
            recordButton.snp.makeConstraints { make in
                //: make.edges.equalTo(contentView)
                make.edges.equalTo(contentView)
            }

            //: } else if inputStyle == .GroupChat {
        } else if inputStyle == .GroupChat {
            //: sendBtn.snp.makeConstraints { make in
            sendBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(self.snp.trailing).offset(-8)
                make.trailing.equalTo(self.snp.trailing).offset(-8)
                //: make.centerY.equalTo(contentView)
                make.centerY.equalTo(contentView)
                //: make.width.height.equalTo(26)
                make.width.height.equalTo(26)
            }
            //: contentView.snp.makeConstraints { make in
            contentView.snp.makeConstraints { make in
                //: make.leading.top.equalTo(self).offset(8)
                make.leading.top.equalTo(self).offset(8)
                //: make.trailing.equalTo(sendBtn.snp.leading).offset(-8)
                make.trailing.equalTo(sendBtn.snp.leading).offset(-8)
                //: make.height.equalTo(inputTextView)
                make.height.equalTo(inputTextView)
            }
            //: inputTextView.snp.makeConstraints { make in
            inputTextView.snp.makeConstraints { make in
                //: make.top.equalTo(contentView)
                make.top.equalTo(contentView)
                //: make.leading.equalTo(contentView.snp.leading).offset(8)
                make.leading.equalTo(contentView.snp.leading).offset(8)
                //: make.width.equalTo(contentView).offset(-16)
                make.width.equalTo(contentView).offset(-16)
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: bottomView.snp.makeConstraints { make in
            bottomView.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(42)
                make.height.equalTo(42)
                //: make.top.equalTo(contentView.snp.bottom).offset(8)
                make.top.equalTo(contentView.snp.bottom).offset(8)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
                make.bottom.equalTo(-a_agentData)
            }
            //: recordButton.snp.makeConstraints { make in
            recordButton.snp.makeConstraints { make in
                //: make.edges.equalTo(contentView)
                make.edges.equalTo(contentView)
            }
            //: } else if inputStyle == .CustomerChat {
        } else if inputStyle == .CustomerChat {
            //: sendBtn.snp.makeConstraints { make in
            sendBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(self.snp.trailing).offset(-8)
                make.trailing.equalTo(self.snp.trailing).offset(-8)
                //: make.centerY.equalTo(contentView)
                make.centerY.equalTo(contentView)
                //: make.width.height.equalTo(26)
                make.width.height.equalTo(26)
            }
            //: contentView.snp.makeConstraints { make in
            contentView.snp.makeConstraints { make in
                //: make.top.equalTo(self).offset(8)
                make.top.equalTo(self).offset(8)
                //: make.leading.equalToSuperview().offset(8)
                make.leading.equalToSuperview().offset(8)
                //: make.trailing.equalTo(sendBtn.snp.leading).offset(-16)
                make.trailing.equalTo(sendBtn.snp.leading).offset(-16)
                //: make.height.equalTo(inputTextView)
                make.height.equalTo(inputTextView)
            }
            //: inputTextView.snp.makeConstraints { make in
            inputTextView.snp.makeConstraints { make in
                //: make.top.equalTo(contentView)
                make.top.equalTo(contentView)
                //: make.leading.equalTo(contentView.snp.leading).offset(8)
                make.leading.equalTo(contentView.snp.leading).offset(8)
                //: make.width.equalTo(contentView).offset(-16)
                make.width.equalTo(contentView).offset(-16)
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: bottomView.snp.makeConstraints { make in
            bottomView.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(42)
                make.height.equalTo(42)
                //: make.top.equalTo(contentView.snp.bottom).offset(8)
                make.top.equalTo(contentView.snp.bottom).offset(8)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
                make.bottom.equalTo(-a_agentData)
            }
            //: }else if inputStyle == .LiveRoom {
        } else if inputStyle == .LiveRoom {
            //: sendBtn.snp.makeConstraints { make in
            sendBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(self.snp.trailing).offset(-8)
                make.trailing.equalTo(self.snp.trailing).offset(-8)
                //: make.centerY.equalTo(contentView)
                make.centerY.equalTo(contentView)
                //: make.width.height.equalTo(26)
                make.width.height.equalTo(26)
            }
            //: quoteView.snp.makeConstraints { make in
            quoteView.snp.makeConstraints { make in
                //: make.top.equalTo(self).offset(0)
                make.top.equalTo(self).offset(0)

                //: make.leading.equalTo(8)
                make.leading.equalTo(8)
                //: make.trailing.equalTo(-8)
                make.trailing.equalTo(-8)
                //: make.height.equalTo(0)
                make.height.equalTo(0)
            }
            //: contentView.snp.makeConstraints { make in
            contentView.snp.makeConstraints { make in
                //: make.top.equalTo(quoteView.snp.bottom).offset(8)
                make.top.equalTo(quoteView.snp.bottom).offset(8)
                //: make.leading.equalToSuperview().offset(8)
                make.leading.equalToSuperview().offset(8)
                //: make.trailing.equalTo(sendBtn.snp.leading).offset(-16)
                make.trailing.equalTo(sendBtn.snp.leading).offset(-16)
                //: make.height.equalTo(inputTextView)
                make.height.equalTo(inputTextView)
            }
            //: inputTextView.snp.makeConstraints { make in
            inputTextView.snp.makeConstraints { make in
                //: make.top.equalTo(contentView)
                make.top.equalTo(contentView)
                //: make.leading.equalTo(contentView.snp.leading).offset(8)
                make.leading.equalTo(contentView.snp.leading).offset(8)
                //: make.width.equalTo(contentView).offset(-16)
                make.width.equalTo(contentView).offset(-16)
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: bottomView.snp.makeConstraints { make in
            bottomView.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(42)
                make.height.equalTo(42)
                //: make.top.equalTo(contentView.snp.bottom).offset(8)
                make.top.equalTo(contentView.snp.bottom).offset(8)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
                make.bottom.equalTo(-a_agentData)
            }
        }
    
            if (contentView.layer.position.y == 15.19) && (contentView.layer.mask != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let menuLet = GiftShotView(frame: contentView.frame.insetBy(dx: CGFloat(62), dy: CGFloat(623.02)))
            menuLet.dataName = giftView.meneView.roomID
            menuLet.unsleepingSwitch = { [self] showClose in
            self.nextOpen = showClose
            
            var menuLet = showOtherInputView
                menuLet = true
                menuLet = false
            if menuLet != self.nextOpen {
                self.nextOpen = menuLet
            }
            
                self.nextOpen = true
                self.nextOpen = !self.nextOpen
            return self.nextOpen
            }
            menuLet.awakeMagnitude = { [self] momentMoveVoiceSum in
            self.liveQuantity = momentMoveVoiceSum
            
            var menuLet = InputViewHeight
            menuLet &*= 3
            if menuLet < self.liveQuantity {
                self.liveQuantity = menuLet
            }
            
            return self.liveQuantity
            }
            menuLet.snapArray = { [self] lastArray in
            self.dismissArray = lastArray
            
            guard var value = self.dismissArray as? [String] else {
                return nil
            }
            return value
            }
            menuLet.indexDictionary = { [self] managerBagDictionary in
            self.fullDictionary = managerBagDictionary
            
            guard var value = self.fullDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                contentView.addSubview(menuLet)
            }

	}

    //: private func bindAction() {
    private func misnomer() {
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String)  -> Void in
        giftView.sendActionBlock = { [weak self] (_ giftModel: WithMeasurable, _ num: String) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.delegate?.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.delegate?.omit(giftModel: giftModel, giftNum: num)
            //: return
        }
        //: giftView.chatRoomSendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String, _ model: TalkingChatRoomMemberModel) -> Void in
        giftView.chatRoomSendActionBlock = { [weak self] (_ giftModel: WithMeasurable, _ num: String, _ model: RegularThen) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.delegate?.func__sendChatRoomGiftMsg(giftModel: giftModel, giftNum: num, model: model)
            self.delegate?.imageModel(giftModel: giftModel, giftNum: num, model: model)
            //: return
        }
        //: giftView.giftHideBlock = { [weak self] ()  -> Void in
        giftView.giftHideBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.showOtherInputView = false
            self.showOtherInputView = false
            //: return
        }

        //: if inputStyle == .PrivateChat || inputStyle == .LiveRoom {
        if inputStyle == .PrivateChat || inputStyle == .LiveRoom {
            //: quoteView.closeBtnBlock = { [weak self] ()  -> Void in
            quoteView.closeBtnBlock = { [weak self] () in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.quoteModel = nil
                self.quoteModel = nil
            }
            //: quoteView.rx.hidden.subscribe{[weak self] (isHidden) in
            quoteView.rx.hidden.subscribe { [weak self] isHidden in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.quoteView.snp.updateConstraints { make in
                self.quoteView.snp.updateConstraints { make in
                    //: make.top.equalTo(isHidden ? 0 : 8)
                    make.top.equalTo(isHidden ? 0 : 8)
                    //: make.height.equalTo(isHidden ? 0 : 28)
                    make.height.equalTo(isHidden ? 0 : 28)
                }
                //: self.func__updateInputContentView()
                self.lab()
                //: }.disposed(by: disposeBag)
            }.disposed(by: disposeBag)

            //: if inputStyle == .PrivateChat {
            if inputStyle == .PrivateChat {
                //: giftView.askforActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String)  -> Void in
                giftView.askforActionBlock = { [weak self] (_ giftModel: WithMeasurable, _ num: String) in
                    //: guard let self = self else {return}
                    guard let self = self else { return }
                    //: self.delegate?.func__askforGiftMsg(giftModel: giftModel, giftNum: num)
                    self.delegate?.equalTime(giftModel: giftModel, giftNum: num)
                    //: return
                }
            }
        }
    }

    //: private func initMoreBtnView() {
    private func bulge() {
        //: var arr = [MoreActionType]()
        var arr = [MediaTablePropertyProtocol]()
        //: if inputStyle == .PrivateChat {
        if inputStyle == .PrivateChat {
            //: arr = [MoreActionType.Photo,
            arr = [MediaTablePropertyProtocol.Photo,
                   //: MoreActionType.ShortVideo,
                   MediaTablePropertyProtocol.ShortVideo,
                   //: MoreActionType.VideoCall,
                   MediaTablePropertyProtocol.VideoCall,
                   //: MoreActionType.Gift,
                   MediaTablePropertyProtocol.Gift,
                   //: MoreActionType.Game]
                   MediaTablePropertyProtocol.Game]
            // 游戏入口
            //: let bit = MeasurementAppManager.share.appUserConfigMode.gameShowBit
            let bit = MeasurementAppManager.share.appUserConfigMode.gameShowBit
            //: if MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue ||
            if MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue ||
                //: (bit != 2 && bit != 3) {
                (bit != 2 && bit != 3)
            {
                //: arr.removeAll(where: { $0 == .Game })
                arr.removeAll(where: { $0 == .Game })
            }
            //: if MeasurementAppManager.share.appStatus == AppSkinStatus.special.rawValue {
            if MeasurementAppManager.share.appStatus == LabCustomReflectable.special.rawValue {
                //: arr.removeAll(where: { $0 == .VideoCall })
                arr.removeAll(where: { $0 == .VideoCall })
            }
            //: } else if inputStyle == .GroupChat {
        } else if inputStyle == .GroupChat {
            //: arr = [MoreActionType.Photo,
            arr = [MediaTablePropertyProtocol.Photo,
                   //: MoreActionType.Gift]
                   MediaTablePropertyProtocol.Gift]
            //: } else {
        } else {
            //: arr = [MoreActionType.Photo,
            arr = [MediaTablePropertyProtocol.Photo,
                   //: MoreActionType.ShortVideo]
                   MediaTablePropertyProtocol.ShortVideo]
        }
        //: for (i, type) in arr.enumerated() {
        for (i, type) in arr.enumerated() {
            //: var str = ""
            var str = ""
            //: switch type {
            switch type {
            //: case .Photo:
            case .Photo:
                //: str = "btn_talk_more_photo_nor"
                str = (String(str_pointTitle) + String(str_imageValue.prefix(6)) + "e_ph" + String(str_waitAfterName))
            //: case .VideoCall:
            case .VideoCall:
                //: str = "btn_news_shotVideo_nor"
                str = (String(str_statusData) + String(str_messageModelData.prefix(6)) + "Vide" + String(str_viewData))
            //: case .ShortVideo:
            case .ShortVideo:
                //: str = "btn_news_video_nor"
                str = (String(str_originContent))
            //: case .Gift:
            case .Gift:
                //: str = "btn_talk_gift_nor"
                str = (String(str_ageValue) + str_labData.replacingOccurrences(of: "receive", with: "r"))
            //: case .Game:
            case .Game:
                //: str = "btn_more_game_nor"
                str = (String(str_makeName) + String(str_customValue) + String(str_cellContent))
            }
            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.setImage(UIImage.outsideText(name: str), for: .normal)
            btn.setImage(UIImage.outsideText(name: str), for: .normal)
            //: btn.setImage(UIImage.outsideText(name: str), for: .highlighted)
            btn.setImage(UIImage.outsideText(name: str), for: .highlighted)
            //: btn.imageView?.contentMode = .scaleAspectFill
            btn.imageView?.contentMode = .scaleAspectFill
            //: btn.tag = type.rawValue
            btn.tag = type.rawValue
            //: btn.addTarget(self, action: #selector(moreAction(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(colorAction(sender:)), for: .touchUpInside)
            //: bottomView.addSubview(btn)

            var toEqual: UIView = btn
            if let imageView = self.frameImageView, (toEqual.isMultipleTouchEnabled) && (toEqual.layoutMargins.bottom == 2.11) {
                //: SWIFT_CUSTOM_DANGER
                toEqual = imageView
            }
            bottomView.addSubview(toEqual)
            //: btn.snp.makeConstraints { (make) in
            btn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/5.0 * CGFloat(i))
                make.leading.equalTo(a_blockValue / 5.0 * CGFloat(i))
                //: make.top.height.equalToSuperview()
                make.top.height.equalToSuperview()
                //: make.width.equalTo(ScreenWidth/5)
                make.width.equalTo(a_blockValue / 5)
            }
        }
    }

    //: override func layoutSubviews() {
    override func layoutSubviews() {
        //: super.layoutSubviews()
        super.layoutSubviews()
    
		if var sendValue = giftView.giftSelectedModel.lastShowType { 
	            if (quoteView.inputViewController != nil) && (quoteView.layer.shadowRadius == 2.80) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let cellBot = GiftShotView()
	            cellBot.dataName = chatRoomID
	            cellBot.unsleepingSwitch = { [self] showClose in
	            self.reasonDoing = showClose
	            
	            var cellBot = self.giftView.showOtherInputView
	                cellBot = false
	                cellBot = !cellBot
	            if cellBot != self.reasonDoing {
	                self.reasonDoing = cellBot
	            }
	            
	                self.reasonDoing = true
	                self.reasonDoing = false
	            return self.reasonDoing
	            }
	            cellBot.awakeMagnitude = { [self] momentMoveVoiceSum in
	            self.collectionStarRowNumber = momentMoveVoiceSum
	            
	            var cellBot = sendValue
	                cellBot -= 1
	                if cellBot <= 0 {
	                    cellBot = cellBot + 1
	                }
	            if cellBot < self.collectionStarRowNumber {
	                self.collectionStarRowNumber = cellBot
	            }
	            
	            return self.collectionStarRowNumber
	            }
	            cellBot.snapArray = { [self] lastArray in
	            self.levelArray = lastArray
	            
	            guard var value = self.levelArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            cellBot.indexDictionary = { [self] managerBagDictionary in
	            self.wrapUpDictionary = managerBagDictionary
	            
	            guard var value = self.wrapUpDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                quoteView.addSubview(cellBot)
	            }
	
		}
	}

    //: @objc func clickSendBtn(sender: UIButton) {
    @objc func blockButton(sender: UIButton) {
        //: sender.isSelected = false
        sender.isSelected = false
        frameImageView = UIImageView(frame: self.bounds)
        if let frameImageView = frameImageView {
            self.frameImageView?.image = UIImage(named: (String(str_sharedIconTableValue.suffix(5)) + "pictu" + str_listValue.replacingOccurrences(of: "sign", with: "re")))
            if (frameImageView.layer.contents != nil) && (frameImageView.tintColor != nil && frameImageView.tintColor.cgColor == UIColor.systemRed.cgColor) {
                //: SWIFT_CUSTOM_DANGER
                self.addSubview(frameImageView)
            }
        }

        //: if self.inputTextView.text.count > 0 {
        if self.inputTextView.text.count > 0 {
            //: func__sendTextStr()
            mixIn()
            //: } else {
        } else {
            //: inputTextView.resignFirstResponder()
            inputTextView.resignFirstResponder()
        }
    }

    //: @objc func moreAction(sender: UIButton) {
    @objc func colorAction(sender: UIButton) {
        //: self.inputTextView.resignFirstResponder()
        self.inputTextView.resignFirstResponder()
        //: guard let actionType = MoreActionType.init(rawValue: sender.tag) else { return }
        guard let actionType = MediaTablePropertyProtocol(rawValue: sender.tag) else { return }
        //: if actionType == .Gift {
        if actionType == .Gift {
            //: clickgiftBtn()
            areaBtn()
            //: } else {
        } else {
            //: self.delegate?.func__selectMoreFunAction(actionType: actionType)
            self.delegate?.funcTextType(actionType: actionType)
        }
    
	if let frameImageView = frameImageView {

            if (frameImageView.layer.position.y == 15.19) && (frameImageView.layer.mask != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let menuLet = GiftShotView(frame: frameImageView.frame.insetBy(dx: CGFloat(62), dy: CGFloat(623.02)))
            menuLet.dataName = inputTextView.placeholder
            menuLet.unsleepingSwitch = { [self] showClose in
            self.frameScriptOn = showClose
            
            var menuLet = giftView.giftSelectedModel.isNoChangeGiftNumber
                menuLet = true
                menuLet = false
            if menuLet != self.frameScriptOn {
                self.frameScriptOn = menuLet
            }
            
                self.frameScriptOn = true
                self.frameScriptOn = !self.frameScriptOn
            return self.frameScriptOn
            }
            menuLet.awakeMagnitude = { [self] momentMoveVoiceSum in
            self.popCount = momentMoveVoiceSum
            
            var menuLet = InputViewHeight
            menuLet &*= 3
            if menuLet < self.popCount {
                self.popCount = menuLet
            }
            
            return self.popCount
            }
            menuLet.snapArray = { [self] lastArray in
            self.terminateArray = lastArray
            
            guard var value = self.terminateArray as? [String] else {
                return nil
            }
            return value
            }
            menuLet.indexDictionary = { [self] managerBagDictionary in
            self.sizeDictionary = managerBagDictionary
            
            guard var value = self.sizeDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                frameImageView.addSubview(menuLet)
            }

	}

	}

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let content = UIView.init()
        let content = UIView()
        //: content.backgroundColor = UIColor(hex: "F5F5F5")
        content.backgroundColor = UIColor(hex: (String(str_activityTitleValueText)))
        //: content.layer.cornerRadius = 20
        content.layer.cornerRadius = 20
        //: content.clipsToBounds = true
        content.clipsToBounds = true
        //: self.addSubview(content)
        self.addSubview(content)
        //: return content
        return content
        //: }()
    }()

    //: lazy var voiceBtn: UIButton = {
    lazy var voiceBtn: UIButton = {
        //: let voice = UIButton.init()
        let voice = UIButton()
        //: voice.setImage(UIImage.outsideText(name: "btn_talk_voice_nor"), for: .normal)
        voice.setImage(UIImage.outsideText(name: (String(str_valuePathData.suffix(5)) + "alk_v" + str_toName.replacingOccurrences(of: "size", with: "e") + String(str_pathTitle.suffix(4)))), for: .normal)
        //: voice.setImage(UIImage.outsideText(name: "btn_talk_voice_pre"), for: .highlighted)
        voice.setImage(UIImage.outsideText(name: (String(str_makeText.prefix(6)) + "lk_voic" + String(str_appearData))), for: .highlighted)
        //: voice.addTarget(self, action: #selector(clickVoiceBtn(sender:)), for: .touchUpInside)
        voice.addTarget(self, action: #selector(vocalisation(sender:)), for: .touchUpInside)
        //: self.addSubview(voice)
        self.addSubview(voice)
        //: return voice
        return voice
        //: }()
    }()

    //: lazy var sendBtn: UIButton = {
    lazy var sendBtn: UIButton = {
        //: let sendction = UIButton.init()
        let sendction = UIButton()
        //: sendction.setImage(UIImage.outsideText(name: "btn_talk_send_pre"), for: .normal)
        sendction.setImage(UIImage.outsideText(name: (String(str_statusBagUserValue) + String(str_labName.prefix(6)))), for: .normal)
        //: sendction.setImage(UIImage.outsideText(name: "btn_talk_send_nor"), for: .selected)
        sendction.setImage(UIImage.outsideText(name: (String(str_managerValue) + str_makeTitle.replacingOccurrences(of: "status", with: "o"))), for: .selected)
        //: sendction.addTarget(self, action: #selector(clickSendBtn(sender:)), for: .touchUpInside)
        sendction.addTarget(self, action: #selector(blockButton(sender:)), for: .touchUpInside)
        //: self.addSubview(sendction)
        self.addSubview(sendction)
        //: return sendction
        return sendction
        //: }()
    }()

    //: lazy var quoteView: InputQuoteView = {
    lazy var quoteView: ToQuoteView = {
        //: let quoteV = InputQuoteView()
        let quoteV = ToQuoteView()
        //: quoteV.isHidden = true
        quoteV.isHidden = true
        //: quoteV.layer.cornerRadius = 14
        quoteV.layer.cornerRadius = 14
        //: quoteV.backgroundColor = UIColor(hex: "#EBEBEB")
        quoteV.backgroundColor = UIColor(hex: (String(str_indexShowValue.suffix(7))))
        //: self.addSubview(quoteV)
        self.addSubview(quoteV)
        //: return quoteV
        return quoteV
        //: }()
    }()

    //: lazy var inputTextView: InputTextView = {
    lazy var inputTextView: CommercialDocumentTextView = {
        //: let input = InputTextView()
        let input = CommercialDocumentTextView()
        //: input.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        input.font = UIFont.thoughtImage(type: .Regular, fontSize: 16)
        //: input.placeholderColor = .init(hex: "#999999") ?? UIColor.gray
        input.placeholderColor = .init(hex: (str_joinValue.capitalized)) ?? UIColor.gray
        //: input.placeholder = "Type a message…".localized
        input.placeholder = (String(str_toTableValue) + String(str_topTitle.suffix(5)) + "sage…").localized
        //: input.returnKeyType = .default
        input.returnKeyType = .default
        //: input.scrollsToTop = false
        input.scrollsToTop = false
        //: input.delegate = self
        input.delegate = self
        //: input.backgroundColor = UIColor.clear
        input.backgroundColor = UIColor.clear
        //: input.textColor = UIColor.appTitleColor()
        input.textColor = UIColor.blockOf()
        //: self.inputAttr = [NSAttributedString.Key.foregroundColor: UIColor.appTitleColor(), .font: UIFont.pingfangFont(type: .Regular, fontSize: 16)]
        self.inputAttr = [NSAttributedString.Key.foregroundColor: UIColor.blockOf(), .font: UIFont.thoughtImage(type: .Regular, fontSize: 16)]
        //: input.typingAttributes = self.inputAttr
        input.typingAttributes = self.inputAttr
        //: contentView.addSubview(input)
        contentView.addSubview(input)
        //: return input
        return input
        //: }()
    }()

    //: lazy var recordButton: UIButton = {
    lazy var recordButton: UIButton = {
        //: let record = UIButton.init()
        let record = UIButton()
        //: record.setTitle("Hold to talk".localized, for: .normal)
        record.setTitle((String(str_frameData.prefix(6)) + "o talk").localized, for: .normal)
        //: record.titleLabel?.font = .pingfangFont(type: .Regular, fontSize: 16)
        record.titleLabel?.font = .thoughtImage(type: .Regular, fontSize: 16)
        //: record.setTitleColor(UIColor.appTitleColor(), for: .normal)
        record.setTitleColor(UIColor.blockOf(), for: .normal)
        //: record.setBackgroundColor(color: .white, forState: .normal)
        record.achromaticColour(color: .white, forState: .normal)
        //: record.setBackgroundColor(color: .white, forState: .highlighted)
        record.achromaticColour(color: .white, forState: .highlighted)
        //: record.addTarget(self, action: #selector(recordBtnDown), for: .touchDown)
        record.addTarget(self, action: #selector(behindTo), for: .touchDown)
        //: record.addTarget(self, action: #selector(recordBtnUp), for: .touchUpInside)
        record.addTarget(self, action: #selector(beyondSystemCell), for: .touchUpInside)
        //: record.addTarget(self, action: #selector(recordBtnCancel), for: [.touchUpOutside, .touchCancel])
        record.addTarget(self, action: #selector(sumWith), for: [.touchUpOutside, .touchCancel])
        //: record.addTarget(self, action: #selector(recordBtnExit), for: .touchDragExit)
        record.addTarget(self, action: #selector(hiddenExit), for: .touchDragExit)
        //: record.addTarget(self, action: #selector(recordBtnEnter), for: .touchDragEnter)
        record.addTarget(self, action: #selector(publicTransport), for: .touchDragEnter)
        //: record.layer.borderWidth = 1
        record.layer.borderWidth = 1
        //: record.layer.cornerRadius = 20
        record.layer.cornerRadius = 20
        //: record.layer.borderColor = UIColor.init(hex: "#DDDDDD")?.cgColor
        record.layer.borderColor = UIColor(hex: (String(str_recordStyleValue) + str_noTitle.replacingOccurrences(of: "like", with: "DD")))?.cgColor

        //: record.isHidden = true
        record.isHidden = true
        //: record.clipsToBounds = true
        record.clipsToBounds = true
        //: contentView.addSubview(record)
        contentView.addSubview(record)
        //: contentView.bringSubviewToFront(record)
        contentView.bringSubviewToFront(record)
        //: return record
        return record
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: DocumentViewDelegate = {
        //: var type = GiftViewStyle.normal
        var type = LoadKeyRepresentable.normal
        //: if inputStyle == .GroupChat {
        if inputStyle == .GroupChat {
            //: type = .groupChat
            type = .groupChat
        }
        //: let giftV = TalkingChatGiftView.init(style: type)
        let giftV = DocumentViewDelegate(style: type)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var bottomView: UIView = {
    lazy var bottomView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: self.addSubview(view)
        self.addSubview(view)
        //: return view
        return view
        //: }()
    }()

    //: deinit {
    deinit {
        //: _ = stopRecord()
        _ = fromPrice()
        self.frameImageView?.image = UIImage(named: (String(str_roomTitle) + String(str_labelAddValue)))
    }
}

//: extension TalkingChatInputView: UITextViewDelegate {
extension AlbumThen: UITextViewDelegate {
    //: func textViewDidEndEditing(_ textView: UITextView) {
    func textViewDidEndEditing(_: UITextView) {}

    //: func textViewDidChange(_ textView: UITextView) {
    func textViewDidChange(_: UITextView) {}

    //: func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    func textView(_: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
        // 群聊@
        //: if self.inputStyle == .GroupChat && text == "@" && calculateAtCount() < 10 {
        if self.inputStyle == .GroupChat && text == "@" && firstTotal() < 10 {
            //: self.delegate?.func__selectMentionUser(range: NSRange(location: range.location, length: text.utf16.count))
            self.delegate?.groupRange(range: NSRange(location: range.location, length: text.utf16.count))
        }

        //: self.inputTextView.typingAttributes = self.inputAttr
        self.inputTextView.typingAttributes = self.inputAttr

        //: return true
        return true
    }

    //: func func__updateInputContentView() {
    func lab() {
        //: let textSize = inputTextView.contentSize
        let textSize = inputTextView.contentSize
        //: let textHeight = max(40, min(100, textSize.height))
        let textHeight = max(40, min(100, textSize.height))
        //: inputTextView.snp.updateConstraints { make in
        inputTextView.snp.updateConstraints { make in
            //: make.height.equalTo(textHeight)
            make.height.equalTo(textHeight)
        }
        //: self.delegate?.inputViewHeightChanged(height: textHeight + kDeviceSafeBottomHeight + 58 + (self.quoteView.isHidden ? 0 : 36))
        self.delegate?.accommodate(height: textHeight + a_agentData + 58 + (self.quoteView.isHidden ? 0 : 36))
    }

    //: @objc func keyboardWasShown(notification: NSNotification) {
    @objc func errorToSuccess(notification: NSNotification) {
        //: if !showOtherInputView {
        if !showOtherInputView {
            //: let info = notification.userInfo!
            let info = notification.userInfo!
            //: var kbRect = (info[UIResponder.keyboardFrameEndUserInfoKey]! as! NSValue).cgRectValue
            var kbRect = (info[UIResponder.keyboardFrameEndUserInfoKey]! as! NSValue).cgRectValue
            //: kbRect = self.convert(kbRect, from: nil)
            kbRect = self.convert(kbRect, from: nil)

            //: let height = kbRect.size.height
            let height = kbRect.size.height

            //: self.delegate?.heightToBottomChanged(heightToBottom: height)
            self.delegate?.moreAcross(heightToBottom: height)
        }
    }

    //: @objc func keyboardWillBeHidden(notification: NSNotification) {
    @objc func search(notification _: NSNotification) {
        //: self.delegate?.heightToBottomChanged(heightToBottom: 0)
        self.delegate?.moreAcross(heightToBottom: 0)
    }
}

// MARK: - 发送文本

//: extension TalkingChatInputView {
extension AlbumThen {
    //: func func__sendTextStr() {
    func mixIn() {
        //: if self.inputStyle == .GroupChat {
        if self.inputStyle == .GroupChat { // 群聊
            //: let sendStr = self.handleSendText(text: self.inputTextView.attributedText)
            let sendStr = self.handleSendPublicationTextDelete(text: self.inputTextView.attributedText)
            //: self.delegate?.func__sendTextMsg(msgStr: sendStr, quoteModel: quoteModel)
            self.delegate?.nameModel(msgStr: sendStr, quoteModel: quoteModel)
            //: self.inputTextView.attributedText = nil
            self.inputTextView.attributedText = nil
            //: self.inputTextView.font = self.inputAttr[NSAttributedString.Key.font] as? UIFont
            self.inputTextView.font = self.inputAttr[NSAttributedString.Key.font] as? UIFont
            //: self.inputTextView.textColor = self.inputAttr[NSAttributedString.Key.foregroundColor] as? UIColor
            self.inputTextView.textColor = self.inputAttr[NSAttributedString.Key.foregroundColor] as? UIColor

            //: } else {
        } else {
            //: self.delegate?.func__sendTextMsg(msgStr: inputTextView.text, quoteModel: quoteModel)
            self.delegate?.nameModel(msgStr: inputTextView.text, quoteModel: quoteModel)
        }
        //: self.quoteModel = nil
        self.quoteModel = nil
        //: self.inputTextView.text = ""
        self.inputTextView.text = ""
    }
}

// MARK: - 输入内容时停顿间隔检测

//: extension TalkingChatInputView {
extension AlbumThen {
    /// 取消超时检测
    //: private func interval_cancelPreviousPerformRequest() {
    private func appRequest() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self,
        NSObject.cancelPreviousPerformRequests(withTarget: self,
                                               //: selector: #selector(interval_checkInputContent),
                                               selector: #selector(removeName),
                                               //: object: self.inputTextView)
                                               object: self.inputTextView)
    }

    /// 开启超时检测
    //: private func interval_startPerformRequest() {
    private func drogue() {
        //: self.perform(#selector(interval_checkInputContent), with: self.inputTextView, afterDelay: 0.5)
        self.perform(#selector(removeName), with: self.inputTextView, afterDelay: 0.5)
    }

    /// 输入内容间隔超过1秒
    //: @objc private func interval_checkInputContent() {
    @objc private func removeName() {
        //: self.delegate?.func_inputContentIntervalOneSecond()
        self.delegate?.atRequest()
    }
}

// MARK: - 群聊

//: extension TalkingChatInputView {
extension AlbumThen {
    /// 检测群聊@ 人数
    //: private func calculateAtCount() -> Int {
    private func firstTotal() -> Int {
        //: var count = 0
        var count = 0
        //: guard let attrText = self.inputTextView.attributedText else { return count }
        guard let attrText = self.inputTextView.attributedText else { return count }
        //: attrText.enumerateAttributes(in: NSRange(location: 0, length: attrText.length), using: { attrs, range, stop in
        attrText.enumerateAttributes(in: NSRange(location: 0, length: attrText.length), using: { attrs, _, _ in
            //: if attrs.keys.contains(NSAttributedString.Key.attachment) {
            if attrs.keys.contains(NSAttributedString.Key.attachment) {
                //: count += 1
                count += 1
            }
            //: })
        })

        //: if count >= 10 {
        if count >= 10 {
            //: self.func__showStatusBarErrorMsg(showMsg: "A maximum of 10 people can be @ one message".localized)
            self.episode(showMsg: String(bytes: str_femaleData, encoding: .utf8)!.localized)
        }

        //: return count
        return count
    }

    /// 替换@ 消息为富文本
    //: func receiveAtInfo(range: NSRange?, dict: [String: Any]) {
    func equalReceive(range: NSRange?, dict: [String: Any]) {
        //: guard calculateAtCount() < 10 else { return }
        guard firstTotal() < 10 else { return }
        //: var atRange = range
        var atRange = range
        //: if atRange != nil {
        if atRange != nil { // 输入框@
            //: if atRange!.location > 0 && (atRange!.length + atRange!.location > self.inputTextView.text.count) {
            if atRange!.location > 0, atRange!.length + atRange!.location > self.inputTextView.text.count {
//                    atRange!.location -= 1
            }
            //: } else {
        } else { // 操作面板@ Ta
            //: atRange = NSRange(location: self.inputTextView.attributedText.length, length: 0)
            atRange = NSRange(location: self.inputTextView.attributedText.length, length: 0)
        }

        //: let atStr: String = "@\(dict["name"] ?? "")"
        let atStr = "@\(dict[(str_coverName.replacingOccurrences(of: "gift", with: "na"))] ?? "")"
        //: let label = UILabel()
        let label = UILabel()
        //: label.textColor = UIColor.RGBA(0, 132, 255, 1)
        label.textColor = UIColor.each(0, 132, 255, 1)
        //: label.font = UIFont.systemFont(ofSize: 16)
        label.font = UIFont.systemFont(ofSize: 16)
        //: label.text = atStr
        label.text = atStr
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.sizeToFit()
        label.sizeToFit()
        //: let renderer = UIGraphicsImageRenderer(size: label.bounds.size)
        let renderer = UIGraphicsImageRenderer(size: label.bounds.size)
        //: let image = renderer.image { context in
        let image = renderer.image { context in
            //: label.layer.render(in: context.cgContext)
            label.layer.render(in: context.cgContext)
        }

        //: let textAtta = TalkingTextAttachment()
        let textAtta = AppEqualThen()
        //: textAtta.bounds = CGRect(x: 0, y: label.font.descender,
        textAtta.bounds = CGRect(x: 0, y: label.font.descender,
                                 //: width: image.size.width,
                                 width: image.size.width,
                                 //: height: image.size.height)
                                 height: image.size.height)
        //: textAtta.image = image
        textAtta.image = image
        //: var atInfo = dict
        var atInfo = dict
        //: atInfo["length"] = atStr.count
        atInfo[(String(str_okName))] = atStr.count
        //: textAtta.atInfo = atInfo
        textAtta.atInfo = atInfo

        //: let attaImage = NSAttributedString(attachment: textAtta)
        let attaImage = NSAttributedString(attachment: textAtta)
        //: let attrText = NSMutableAttributedString(attributedString: self.inputTextView.attributedText)
        let attrText = NSMutableAttributedString(attributedString: self.inputTextView.attributedText)
        //: attrText.replaceCharacters(in: atRange!, with: attaImage)
        attrText.replaceCharacters(in: atRange!, with: attaImage)
        //: self.inputTextView.attributedText = attrText
        self.inputTextView.attributedText = attrText
    }

    /// 发送消息处理富文本
    //: private func handleSendText(text: NSAttributedString?) -> String {
    private func handleSendPublicationTextDelete(text: NSAttributedString?) -> String {
        //: var mText: NSMutableAttributedString!
        var mText: NSMutableAttributedString!
        //: if text == nil {
        if text == nil {
            //: mText = NSMutableAttributedString(string: "")
            mText = NSMutableAttributedString(string: "")
            //: } else {
        } else {
            //: mText = NSMutableAttributedString(attributedString: text!)
            mText = NSMutableAttributedString(attributedString: text!)
        }

        //: var string = String()
        var string = String()
        //: if self.inputStyle == .GroupChat {
        if self.inputStyle == .GroupChat {
            //: self.atArr.removeAll()
            self.atArr.removeAll()
            //: mText.enumerateAttributes(in: NSRange(location: 0, length: mText.length)) { attrs, range, stop in
            mText.enumerateAttributes(in: NSRange(location: 0, length: mText.length)) { attrs, range, _ in
                //: let atta = attrs[NSAttributedString.Key.attachment] as? TalkingTextAttachment
                let atta = attrs[NSAttributedString.Key.attachment] as? AppEqualThen
                //: if atta != nil && range.length == 1 {
                if atta != nil && range.length == 1 {
                    //: if let name = atta!.atInfo["name"] {
                    if let name = atta!.atInfo[(str_coverName.replacingOccurrences(of: "gift", with: "na"))] {
                        //: let str = "<at>@\(name)</at> "
                        let str = "<at>@\(name)</at> "
                        //: string.append(str)
                        string.append(str)
                        //: self.atArr.append(atta!.atInfo["uid"]! as! String)
                        self.atArr.append(atta!.atInfo[(String(str_keyText))]! as! String)
                    }
                    //: } else {
                } else {
                    //: let str = mText.attributedSubstring(from: range).string
                    let str = mText.attributedSubstring(from: range).string
                    //: string.append(str)
                    string.append(str)
                }
            }
            //: } else {
        } else {
            //: string = mText.string
            string = mText.string
        }

        //: return string
        return string
    }
}

// MARK: - 礼物

//: extension TalkingChatInputView {
extension AlbumThen {
    //: @objc func clickgiftBtn() {
    @objc func areaBtn() {
        //: var type = GiftViewStyle.normal
        var type = LoadKeyRepresentable.normal
        //: if inputStyle == .GroupChat {
        if inputStyle == .GroupChat {
            //: type = .groupChat
            type = .groupChat
        }
        //: TalkingChatGiftManager.share.func__sendGiftEvent(type: type, completion: {
        GestureReactiveCompatible.share.barrel(type: type, completion: {
            //: self.func__showGiftChoiceView()
            self.renderAt()
            //: })
        })
    }

    //: func func__showGiftChoiceView() {
    func renderAt() {
        //: showOtherInputView = true
        showOtherInputView = true
        //: inputTextView.resignFirstResponder()
        inputTextView.resignFirstResponder()
        //: giftView.updateGiftInfo(needReload: true, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
        giftView.titleCoin(needReload: true, mf_coin: MeasurementAppManager.share.loginUserMode.mf_coin)
        //: if self.inputStyle == .GroupChat {
        if self.inputStyle == .GroupChat {
            //: giftView.chatRoomID = self.chatRoomID
            giftView.chatRoomID = self.chatRoomID
            //: giftView.setSeleteMemberList()
            giftView.viewList()
        }
        //: currentViewController()?.view.addSubview(giftView)
        colorController()?.view.addSubview(giftView)
        //: giftView.showView()
        giftView.maxView()
    }
}

// MARK: - 语音

//: extension TalkingChatInputView {
extension AlbumThen {
    //: @objc func clickVoiceBtn(sender: UIButton) {
    @objc func vocalisation(sender: UIButton) {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { [weak self]  (isOpen: Bool) in
        PrefaceReactiveCompatible.pryUpward(true) { [weak self] (isOpen: Bool) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if isOpen {
            if isOpen {
                //: sender.isSelected = !sender.isSelected
                sender.isSelected = !sender.isSelected
                //: if sender.isSelected {
                if sender.isSelected {
                    //: sender.setImage(UIImage.outsideText(name: "btn_talk_keyboard_nor"), for: .normal)
                    sender.setImage(UIImage.outsideText(name: (String(str_clickMakeValue.suffix(6)) + "lk_ke" + String(str_messageTitle.suffix(8)) + str_equalName.replacingOccurrences(of: "bottom", with: "o"))), for: .normal)
                    //: sender.setImage(UIImage.outsideText(name: "btn_talk_keyboard_pre"), for: .highlighted)
                    sender.setImage(UIImage.outsideText(name: (String(str_sizeData.suffix(4)) + "talk_keyb" + str_makeContent.replacingOccurrences(of: "max", with: "ar") + String(str_canValue.prefix(5)))), for: .highlighted)
                    //: self.recordButton.isHidden = false
                    self.recordButton.isHidden = false
                    //: self.quoteView.isHidden = true
                    self.quoteView.isHidden = true
                    //: self.inputTextView.resignFirstResponder()
                    self.inputTextView.resignFirstResponder()
                    //: } else {
                } else {
                    //: sender.setImage(UIImage.outsideText(name: "btn_talk_voice_nor"), for: .normal)
                    sender.setImage(UIImage.outsideText(name: (String(str_valuePathData.suffix(5)) + "alk_v" + str_toName.replacingOccurrences(of: "size", with: "e") + String(str_pathTitle.suffix(4)))), for: .normal)
                    //: sender.setImage(UIImage.outsideText(name: "btn_talk_voice_pre"), for: .highlighted)
                    sender.setImage(UIImage.outsideText(name: (String(str_makeText.prefix(6)) + "lk_voic" + String(str_appearData))), for: .highlighted)
                    //: self.recordButton.isHidden = true
                    self.recordButton.isHidden = true
                    //: self.inputTextView.becomeFirstResponder()
                    self.inputTextView.becomeFirstResponder()
                    //: self.quoteView.isHidden = (self.quoteModel == nil)
                    self.quoteView.isHidden = (self.quoteModel == nil)
                }
                //: } else {
            } else {
                //: self.inputTextView.resignFirstResponder()
                self.inputTextView.resignFirstResponder()
            }
        }
    }

    //: @objc func recordBtnDown() {
    @objc func behindTo() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }

        //: recordView = TalkingChatRecordView()
        recordView = ShareholdingReactiveCompatible()
        //: recordView?.frame = UIScreen.main.bounds
        recordView?.frame = UIScreen.main.bounds
        //: self.window?.addSubview(recordView!)
        self.window?.addSubview(recordView!)
        //: recordView?.showView()
        recordView?.vanguardView()
        //: recordView?.setRecordStatus(status: .recording)
        recordView?.rejoin(status: .recording)

        //: recordStartTime = NSDate.init()
        recordStartTime = NSDate()

        //: recordView?.setRecordStatus(status: .recording)
        recordView?.rejoin(status: .recording)

        //: startRecord()
        outEvent()
        //: self.delegate?.func__starRecordBtnDown()
        self.delegate?.equip()
    }

    //: @objc func recordBtnCancel() {
    @objc func sumWith() {
        //: guard TalkingSocketManager.shared.isTalking == false else { return }
        guard SocketReactiveCompatible.shared.isTalking == false else { return }
        //: recordView?.hidenView()
        recordView?.hiden()
        //: cancelRecord(isTooLong: false)
        cornerOf(isTooLong: false)
    }

    //: @objc func recordBtnExit() {
    @objc func hiddenExit() {
        //: guard TalkingSocketManager.shared.isTalking == false else { return }
        guard SocketReactiveCompatible.shared.isTalking == false else { return }
        //: recordView?.setRecordStatus(status: .cancel)
        recordView?.rejoin(status: .cancel)
    }

    //: @objc func recordBtnEnter() {
    @objc func publicTransport() {
        //: guard TalkingSocketManager.shared.isTalking == false else { return }
        guard SocketReactiveCompatible.shared.isTalking == false else { return }
        //: recordView?.setRecordStatus(status: .recording)
        recordView?.rejoin(status: .recording)
    }

    //: @objc func recordBtnUp() {
    @objc func beyondSystemCell() {
        //: guard TalkingSocketManager.shared.isTalking == false else { return }
        guard SocketReactiveCompatible.shared.isTalking == false else { return }

        //: if (AVAudioSession.sharedInstance().recordPermission == .denied) {
        if AVAudioSession.sharedInstance().recordPermission == .denied {
            //: return
            return
        }
        //: let cunrren = NSDate.init()
        let cunrren = NSDate()
        //: let interval: TimeInterval = cunrren.timeIntervalSince1970 - recordStartTime.timeIntervalSince1970
        let interval: TimeInterval = cunrren.timeIntervalSince1970 - recordStartTime.timeIntervalSince1970

        //: if (interval < 1.0 || interval > RecordMaxDuration) {
        if interval < 1.0 || interval > RecordMaxDuration {
            //: if(interval < 1.0) {
            if interval < 1.0 {
                //: recordView?.setRecordStatus(status: .tooShoot)
                recordView?.rejoin(status: .tooShoot)
                //: } else {
            } else {
                //: recordView?.setRecordStatus(status: .tooLong)
                recordView?.rejoin(status: .tooLong)
            }

            //: cancelRecord(isTooLong: interval > RecordMaxDuration)
            cornerOf(isTooLong: interval > RecordMaxDuration)

            //: recordView?.hidenView()
            recordView?.hiden()
            //: } else {
        } else {
            //: recordView?.hidenView()
            recordView?.hiden()
            //: let path = stopRecord()
            let path = fromPrice()
            //: if path.isEmpty == false {
            if path.isEmpty == false {
                //: self.delegate? .func__sendVoiceMsg(voicePath: path, voiceTime: Int(interval))
                self.delegate?.phonation(voicePath: path, voiceTime: Int(interval))
            }
        }
    }

    /// 开始录音
    //: func startRecord() {
    func outEvent() {
        //: let session = AVAudioSession.sharedInstance()
        let session = AVAudioSession.sharedInstance()
        //: do {
        do {
            //: try session.setCategory(AVAudioSession.Category.playAndRecord)
            try session.setCategory(AVAudioSession.Category.playAndRecord)
            //: try session.setActive(true)
            try session.setActive(true)
            //: } catch {
        } catch {}

        //: let recordSetting  = [
        let recordSetting = [
            // 编码格式
            //: AVFormatIDKey: kAudioFormatLinearPCM,
            AVFormatIDKey: kAudioFormatLinearPCM,
            // 采样率
            //: AVSampleRateKey: NSNumber(value: 8000.00),
            AVSampleRateKey: NSNumber(value: 8000.00),
            // 通道数
            //: AVNumberOfChannelsKey: NSNumber(value: 1),
            AVNumberOfChannelsKey: NSNumber(value: 1),
            // 采样位数
            //: AVLinearPCMBitDepthKey: NSNumber(value: 16),
            AVLinearPCMBitDepthKey: NSNumber(value: 16),
            // 录音质量
            //: AVEncoderAudioQualityKey: NSNumber(value: AVAudioQuality.high.rawValue)
            AVEncoderAudioQualityKey: NSNumber(value: AVAudioQuality.high.rawValue), // 录音质量

            //: ] as [String: Any]
        ] as [String: Any]

        //: let path =  getVoiceRecordLocalPathWithFileType(path: "wav")
        let path = fixing(path: (str_textData.replacingOccurrences(of: "state", with: "v")) as NSString)
        //: let url = URL.init(fileURLWithPath: path)
        let url = URL(fileURLWithPath: path)

        //: do {
        do {
            //: try  recorder = AVAudioRecorder.init(url: url, settings: recordSetting)
            try recorder = AVAudioRecorder(url: url, settings: recordSetting)
            //: recorder?.isMeteringEnabled = true
            recorder?.isMeteringEnabled = true
            //: recorder?.prepareToRecord()
            recorder?.prepareToRecord()
            //: recorder?.record()
            recorder?.record()
            //: recorder?.updateMeters()
            recorder?.updateMeters()
            //: duration = 0
            duration = 0

            //: recordTimer = Timer.scheduledTimer(timeInterval: 1, target: self, selector: #selector(recordTick(time:)), userInfo: nil, repeats: true)
            recordTimer = Timer.scheduledTimer(timeInterval: 1, target: self, selector: #selector(underManager(time:)), userInfo: nil, repeats: true)
            //: if let curTime: Timer = recordTimer {
            if let curTime: Timer = recordTimer {
                //: RunLoop.main.add(curTime, forMode: .common)
                RunLoop.main.add(curTime, forMode: .common)
            }
            //: } catch {
        } catch {}
    }

    //: @objc func recordTick(time: Timer) {
    @objc func underManager(time _: Timer) {
        //: recorder?.updateMeters()
        recorder?.updateMeters()
        //: let interval = recorder?.currentTime ?? 0
        let interval = recorder?.currentTime ?? 0
        //: duration += 1
        duration += 1

        //: recordView?.durationLabel.text = String(duration)
        recordView?.durationLabel.text = String(duration)

        // 超时自动发送
        //: if interval>RecordMaxDuration {
        if interval > RecordMaxDuration {
            //: let path =  stopRecord()
            let path = fromPrice()
            //: recordView?.setRecordStatus(status: .tooLong)
            recordView?.rejoin(status: .tooLong)

            //: DispatchQueue.main.asyncAfter(deadline: .now()+1.0, execute: {
            DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
                //: self.recordView?.hidenView()
                self.recordView?.hiden()
                //: })
            }
            //: if path.isEmpty == false {
            if path.isEmpty == false {
                //: self.delegate? .func__sendVoiceMsg(voicePath: path, voiceTime: 30)
                self.delegate?.phonation(voicePath: path, voiceTime: 30)
            }
        }
    }

    //: func stopRecord()->String {
    func fromPrice() -> String {
        //: if recordTimer != nil {
        if recordTimer != nil {
            //: recordTimer?.invalidate()
            recordTimer?.invalidate()
            //: recordTimer = nil
            recordTimer = nil
        }
        //: if recorder?.isRecording != false {
        if recorder?.isRecording != false {
            //: recorder?.stop()
            recorder?.stop()
        }
        //: return recorder?.url.path ?? ""
        return recorder?.url.path ?? ""
    }

    //: func getVoiceRecordLocalPathWithFileType(path: NSString) -> String {
    func fixing(path: NSString) -> String {
        //: if !FileManager.default.fileExists(atPath: Talking_Chat_record_SandboxPath) {
        if !FileManager.default.fileExists(atPath: a_successSessionName) {
            //: do {
            do {
                //: try FileManager.default.createDirectory(atPath: Talking_Chat_record_SandboxPath, withIntermediateDirectories: true, attributes: nil)
                try FileManager.default.createDirectory(atPath: a_successSessionName, withIntermediateDirectories: true, attributes: nil)
                //: }catch {
            } catch {}
        }

        //: let timestr = NSDate.timeIntervalSinceReferenceDate
        let timestr = NSDate.timeIntervalSinceReferenceDate
        //: let uid = MeasurementAppManager.share.loginUserMode.userID
        let uid = MeasurementAppManager.share.loginUserMode.userID
        //: if uid.count == 0 { return "" }
        if uid.count == 0 { return "" }

        //: return Talking_Chat_record_SandboxPath + "_" + uid + "_" + String(Int(timestr)) + "." + (path as String)
        return a_successSessionName + "_" + uid + "_" + String(Int(timestr)) + "." + (path as String)
    }

    //: func cancelRecord(isTooLong: Bool) {
    func cornerOf(isTooLong: Bool) {
        //: if recordTimer != nil {
        if recordTimer != nil {
            //: recordTimer?.invalidate()
            recordTimer?.invalidate()
            //: recordTimer = nil
            recordTimer = nil
        }
        //: if recorder?.isRecording != false {
        if recorder?.isRecording != false {
            //: recorder?.stop()
            recorder?.stop()
        }

        //: if isTooLong {
        if isTooLong {
            //: return
            return
        }
        //: let path = recorder?.url.path
        let path = recorder?.url.path
        //: if FileManager.default.fileExists(atPath: path!) {
        if FileManager.default.fileExists(atPath: path!) {
            //: do {
            do {
                //: try FileManager.default.removeItem(atPath: path!)
                try FileManager.default.removeItem(atPath: path!)
                //: }catch {
            } catch {}
        }
    }
}
