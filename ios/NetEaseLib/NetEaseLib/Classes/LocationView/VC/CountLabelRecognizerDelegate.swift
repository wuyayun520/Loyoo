
//: Declare String Begin

/*: "visualisation_img" :*/
fileprivate let str_lessData:String = "visscale"
fileprivate let str_dataTitle:String = "TION"

/*: "nav_back_black_nor" :*/
fileprivate let str_messageValue:[Character] = ["n","a","v","_","b","a","c","k","_"]
fileprivate let str_centerLabContent:String = "blaguide"

/*: "Email Address" :*/
fileprivate let str_headData:String = "result text each model timingEmail "
fileprivate let str_sourceName:String = "eincomeincome"

/*: "F4F6FA" :*/
fileprivate let str_unknownTitle:String = "thoughtF6F"
fileprivate let str_makeName:String = "object"

/*: "Add an email address" :*/
fileprivate let str_roomCurrentTitle:String = "Add anroom let party"
fileprivate let str_imageValue:String = "il addresstop var layer container"
fileprivate let str_positionName:String = "inside"

/*: "BBBBBB" :*/
fileprivate let str_clickAgeContent:[Character] = ["B","B","B","B","B"]
fileprivate let str_tempRawTitle:String = "b"

/*: "Only you will see your email on your profile." :*/
fileprivate let str_makeValue:[UInt8] = [0x4f,0x6e,0x6c,0x79,0x20,0x79,0x6f,0x75,0x20,0x77,0x69,0x6c,0x6c,0x20,0x73,0x65,0x65,0x20,0x79,0x6f,0x75,0x72,0x20,0x65,0x6d,0x61,0x69,0x6c,0x20,0x6f,0x6e,0x20,0x79,0x6f,0x75,0x72,0x20,0x70,0x72,0x6f,0x66,0x69,0x6c,0x65,0x2e]

/*: "Send verification email" :*/
fileprivate let str_equalName:String = "Send view attach"
fileprivate let str_atValue:String = "ficalla"
fileprivate let str_blockContent:String = "emwhitel"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CountLabelRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/1.
//

//: import UIKit
import UIKit

//: class TalkingLoginBindEmailVC: TalkingBaseViewController {
class CountLabelRecognizerDelegate: PropertyViewController {
	var videoCount: Int = 63
	var sourceDictionary: [AnyHashable: String] = [:]
	var effectQuantity: Int = 48
	var callDictionary: [AnyHashable: String] = [:]

    var magnitudeersectionImageView: UIImageView?

    // 是否展示返回按钮
    //: var isBack = true
    var isBack = true
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: false)
            labUp(isOpen: false)
        }
    
	if let magnitudeersectionImageView = magnitudeersectionImageView {

            if (magnitudeersectionImageView.clipsToBounds) && (magnitudeersectionImageView.layer.contains(CGPoint(x: CGFloat(55), y: CGFloat(98)))) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let takeAwayLet = StandView()

            
            
            takeAwayLet.lastQuantity = { [self] emptyQuantity in
            self.videoCount = emptyQuantity
            
            return self.videoCount
            }
            takeAwayLet.pathFrameAcceptDictionary = { [self] abdicateDictionary in
            self.sourceDictionary = abdicateDictionary
            
            guard var value = self.sourceDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                magnitudeersectionImageView.addSubview(takeAwayLet)
            }

	}

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: true)
            labUp(isOpen: true)
        }
    
	if let magnitudeersectionImageView = magnitudeersectionImageView {

            if (magnitudeersectionImageView.clipsToBounds) && (magnitudeersectionImageView.layer.contains(CGPoint(x: CGFloat(55), y: CGFloat(98)))) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let takeAwayLet = StandView()

            
            
            takeAwayLet.lastQuantity = { [self] emptyQuantity in
            self.effectQuantity = emptyQuantity
            
            return self.effectQuantity
            }
            takeAwayLet.pathFrameAcceptDictionary = { [self] abdicateDictionary in
            self.callDictionary = abdicateDictionary
            
            guard var value = self.callDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                magnitudeersectionImageView.addSubview(takeAwayLet)
            }

	}

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: self.setupSubviews()
        self.generatePhone()
        //: self.setupSubViewsConstraint()
        self.outspoken()
        //: self.bindInteraction()
        self.image()
        //: addTapGestureRecognizer()
        moveRecognizer()

        magnitudeersectionImageView = UIImageView(frame: self.view.bounds.standardized)
        if let magnitudeersectionImageView = magnitudeersectionImageView {
            self.magnitudeersectionImageView?.image = UIImage(named: (str_lessData.replacingOccurrences(of: "scale", with: "u") + "alisa" + str_dataTitle.lowercased() + "_img"))
            if (magnitudeersectionImageView.restorationIdentifier != nil) && (magnitudeersectionImageView.convert(magnitudeersectionImageView.frame.intersection(CGRect(x: CGFloat(0), y: CGFloat(magnitudeersectionImageView.isExclusiveTouch ? 3 : 4), width: CGFloat(0), height: CGFloat(magnitudeersectionImageView.semanticContentAttribute.rawValue))), from: magnitudeersectionImageView.superview).size.height == 50.10) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(magnitudeersectionImageView)
            }
        }
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy Load

    //: private lazy var backBtn: UIButton = {
    private lazy var backBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.outsideText(name: "nav_back_black_nor"), for: .normal)
        btn.setImage(UIImage.outsideText(name: (String(str_messageValue) + str_centerLabContent.replacingOccurrences(of: "guide", with: "c") + "k_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(backBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(close), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var titleLabel: UILabel = {
    private lazy var titleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17.0)
        label.font = .thoughtImage(type: .Semibold, fontSize: 17.0)
        //: label.textColor = .appTitleColor()
        label.textColor = .blockOf()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Email Address".localized
        label.text = (String(str_headData.suffix(6)) + "Addr" + str_sourceName.replacingOccurrences(of: "income", with: "s")).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var emailInputView: UITextField = {
    private lazy var emailInputView: UITextField = {
        //: let textField = UITextField()
        let textField = UITextField()
        //: textField.backgroundColor = UIColor(hex: "F4F6FA")
        textField.backgroundColor = UIColor(hex: (str_unknownTitle.replacingOccurrences(of: "thought", with: "F4") + str_makeName.replacingOccurrences(of: "object", with: "A")))
        //: textField.font = .pingfangFont(type: .Medium, fontSize: 17)
        textField.font = .thoughtImage(type: .Medium, fontSize: 17)
        //: textField.textColor = .appTitleColor()
        textField.textColor = .blockOf()
        //: textField.layer.cornerRadius = 25
        textField.layer.cornerRadius = 25
        //: textField.textAlignment = .center
        textField.textAlignment = .center
        //: textField.clipsToBounds = true
        textField.clipsToBounds = true
        //: textField.attributedPlaceholder = NSAttributedString(string: "Add an email address".localized, attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: "BBBBBB")!])
        textField.attributedPlaceholder = NSAttributedString(string: (String(str_roomCurrentTitle.prefix(6)) + " ema" + String(str_imageValue.prefix(9)) + str_positionName.replacingOccurrences(of: "inside", with: "s")).localized, attributes: [NSAttributedString.Key.font: UIFont.thoughtImage(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: (String(str_clickAgeContent) + str_tempRawTitle.uppercased()))!])
        //: textField.keyboardType = .default
        textField.keyboardType = .default
        //: textField.returnKeyType = .done
        textField.returnKeyType = .done
        //: textField.clearButtonMode = .whileEditing
        textField.clearButtonMode = .whileEditing
        //: return textField
        return textField
        //: }()
    }()

    //: private lazy var emailTipLabel: UILabel = {
    private lazy var emailTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15.0)
        label.font = .thoughtImage(type: .Regular, fontSize: 15.0)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .observerShared()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Only you will see your email on your profile.".localized
        label.text = String(bytes: str_makeValue, encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.thoughtImage(type: .Regular, fontSize: 18)
        //: btn.setTitle("Send verification email".localized, for: .normal)
        btn.setTitle((String(str_equalName.prefix(5)) + "veri" + str_atValue.replacingOccurrences(of: "call", with: "c") + "tion " + str_blockContent.replacingOccurrences(of: "white", with: "ai")).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.stopAcross(colors: UIColor.doRange(), size: CGSize(width: a_blockValue - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
        if AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.es.rawValue || AddrConstraintReactiveCompatible.share.interfaceLang == ActualWithSubscriptType.pt.rawValue {
            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
            btn.titleLabel?.font = UIFont.thoughtImage(type: .Regular, fontSize: 16)
        }
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginBindEmailVC {
extension CountLabelRecognizerDelegate {
    //: @objc func backBtnClick() {
    @objc func close() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    //: func checkCommitButtonState() {
    func viewSize() {
        //: if emailInputView.text!.isEmptyString {
        if emailInputView.text!.isEmptyString {
            //: commitButton.isEnabled = false
            commitButton.isEnabled = false
            //: } else {
        } else {
            //: commitButton.isEnabled = true
            commitButton.isEnabled = true
        }
    }

    //: func func__getEmailCodeAction() {
    func color() {
        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        SizeHungReactiveCompatible.exhibitRemoveCuttingEdge(superView: view)
        //: TalkingLoginRequestTool.req_getBindEmailCode(emailStr: emailInputView.text!, isBind: true) { succeed, result, errorModel in
        LoginReactiveCompatible.object(emailStr: emailInputView.text!, isBind: true) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: if succeed {
            if succeed {
                //: self.func__pushToPhoneCodeVC()
                self.nudgeRecordOmertaDigitiserExtensionPhoneVc()
            }
        }
    }

    //: func func__pushToPhoneCodeVC() {
    func nudgeRecordOmertaDigitiserExtensionPhoneVc() {
        //: let vc = TalkingLoginBindInputCodeVC()
        let vc = ValueViewController()
        //: vc.phoneOrEmailStr = emailInputView.text!
        vc.phoneOrEmailStr = emailInputView.text!
        //: vc.viewType = .BindEmail
        vc.viewType = .BindEmail
        //: navigationController?.pushViewController(vc, animated: true)
        navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingLoginBindEmailVC {
extension CountLabelRecognizerDelegate {
    // 添加视图
    //: private func setupSubviews() {
    private func generatePhone() {
        //: backBtn.isHidden = !isBack
        backBtn.isHidden = !isBack
        //: view.addSubview(backBtn)
        view.addSubview(backBtn)
        //: view.bringSubviewToFront(backBtn)
        view.bringSubviewToFront(backBtn)
        //: view.addSubview(titleLabel)
        view.addSubview(titleLabel)
        //: view.addSubview(emailInputView)
        view.addSubview(emailInputView)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
        //: view.addSubview(emailTipLabel)
        view.addSubview(emailTipLabel)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func outspoken() {
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(6)
            make.leading.equalTo(6)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(a_limitRecordScreenName)
            //: make.size.equalTo(44)
            make.size.equalTo(44)
        }
        //: titleLabel.snp.makeConstraints { make in
        titleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(backBtn.snp.bottom).offset(44)
            make.top.equalTo(backBtn.snp.bottom).offset(44)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: emailInputView.snp.makeConstraints { make in
        emailInputView.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(titleLabel.snp.bottom).offset(40)
            make.top.equalTo(titleLabel.snp.bottom).offset(40)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(emailInputView.snp.bottom).offset(40)
            make.top.equalTo(emailInputView.snp.bottom).offset(40)
        }
        //: emailTipLabel.snp.makeConstraints { make in
        emailTipLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(commitButton.snp.bottom).offset(24)
            make.top.equalTo(commitButton.snp.bottom).offset(24)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func image() {
        //: emailInputView.rx.text.orEmpty.asObservable()
        emailInputView.rx.text.orEmpty.asObservable()
            //: .subscribe(onNext: { [weak self] _ in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.checkCommitButtonState()
                self.viewSize()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__getEmailCodeAction()
                self.color()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
