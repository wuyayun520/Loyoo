
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_toValue:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "open_picture" :*/
fileprivate let str_colorTitle:[Character] = ["o","p","e","n","_","p","i","c","t","u","r","e"]

/*: "Load..." :*/
fileprivate let str_userBackText:String = "Load...let make view model"

/*: "mount_img" :*/
fileprivate let str_maleValue:String = "mount_imgtool lock room equal"

/*: "Skip" :*/
fileprivate let str_keyAgentName:String = "Skiplab extra else cell"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_playerAppReturnContent:[UInt8] = [0x48,0x74,0x74,0x70,0x54,0x6f,0x6f,0x6c,0x2e,0x4e,0x61,0x74,0x69,0x76,0x65,0x54,0x6f,0x4a,0x73,0x28,0x27,0x72,0x65,0x63,0x68,0x61,0x72,0x67,0x65,0x27,0x29]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_backgroundName:[UInt8] = [0x77,0x69,0x6e,0x64,0x6f,0x77,0x2e,0x6f,0x6e,0x50,0x61,0x67,0x65,0x53,0x68,0x6f,0x77,0x26,0x26,0x6f,0x6e,0x50,0x61,0x67,0x65,0x53,0x68,0x6f,0x77,0x28,0x29,0x3b]

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_withViewToData:[Character] = ["j","s","E","v","e","n","t"]
fileprivate let str_outsideCallName:String = "(onPcount image kit"
fileprivate let str_backName:[Character] = ["a","g","e","S","h","o","w",")",":"," "]

/*: --- :*/
fileprivate let str_showValue:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_iconSizeValue:[UInt8] = [0x77,0x69,0x6e,0x64,0x6f,0x77,0x2e,0x6f,0x6e,0x50,0x61,0x67,0x65,0x48,0x69,0x64,0x65,0x26,0x26,0x6f,0x6e,0x50,0x61,0x67,0x65,0x48,0x69,0x64,0x65,0x28,0x29,0x3b]

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_whiteTitle:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let str_atName:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let str_buttonTitle:[Character] = ["c","a","n","G"]
fileprivate let str_proTitle:String = "share hidden for modeoBack"

/*: "PHPSESSID= :*/
fileprivate let str_viewName:[Character] = ["P","H","P","S","E","S","S","I","D"]
fileprivate let str_tipMediumName:String = "add"

/*: ";uid= :*/
fileprivate let str_colorAtValue:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let str_originalStartElseData:String = "kit status table guardCookie"

/*: "nav_back_black_nor" :*/
fileprivate let str_frameTitle:String = "after info camera tonav_ba"
fileprivate let str_topItemName:String = "lack_nofill time local"
fileprivate let str_indexTitle:[Character] = ["r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let str_imageTitle:String = "ico_inself pop var"
fileprivate let str_modeAfterName:String = "image height image medium_list_s"
fileprivate let str_addText:[Character] = ["m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_fillValue:[UInt8] = [0x64,0x6f,0x63,0x75,0x6d,0x65,0x6e,0x74,0x2e,0x67,0x65,0x74,0x45,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x73,0x42,0x79,0x54,0x61,0x67,0x4e,0x61,0x6d,0x65,0x28,0x27,0x62,0x6f,0x64,0x79,0x27,0x29,0x5b,0x30,0x5d,0x2e,0x73,0x74,0x79,0x6c,0x65,0x2e,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x3d,0x27,0x72,0x67,0x62,0x61,0x28,0x30,0x2c,0x30,0x2c,0x30,0x2c,0x30,0x29,0x27]

/*: "Alert" :*/
fileprivate let str_ofName:String = "Alertnot line manager white"

/*: "OK" :*/
fileprivate let str_statusText:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let str_centerBarVarTitle:String = "Canclmask succeed if wrap will"
fileprivate let str_indexModelTitle:[Character] = ["e"]

/*: "Done" :*/
fileprivate let str_fieldFormalData:String = "model view return main returnDone"

/*: "js call method name =  :*/
fileprivate let str_makeData:[Character] = ["j"]
fileprivate let str_finishValue:String = "text layer height in states cal"
fileprivate let str_tapName:String = "in image modelod nam"

/*: , message =  :*/
fileprivate let str_withText:[Character] = [","," ","m","e","s","s"]
fileprivate let str_calledLabText:String = "age = name last finish leading"

/*: "http://" :*/
fileprivate let str_screenTitle:[Character] = ["h","t","t","p",":","/"]
fileprivate let str_withEditData:[Character] = ["/"]

/*: "https://" :*/
fileprivate let str_financialValue:String = "https:/video self"
fileprivate let str_itemName:[Character] = ["/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_fileData:[UInt8] = [0x76,0x61,0x72,0x20,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x4e,0x61,0x6d,0x65,0x73,0x20,0x3d,0x20,0x64,0x6f,0x63,0x75,0x6d,0x65,0x6e,0x74,0x2e,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x2e,0x73,0x70,0x6c,0x69,0x74,0x28,0x27,0x3b,0x20,0x27,0x29,0x2e,0x6d,0x61,0x70,0x28,0x66,0x75,0x6e,0x63,0x74,0x69,0x6f,0x6e,0x28,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x29,0x20,0x7b,0x20,0x72,0x65,0x74,0x75,0x72,0x6e,0x20,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x2e,0x73,0x70,0x6c,0x69,0x74,0x28,0x27,0x3d,0x27,0x29,0x5b,0x30,0x5d,0x20,0x7d,0x20,0x29,0x3b,0xa]

/*: "uid= :*/
fileprivate let str_sharedName:String = "uid=var and up next content"

/*: ;domain= :*/
fileprivate let str_modeName:String = ";dobject layer self inside"
fileprivate let str_objectTitle:String = "omain=data you page"

/*: ;path=/" :*/
fileprivate let str_matchValue:String = "open shared to manager var;path=/"

/*: "document.cookie=' :*/
fileprivate let str_whiteModeValue:[Character] = ["d","o","c","u","m","e"]
fileprivate let str_conversationVideoName:[Character] = ["n","t",".","c","o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let str_taskValue:String = "\';\n"

/*: ;domin= :*/
fileprivate let str_viewLabTitle:[Character] = [";","d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_toTitle:[UInt8] = [0x69,0x66,0x20,0x28,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x4e,0x61,0x6d,0x65,0x73,0x2e,0x69,0x6e,0x64,0x65,0x78,0x4f,0x66,0x28,0x27,0x50,0x48,0x50,0x53,0x45,0x53,0x53,0x49,0x44,0x27,0x29,0x20,0x3d,0x3d,0x20,0x2d,0x31,0x29,0x20,0x7b,0x20,0x64,0x6f,0x63,0x75,0x6d,0x65,0x6e,0x74,0x2e,0x63,0x6f,0x6f,0x6b,0x69,0x65,0x3d,0x27]

/*: '; };\n" :*/
fileprivate let str_titleName:String = "\'; };\n"

/*: '" :*/
fileprivate let str_bubbleName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let str_shareValue:[Character] = ["h","5","D","o","m","a"]
fileprivate let str_failFailureName:[Character] = ["i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let str_pinTitle:[Character] = ["g"]
fileprivate let str_addValue:[Character] = ["e","t","A","l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let str_tableToValue:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let str_clubViewValue:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let str_minimizeData:[Character] = ["a","p","p","C","o"]
fileprivate let str_verticalRowFemaleName:String = "self succeed makeokies = "

/*: "UID" :*/
fileprivate let str_equalValue:String = "UtabD"

/*: "navigator.userAgent" :*/
fileprivate let str_requestTitle:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s","e"]
fileprivate let str_inputTitle:String = "size normal tablerAgent"

/*: "xunwu_iphone" :*/
fileprivate let str_viewTitle:String = "xunwu_size button view image"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_shareData:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e"]
fileprivate let str_statusTitle:[Character] = ["r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let str_userText:[Character] = [" ","x","u","n","w"]
fileprivate let str_micName:String = "view free lab color selfu_pac"

/*:  xunwu_bundleId/ :*/
fileprivate let str_tingData:String = " xunwumodel equal"
fileprivate let str_modelData:String = "eId/view photo"

/*:   webkit/1" :*/
fileprivate let str_borderData:String = "  webkistage screen voice var"
fileprivate let str_containerData:[Character] = ["t","/","1"]

/*: "  :*/
fileprivate let str_countTitle:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let str_positionName:String = " xunwrecord click self title"
fileprivate let str_resultName:String = "class return status equalvice_id/"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MForefrontViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class MForefrontViewController: UserRecognizerDelegate {
	var giftOff: Bool = true
	var atSum: Int = 33
	var countSphereTotal: Double = 39.5
	var statusText: String = "mini"
	var modelArray: [AnyHashable] = []
	var modelDoing: Bool = false
	var playerMagnitude: Int = 95
	var fromImageQuantity: Double = 53.6
	var remoteText: String = "display"
	var femaleArray: [AnyHashable] = []

    var itemImageView: UIImageView?

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: LabConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: BrandSubscriptType?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: BrandSubscriptType?, webConfig: LabConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_toValue, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        self.itemImageView?.image = UIImage(named: (String(str_colorTitle)))
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        messUpShow()
    
		if var imageValue = urlStr { 
			if var politicalPartyValue = self.tranConfig?.visualBackAlpha { 
				if var appearValue = urlStr { 
					if var startValue = config?.widthHeight { 
						if var cocktailLoungeValue = config?.showLoadingView { 
					            if (closeBtn.layer.contentsRect.size.width != 1) && (closeBtn.mask != nil) {
					                //: SWIFT_CUSTOM_DANGER_File_Call
					                let landLet = LineView()
					            landLet.imageOn = cocktailLoungeValue
					            landLet.cubeNumber = startValue
					            landLet.resultText = appearValue
					            
					            landLet.inscriptionOn = { [self] periodicTableOff in
					            self.modelDoing = periodicTableOff
					            
					            var landLet = isModal
					            landLet = true
					            if landLet != self.modelDoing {
					                self.modelDoing = landLet
					            }
					            
					                self.modelDoing = false
					                self.modelDoing = !self.modelDoing
					            return self.modelDoing
					            }
					            landLet.colorIconQuantity = { [self] filterMagnitude in
					            self.playerMagnitude = filterMagnitude
					            
					            return self.playerMagnitude
					            }
					            landLet.giftMagnitude = { [self] timeNumber in
					            self.fromImageQuantity = timeNumber
					            
					            var landLet = politicalPartyValue
					                landLet -= 1
					                if landLet != 94 {
					                    landLet = landLet + 1
					                }
					            if landLet < self.fromImageQuantity {
					                self.fromImageQuantity = landLet
					            }
					            
					            return self.fromImageQuantity
					            }
					            landLet.imageContent = { [self] viewStartContent in
					            self.remoteText = viewStartContent
					            
					            var landLet = imageValue
					            landLet = landLet.lowercased() + "message"
					            if landLet.contains(self.remoteText) {
					                self.remoteText = landLet
					            }
					            
					            return self.remoteText
					            }
					            landLet.voiceInfoGiftArray = { [self] giftExamineArray in
					            self.femaleArray = giftExamineArray
					            
					            guard var value = self.femaleArray as? [String] else {
					                return nil
					            }
					            return value
					            }
					                closeBtn.addSubview(landLet)
					            }
					
						}
					}
				}
			}
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        pageHideEventOpenMake()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    
		if var pointValue = urlStr { 
			if var takeTheFieldValue = self.tranConfig?.duration { 
				if var sectionMakeValue = self.urlStr { 
					if var backValue = config?.widthHeight { 
					if let itemImageView = itemImageView {
				
				            if (!itemImageView.autoresizesSubviews) && (itemImageView.constraintsAffectingLayout(for: .horizontal).count == 58) {
				                //: SWIFT_CUSTOM_DANGER_File_Call
				                let heightRowExplain = LineView(frame: itemImageView.bounds.offsetBy(dx: CGFloat(254.56), dy: CGFloat(514.61)))
				            heightRowExplain.imageOn = self.isModal
				            heightRowExplain.cubeNumber = backValue
				            heightRowExplain.resultText = sectionMakeValue
				            
				            heightRowExplain.inscriptionOn = { [self] periodicTableOff in
				            self.giftOff = periodicTableOff
				            
				            var heightRowExplain = isVideoCallRechargeHalfPage
				            heightRowExplain = false
				            if heightRowExplain != self.giftOff {
				                self.giftOff = heightRowExplain
				            }
				            
				            self.giftOff = true
				            return self.giftOff
				            }
				            heightRowExplain.colorIconQuantity = { [self] filterMagnitude in
				            self.atSum = filterMagnitude
				            
				            return self.atSum
				            }
				            heightRowExplain.giftMagnitude = { [self] timeNumber in
				            self.countSphereTotal = timeNumber
				            
				            var heightRowExplain = takeTheFieldValue
				            heightRowExplain = 0
				            if heightRowExplain < self.countSphereTotal {
				                self.countSphereTotal = heightRowExplain
				            }
				            
				            return self.countSphereTotal
				            }
				            heightRowExplain.imageContent = { [self] viewStartContent in
				            self.statusText = viewStartContent
				            
				            var heightRowExplain = pointValue
				            heightRowExplain = String(heightRowExplain.lowercased() + "load")
				            if heightRowExplain.contains(self.statusText) {
				                self.statusText = heightRowExplain
				            }
				            
				            return self.statusText
				            }
				            heightRowExplain.voiceInfoGiftArray = { [self] giftExamineArray in
				            self.modelArray = giftExamineArray
				            
				            guard var value = self.modelArray as? [String] else {
				                return nil
				            }
				            return value
				            }
				                itemImageView.addSubview(heightRowExplain)
				            }
				
					}
				
					}
				}
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(str_userBackText.prefix(7)))
        //: addNotification()
        onError()
        //: func__checkConfig()
        enterConfig()
        //: func__setUpWebView()
        towardSetSort()
        //: func__registerObserver()
        tableObserver()
        //: func__beginStartRequest()
        addFunc()
        //: func_checkAppleIAP()
        checkData()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: a_callHomeData, object: nil)
            }
        }

        itemImageView = UIImageView(frame: self.view.frame.insetBy(dx: CGFloat(self.view.canBecomeFocused ? 4 : 1), dy: CGFloat(Double(self.view.frame.size.height))))
        if let itemImageView = itemImageView {
            self.itemImageView?.image = UIImage(named: (String(str_maleValue.prefix(9))))
            if (itemImageView.inputView != nil) && (itemImageView.animationRepeatCount == Int(itemImageView.center.x)) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(itemImageView)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            doList(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.offt(), .font: UIFont.thoughtImage(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_keyAgentName.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension MForefrontViewController {
    /// 添加通知
    //: private func addNotification() {
    private func onError() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(nonopening),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: a_noText,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(messUpShow),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func nonopening() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_playerAppReturnContent, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func messUpShow() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_backgroundName, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_withViewToData) + String(str_outsideCallName.prefix(4)) + String(str_backName)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func pageHideEventOpenMake() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_iconSizeValue, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_whiteTitle)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension MForefrontViewController {
    //: func func__registerObserver() {
    func tableObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(str_atName)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_buttonTitle) + String(str_proTitle.suffix(5))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(cornerView), name: a_cornerData, object: nil)
    }

    //: func func__setUpWebView() {
    func towardSetSort() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        addcustom()
        //: func__addBridgeMethod()
        overcrossingCenter()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.playerEqual()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            SizeHungReactiveCompatible.exhibitRemoveCuttingEdge(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(a_limitRecordScreenName)
            }
        }
    }

    //: func func__beginStartRequest() {
    func addFunc() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.owner()
            }
            //: } else {
        } else {
            //: func__startRequest()
            owner()
        }
    }

    //: func func__startRequest() {
    func owner() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = MeasurementAppManager.share.loginUid
        let uid = MeasurementAppManager.share.loginUid
        //: let ssid = MeasurementAppManager.share.loginSessionId
        let ssid = MeasurementAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(str_viewName) + str_tipMediumName.replacingOccurrences(of: "add", with: "=")) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(str_colorAtValue)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(str_originalStartElseData.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        primaFacieFuncHeather()
        //: func__initRightButton()
        cuttingEdgeButton()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func checkData() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            MediaTransactionObserver.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.titleAt(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            MediaTransactionObserver.shared.lock()
        }
    }

    //: func func__initLeftButton() {
    func anyInit() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.outsideText(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.outsideText(name: (String(str_frameTitle.suffix(6)) + "ck_b" + String(str_topItemName.prefix(7)) + String(str_indexTitle))), style: .plain, target: self, action: #selector(underEqual(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func cuttingEdgeButton() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(BrandSubscriptType.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.outsideText(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.outsideText(name: (String(str_imageTitle.prefix(6)) + "timate" + String(str_modeAfterName.suffix(7)) + String(str_addText))), style: .plain, target: self, action: #selector(inserter))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func underEqual(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        dustBag()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.colorController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: MForefrontViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! MForefrontViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! MForefrontViewController).messUpShow()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func enterConfig() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func destroyText(sucess: Bool) {
        //: ProgressHUD.dismiss()
        SizeHungReactiveCompatible.dataMessage()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func primaFacieFuncHeather() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_fillValue, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func pullUpUrl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: LimitPushManager.share.func__pushToWebVC(urlStr: url)
            LimitPushManager.share.comb(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func makeOpen(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func cornerView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: a_cornerData, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func inserter() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = PicViewDelegate(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue))
        //: view.show()
        view.sectionCell()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension MForefrontViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            SizeHungReactiveCompatible.exhibitRemoveCuttingEdge(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            destroyText(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        anyInit()
        //: func__clearJSBgColor()
        primaFacieFuncHeather()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            destroyText(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            destroyText(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(str_ofName.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(str_centerBarVarTitle.prefix(5)) + String(str_indexModelTitle)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(str_fieldFormalData.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension MForefrontViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_makeData) + String(str_finishValue.suffix(5)) + "l meth" + String(str_tapName.suffix(6)) + "e = ") + "\(message.name)" + (String(str_withText) + String(str_calledLabText.prefix(6))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.ofAt(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func overcrossingCenter() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in DeleteBasicType.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func dustBag() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in DeleteBasicType.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension MForefrontViewController {
    //: func func__addUserScript() {
    func popIn() {
        //: var htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(str_screenTitle) + String(str_withEditData))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_screenTitle) + String(str_withEditData)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(str_financialValue.prefix(7)) + String(str_itemName))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_financialValue.prefix(7)) + String(str_itemName)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = MeasurementAppManager.share.loginUserMode.userID
        let tempUid: String = MeasurementAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = MeasurementAppManager.share.loginSessionId
        let tempSessionID: String = MeasurementAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_fileData, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_sharedName.prefix(4))) + "\(tempUid)" + (String(str_modeName.prefix(2)) + String(str_objectTitle.prefix(6))) + "\(htmlDomain)" + (String(str_matchValue.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(str_whiteModeValue) + String(str_conversationVideoName)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(str_viewName) + str_tipMediumName.replacingOccurrences(of: "add", with: "=")) + "\(tempSessionID)" + (String(str_viewLabTitle)) + "\(htmlDomain)" + (String(str_matchValue.suffix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(str_whiteModeValue) + String(str_conversationVideoName)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_toTitle, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_sharedName.prefix(4))) + "\(tempUid)" + (String(str_modeName.prefix(2)) + String(str_objectTitle.prefix(6))) + "\(htmlDomain)" + (String(str_matchValue.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(str_whiteModeValue) + String(str_conversationVideoName)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_sharedName.prefix(4))) + "\(tempSessionID)" + (String(str_modeName.prefix(2)) + String(str_objectTitle.prefix(6))) + "\(htmlDomain)" + (String(str_matchValue.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(str_whiteModeValue) + String(str_conversationVideoName)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func doList(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_shareValue) + String(str_failFailureName)) + "\(htmlDomain)")
            //: let userID: String = MeasurementAppManager.share.loginUserMode.userID
            let userID: String = MeasurementAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_pinTitle) + String(str_addValue)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(str_tableToValue)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(str_clubViewValue)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.addFunc()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.canulateChange(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func canulateChange(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = MeasurementAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_shareValue) + String(str_failFailureName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: AddrConstraintReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_minimizeData) + String(str_verticalRowFemaleName.suffix(8))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (str_equalValue.replacingOccurrences(of: "tab", with: "I")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_clubViewValue))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(str_tableToValue)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_tableToValue))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.addFunc()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func addcustom() {
        self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(str_requestTitle) + String(str_inputTitle.suffix(6))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(str_viewTitle.prefix(6)) + "iphone")) {
                let str = "xunwu_iphone xunwu_version/\(a_conversationTitle) xunwu_package/\(a_pathValue) xunwu_bundleId/\(a_countDisplayValue)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.betweenMessage { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_positionName.prefix(5)) + "u_de" + String(str_resultName.suffix(8))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
