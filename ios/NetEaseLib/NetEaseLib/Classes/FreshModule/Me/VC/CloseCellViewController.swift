
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_fatalText:[UInt8] = [0x69,0x6e,0x69,0x74,0x28,0x63,0x6f,0x64,0x65,0x72,0x3a,0x29,0x20,0x68,0x61,0x73,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x65,0x6e,0x20,0x69,0x6d,0x70,0x6c,0x65,0x6d,0x65,0x6e,0x74,0x65,0x64]

/*: "equal_observe_pic" :*/
fileprivate let str_sizeText:String = "text frameequal_"
fileprivate let str_sendData:String = "e_pipath for close fill image"
fileprivate let str_shareName:String = "frame"

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let str_equalData:[UInt8] = [0x4f,0x70,0x65,0x6e,0x20,0x74,0x68,0x65,0x6e,0x20,0x25,0x40,0x20,0x77,0x69,0x6c,0x6c,0x20,0x72,0x65,0x63,0x6f,0x6d,0x6d,0x65,0x6e,0x64,0x20,0x75,0x73,0x65,0x72,0x73,0x20,0x74,0x6f,0x20,0x79,0x6f,0x75,0x2e,0x20,0x41,0x20,0x6e,0x65,0x77,0x20,0x63,0x68,0x61,0x74,0x20,0x77,0x69,0x6c,0x6c,0x20,0x62,0x65,0x20,0x6d,0x61,0x64,0x65,0x20,0x61,0x66,0x74,0x65,0x72,0x20,0x61,0x20,0x73,0x75,0x63,0x63,0x65,0x73,0x73,0x66,0x75,0x6c,0x20,0x72,0x65,0x63,0x6f,0x6d,0x6d,0x65,0x6e,0x64,0x61,0x74,0x69,0x6f,0x6e,0x2e]

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_tipTitle:[UInt8] = [0x2e,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x63,0x61,0x65,0x20,0x74,0x65,0x65,0x72,0x67,0x20,0x6f,0x74,0x20,0x79,0x74,0x69,0x74,0x6e,0x65,0x64,0x69,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x55,0x20,0x2e,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x73,0x72,0x65,0x73,0x75,0x20,0x64,0x6e,0x65,0x6d,0x6d,0x6f,0x63,0x65,0x72,0x20,0x6c,0x6c,0x69,0x77,0x20,0x65,0x6d,0x61,0x6e,0x79,0x6c,0x70,0x70,0x41,0x20,0x6e,0x65,0x68,0x74,0x20,0x6e,0x65,0x70,0x4f]

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let str_pointData:[UInt8] = [0x59,0x6f,0x75,0x72,0x20,0x61,0x76,0x61,0x74,0x61,0x72,0x20,0x61,0x6e,0x64,0x20,0x6e,0x69,0x63,0x6b,0x6e,0x61,0x6d,0x65,0x20,0x77,0x69,0x6c,0x6c,0x20,0x6e,0x6f,0x74,0x20,0x62,0x65,0x20,0x64,0x69,0x73,0x70,0x6c,0x61,0x79,0x65,0x64,0x20,0x69,0x6e,0x20,0x74,0x68,0x65,0x20,0x72,0x61,0x6e,0x6b,0x69,0x6e,0x67,0x2e]

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_styleData:[UInt8] = [0x2e,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x63,0x61,0x65,0x20,0x74,0x65,0x65,0x72,0x67,0x20,0x6f,0x74,0x20,0x79,0x74,0x69,0x74,0x6e,0x65,0x64,0x69,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x55,0x20,0x2e,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x73,0x72,0x65,0x73,0x75,0x20,0x64,0x6e,0x65,0x6d,0x6d,0x6f,0x63,0x65,0x72,0x20,0x6c,0x6c,0x69,0x77,0x20,0x40,0x25,0x20,0x6e,0x65,0x68,0x74,0x20,0x6e,0x65,0x70,0x4f]

/*: "MediumView" :*/
fileprivate let str_agentData:[Character] = ["T","a","l","k","i","n","g","S"]
fileprivate let str_viewData:[Character] = ["e"]
fileprivate let str_removeShareTitle:String = "ttmodel"

/*: "#FF2348" :*/
fileprivate let str_errorName:[Character] = ["#","F","F","2","3","4","8"]

/*: "ModelNotifCell" :*/
fileprivate let str_resultTitle:[Character] = ["T","a","l","k","i","n","g","S","e","t"]
fileprivate let str_callSlideData:String = "tinmin"
fileprivate let str_imageAreaAsName:String = "shared replace viewCell"

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let str_valueDataText:[UInt8] = [0x54,0x6f,0x20,0x73,0x65,0x74,0x20,0x75,0x70,0x20,0x61,0x20,0x70,0x61,0x73,0x73,0x77,0x6f,0x72,0x64,0x2c,0x20,0x79,0x6f,0x75,0x20,0x6e,0x65,0x65,0x64,0x20,0x74,0x6f,0x20,0x76,0x65,0x72,0x69,0x66,0x79,0x20,0x74,0x68,0x65,0x20,0x61,0x63,0x63,0x6f,0x75,0x6e,0x74,0x2c,0x20,0x63,0x68,0x6f,0x6f,0x73,0x65,0x20,0x79,0x6f,0x75,0x72,0x20,0x76,0x65,0x72,0x69,0x66,0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x20,0x6d,0x65,0x74,0x68,0x6f,0x64]

/*: "Verified by Phone" :*/
fileprivate let str_itemValue:[Character] = ["V","e","r","i","f","i"]
fileprivate let str_voiceText:String = "ed by height status"

/*: "Verified by Email" :*/
fileprivate let str_titleData:String = "message add share sexVerif"
fileprivate let str_fatalOfValue:[Character] = [" ","E","m","a","i","l"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let str_frameData:[UInt8] = []

/*: "UNRECOVERABLE" :*/
fileprivate let str_imageAcceptValue:[Character] = ["U","N","R","E","C","O","V","E","R","A","B","L","E"]

/*: "Delete Account" :*/
fileprivate let str_titleCancelValue:[Character] = ["D","e","l","e","t"]
fileprivate let str_shareData:[Character] = ["e"," ","A","c","c","o","u","n","t"]

/*: "Cancel" :*/
fileprivate let str_fullValue:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let str_makeTitle:String = "Deletewindow mental else if"

/*: " ( :*/
fileprivate let str_labelName:String = " voice"

/*: s)" :*/
fileprivate let str_indexData:String = "s)selected var false float"

/*: "Logout succeeded!" :*/
fileprivate let str_showValue:String = "Logouimage key list border add"
fileprivate let str_ofName:String = "calleed"
fileprivate let str_menuName:[Character] = ["e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CloseCellViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum StatuteNameSubscriptType: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum MTextConvertible: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class CloseCellViewController: PropertyViewController {
    var sendToImageView: UIImageView?
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = MMeasurable()

    //: var  type = SettingsUniversal.Contact_information
    var type = StatuteNameSubscriptType.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_fatalText, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.playerEqual()
        //: designView()
        loadDown()
        //: getSettingData()
        methodOfChoice()

        sendToImageView = UIImageView(frame: self.view.frame)
        if let sendToImageView = sendToImageView {
            self.sendToImageView?.image = UIImage(named: (String(str_sizeText.suffix(6)) + "observ" + String(str_sendData.prefix(4)) + str_shareName.replacingOccurrences(of: "frame", with: "c")))
            if (sendToImageView.tintAdjustmentMode == .dimmed) && (sendToImageView.convert(CGPoint(x: 0, y: CGFloat(Double(sendToImageView.frame.origin.y))), from: sendToImageView.superview).x == 87.25) {
                //: SWIFT_CUSTOM_DANGER
                self.view.addSubview(sendToImageView)
            }
        }
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [MTextConvertible] = {
        //: var array = [SettingsUniversalType]()
        var array = [MTextConvertible]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: a_blockValue, height: a_formalValue - a_userTitle), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized, "Your avatar and nickname will not be displayed in the ranking.".localized]
        var array = ["", String(bytes: str_equalData, encoding: .utf8)!.random(a_callValue), String(bytes: str_tipTitle.reversed(), encoding: .utf8)!.localized, String(bytes: str_pointData, encoding: .utf8)!.localized]
        //: if MeasurementAppManager.share.loginUserMode.sex == "2" {
        if MeasurementAppManager.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: str_styleData.reversed(), encoding: .utf8)!.random(a_callValue)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension CloseCellViewController {
    //: func getSettingData() {
    func methodOfChoice() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        ConstraintRequestTool.moveImage(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<MMeasurable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func offStatus(type: StatuteNameSubscriptType) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.Phone_Number)
            DataSource.append(.Phone_Number)
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if MeasurementAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 || MeasurementAppManager.share.loginUserMode.email.count > 0 {
            if MeasurementAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 || MeasurementAppManager.share.loginUserMode.email.count > 0 {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func fileWithAdd() {
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        LoginReactiveCompatible.user { succeed, _, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard succeed else { return }
            guard succeed else { return }
            //: LimitPushManager.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: MeasurementAppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
            LimitPushManager.share.use(type: .PhoneLogin, content: MeasurementAppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func email() {
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        LoginReactiveCompatible.name { succeed, _, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: guard succeed else { return }
            guard succeed else { return }
            //: LimitPushManager.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: MeasurementAppManager.share.loginUserMode.email, isNewPwd: false)
            LimitPushManager.share.use(type: .EmailLogin, content: MeasurementAppManager.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension CloseCellViewController: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if MeasurementAppManager.share.loginUserMode.sex == "2" {
                if MeasurementAppManager.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let identifier = "TalkingSettingCell"
            let identifier = (String(str_agentData) + String(str_viewData) + str_removeShareTitle.replacingOccurrences(of: "model", with: "in") + "gCell")
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingCell
            let cell: MediumView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! MediumView

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.instance(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == MTextConvertible.Phone_Number {
                //: if MeasurementAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if MeasurementAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: MeasurementAppManager.share.loginUserMode.mobile ?? "")
                    cell.frank(detail: MeasurementAppManager.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .blockOf()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == MTextConvertible.Email_Address {
                //: if MeasurementAppManager.share.loginUserMode.email.count > 0 {
                if MeasurementAppManager.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: MeasurementAppManager.share.loginUserMode.email )
                    cell.frank(detail: MeasurementAppManager.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == MTextConvertible.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == MTextConvertible.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(str_errorName)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let identifier = "TalkingSettingNotifCell"
            let identifier = (String(str_resultTitle) + str_callSlideData.replacingOccurrences(of: "min", with: "g") + "Notif" + String(str_imageAreaAsName.suffix(4)))
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingNotifCell
            let cell: ModelNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ModelNotifCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.circumference(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.aggregationTop(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.addReplace(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.addReplace(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.addReplace(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.addReplace(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.addReplace(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == MTextConvertible.Phone_Number.rawValue {
                //: let mobile = MeasurementAppManager.share.loginUserMode.mobile ?? ""
                let mobile = MeasurementAppManager.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = ToPhoneVc()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == MTextConvertible.Email_Address.rawValue {
                //: if MeasurementAppManager.share.loginUserMode.email.count <= 0 {
                if MeasurementAppManager.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = CountLabelRecognizerDelegate()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == MTextConvertible.DeleteA.rawValue {
                //: writeOff()
                onWriteWith()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == MTextConvertible.SetPassword.rawValue {
                //: let mobile = MeasurementAppManager.share.loginUserMode.mobile ?? ""
                let mobile = MeasurementAppManager.share.loginUserMode.mobile ?? ""
                //: let email = MeasurementAppManager.share.loginUserMode.email
                let email = MeasurementAppManager.share.loginUserMode.email
                //: if !mobile.isEmpty, !email.isEmpty {
                if !mobile.isEmpty, !email.isEmpty { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = MediumDataSource(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: str_valueDataText, encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.pingfangRugularFont(fontSize: 12)
                    vc.titleFont = UIFont.drogueSize(fontSize: 12)
                    //: vc.titleColor = UIColor.appValueDetailColor()
                    vc.titleColor = UIColor.observerShared()
                    //: vc.cellTitleFont = UIFont.pingfangMediumFont(fontSize: 16)
                    vc.cellTitleFont = UIFont.methodPlay(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.messageWith(cellTitleList: [(String(str_itemValue) + String(str_voiceText.prefix(6)) + "Phone").localized, (String(str_titleData.suffix(5)) + "ied by" + String(str_fatalOfValue)).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.fileWithAdd()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.email()
                        }
                    }

                    //: } else if !mobile.isEmpty {
                } else if !mobile.isEmpty {
                    //: req_sendModifyPwdSms()
                    fileWithAdd()

                    //: } else if !email.isEmpty {
                } else if !email.isEmpty {
                    //: req_sendModifyPwdEmail()
                    self.email()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension CloseCellViewController {
    /// 注销
    //: private func writeOff() {
    private func onWriteWith() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard PrefaceReactiveCompatible.picStart() == false else { return }
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: str_frameData.reversed(), encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.placeTip((String(str_imageAcceptValue)))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.atAfterPosition(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        TitleAlertShow.outsideWill(title: (String(str_titleCancelValue) + String(str_shareData)).localized, attributedMessage: string, leftBtnTitle: (String(str_fullValue)).localized, rightBtnTitle: (String(str_makeTitle.prefix(6))).localized) {
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            TitleAlertShow.lastAlert()

            //: self.LockAccount()
            self.nameEnable()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                TitleAlertShow.contentTouch(title: (String(str_makeTitle.prefix(6))).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                TitleAlertShow.contentTouch(title: (String(str_makeTitle.prefix(6))).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func nameEnable() {
        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        ConstraintRequestTool.giveItAWhirlCompletion(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.objectOf()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.afterTitle(showMsg: (String(str_showValue.prefix(5)) + "t su" + str_ofName.replacingOccurrences(of: "call", with: "cc") + String(str_menuName)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func objectOf() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SocketReactiveCompatible.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.episode(showMsg: a_viewNetValue)
            //: return
            return
        }

        //: ProgressHUD.show()
        SizeHungReactiveCompatible.picCurrent()
        //: TalkingLoginRequestTool.req_loginOut { t in
        LoginReactiveCompatible.index { t in
            //: ProgressHUD.dismiss()
            SizeHungReactiveCompatible.dataMessage()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: a_senseContent, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension CloseCellViewController {
    //: private func designView() {
    private func loadDown() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: "TalkingSettingCell")
        MainTable.register(MediumView.self, forCellReuseIdentifier: (String(str_agentData) + String(str_viewData) + str_removeShareTitle.replacingOccurrences(of: "model", with: "in") + "gCell"))
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: "TalkingSettingNotifCell")
        MainTable.register(ModelNotifCell.self, forCellReuseIdentifier: (String(str_resultTitle) + str_callSlideData.replacingOccurrences(of: "min", with: "g") + "Notif" + String(str_imageAreaAsName.suffix(4))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
